{
  "src/modules/lost/infrastructure/lost-listing-read.ts#last_known_location": "Legacy fallback key. Pre-Lost&Found-Fase-1 status_changed rows (before location_description existed) stored the lost location under last_known_location; some historical rows on production predate the rename. No current writer schema (statusChanged in lib/events/event-schemas.ts) emits this key — new writes only ever set location_description. This read is a read-only fallback for old rows, not a live write path. See also src/modules/pets/application/read/load-public-credential.ts#last_known_location for the same historical fallback on the public credential.",
  "src/modules/pets/application/read/load-public-credential.ts#last_known_location": "Same legacy fallback as src/modules/lost/infrastructure/lost-listing-read.ts#last_known_location (see that entry) — the public credential's lost-pet location line falls back to the pre-rename key for historical status_changed rows. Moved out of the page with the loader (Track 2, 2026-08-21); the exception is unchanged, only its path is.",
  "lib/analytics/dashboards/surveillance.ts#signal_event_id": "NOT a pet_events read at all, and not legacy either - this is the one exception shape this fence documents in its own header but cannot see. `signal_event_id` lives in a CASE_EVENTS payload, written by `openOutbreakInvestigation` (src/modules/surveillance/application/outbreak-investigation.ts) as a dedicated `entry_type: 'signal_link'` row. The fence already refuses to count the drizzle-interpolated form `${caseEvents.payload}->>'key'` for exactly this reason ('db/schema.ts has other JSONB payload columns... caseEvents...'), but both reads here are RAW SQL with a `ce.` alias inside a correlated subquery, which matches its `alias.payload->>'key'` shape and cannot be told apart from a pet_events alias by text. Two sites, one key: the signals feed (fetchSurveillanceSignals, so a row can link to the investigation it already opened) and the untriaged counter (fetchVigilanciaMetrics). HOW TO RETIRE THIS ENTRY rather than let it rot: teach the fence to resolve a raw alias to its FROM/JOIN table instead of assuming pet_events. Until then this is a scope gap in the fence, recorded here, not a justified ghost read - the key is written, and the writer is named above."
}
