// `@dim/contract/icons` — the shared icon-name vocabulary, framework-free.
//
// One module today. The subpath exists so the Expo app's very first render
// does not have to load the event vocabulary or the zod schemas to name a
// glyph — the same reasoning `./tokens` records.
export * from "./pet-profile-icons.ts";
