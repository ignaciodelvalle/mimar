# Conventions canon — End-to-end (Playwright)

> Snapshot: `d7dbf25f7` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-25
> Verified against code on 2026-09-25 by canon v4 + blind calibration · Status: reviewed
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

[← canon index](../conventions-canon.md) · scope `e2e` · 29 rules (2 ENFORCED, 2 PARTIAL, 25 UNENFORCED).

| Id | Rule | Source | Status | Enforcer | Basis |
| --- | --- | --- | --- | --- | --- |
| CANON-457 | The e2e Playwright job's Upload report step must run on always(), not failure(), because a timeout-minutes kill reports as cancelled and failure() is false for a cancelled job. | `.github/workflows/ci.yml:1041` | UNENFORCED | `.github/workflows/ci.yml:1040-1050` | .github/workflows/ci.yml:1046 `if: always()` on the Upload Playwright HTML report step IS the mechanism; editing it to `failure()` makes nothing exit non-zero. No fence reads workflow step conditions — check-scheduled-fence-refs only parses `schedule:`, `ref:`, `uses:` and `audited:`. *Self-executing declaration, true at this SHA; the reasoning is in the comment at :1041-1045. The v3 basis says outright 'The workflow line IS the mechanism' and still recorded ENFORCED.* |
| CANON-458 | e2e-nightly must check out the branch staging is deployed from (main), because a schedule trigger checks out only the default branch by default. | `dim-interno:.github/workflows/e2e-nightly.yml` | UNENFORCED | — | e2e-nightly.yml moved to the private companion repository on 2026-09-24 with every staging-facing scheduled job (dim-interno:.github/workflows/e2e-nightly.yml). It checks out `repository: ignaciodelvalle/mimar`, `ref: main` — a literal held by hand. scripts/check-scheduled-fence-refs.ts scans only this repository's .github/workflows/ (scheduledWorkflows, :436-442), so nothing that can fail reads the file any more — the declaration IS the mechanism. *Was fenced by lint:sched-refs while the workflow lived in this repository. No private-enforcer convention exists in the canon, so the row is marked by what this repository can prove.* |
| CANON-459 | Playwright e2e is not part of pnpm verify; it runs only as CI's own job and nightly against staging. | `CLAUDE.md:47` | UNENFORCED | — | True on the live tree (the verify string at package.json:79 contains no playwright step; __tests__/db-reachability.ts:76 excludes e2e from the vitest walk) but neither direction can FAIL: adding `playwright test` to verify breaks nothing, and an e2e spec dropped into the vitest walk is skipped, not flagged. *No scripts/check-e2e-*.ts exists in the repo — confirmed by listing scripts/ and filtering for e2e/playwright.* |
| CANON-460 | pnpm test:e2e:smoke (public-smoke + crisis-owner-lost-flow specs) is expected before pushing feature ranges that touch public or lost-mode surfaces (/p/[token], /encontre, /perdidas, mark-lost, landing). | `CONTRIBUTING.md:84-89` | UNENFORCED | `package.json:133` test:e2e:smoke | package.json:133 defines test:e2e:smoke as exactly the two named specs, so the instrument exists and is correct — but 'expected before pushing' is a human obligation with no gate: nothing detects that a push touched a public/lost surface without it. *Not upgraded to PARTIAL: PARTIAL-by-wiring applies where the CHECK exists and only the wiring is missing; here the check itself (did this range touch those surfaces?) does not exist.* |
| CANON-461 | e2e tests must run against the built Next.js app (next build && next start), never the dev server. | `e2e/README.md#e2e--playwright` | UNENFORCED | `playwright.config.ts:6,18,116` | playwright.config.ts:116-119 is a webServer declaration — `pnpm build && pnpm start` (or `pnpm start` under NEXT_BUILT). Editing that command to `pnpm dev` fails nothing: no scripts/check-* fence and no test reads any playwright config (searched scripts/, __tests__/, .github/). The declaration IS the mechanism, so it is true at this SHA and unguarded. *self-executing declaration, true at this SHA. The basis holds ENFORCED on 'the config IS the mechanism for every local and CI run of this config' — the same shape as the CANON-002 calibration case (P10). ci.yml's e2e job builds and sets NEXT_BUILT, but no step exits 1 on the condition the rule names, so R2's CI exception does not apply.* |
| CANON-462 | e2e specs must never hardcode DB ids or tokens — real pet/org tokens are discovered at runtime from a live page (e.g. `/adoptar`, `/perdidas`), and the test skips cleanly when the seed has no matching fixture. | `e2e/README.md#layout`<br>`e2e/README.md#conventions` | UNENFORCED | — | The nearest fence, lint:uuid (scripts/check-uuid-literals.ts), scopes itself to app/, components/, lib/, src/ (:13) and explicitly exempts tests and fixtures (:14-16) — so e2e/ is outside its corpus twice over. Nothing scans e2e specs for literal tokens or ids. *Note the tension with CANON-479, which forbids skipping on a seeded fixture; the README's own 'skips cleanly' clause is the one CANON-468/469/470 narrow to environment-branching.* |
| CANON-463 | chapas.spec.ts must clean up its own lote via deleteTagsByLotePrefix against the local DB only, since the product has no "delete a chapa" flow. | `e2e/README.md#layout` | UNENFORCED | — | A per-spec obligation with no fence: nothing asserts chapas.spec.ts calls the cleanup helper, and nothing prevents the helper being pointed at a non-local target. There is no scripts/check-e2e-*.ts in the repo at all. |
| CANON-464 | api-v1-auth-refusals.spec.ts must sign in nowhere — every case must be a refusal so the suite never spends the auth_login_email budget. | `e2e/README.md#layout` | UNENFORCED | — | A property of one spec's contents. No fence asserts the absence of a loginAs call in that file, and the rate-limit budget it protects is only observable as a downstream flake. |
| CANON-465 | The demo/ narrated recording scripts must not be run as part of a normal e2e pass. | `e2e/README.md#layout` | UNENFORCED | `playwright.config.ts:63`<br>`playwright.staging.config.ts:121` | playwright.config.ts:63 `testIgnore: ["demo/**", "perf/**"]` is a config declaration; :57-62 says four configs carry it and asks the reader to "keep them in sync", recording that local3000 was missed in a previous pass. No fence compares the configs and no test reads them, so deleting the line fails nothing directly. *self-executing declaration, true at this SHA. The basis reaches ENFORCED from the same two config lines while its own notes concede 'Nothing fences that synchronisation' — that concession is the verdict under R2. Removing the line would eventually blow globalTimeout (:82) in CI, but that is a consequence of run length, not a check of this predicate.* |
| CANON-466 | New spec files under e2e/ (outside demo/ and perf/) automatically join the nightly staging pass — no workflow file change is needed. | `e2e/README.md#layout` | UNENFORCED | `playwright.staging.config.ts:120-121` | playwright.staging.config.ts:120-121 declares testDir './e2e' + testIgnore ['demo/**','perf/**'] and dim-interno:.github/workflows/e2e-nightly.yml runs that config on cron (moved to the private companion repository on 2026-09-24 with every staging-facing scheduled job). The auto-inclusion IS the declaration; widening testIgnore fails nothing. No fence reads either file (rg over scripts/, __tests__/ = 0; check-scheduled-fence-refs.ts has no testDir/testIgnore subject). *self-executing declaration, true at this SHA; the previous basis reaches the same description ('the config IS the mechanism') but rated it ENFORCED* |
| CANON-467 | CI's fresh-seed DB must be the judge of a passing suite, never a developer's laptop DB, which accumulates state that makes local runs unreliable. | `e2e/README.md#conventions` | UNENFORCED | — | A rule about which verdict a human should believe. Nothing can fail on it — a green local full-suite run is indistinguishable from any other, and no gate refuses a locally-produced e2e report as evidence. *Structurally supported (CI runs `pnpm exec supabase start` + bootstrap before the e2e job, ci.yml:1034 area) but that supports the rule rather than enforcing it.* |
| CANON-468 | A fixture-gated check must branch on the ENVIRONMENT, never on the data — test.skip(rowsFound === 0, ...) is disallowed; use the e2e/_seed-profile.ts seed profile instead. | `e2e/README.md#conventions` | UNENFORCED | `__tests__/e2e-seed-profile.test.ts:13-33,35-60` | __tests__/e2e-seed-profile.test.ts:13-68 only exercises resolveSeedProfile/seedFixtureVerdict on literal args; nothing scans e2e/*.spec.ts. The banned shape is live and unfenced: e2e/print-surfaces.spec.ts:53,76,87 test.skip((await petLink.count()) === 0, ...) and e2e/race-battery.spec.ts:74 — verbatim the construct the rule disallows. *v3 reached the same gap ('nothing scans e2e/*.spec.ts for the BANNED shape') but capped at PARTIAL. R7's second branch: the rule's predicate IS adoption of the seed profile, so the helper's own test proves nothing about callers and no fence mandates the single door.* |
| CANON-469 | Under the bootstrap seed profile (CI default), a missing fixture must SKIP with a reason naming the coverage hole. | `e2e/README.md#conventions` | PARTIAL (adoption-unfenced) | `__tests__/e2e-seed-profile.test.ts:14,35,59` | __tests__/e2e-seed-profile.test.ts:59-68 asserts seedFixtureVerdict(0, ..., 'bootstrap') returns verdict 'skip' with a reason containing 'NO COVERAGE' — the helper's behaviour is genuinely pinned. No caller is forced through it: e2e/print-surfaces.spec.ts:53,76,87 and e2e/race-battery.spec.ts:74 gate on data with no profile at all. *R7 first branch — the rule states the mechanism's BEHAVIOUR, adoption implied, so PARTIAL not ENFORCED. v3 called this ENFORCED while calling the sibling CANON-479 adoption-unfenced on the same file; the gap is the same one. The bootstrap case pins only the 'NO COVERAGE' marker — the fixture/unmeasured naming is pinned on the 'full' branch (:46-53), not here.* |
| CANON-470 | Under the full seed profile (staging/nightly), a missing fixture must FAIL. | `e2e/README.md#conventions` | PARTIAL (adoption-unfenced) | `__tests__/e2e-seed-profile.test.ts:18,41` | __tests__/e2e-seed-profile.test.ts:41-53 asserts seedFixtureVerdict(0, ..., 'full').verdict === 'fail' and that the reason names both the fixture ('/perdidas') and what went unmeasured ('the axe audit'). Callers escape: e2e/print-surfaces.spec.ts:53,76,87 skip on (await petLink.count()) === 0 regardless of profile, so under 'full' a missing fixture there still skips instead of failing. *Helper behaviour fully pinned in both directions (:36-39 runs when present); the rule's effect on the corpus is not. Same R7 first-branch reasoning as CANON-469.* |
| CANON-471 | e2e assertions must be deterministic: web-first assertions only, no arbitrary sleep, no reliance on Date.now() or randomness. | `e2e/README.md#conventions` | UNENFORCED | — | No fence scans e2e specs for waitForTimeout, Date.now() or Math.random(). biome's rules apply to the repo but none of them bans these; there is no scripts/check-e2e-*.ts. *A cheap, high-value fence candidate: the three banned shapes are all grep-able literals in a bounded corpus.* |
| CANON-472 | loginAs must call resetAuthLoginRateLimits() before every real sign-in; any spec with its own private login must do the same. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | — | The helper exists (e2e/demo/_db-cleanup.ts, used by e2e/demo/_helpers.ts) but nothing enforces the 'any spec with its own private login' clause: exactly one spec calls it directly today (e2e/owner-ia-p6.spec.ts), and a new private login would be caught only as a rate-limit flake. |
| CANON-473 | The anonymous denuncia rate limit is 1/min per IP; e2e specs must pass a unique uniqueIp() per walk to avoid tripping it. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | — | No fence requires a uniqueIp() argument at a denuncia call site in e2e specs. The limit itself lives in the product's rate-limit config; the spec-side obligation has no mechanism. *number carried from the doc; the live rate-limit constant was NOT re-read in this pass, so treat 1/min as docSays until confirmed against lib/rate-limit.ts.* |
| CANON-474 | Against a staging target, uniqueIp() is not honoured (Vercel overwrites x-real-ip); protection must instead be structural — sign in once per account per worker and space out repeated anonymous writes. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | — | A target-dependent authoring rule. Nothing distinguishes a local-only technique used against staging; the failure appears as a nightly red, which is the symptom the rule exists to explain rather than a fence. |
| CANON-475 | e2e specs must never assert a 404 by HTTP status — assert the branded-not-found surface (testid) instead, since a streaming route can answer 200 with a denied page after headers already went out. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | `__tests__/e2e-page-identity.test.ts:58-106` | __tests__/e2e-page-identity.test.ts:58-106 pins NOT_FOUND_HEADING against every app/**/not-found.tsx and that BrandedNotFound carries the testid (:102-104); no line inspects e2e spec source for a status-404 assertion. Live counterexamples pass green: e2e/api-v1-public-contract.spec.ts:148 expect(res.status()).toBe(404), e2e/cross-tenant-isolation.spec.ts:126 toContain(status) over [200,404]. *The v3 basis rests on the instrument being fenced. The rule's predicate is a PROHIBITION on spec authors, not the instrument's behaviour, so R7's second branch applies: the only walker over the e2e corpus is e2e-sign-in-route.test.ts:45, and it bans the /login literal.* |
| CANON-476 | e2e specs must never wait on a post-action URL — assert the outcome the mutation produces, since useActionRedirect's window.location.assign drops often enough to matter. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | — | No fence scans e2e specs for waitForURL. lint:action-redirect (scripts/check-action-redirect.ts, in verify) governs the PRODUCT side — how server actions redirect — not how a spec waits on one. |
| CANON-477 | e2e dates must be computed as ART-local via Intl.DateTimeFormat with America/Argentina/Buenos_Aires, never via toISOString(), because the server rejects future dates in Argentina time. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | — | lint:timezone (scripts/check-timezone-*.ts) is in the verify chain but its corpus is product source, not e2e/. Nothing scans specs for toISOString() used as a date input. *Same family as CANON-202/203 (host clock vs the authoritative clock) and equally unfenced; a single 'clock discipline' fence could cover all three corpora.* |
| CANON-478 | e2e specs must gate every DOM read behind an auto-retrying expect() first — Locator.count() does not auto-retry and must never be the sole basis for a test.skip(). | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | — | No fence scans e2e specs for `.count()` feeding a test.skip. This is the same banned shape as CANON-468's second clause and is unfenced for the same reason: no scripts/check-e2e-*.ts exists. |
| CANON-479 | e2e skips must be reserved for genuine ENVIRONMENTAL conditions, never for a fixture the repo's own seed guarantees — a guaranteed fixture must be asserted, so a missing one turns red. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | `__tests__/e2e-seed-profile.test.ts:35-60` | __tests__/e2e-seed-profile.test.ts:35-68 judges only the helper's return values on literal arguments. e2e/print-surfaces.spec.ts:53 skips with 'No adoptable pets seeded — no credential to print.' — a fixture the repo's own seed provides — and nothing turns red; no fence reads e2e spec source for skip predicates. *v3 named the same adoption gap and capped at PARTIAL. The rule's predicate is a directive to spec authors ('skips are for environmental conditions; if the seed guarantees it, ASSERT it'), i.e. adoption proper → R7 second branch, and a live violating skip exists. Blind calibration dissent (1 of 18 sampled rows): the blind reader derived PARTIAL (adoption-unfenced) where v4 says UNENFORCED. v4's verdict stands — the rule's predicate is adoption itself and no production path is forced through the helper.* |
| CANON-480 | Every remaining e2e skip must print its reason, since CI's e2e job uses --reporter=list,github,html and only "list" surfaces skip reasons in the job log. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | `.github/workflows/ci.yml:1034` | .github/workflows/ci.yml:1034 `pnpm e2e --reporter=list,github,html` is a config string and checks nothing about skips. The rule's predicate is adoption ('every remaining skip prints its reason') and only the helper is pinned: __tests__/e2e-seed-profile.test.ts:66-67 asserts the seed-profile outcome carries a reason, while each spec is free to call test.skip with an empty string (e2e/print-surfaces.spec.ts:185 passes "" on its non-skip branch). *No scripts/check-*.ts scans e2e/ for skips without a reason. The v3 basis called the reporter half 'the enforced half', but a reporter flag is R2 config: with the obligation half unfenced and the config half not an enforcer at all, there is no covered part left to make this PARTIAL.* |
| CANON-481 | Multi-step wizard specs must walk every step and click the actual last-step submit button, never an earlier step's button of the same name. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | — | An authoring rule about locator choice inside a spec. No fence models wizard step structure, and the failure mode it prevents (a wait on an element that step never renders) surfaces only as a timeout. |
| CANON-482 | playwright.config.ts must set actionTimeout: 15s so an unbounded Playwright action fails legibly instead of silently eating the whole test budget. | `e2e/README.md#hard-won-rules-2026-0803-04-retiring-a-standing-ci-red` | UNENFORCED | `playwright.config.ts:92,103` | playwright.config.ts:103 `actionTimeout: 15_000` (reason at :92) is a value the runner consumes; nothing can FAIL on it being edited to 0. rg over scripts/ and __tests__/ for 'playwright.config' returns zero — no fence or test pins it. The previous basis says the same thing ('the config line IS the mechanism'), which is the P10/CANON-002 test for a self-executing declaration. *self-executing declaration, true at this SHA (15_000 live)* |
| CANON-512 | The public-credential smoke gate's PII assertion must be able to fail: findPiiLeaks must catch a leaked display name, email, and phone — including a phone reformatted into a tel: href — case-insensitively; and NOT_FOUND_HEADING must match every real app/**/not-found.tsx boundary while not firing on ordinary page copy. | `__tests__/e2e-page-identity.test.ts:58-131,165-201` | ENFORCED | `__tests__/e2e-page-identity.test.ts` | __tests__/e2e-page-identity.test.ts:170-204 catches a leaked displayName, email, literal phone, a tel: href reformat (:188-199, match 'digits') and case-insensitivity (:201-204); :55/:66-78 loops the REAL app/**/not-found.tsx files with a non-vacuity floor at :63 (>=5), and :111-135 pins that ordinary copy does not match. *Anti-vacuity explicit on both halves: :218-235 reproduces the hardcoded-persona bug; :131-134 forbids an empty needle. v3 basis was file-existence only; the predicate checks out on the lines.* |
| CANON-513 | The sign-in route must live in exactly one place, and the old route's 308 redirect must stay in place. | `__tests__/e2e-sign-in-route.test.ts:65,125,148` | ENFORCED | `__tests__/e2e-sign-in-route.test.ts` | __tests__/e2e-sign-in-route.test.ts:95-103 fails on any e2e/**/*.ts CODE line matching /(?<![\w-])\/login\b/ outside the owning module (:30,:45), with a non-vacuity floor at :70 (>20 files) and a teeth test at :106-122 pinning the three live spellings; :148-158 reads app/(auth)/login/page.tsx and requires permanentRedirect + SIGN_IN_PATH + URLSearchParams. *Scope is e2e per the row. The ban is on the route SEGMENT, not on enumerated spellings, and codeLines(:58-63) keeps prose free while still catching trailing-comment code.* |
| CANON-514 | Every Playwright config in the repo must exclude demo/ and perf/ — the exclusion is per-config and playwright.local3000.config.ts was once missed, inheriting demo/'s 116 minutes into a local full-suite run. | `playwright.config.ts:51,60-63` | UNENFORCED | `playwright.config.ts` | playwright.config.ts:51,60-63 — cited by the reclassification pass as an enforcer no draft-1 canon row stated; status/wired as reported by the reclassifying group. *added in v2 from unmapped enforcer* |
