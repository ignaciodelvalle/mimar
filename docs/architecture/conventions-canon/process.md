# Conventions canon — Process, CI and the gate chain

> Snapshot: `d7dbf25f7` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-26
> Verified against code on 2026-09-26 by canon v4 + blind calibration · Status: reviewed
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

[← canon index](../conventions-canon.md) · scope `process` · 153 rules (20 ENFORCED, 20 PARTIAL, 113 UNENFORCED).

| Id | Rule | Source | Status | Enforcer | Basis |
| --- | --- | --- | --- | --- | --- |
| CANON-001 | CI must run on push to main, develop, and every integration/** branch, and on pull_request to main/develop, not only on push to main. | `.github/workflows/ci.yml:3` | UNENFORCED | `.github/workflows/ci.yml:8-12` | .github/workflows/ci.yml:8-12 is the `on:` trigger list itself (push branches [main, develop, integration/**]; pull_request [main, develop]). No fence reads it: check-ci-lint-parity.ts:20 only looks for literal `pnpm lint:<name>` tokens, check-scheduled-fence-refs only handles `schedule:` workflows. Deleting integration/** fails nothing. *Self-executing declaration, true at this SHA. The v3 basis says so in its own words ('The config line IS the mechanism', 'no fence fails if the list is edited') and still recorded ENFORCED — the same P10 error the judge caught on CANON-002.* |
| CANON-002 | The pnpm/action-setup step must carry no explicit version input, deriving the pnpm version solely from package.json's packageManager field. | `.github/workflows/ci.yml:24` | UNENFORCED | `.github/workflows/ci.yml:23-29` | [judge, sample re-refute] .github/workflows/ci.yml:23-29 and :693-699 — `uses: pnpm/action-setup@v4` with no `version:` input, and nothing else. rg over scripts/ and __tests__/ for `packageManager` / `pnpm/action-setup` finds only __tests__/scheduled-fence-refs.test.ts:60,71, which are inline YAML fixtures for the ref-pin fence, not a version-input check. — P1 / P10 — a self-executing declaration ('file F must say X') is UNENFORCED unless a fence checks the declaration; the file being the runtime is not an enforcer, and the P10 exception (a CI step that itself exit-1s on the condition) does not apply: no step fails when a `version:` input is added. The action's own failure only fires when the two strings DIFFER, so a matching explicit version violates the rule and passes. *Same self-executing-config class as CANON-001. mobile-export-nightly.yml repeats the pattern.* |
| CANON-003 | CI's dependency-audit job must fail on HIGH or CRITICAL advisories, with GHSA-fx2h-pf6j-xcff excluded as dev-only. | `.github/workflows/ci.yml:454` | ENFORCED | `.github/workflows/ci.yml:396-454` | .github/workflows/ci.yml:454 `run: pnpm audit --audit-level=high --ignore GHSA-fx2h-pf6j-xcff` — the step's OWN command exits non-zero on any HIGH/CRITICAL advisory other than the named GHSA, which is the R2 CI exception rather than a declaration. *ci.yml:433-448 gives an opt-in bypass ([skip-audit] token / ci:skip-audit label); an explicit labelled escape, not a silent one.* |
| CANON-004 | CI's DB-backed fence steps must each run as its own step with an explicit if guard so one red fence does not skip the rest or starve the vitest suite of a chance to report. | `.github/workflows/ci.yml:774` | UNENFORCED | `.github/workflows/ci.yml:817-886` | .github/workflows/ci.yml:817-886 is the per-step shape itself (each fence its own step + `if: ${{ !cancelled() && steps.seed.outcome == 'success' }}`). Nothing checks that shape: scripts/check-ci-lint-parity.ts:20 asserts only that a literal `pnpm lint:<name>` token appears somewhere in the file, so re-chaining the five fences into one `a && b && c` step passes it. *Self-executing declaration, true at this SHA. The v3 basis already recorded that lint:ci-parity 'guarantees each lint NAME is invoked somewhere in ci.yml, not the per-step structure' — which is the reason this is not an enforcer of THIS rule. The workflow's own header at :808 asks humans to keep the `if:`; an instruction, not a fence.* |
| CANON-005 | CI's test job must run pnpm test:verified (not test:coverage) so the suite-coverage verdict runs even when vitest's own exit code lies. | `.github/workflows/ci.yml:893` | UNENFORCED | `.github/workflows/ci.yml:883-905`<br>`scripts/run-verified-suite.ts` | .github/workflows/ci.yml:905 `run: pnpm test:verified -- --coverage` is the declaration of which command the job invokes. Nothing exits 1 when it is swapped back to test:coverage: rg for `test:verified\|test:coverage` across scripts/check-ci-lint-parity.ts and scripts/check-scheduled-fence-refs.ts returns zero hits, and lint:ci-parity matches only `pnpm lint:<name>` tokens. *run-verified-suite.ts exits 1 on a bad SUITE, which is a different subject than which command CI chose to run. ci.yml:893-904 is prose explaining the choice, not a check of it.* |
| CANON-006 | The CI test job's timeout must be set with real headroom above measured worst-case duration, and raising it must be a last resort, not a routine bump. | `.github/workflows/ci.yml:680` | UNENFORCED | `.github/workflows/ci.yml:641` | [judge, sample re-refute] .github/workflows/ci.yml:641 `timeout-minutes: 30` with the 2026-07-31 narrative at :665-683. Nothing measures a worst-case duration, nothing compares it to the budget, and nothing can fail when the number is raised — the value is only read by the runner that kills the job. — P1 / P10 + rulebook 2 — a config declaration is not an enforcer; both of the rule's clauses ('real headroom above measured worst-case', 'raising it is a last resort') are unfalsifiable in-tree. *Live/doc divergence inside one file: the comment at ci.yml:665 still says 'timeout-minutes raised from 15 -> 20' while the live value at :641 is 30.* |
| CANON-007 | CodeQL must NOT pin an explicit ref on its schedule trigger, so its weekly scan intentionally scans the default branch (main) for correct SARIF attribution. | `.github/workflows/codeql.yml:10` | ENFORCED | `scripts/check-scheduled-fence-refs.ts:106-117`<br>`.github/workflows/codeql.yml:34-42`<br>`__tests__/scheduled-fence-refs.test.ts:353-355` | scripts/check-scheduled-fence-refs.ts:106-117 lists codeql.yml in REF_EXEMPT with the SARIF-attribution argument; refFindings :451-458 fails if that file ever pins a ref ('is listed in REF_EXEMPT but now pins a ref'), exemptionFindings :478-487 fails if it stops being a scheduled workflow, and __tests__/scheduled-fence-refs.test.ts:353-355 fails if the reason falls to 40 chars or fewer. Findings exit 1 at :745-757; lint:sched-refs is in verify (package.json:76,79). *The exemption is checked in both directions, so 'must NOT pin a ref' is the failing condition.* |
| CANON-008 | CodeQL must run with the security-extended query set on push to main/develop/integration/** and pull_request, plus a weekly schedule. | `.github/workflows/codeql.yml:60` | PARTIAL (subset) | `.github/workflows/codeql.yml:34-42`<br>`.github/workflows/codeql.yml:57-64` | scripts/check-scheduled-fence-refs.ts:106-117 lists codeql.yml in REF_EXEMPT and :482-485 fails when an exempt entry is no longer a scheduled workflow — so the weekly cron (.github/workflows/codeql.yml:39-42) IS fenced by lint:sched-refs (in the verify chain, package.json:76/79). The other two conjuncts are not: rg for `security-extended` and `queries:` across scripts/ and __tests__/ returns nothing, so codeql.yml:64 and the push/pull_request branch lists at :34-38 are bare declarations. *Uncovered part: `queries: security-extended` (.github/workflows/codeql.yml:64) and the trigger lists (:35-38). The v3 basis's stated ground — 'Every clause of the rule is a literal in the file' — is P10, not enforcement.* |
| CANON-009 | panorama-qa-nightly and other scheduled workflows must check out the literal branch main (not an interpolated vars.* value), since an empty ref: silently falls back to the default branch. | `dim-interno:.github/workflows/panorama-qa-nightly.yml` | PARTIAL (subset) | `scripts/check-scheduled-fence-refs.ts:85`<br>`scripts/check-scheduled-fence-refs.ts:447-465` | scripts/check-scheduled-fence-refs.ts:461-467 — for every non-exempt scheduled workflow IN THIS REPOSITORY, each checkout step must satisfy step.ref === DEPLOY_REF ('main', :85); a missing ref or any other literal (a `${{ vars.X }}` interpolation included) becomes a finding and exits 1 at :745-757. __tests__/scheduled-fence-refs.test.ts:380-390 asserts the set of pinned refs across the real tree is exactly [DEPLOY_REF] and non-empty (today: mobile-export-nightly.yml). Uncovered part: panorama-qa-nightly.yml, the workflow the rule names, moved to the private companion repository on 2026-09-24 with every staging-facing scheduled job (dim-interno:.github/workflows/panorama-qa-nightly.yml); its `ref: main` literal is held by hand there and no fence reads that repository. *Covered by the same predicate as every public scheduled workflow; the named subject is outside the scan since the move.* |
| CANON-010 | The panorama QA nightly report-only scripts must never turn the run red on a finding; only the surrounding setup steps (bootstrap, seed, build, start) may fail the job. | `dim-interno:.github/workflows/panorama-qa-nightly.yml` | UNENFORCED | — | No enforcer, and the rule is FALSE on the live tree: scripts/qa-panorama-chaos.ts:848 exits `summary.passed ? 0 : 1`, and the chaos step of dim-interno:.github/workflows/panorama-qa-nightly.yml (moved to the private companion repository on 2026-09-24 with every staging-facing scheduled job) runs it with no `\|\| true` and no continue-on-error — a failed chaos round turns the nightly red. |
| CANON-012 | Staging health must be polled by an external, checkout-free curl+jq job every ~15 minutes asserting HTTP 200 and status "ok". | `.github/workflows/staging-health.yml:8` | ENFORCED | `.github/workflows/staging-health.yml:37-42`<br>`.github/workflows/staging-health.yml:58-80` | .github/workflows/staging-health.yml:66-79 — the health-poll step curls the endpoint into body.json (:69), reads `.status` with jq (:75), and `exit 1` at :78 when HTTP != 200 or status != ok; the job has no checkout (:58-80). The schedule's existence is separately fenced: staging-health.yml is in ALERT_EXEMPT and check-scheduled-fence-refs.ts:482-485 fails if it stops being a scheduled workflow. *Only the exact `*/15` cadence (:41) is a bare cron declaration; the rule hedges it as '~15 minutes', and the assertion half is a real failing command, so this is not the P10 shape.* |
| CANON-013 | The daily synthetic-monitor job must skip (green, not failed) when the STAGING_URL secret is absent, rather than hard-failing. | `dim-interno:.github/workflows/staging-synthetic.yml` | UNENFORCED | — | The synthetic-monitor job moved to the private companion repository on 2026-09-24 with every staging-facing scheduled job (dim-interno:.github/workflows/staging-synthetic.yml; it signs in on staging, so it needs E2E_STAGING_PASSWORD). Its guard step reads secrets.STAGING_URL, sets outputs.run, and every later step carries `if: steps.guard.outputs.run == 'true'` — the declaration IS the mechanism. Nothing in this repository reads that file, and nothing exits non-zero if the guard is replaced by a hard failure. *Self-executing declaration, true in dim-interno at this date. The public staging-health.yml keeps only the checkout-free health poll (CANON-012).* |
| CANON-014 | Any AI agent orienting on a public route, token, or PII field must load the Privacidad y manejo de datos section first. | `AGENTS.md:47` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-015 | Two AI agents (Cowork and Claude Code) must never work this repo in parallel; sessions are serialized to one at a time. | `AGENTS.md:56` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-016 | Claude Code's live, verified check of repo/test/build state wins over Cowork's assertion whenever the two disagree. | `AGENTS.md:58` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-017 | Cowork must not assert git/test/environment state as settled, and must flag environment problems as checkable claims rather than 'fixing' them from its own sandbox. | `AGENTS.md:59` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-018 | Every handoff between agents must be stamped with the branch and HEAD SHA it was written against, separating DONE (with commit SHA) from TODO (unverified), backed by a SHA, file:line, or pasted command output. | `AGENTS.md:60` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-019 | The receiver of a handoff (Claude Code) must verify every claim against the live repo before acting — a handoff is input, not truth; never trust prose alone. | `AGENTS.md:60`<br>`dim-interno:docs/design/handoffs/README.md:14-16` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-020 | Agents may produce artifacts, but Ignacio must authorize anything that hits production or external services or is hard to reverse (remote migrations, deploys, pushing to origin, opening PRs, dashboard/account toggles). | `AGENTS.md:62` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Adjacent partial machinery exists for the DB half only: scripts/check-function-parity.ts:25-31,179-196 refuses a remote DATABASE_URL without --allow-remote. It constrains scripts, not agents.* |
| CANON-021 | No throwaway prototypes; every milestone shipped must be usable. | `AGENTS.md:145` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-022 | The repo must be maintained publishable-at-any-moment: no secrets, no plaintext PII, clean history. | `AGENTS.md:146` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Since L-24 the 'no secrets' half is fenced for tracked files at HEAD by CANON-532 (lint:secrets); history is not scanned, so 'clean history' is still applied by hand and this row's whole-rule verdict was not re-graded here. The 'no plaintext PII' half is covered independently by the DNI-hash rules and the profiles_institutional_no_pii DB CHECK (CANON-168), not by anything about repo publishability. Since R9 (2026-09-24) the 'publishable' half is additionally fenced by CANON-539 (lint:public-boundary): private doc categories, private path shapes, personal mailboxes, credential pairs and valued env files in tracked files at HEAD.* |
| CANON-023 | Aggregation must be a first-class architectural concern, preserving privacy via k-anonymity and requiring opt-in for granular contribution. | `AGENTS.md:148` | PARTIAL (subset) | `src/modules/panorama/infrastructure/cube-builder.ts:15-25`<br>`__tests__/govt-dashboards.test.ts`<br>`__tests__/surveillance-province-choropleth-parity.test.ts` | src/modules/panorama/infrastructure/__tests__/province-kanon.test.ts:91-143 asserts sub-k cells are emitted with count null, the boundary is >= k, and complementary suppression fires; lib/metrics/program-health.ts:421/446 applies K_ANON_MIN. cube-builder.ts:15-18 states no sub-k value enters the store. Nothing found enforces the rule's second limb, opt-in for granular contribution. Since pilot/t3-r3: __tests__/surveillance-province-choropleth-parity.test.ts feeds the same LocalityCaseCount rows through foldCasesPerProvince and aggregateChoroplethData and asserts they agree, including under suppressSmallCells/complementarySuppress, so the /gob/vigilancia choropleth's hand-kept fold cannot silently drift from the k-anonymity-suppressing implementation it duplicates. *k-anonymity limb is covered by db-project tests; the opt-in limb is architectural prose. Same conclusion as the previous basis, reached from a different test file.* |
| CANON-024 | DIM is the stable code/schema/token identifier and must never be renamed; MiMAR is the user-facing brand shown in app metadata, copy, and marketing. | `AGENTS.md:1275` | PARTIAL (subset) | `scripts/check-brand-casing.ts:31-34`<br>`scripts/check-brand-casing.ts:140-143` | scripts/check-brand-casing.ts:140 (wrong-cased brand) and :186/:324-331 (bare DIM codename) fail on the user-facing-brand half over {app,components,lib,packages,src}/**/*.ts(x). Nothing fails on the other half — 'DIM is the stable code/schema/token identifier, never renamed': lib/infra/publicToken.ts:68 generatePrefixedToken("DIM") is unpinned, and packages/contract/src/input/__tests__/transfer.test.ts:33 only uses a literal fixture. *The v2 basis reached ENFORCED from the same two fence rules but never asks what fails if the DIM identifier is renamed — I searched for a token-prefix pin and found none. The rule text also spells the brand 'MiMAR', which the fence itself FAILS on (canonical casing is miMAR, :143): that sentence is stale, the fence is the live authority.* |
| CANON-025 | Any new UI exception to these conventions must be written into the PR description and linked from the relevant design critique doc, never left as a silent, un-tracked exception. | `AGENTS.md:1404` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-027 | Do not remove the globalSetup entry from vitest.config.ts; its teardown is an intentional no-op, but the entry still sets VITEST=true before the first db/index.ts import, forces DATABASE_URL to the local stack, and sweeps eventless pet rows. | `AGENTS.md:1500` | UNENFORCED | `vitest.config.ts:124`<br>`__tests__/global-setup.ts:115-126` | vitest.config.ts:124 globalSetup: ["./__tests__/global-setup.ts"] is the declaration itself and __tests__/global-setup.ts:115-126 its implementation, not a check on it; an rg for globalSetup across ts/tsx/mjs/yml/json finds no assertion outside the config. setup() calls its own DATABASE_URL forcing 'Belt-and-suspenders … Per-file setup.ts handles workers' (:119-120), so deleting the entry fails nothing deterministically. *Followed the previous basis's lead and opened scripts/check-spine-integrity.ts:188-189 (lint:spine, in verify): it does fail on pets with no pet_events row, so eventless debris left by an aborted run surfaces there. That is a downstream, run-dependent consequence of the missing sweep, not a check on the entry, and the VITEST=true and DATABASE_URL limbs stay unfenced — verdict unchanged.* |
| CANON-028 | The set of `lint:*` and `verify:*` gates `pnpm verify` chains must equal the set that actually runs somewhere in the CI workflow YAML, checked by a literal `pnpm lint:<name>` token match — a comment merely mentioning a lint does not count as running it. | `AGENTS.md:1552`<br>`scripts/check-ci-lint-parity.ts:16`<br>`scripts/check-ci-lint-parity.ts:39`<br>`__tests__/check-ci-lint-parity.test.ts:1-9,70-119` | PARTIAL (declared-limit) | `scripts/check-ci-lint-parity.ts:56-82`<br>`__tests__/check-ci-lint-parity.test.ts:100-119` | scripts/check-ci-lint-parity.ts:79-82 computes gatesInVerify minus gatesInWorkflow and exits 1 (:112-129), comments stripped first (:69-72), with anti-vacuity guards at :89-96 and :101-108. One direction only, and declared as intended: __tests__/check-ci-lint-parity.test.ts:109-111 pins 'ignores gates CI runs that verify does not — parity is one-directional', so the sets are never proven EQUAL as the rule states. *Draft said partialShape subset from the script alone; opened the basis-cited test line :109 and it declares the one-directionality deliberately, which is the declared-limit shape. Status unchanged. Also reads exactly one workflow file (:48).* |
| CANON-029 | No test may assert an expected value re-derived from the same code/template path under test; assert against an independently-stated expected value. | `AGENTS.md:1589`<br>`docs/agents/README.md:81-88` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-030 | Always read AGENTS.md first in a new session. | `AGENTS.md:1759` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-031 | Never break a core principle without explicit user agreement and a corresponding update to AGENTS.md. | `AGENTS.md:1761` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-032 | If asked to 'fix' historical event data, push back; the correct fix is a correction event, never a mutation. | `AGENTS.md:1762` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *The mutation itself is blocked by the append-only DB triggers (db/triggers.sql, guarded by scripts/check-function-parity.ts at ci.yml:853-881). What has no enforcer is the AGENT-BEHAVIOUR clause this row states ('push back and write a correction event').* |
| CANON-033 | When asking the non-technical user to run a command, explain in one sentence what it does; when showing an error, explain it in plain language before suggesting a fix. | `AGENTS.md:1764` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-034 | UI copy must be Spanish (es-AR); identifiers, comments, and docs must be English. | `CLAUDE.md:16` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Neighbouring language fences exist for narrower subjects (lint:maplibre forces es-AR MapLibre controls; cron-registry-parity.test.ts:141-205 forces es-AR cron labels), but nothing checks the general rule.* |
| CANON-035 | No design decision may harm the Mi Argentina federation path. | `CLAUDE.md:18` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-036 | Definition of Done is `pnpm verify` AND `pnpm test:verified` green — never plain `pnpm test` — with the verdict line `reported N file(s); N discovered; 0 failing test(s); 0 broken file(s)` pasted as evidence, and the change committed with Conventional Commits and no AI attribution. | `CLAUDE.md:22`<br>`docs/agents/collaborating-writer.md:15-22` | PARTIAL (subset) | `scripts/check-suite-coverage.ts:171-179`<br>`scripts/run-verified-suite.ts:101-113` | scripts/check-suite-coverage.ts:171/:229 and scripts/run-verified-suite.ts:101-113 make the test:verified half of the definition honest (verdict computed independently, then vitest's exit code refolded), and .github/workflows/ci.yml:905 runs it. The COMMIT half has no mechanism: package.json declares no commitlint, husky, simple-git-hooks or lint-staged, so conventional-commit shape and 'no AI attribution' can fail nothing, and nothing observes whether the evidence line was pasted. *Uncovered part named: commit-message convention + AI attribution + the obligation to run both gates at all. CONTRIBUTING.md:79,90 still prescribes plain `pnpm test` and never mentions test:verified — the repo's own contributor doc contradicts the rule with nothing failing.* |
| CANON-037 | `pnpm test`'s exit code is not a valid verdict — it lies in both directions (false-green when a worker dies mid-run and drops a whole file, false-red on a benign teardown crash) — so it must never be used as the Definition-of-Done gate. | `CLAUDE.md:24`<br>`scripts/check-suite-coverage.ts:1-18` | PARTIAL (subset) | `scripts/check-suite-coverage.ts:1-45`<br>`scripts/check-suite-coverage.ts:121-173`<br>`__tests__/check-suite-coverage.test.ts` | Covered half: gradeSuiteReport (scripts/check-suite-coverage.ts:121-173) never reads an exit code — it diffs the report against discoverTestFiles() and grades broken/pending files — and __tests__/check-suite-coverage.test.ts:52,69,113 pin all three false-green shapes with red controls, so a regression to trusting `success: true` fails. Uncovered half: that grader only ever runs INSIDE the right command (run-verified-suite.ts:101-105 spawns it), so nothing fails when a person runs `pnpm test` and reads its exit code as the verdict — and CONTRIBUTING.md:79,90 still prescribes exactly that, naming test:verified nowhere. *My blind draft said UNENFORCED, reading the rule's operative clause as the human obligation ('must never be used as the DoD gate'), which no instrument can fail on. The previous basis cited __tests__/check-suite-coverage.test.ts:52-142; those red controls (which I had listed) do enforce the other half — the DoD gate's verdict may not rest on the exit code — so the honest verdict is PARTIAL, not UNENFORCED and not ENFORCED. scripts/check-ci-lint-parity.ts (lint:ci-parity) contains no reference to test:verified or pnpm test, so ci.yml:905 pins nothing either.* |
| CANON-038 | A positional file-path filter passed to `pnpm test:verified` must be rejected — the run skips the coverage verdict loudly rather than reporting a false "a worker died" verdict. | `CLAUDE.md:24`<br>`scripts/run-verified-suite.ts:57` | UNENFORCED | `scripts/run-verified-suite.ts:66`<br>`scripts/run-verified-suite.ts:88-96` | The behaviour EXISTS exactly as stated — run-verified-suite.ts:66 detects a non-flag positional and :88-96 prints the loud es-AR skip notice before exiting with vitest's own status — but nothing fails if it is removed: __tests__/check-suite-coverage.test.ts covers gradeSuiteReport only, and no test imports run-verified-suite.ts. |
| CANON-039 | Treat a test:verified run as passing only when the verdict line reads 'reported N file(s); N discovered; 0 failing test(s); 0 broken file(s)'; any short file count, failing test, or broken file is a fail with no re-roll. | `CLAUDE.md:26-29` | ENFORCED | `scripts/check-suite-coverage.ts:171`<br>`scripts/check-suite-coverage.ts:178-179`<br>`scripts/check-suite-coverage.ts:229` | scripts/check-suite-coverage.ts:171 defines ok as exactly the rule's three conditions (missing.length === 0 && broken.length === 0 && failedTests === 0), :178-179 prints that verdict line verbatim, and :229 exits 1 whenever ok is false — with `discovered` coming from discoverTestFiles() (:58,:191), the same authority vitest's include uses, so a short file count cannot be self-reported away. run-verified-suite.ts:107 propagates that exit. *Same lines as the previous basis, different reading, so no new evidence was needed. It capped at PARTIAL/subset on the 'no re-roll' rider; I hold ENFORCED because the rule's pass CRITERION is computed and failed by the machine — CANON-283's shape (rule text == fence shape) — while 'no re-roll, no judgement call' is a rider on how a human reacts to a red, not a part of the rule's file/path scope, and CLAUDE.md itself grants exactly one re-run for the worker-crash signature. Filtered runs skip the verdict loudly (run-verified-suite.ts:88-96).* |
| CANON-040 | A broken file carrying a mock/collection/import error may not be committed. | `CLAUDE.md:31` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *check-suite-coverage.ts:152-160 makes the run red; nothing stands between a red run and a commit (no hook, no CI required-check config in the repo).* |
| CANON-041 | When a broken-file re-run is clean and the victim is unrelated to the change, re-run once and commit with both verdict lines quoted; never re-run more than once chasing a nicer number. | `CLAUDE.md:33` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-042 | If the same victim file breaks twice, treat that file as the failure; if a different victim breaks each run, treat it as a run-level fault, not yours. | `CLAUDE.md:35` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-043 | Prove an open worker defect with a baseline control (a separate git worktree at the base commit), never with another run on your own tree. | `CLAUDE.md:37` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-044 | Run the baseline control twice; a single clean control run is not evidence of absence. | `CLAUDE.md:39` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-045 | A suite that answers differently across two runs on one unchanged tree fails the Definition of Done, regardless of which answer is newer. | `CLAUDE.md:43` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-046 | A run may be committed only when it shows the teardown-crash signature exactly (clean verdict line, run-verified-suite exits 1, log contains 'Worker exited unexpectedly'), and the commit message must state this and quote the verdict line. | `CLAUDE.md:45` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *The signature itself is real and produced by the tooling (run-verified-suite.ts:108-113 re-folds vitest's exit code after a clean verdict); the commit-time discipline around it is unenforced.* |
| CANON-047 | Writing a migration file is agent work; applying a migration to a remote database is Ignacio's — it must be handed over, never run by the agent. | `CLAUDE.md:45`<br>`docs/agents/collaborating-writer.md:43-44` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-048 | Delivery strategy defaults to single-pr with size:exception; when an SDD forecast recommends chained PRs, note it in the summary but do not stop to ask. | `CLAUDE.md:51` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-049 | Every agent must be routed to its one-page role contract (`docs/agents/README.md`) before it acts, and must be pointed at its own page when briefed. | `CLAUDE.md:55`<br>`docs/agents/README.md:1-23` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-050 | Every Agent tool delegation call must carry an explicit model, using sonnet for mechanical work. | `CLAUDE.md:55` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-051 | Code descends from documents: the feature lifecycle proceeds spec (`specs/`, reviewed/locked) → plan (`plans/`, executable) → Claude Code execution; raise tension with what's written before coding around it. | `CLAUDE.md:56`<br>`docs/superpowers/README.md:10-15` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-052 | Engram is the SDD artifact store, using topic keys sdd/{change}/*, and session summaries are mandatory. | `CLAUDE.md:57` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-053 | Poll every background child within the same turn it was launched; a turn may not end with a live child unpolled. | `CLAUDE.md:58`<br>`docs/agents/README.md:30-35` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-054 | Before pushing a commit range, run a fresh-context, read-only adversarial reviewer over it — pointed at the writer's own new tests and fences, since that is what the writer is least able to audit itself. | `CLAUDE.md:59`<br>`docs/agents/README.md:50-63`<br>`docs/agents/collaborating-writer.md:45-49` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-055 | A second parallel writer may run only in its own git worktree with disjoint file territory and its own targeted tests, landing through a serial integration merge gate. | `CLAUDE.md:60`<br>`docs/agents/README.md:64-69` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-056 | Before treating a fresh-worktree gate as evidence, run supabase status -o env to cure the missing .env.local, which otherwise makes __tests__/rls/*.test.ts report broken with credential-shaped errors. | `CLAUDE.md:60` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Merged CANON-078 (same subject: write the four env keys from `supabase status -o env` before trusting a red on the six __tests__/rls/* files in a fresh worktree). Both rows describe the same environmental red signature; neither has a fence.* |
| CANON-057 | When a tested constant disagrees with a design-handoff token table, the validated code is the source of truth — flag the handoff rather than following it. | `CLAUDE.md:61`<br>`docs/agents/README.md:70-76` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-058 | The supported Node range is 22.23.0-22.x, closed at both ends, and is enforced by pnpm lint:node-version. | `CONTRIBUTING.md:7-9` | ENFORCED | `scripts/check-node-version.ts:87`<br>`scripts/check-node-version.ts:171-219`<br>`package.json:195-197` | scripts/check-node-version.ts:87 CLOSED_RANGE accepts only `>=X.Y.Z <M`, :186-195 fails an open range, :210-217 forces ceiling = pin major+1, and :261-279 exits 1 when the running process is outside it. package.json:193 is '>=22.23.0 <23'; lint:node-version is verify gate #1 (package.json:79). *Live floor equals the documented 22.23.0. The floor DIGITS are not themselves fenced (:197-209 permits a floor below the pin), so lowering engines.node to >=22.18.0 would still pass; closedness and the running-Node check are genuinely enforced.* |
| CANON-059 | Never push directly to main; changes arrive as pull requests from short-lived branches named by intent (feat/, fix/, chore/, docs/) and branched from main, the trunk staging deploys from. | `CONTRIBUTING.md:46-52` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Branch protection lives in GitHub repo settings, not in the tree; CANON-083 records that required_linear_history was deliberately turned off on 2026-08-28. Rule text rewritten 2026-09-24 (R9): CONTRIBUTING.md still described a develop-based flow, but origin/develop was last touched 2026-06-20 and sits thousands of commits behind main, which is the trunk and the branch staging deploys from (scripts/check-scheduled-fence-refs.ts DEFAULT_BRANCH).* |
| CANON-060 | Commits must use Conventional Commits scoped to the area touched; check existing scopes in git log before adding a new one. | `CONTRIBUTING.md:56-60` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-061 | Run pnpm verify, pnpm test, pnpm test:unit, and pnpm test:e2e:smoke locally before pushing; do not outsource this to CI. | `CONTRIBUTING.md:75-82` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *The rule names `pnpm test`, which CLAUDE.md's Definition of Done forbids as evidence — the same CONTRIBUTING.md staleness recorded on CANON-036.* |
| CANON-062 | If a step fails on your branch but passes on develop, treat your branch as the source of the failure and fix it before opening the PR. | `CONTRIBUTING.md:93` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-063 | A scheduled workflow's `schedule:` trigger is read only from the default branch, and a schedule run always executes the default branch's own copy of the workflow file — so a workflow file that lives only on a feature/integration branch has no schedule at all, and new fence steps only take effect after merge to main. | `CONTRIBUTING.md:103-111`<br>`CONTRIBUTING.md:117-119`<br>`scripts/check-scheduled-fence-refs.ts:15` | ENFORCED | `scripts/check-scheduled-fence-refs.ts:119-141`<br>`scripts/check-scheduled-fence-refs.ts:702-713`<br>`scripts/check-scheduled-fence-refs.ts:733-735` | scripts/check-scheduled-fence-refs.ts:623-628 — a scheduled workflow absent from origin/main and not listed in NOT_ON_DEFAULT_BRANCH (:141, empty) is a finding ('its `schedule:` trigger DOES NOT EXIST'), and the reverse direction (:615-619) fails a listed file that has since been merged; findings exit 1 at :745-757. __tests__/scheduled-fence-refs.test.ts:368-378 asserts defaultBranchFindings is empty against the real tree when origin/main is visible. *The file-presence half hard-fails. The CONTENT half (a fence step live only on this branch) is warn-only by design (:799-808) — that is CANON-504's subject, not double-counted here. Both halves report SKIPPED when origin/main is not in the clone (:770-774).* |
| CANON-064 | Every scheduled GitHub Actions workflow must pin an explicit checkout `ref:` to the branch staging actually ships from (main) — or be a documented `REF_EXEMPT` entry with a reason over 40 characters — while a push/PR workflow (`ci.yml`) must NOT pin a ref. | `CONTRIBUTING.md:112-116`<br>`scripts/check-scheduled-fence-refs.ts:6`<br>`__tests__/scheduled-fence-refs.test.ts:1-18,174-366` | ENFORCED | `scripts/check-scheduled-fence-refs.ts:447-465`<br>`scripts/check-scheduled-fence-refs.ts:487`<br>`__tests__/scheduled-fence-refs.test.ts:353-355` | scripts/check-scheduled-fence-refs.ts:461-467 fails any scheduled workflow whose checkout carries no `ref:` or pins something other than DEPLOY_REF='main'; REF_EXEMPT is the only escape and is checked in both directions (:451-458). __tests__/scheduled-fence-refs.test.ts:349-351 runs refFindings over the real .github/workflows tree, :353-355 requires every exemption reason > 40 chars, and :362-366 asserts ci.yml has no schedule and every one of its checkout steps has ref === null. *All three clauses of the rule (pin, documented exemption with a >40-char reason, ci.yml unpinned) have a line that fails.* |
| CANON-065 | pnpm lint:sched-refs is the single place the deploy branch name is decided, and any exemption to its ref/alert checks must be recorded there with the argument that justifies it, checked in both directions. | `CONTRIBUTING.md:136-140` | ENFORCED | `scripts/check-scheduled-fence-refs.ts:85`<br>`scripts/check-scheduled-fence-refs.ts:426-430`<br>`__tests__/scheduled-fence-refs.test.ts:353-355` | DEPLOY_REF (scripts/check-scheduled-fence-refs.ts:85) is the only literal every workflow ref is compared against (:462); __tests__/scheduled-fence-refs.test.ts:380-390 fails unless the de-duplicated set of refs pinned across the real scheduled tree equals exactly [DEPLOY_REF] and is non-empty, so a second literal cannot drift in. Both exemption lists live in the same file and are checked in both directions (:451-458 REF_EXEMPT, :511-517 ALERT_EXEMPT, :478-487 staleness) with reasons pinned > 40 chars (test :353-355, :501-502). *Zero-scheduled-workflow non-vacuity floor at :719-724; deployRefResolves :426-430 asks the remote whether the branch still exists and only reports on an exact false (:737-743).* |
| CANON-066 | Every scheduled gate must wire the `.github/actions/red-streak-alert` composite action (or be a documented `ALERT_EXEMPT` entry with its justifying argument, checked in both directions), since GitHub's own failed-workflow email only fires on a green-to-red transition; a job whose steps can all be skipped by a guard must publish that guard's result as job output `audited:` so the alert action never reads a no-op success as recovery. | `CONTRIBUTING.md:145-150`<br>`CONTRIBUTING.md:155-157`<br>`CONTRIBUTING.md:165-169`<br>`__tests__/scheduled-fence-refs.test.ts:472-547`<br>`.github/workflows/mobile-export-nightly.yml:190` | ENFORCED | `scripts/check-scheduled-fence-refs.ts:506-524`<br>`scripts/check-scheduled-fence-refs.ts:559-572`<br>`.github/actions/red-streak-alert/action.yml`<br>`scripts/check-scheduled-fence-refs.ts:160-185`<br>`.github/workflows/mobile-export-nightly.yml:204-232` | alertFindings (scripts/check-scheduled-fence-refs.ts:520-525) fails every scheduled workflow with no `uses: ./.github/actions/red-streak-alert` unless it is in ALERT_EXEMPT (:160-185), whose entries are checked in reverse (:511-517) and whose reasons must exceed 40 chars (__tests__/scheduled-fence-refs.test.ts:501-502). auditedFindings :561-576 fails a workflow that wires the alert while skipping steps behind an `if: steps.<id>.outputs` guard (STEP_OUTPUT_GUARD :559) without passing `audited:`. Findings exit 1 at :745-757. *Both halves of the rule (alert wiring with a justified exemption, audited: output for guard-skippable jobs) have a failing line; the action's own third state is pinned at test :570-618. Since 2026-09-24 the guard-skippable workflows (db-doctor-staging, authz-audit-staging) are scheduled from dim-interno and pass `audited:` there; the fence covers this repository's scheduled workflows only.* |
| CANON-067 | `scripts/check-suite-coverage.ts` must grade a test file BROKEN — not passed — when it reports a file-level error with zero failing assertions (e.g. a `vi.mock` factory missing an export), or when it still carries a pending/unfinished assertion, even though its file `status` reads `passed`; a skip/todo-only file stays green. | `scripts/check-suite-coverage.ts:29`<br>`scripts/check-suite-coverage.ts:20-31`<br>`scripts/check-suite-coverage.ts:32-40`<br>`__tests__/check-suite-coverage.test.ts:52-193` | ENFORCED | `scripts/check-suite-coverage.ts:136-161`<br>`__tests__/check-suite-coverage.test.ts:69-166` | scripts/check-suite-coverage.ts:152-160 grades a file BROKEN when status === 'failed' with no failing assertion, and also when a passed file left assertions in vitest's 'pending' state (:143 counts only status === 'pending', so skip/todo stay green). __tests__/check-suite-coverage.test.ts pins all four shapes: :69 collection/mock error, :113 pending assertion, :144 skip/todo stays green, :176 the defensive post-report file error. *The verdict line at :178-179 carries broken.length, so a broken file can never stand next to '0 failing test(s)'. I additionally found the :176 defensive shape the previous basis did not list.* |
| CANON-068 | The daily cron dispatcher must refuse to start a job when less time is left than that job's declared ceiling, and every job with a ceiling must derive its actual deadline as `min(own ceiling, dispatcher-handed budget)`. | `__tests__/cron-budget-ceiling.test.ts:1-26,205-359` | ENFORCED | `lib/infra/cron-dispatcher.ts:56-63`<br>`lib/infra/cron-dispatcher.ts:84-87`<br>`__tests__/cron-budget-ceiling.test.ts` | __tests__/cron-budget-ceiling.test.ts:205-300 (group a) drives the dispatcher and asserts an uncooperative ceiling-hitter is skipped rather than crossing the 60 s kill; :301-322 (group b) asserts effectiveDeadlineMs takes min(own ceiling, handed budget). Census half at :323-336 (every declared ceiling honoured by its route), :361-370 (a job reaching a ceiling constant must be in CRON_JOB_CEILINGS), :405-418 (every dispatcher job ceilinged or explicitly exempt, table floor >= 20), over readdirSync(CRON_DIR) at :192. *lib/infra/cron-dispatcher.ts:44-69,84-88 is the code under test (fairShareMs = min(capMs, floor(budgetLeftMs/jobsLeft))); the parity assertions are what stop a declared ceiling from being a claim the code does not back.* |
| CANON-069 | Every cron job the dispatcher runs must be classified as either declared in `CRON_JOB_CEILINGS` or explicitly `CEILING_EXEMPT`; an exemption must reach zero ceiling-declaring modules or it is stale. | `__tests__/cron-budget-ceiling.test.ts:361-421` | ENFORCED | `__tests__/cron-budget-ceiling.test.ts:390-409` | __tests__/cron-budget-ceiling.test.ts:405-410 fails on any DAILY_JOB_ORDER job that is neither in CRON_JOB_CEILINGS nor CEILING_EXEMPT; :361-379 flags an exempt job that still reaches a ceiling as stale, with non-vacuity floors at :382-387 (fleet ≥20, ≥15 reached via imports) and :390-403 pinning each exemption to zero reachable ceilings. |
| CANON-070 | `vercel.json` must schedule only the dispatcher route(s) plus standalone-scheduled jobs; `CRON_REGISTRY` names must equal the snake_case of the job route directories; every job route must declare a matching `CRON_NAME`, record `cron_runs` telemetry, and be auth-gated. | `__tests__/cron-registry-parity.test.ts:61-137` | ENFORCED | `__tests__/cron-registry-parity.test.ts:66-136` | __tests__/cron-registry-parity.test.ts:66-69 asserts vercel.json's cron paths equal dispatcher+standalone dirs exactly; :77-80 registry names = snake_case of job route dirs; :98-107 every job route declares CRON_NAME = snake(dir); :109-119 telemetry; :127-136 auth gate. Every clause of the rule has a failing assertion over a readdir-derived corpus. |
| CANON-071 | A reserved seed account's email address must be spelled out in exactly one file (`scripts/seed-reserved-accounts.ts`); every other call site must import the constant, and seed scripts must run recipient lists through `rejectReservedAccounts()`. | `__tests__/seed-reserved-accounts.test.ts:1-13,85-138` | PARTIAL (adoption-unfenced) | `__tests__/seed-reserved-accounts.test.ts:85-148` | __tests__/seed-reserved-accounts.test.ts:99-113 enforces the single-source half over scripts/e2e/__tests__/app/components/lib/src/db (:39), with non-vacuity at :86-97. The second half is not fenced: :116-148 pins rejectReservedAccounts' BEHAVIOUR on fixtures, and rg shows exactly one caller (scripts/seed-demo-polish.ts:409) — no other seed script is forced to run its recipient list through it. *The v3 basis cites the same lines and treats ':116-148 pins rejectReservedAccounts' filtering' as closing the rule; a helper's own fixture test proves nothing about the seed scripts that never call it.* |
| CANON-072 | A PR touching src/modules/*/application/** must be reviewed against five fitness-hook criteria: no framework imports, plain-data boundary, auth stays at the edge, new error branches carry a code, notifications stay plain-data. | `docs/adr/2026-07-18-native-readiness.md#fitness-hooks` | PARTIAL (subset) | `scripts/check-application-fence.ts:1-44`<br>`biome.json` | scripts/check-application-fence.ts:24-40 + biome.json's noRestrictedImports override cover criterion 1 only (no framework imports in src/modules/*/application/**), with an exemption ratchet at :308-315. Criteria 2-5 (plain-data boundary, auth at the edge, error branches carry a code, notifications plain-data) have no enforcer — rg 'plain-data' over scripts/ returns zero. *process rule ('must be reviewed against'); only 1 of the 5 criteria is machine-checked* |
| CANON-073 | Keep each role contract to at most 10 rules, since long prose is not followed. | `docs/agents/README.md:3-7` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-074 | Verify worktree freshness before touching anything by comparing the integration branch's `git log --oneline -1` against the worktree's own HEAD, and `git reset --hard` if they diverge. | `docs/agents/README.md:37-46` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-075 | Only Ignacio may launch `/code-review ultra`, since it is billed and reserved for a deeper pre-deploy pass. | `docs/agents/README.md:58-59` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-076 | Never `git pull` on origin/main; sync with `git fetch origin && git reset --hard origin/main` and move unpushed work to a branch first. | `docs/agents/collaborating-writer.md:9-14` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-077 | Never re-run the gate to get a nicer number after a red result. | `docs/agents/collaborating-writer.md:21-22` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-079 | Freeze the tree while a gate runs; do not edit any file while `pnpm verify` is in progress. | `docs/agents/collaborating-writer.md:33-36` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Real mechanism behind the norm: many fences scan the whole tree at run time (e.g. seed-reserved-accounts.test.ts:38, check-brand-casing.ts:131), so an edit mid-run changes their corpus. Nothing enforces the freeze.* |
| CANON-080 | Commit with an explicit pathspec; never `git add .` or a bare `git commit -a`, since parallel agents share one index. | `docs/agents/collaborating-writer.md:37-39` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-081 | Commit messages must be es-AR, naming the lie or the domain problem rather than the file; identifiers, comments and docs stay in English. | `docs/agents/collaborating-writer.md:50-58` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *CONTRADICTION (refuter-confirmed): docs/agents/collaborating-writer.md:50-52 says commit messages are es-AR (2026-08-29) while CLAUDE.md invariant #4 groups docs — commits by omission — as English. Practice is es-AR (last 8 subjects, all Spanish).* |
| CANON-082 | Stay in scope; touch only what the task names and report (do not fix) adjacent problems. | `docs/agents/collaborating-writer.md:60-62` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Merged CANON-130 (the subagent page's wording of the same rule: touch only the files/dirs the prompt names; report adjacent problems, do not fix them). NOTE: `lint:scope` (scripts/check-scope-discipline.ts) is a jurisdiction-scope fence, a different subject entirely.* |
| CANON-083 | Do not restore `required_linear_history` on `main`; it was deliberately turned off on 2026-08-28 to allow worktree merge commits. | `docs/agents/collaborating-writer.md:123-125` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Branch-protection settings live in GitHub, outside the tree — unverifiable read-only from the repo at this SHA.* |
| CANON-084 | The gate proves the code matches its author's belief, not that the belief was right, so a green gate does not substitute for adversarial review. | `dim-interno:docs/agents/handoff-2026-08-30.md:5-9` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-085 | A live conflict marker surviving inside a test file must be caught as a broken file by the sweeping vitest fences, since it is invisible to the `lint:*` chain. | `dim-interno:docs/agents/handoff-2026-08-30.md:144-149` | UNENFORCED | — | Reused from the refuter: no fence scans test files for conflict markers; check-notifications-service.ts matched a '<<<<<<<'-shaped pattern only incidentally. The rule describes emergent vitest behaviour, not a checkable predicate. |
| CANON-086 | Before validating a tree, pin Node to the version in `.nvmrc` (22.23.2) and refuse to proceed on any other version. | `dim-interno:docs/agents/handoff-2026-08-30.md:432-441` | PARTIAL (declared-limit) | `scripts/check-node-version.ts:260-279` | scripts/check-node-version.ts:49-52 states 'The rule is floor <= pin < ceiling, not floor == pin', and checkRunningNode :262-263 only tests satisfies(running, engines range = >=22.23.0 <23). A process on 22.23.0 or 22.24.x passes although .nvmrc pins 22.23.2, so 'refuse to proceed on any other version' is not what fails. *Same status as v2; shape recorded as declared-limit rather than subset because the header states the non-coverage explicitly.* |
| CANON-087 | `supabase status -o env` can exit 0 even with part of the stack down; read the "Stopped services" line rather than trusting the exit code. | `dim-interno:docs/agents/handoff-2026-08-30.md:457-461` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-088 | `pnpm test:verified` must be run twice and both runs must agree exactly (verdict line and test counts) to satisfy the Definition of Done. | `dim-interno:docs/agents/handoff-2026-08-30.md:471-487` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *CLAUDE.md's own Definition of Done section states the single-run verdict rule; the 'twice, identical' requirement lives in docs/agents and is applied by hand (see dim-interno:docs/agents/open-work.md:2511-2563).* |
| CANON-089 | Before touching anything in a staging-clickthrough run, read and record the commit the build is serving from the `mimar-version` meta tag, and re-read it at the end of the run. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:9-24` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-090 | If the commit being served does not match the one the tester was told to review, stop and report it rather than continue testing. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:21` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-091 | Load historical facts with the date they actually occurred, not today's date, whenever the form permits it. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:50` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-092 | A tester must enter a flow through the path a real person would use, not a known direct URL; if a feature is not reachable by navigation, that unreachability is itself the finding. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:51` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-093 | A tester must record doubt at the moment it occurs, rather than search for the correct way to do something and then report success. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:52-54` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-094 | A test bitácora line must be written after every single hito (checkpoint), before moving to the next one, never batched to the end of the phase or run. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:388-393` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-095 | Every finding report must separate OBSERVATION (only what was seen on screen), HYPOTHESIS (why, or "none"), and SUGGESTION into distinct fields, never merged. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:424-437` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-096 | Only the UI may be used during the test; no SQL and no direct API access. If a flow cannot be completed through the interface, that is itself the finding. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:498` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-097 | A tester must never touch data they did not create themselves (the demo cast, other reviewers' fixtures) — no edits, deletes, or resolving of queues not opened by the tester. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:494-496` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-098 | A `[CORTE]` cross-role consistency check must be recorded at least once at each of the five named checkpoints, from every role that can view that case. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:84-101` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-099 | Rotation between the seven test personas must complete a full cycle every time, even when one person has nothing new, rather than chasing whoever is currently unblocked. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:115-125` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-100 | The final test report must include a "Verificado y limpio" section listing what was checked and found working, since without it a report with no findings is indistinguishable from a report where nothing was checked. | `dim-interno:docs/agents/master-test-ciudadano-multiagente.md:461` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-101 | Only the integrator may renumber the open-work board's table, and only after auditing every existing pointer to it rather than taking a prior lane's argument on trust. | `dim-interno:docs/agents/open-work.md:113-126` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *THREE draft rows stated one rule (subject dedup): CANON-101 (only the integrator renumbers, after auditing pointers), CANON-120 (lanes are append-only; the integrator renumbers once at the end) and CANON-492 (row numbers are identifiers, never renumbered by a lane). Kept CANON-101 as the fullest statement.* |
| CANON-102 | An independent code reviewer must not be told what was already fixed in the range under review, so as not to bias what they look for. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:7-10` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-103 | When a finding belongs to an enumerable class (policies, buckets, cache columns, sweeps, public routes, SECURITY DEFINER functions), the report must include the complete enumeration of that class, not just the discovered instance. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:78-89` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-104 | A finding's magnitude (real population size) must be verified before proposing a causal explanation for it. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:91-96` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-105 | A proposed fix's premises must be stated as either verified or assumed, explicitly distinguished, because an unverified premise can silently break production. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:97-101` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-106 | A conclusion that something does NOT exist must state what it was checked against, since "not in the repo's migrations" and "not in the live database" are different claims. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:102-111` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-107 | When evaluating a test, ask what specific production change would have to break for the test to fail, not whether the test currently passes. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:60-63` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-108 | A test fixture with only one instance of a grouped entity cannot cover a group-by defect; the fixture must contain at least two of the thing the code groups by. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:65-69` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-109 | Launch one reviewer agent per dimension (authz/RLS, spine integrity, projections, privacy, test quality) plus one adversarial agent that refutes the others' findings before they reach the report. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:54-58` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-110 | A finding report must order findings by potential damage, not by count, and must explicitly list what was checked and found fine. | `dim-interno:docs/agents/prompt-cowork-review-codigo.md:113-116` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-111 | Tag everything a QA/data agent creates with `payload.source = "<run-id>"` or recognizable names; untagged residue is treated as corruption and deleted. | `docs/agents/qa-mutation-contract.md:13-15` | PARTIAL (subset) | `__tests__/global-setup.ts:73-113`<br>`scripts/check-spine-integrity.ts` | scripts/check-spine-integrity.ts:1-27,54-59 exits 1 on any pets row lacking a pet_registered event (only seed_tag='perf' exempt) and is wired as lint:spine inside package.json's verify chain (:79); __tests__/global-setup.ts:73-91 deletes the same eventless rows locally and logs them. Neither reads payload.source: residue that IS anchored in the spine, and every non-pets table, is untouched. *The global-setup sweep enacts the deletion rather than failing (and returns early on a non-local URL, :74); the failing half is lint:spine, whose subject is spine anchoring, not run-id tagging.* |
| CANON-112 | Never touch the curated demo set (named owner/pets/org fixtures); create separate subjects instead. | `docs/agents/qa-mutation-contract.md:16-19` | PARTIAL (subset) | `__tests__/seed-hygiene.test.ts:78-105` | __tests__/seed-hygiene.test.ts:78-100 fails when a RESERVED account acquires pets or org memberships, with a non-vacuity control at :102-125. That is one corner of the curated demo set: nothing detects a QA agent renaming, re-owning or deleting a named demo pet/org fixture that is not a reserved account. *The rule is a process contract for QA agents (docs/agents/qa-mutation-contract.md); the DB guard catches only the assignment-to-reserved-account shape. Basis reached the same subset independently.* |
| CANON-113 | A QA/data agent must refuse to run against any DATABASE_URL that is not 127.0.0.1. | `docs/agents/qa-mutation-contract.md:20` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *Two adjacent mechanisms cover code paths, not agents: check-function-parity's remote refusal and global-setup's URL forcing.* |
| CANON-114 | A QA/data agent must configure its editor/writer for UTF-8 before touching files, or the encoding-fitness test fails the PR. | `docs/agents/qa-mutation-contract.md:21-22` | PARTIAL (subset) | `__tests__/encoding-fitness.test.ts:56-60` | __tests__/encoding-fitness.test.ts:57-63 fixes the corpus to app, components, lib, src, db, scripts, e2e, docs (+ __tests__ for the byte scans) and to 7 extensions, and walk() starts at ROOT/<dir>: apps/** (mobile), packages/** (packages/contract/src/events/event-types.ts), .github/** and every root-level file are never opened, so a file an agent writes there can carry mojibake and nothing fails. *Same subset the v3 basis and its judge named; I add packages/**, which the mobile/workspace split makes live. Non-vacuity is strong where it does scan (:265-277 floor of 1500 files, :279-286 dir existence, :288-305 pattern pins).* |
| CANON-115 | Only one writer may own a handoff file at a time; if it already has fresh content from another session, append an `## Addendum — <who> <when>` section rather than rewrite others' findings. | `docs/agents/qa-mutation-contract.md:23-25` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-116 | Every QA/data agent report must include branch, short SHA, and the exact commands run; claims without reproduction commands are discarded. | `docs/agents/qa-mutation-contract.md:26-28` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-117 | `lint:route-weight` and `lint:csp-prerender` must not be treated as evidence when run standalone (without a prior `pnpm build`); only inside `pnpm verify`, after the build, do they measure for real. | `dim-interno:docs/agents/recommendations-2026-08-30.md:57-73` | PARTIAL (declared-limit) | `scripts/check-route-weight.ts:147-167`<br>`scripts/check-csp-prerender.ts:88-92`<br>`package.json:79`<br>`.github/workflows/ci.yml:394` | scripts/check-route-weight.ts:151-159 and scripts/check-csp-prerender.ts:88-96 exit 1 on a missing build ONLY under --require-build; .github/workflows/ci.yml:376,394 pass that flag, so a build-less CI run is red. A standalone local run prints '[skip] ... NO SE MIDIO NADA' and exits 0 by explicit design (csp-prerender :79-85: 'A developer running this on a build-less box is not a failure'), and verify's build-before-fence ordering is only the package.json:79 script string, which nothing asserts. *The previous basis states check-csp-prerender 'exits 1 outright with no .next/server/app'; the code at :88-96 does that only under --require-build, otherwise console.warn + return. The honest-skip output does mean a standalone run can never print a green measurement line, but no fence fails on the local half of the rule and both headers declare that limit.* |
| CANON-119 | State in `/CLAUDE.md`, next to the Definition of Done, that `pnpm test:verified` does not cover `apps/mobile` (a separate Jest run inside `verify:mobile`). | `dim-interno:docs/agents/recommendations-2026-08-30.md:125-139` | UNENFORCED | — | Not merely unenforced — undone: rg -i 'mobile' over CLAUDE.md returns zero hits at this SHA, so the Definition of Done still does not say that `pnpm test:verified` excludes apps/mobile (checked separately by verify:mobile, package.json:78). |
| CANON-121 | A fence must not anchor on source text alone (e.g. `toContain("isNull(pets.deletedAt)")`); anchor on compiled SQL (`PgDialect().sqlToQuery()`) or behavior instead. | `dim-interno:docs/agents/recommendations-2026-08-30.md:208-222` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-122 | For every new test, name the exact production mutation that breaks it and actually apply it, watch the red, and restore — not merely predict it. | `dim-interno:docs/agents/recommendations-2026-08-30.md:225-241` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-123 | Two or three writer lanes should run at once, each running only targeted tests plus its own fences, with one integrator merging, re-deriving pins, and running the full gate. | `dim-interno:docs/agents/recommendations-2026-08-30.md:250-259` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-124 | The integrator may reject a lane out of a window for an open blocker rather than merge a broken lane in — a clean window of two beats a window of three with one broken inside it. | `dim-interno:docs/agents/recommendations-2026-08-30.md:260-264` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-125 | Hand back an out-of-scope or PO-gated item to the PO with the reason written, rather than half-attempting it. | `dim-interno:docs/agents/recommendations-2026-08-30.md:299-303` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-126 | A subagent runs on the model its parent chose and must not spawn further agents; if the task needs decomposition, it must return that finding instead. | `docs/agents/subagent-card.md:11-12` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-128 | A subagent's final message must be data for its parent (paths, verdicts, structured results, deviations and why), not prose for a human. | `docs/agents/subagent-card.md:15-17` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-129 | If a subagent wrote code it must run `pnpm exec tsc --noEmit` and `pnpm exec biome check --fix <paths>` before returning, pasting real output rather than claiming green. | `docs/agents/subagent-card.md:18-20` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *`pnpm typecheck` and `pnpm lint` (Biome) are both links of the verify chain (package.json:79), so the tree-level property is gated; what has no enforcer is the subagent's obligation to run them before returning.* |
| CANON-131 | Setting retention periods for PII tables is a legal decision, not an engineering one, and requires legal sign-off before any wiring. | `docs/architecture/retention-policy-pending-decision.md#context` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-132 | Provisional conservative retention periods (e.g. "10 years, adjust later") must not be adopted as a stopgap. | `docs/architecture/retention-policy-pending-decision.md#explicitly-rejected-alternatives` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-133 | External agents (non-Claude-Code reviewers, Cowork, QA agents) deliver recommendations only and must never write code into this repo. | `dim-interno:docs/design/handoffs/README.md:9-13` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-134 | Every audit must start by stating its ground truth: the canonical checkout's current branch and short SHA (`git -C C:/dev/dim branch --show-current && git -C C:/dev/dim rev-parse --short HEAD`). | `dim-interno:docs/design/handoffs/README.md:25-29` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-135 | The canonical checkout is `C:/dev/dim`; any path under `.claude/worktrees/**` is a frozen copy of the repo at an older commit and must not be read as current. | `dim-interno:docs/design/handoffs/README.md:31-35` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-136 | Any quantitative claim in a handoff (line counts, file counts, mention counts) must carry the exact command that produced it, run on the canonical checkout at the stated SHA. | `dim-interno:docs/design/handoffs/README.md:36-39` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-137 | An eas update --channel production must be Ignacio-gated: an agent prepares it, but only the PO presses the publish button. | `docs/mobile/ota-policy.md#procedure` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-138 | A spec in `specs/` must be reviewed and locked before implementation begins. | `docs/superpowers/README.md:12` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-139 | A plan in `plans/` must be self-contained, file-level detail, and directly executable by Claude Code. | `docs/superpowers/README.md:13` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-140 | Standing architectural decisions that outlive a single spec/plan pair belong in an ADR under `docs/adr/`. | `docs/superpowers/README.md:17-23` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-141 | A .env.local file must not define any key twice. | `scripts/check-env-local.ts:15` | PARTIAL (manual-wiring) | `scripts/check-env-local.ts:102-108` | scripts/check-env-local.ts:102-108 pushes an error for any key whose parsed line list has length > 1, and :141 exits 1 on any error — the predicate is real. But package.json:97 exposes it only as env:doctor, which is absent from the :79 verify string, and rg over the repo (workflows included) finds no other caller. *The script's own header (:2) states it is NOT part of pnpm verify.* |
| CANON-142 | Every always-required env key (DATABASE_URL, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) must exist and be non-empty in .env.local. | `scripts/check-env-local.ts:30` | PARTIAL (manual-wiring) | `scripts/check-env-local.ts:30-35`<br>`scripts/check-env-local.ts:111-123` | scripts/check-env-local.ts:30-35 lists the four REQUIRED_KEYS verbatim and :111-123 errors on a key that is missing (and not in the shell env) or present-but-empty, exiting 1 at :141. Wiring is package.json:97 env:doctor only — not in the :79 verify chain and referenced by no workflow. *A key supplied through the shell environment rather than the file is accepted (:113-116), which is narrower than "must exist in .env.local" but deliberate.* |
| CANON-143 | env:doctor must never print an env variable's value, only key names and line numbers. | `scripts/check-env-local.ts:20` | UNENFORCED | — | The implementation complies — check-env-local.ts reports only key names and line numbers (:104-133) and says so at :20-21 — but nothing can FAIL if a future edit prints a value: no test imports the script and no fence lints it. |
| CANON-144 | The Node version pinned in .nvmrc, .node-version, package.json engines.node, and every setup-node step must agree, and engines.node must not be an open-ended range. | `scripts/check-node-version.ts:6` | ENFORCED | `scripts/check-node-version.ts:149-169`<br>`scripts/check-node-version.ts:171-219`<br>`scripts/check-node-version.ts:221-258` | scripts/check-node-version.ts:160-167 fails when .node-version != .nvmrc, :186-195 fails a non-closed engines.node, :200-217 fails a floor above the pin or a ceiling != major+1, :247-256 fails any workflow node-version != the pinned major, and :225-245 fail-closes on zero workflows / zero declarations. Every .github/workflows file is *.yml, matching WORKFLOW_GLOB (:79). *Workflow agreement is checked at MAJOR granularity ('22'), which is the fence's stated definition of agreement.* |
| CANON-145 | `scripts/run-verified-suite.ts` must always invoke `check-suite-coverage.ts` regardless of vitest's own exit code (the checker exists specifically to distrust that exit code), and `test:verified` must itself exit non-zero if either the coverage verdict fails or vitest's own exit code is non-zero. | `scripts/run-verified-suite.ts:9`<br>`scripts/run-verified-suite.ts:98` | UNENFORCED | `scripts/run-verified-suite.ts:98-113` | The behaviour is implemented exactly as stated (:98-105 runs check-suite-coverage regardless of vitest's status, :107-113 exits on the verdict first and then re-folds vitest's own code), but nothing fails if it is removed: __tests__/check-suite-coverage.test.ts imports only gradeSuiteReport/formatVerdictLine/timedOut, and no test imports this script. *Downgraded from the draft's ENFORCED. This is the same class as CANON-038: a canonical implementation that no fence can fail on.* |
| CANON-146 | The cron refresh-cube function must run daily at 03:00 UTC and be allowed up to 300 seconds duration. | `vercel.json:6` | UNENFORCED | `vercel.json:4-7`<br>`vercel.json:17-19`<br>`__tests__/cron-registry-parity.test.ts:66-69` | vercel.json:4-7 carries the schedule string and :17-19 the 300s pin — self-executing declarations, true at this SHA. The only check over the file, __tests__/cron-registry-parity.test.ts:66-69, maps crons to their PATH and compares the set to [daily, refresh-cube]; it never reads `schedule` or `maxDuration`. An rg for maxDuration/gru1/the cron expressions across ts/tsx/mjs/yml finds no assertion. *Deleting the refresh-cube entry would fail the parity test; changing 0 3 * * * to any other time, or 300 to 60, fails nothing. The previous basis records the same gap ('the schedule string itself has no fence') while grading ENFORCED.* |
| CANON-147 | The cron daily function must run at 04:00 UTC. | `vercel.json:9` | UNENFORCED | `vercel.json:8-11` | vercel.json:8-11 "schedule": "0 4 * * *" is the mechanism itself; no check reads it. __tests__/cron-registry-parity.test.ts:66-69 compares only the set of cron PATHS to [daily, refresh-cube]; the rule's entire predicate is the time value, which nothing can fail on. *self-executing declaration, true at this SHA. The previous basis says so outright ('Deploy config is the mechanism').* |
| CANON-148 | All app/api/cron/*/route.ts functions default to a 60-second maxDuration, except refresh-cube which gets 300 seconds; the deployment region is pinned to gru1. | `vercel.json:13` | UNENFORCED | `vercel.json:13-21` | vercel.json:13-21 (functions maxDuration 60 / refresh-cube 300, regions ["gru1"]) is a platform config declaration; an rg for maxDuration/gru1 across ts/tsx/mjs/yml returns only unrelated app code and e2e prose (e2e/perf/staging-panorama-perf.spec.ts mentions gru1 in comments). Nothing exits non-zero if any of the three values changes. *self-executing declaration, true at this SHA* |
| CANON-149 | Vitest test files must be partitioned into a parallel "unit" project (no DB reachability) and a serial "db" project (fileParallelism false), computed mechanically by import-graph reachability of db/index.ts. | `vitest.config.ts:9` | ENFORCED | `vitest.config.ts:24`<br>`vitest.config.ts:59-113`<br>`__tests__/project-partition.guard.test.ts:22-71` | __tests__/project-partition.guard.test.ts:25-71 asserts both sides non-empty, db ∪ unit === all and db ∩ unit === ∅, no unit file reaching the DB client at depth 1, no unit file with a direct DB signal, no unit file that isDbTest, and no misfiling into db — over computeTestPartition(), which vitest.config.ts:24 recomputes on every run from __tests__/db-reachability.ts:265-299 (readdir walk + reachability), so there is no manifest to drift. *The guard proves the classifier's invariants; the config's USE of it (:24,:60,:76) is a declaration, but a hand-written include list would drop files and fail the suite verdict (scripts/check-suite-coverage.ts:131).* |
| CANON-150 | The "db" vitest project must run after (never alongside) the "unit" project, per groupOrder, because concurrent scheduling starves the DB project. | `vitest.config.ts:90` | UNENFORCED | `vitest.config.ts:75`<br>`vitest.config.ts:114` | vitest.config.ts:75 sequence:{groupOrder:0} and :114 sequence:{groupOrder:1} ARE the scheduling mechanism; setting both to the same value changes behaviour and fails no check. An rg for groupOrder across ts/tsx/mjs/yml/json finds it only in vitest.config.ts and in explanatory comments (components/panorama/PanoramaConsole.test.tsx). *self-executing declaration, true at this SHA. The previous basis states it verbatim ('The config line IS the scheduling mechanism … no test asserts groupOrder') while grading ENFORCED.* |
| CANON-151 | Coverage instrumentation must include lib/**, app/**, packages/**, src/**, excluding components/**, __tests__/**, node_modules/**, .next/**, db/migrations/**, *.d.ts, e2e/**. | `vitest.config.ts:126` | UNENFORCED | `vitest.config.ts:118-135` | vitest.config.ts:126-135 coverage.include/exclude is a corpus declaration read by the v8 provider; nothing exits non-zero if a glob is removed. The config's own comment (:121-125) proves it — dropping packages/** would have made the coverage number IMPROVE, i.e. gone unpunished. No test or fence reads coverage.include. *The coverage run does gate CI (.github/workflows/ci.yml:905 `pnpm test:verified -- --coverage`), but only through the thresholds, never through the include list — so the wiring question the previous basis answered ('manual') is moot for this row.* |
| CANON-152 | Branch-coverage thresholds per path glob (e.g. lib/business-rules-** 80%, app/api/** 8%) are ratchet floors that may only be raised, never lowered below what is achieved. | `vitest.config.ts:136` | ENFORCED | `vitest.config.ts:143-161` | vitest.config.ts:143-161 declares per-glob branch floors (lib/business-rules-** 80, lib/**-rules/** 80, lib/** 55, app/actions/** 22, app/api/** 8, src/modules/**/domain/** 88, src/modules/** 55) which fail the vitest run below them, and .github/workflows/ci.yml:905 runs `pnpm test:verified -- --coverage` (with :888-891 naming them RATCHET FLOORS), so the floors gate CI. The rule TEXT is itself the ratchet. *Not in `pnpm verify`; the previous basis capped it at manual-wiring, but ci.yml:905 — which that same basis cites on CANON-151 — is a CI step that exits non-zero, which R8/R9 accept. Caveat: a floor is a hand-edited literal, so nothing pins it to measured coverage.* |
| CANON-153 | Test file paths containing Next.js route-dir glob-special characters (parens, brackets) must be escaped before being passed to vitest's include globs. | `vitest.config.ts:26` | ENFORCED | `vitest.config.ts:26-33`<br>`scripts/check-suite-coverage.ts:131`<br>`scripts/check-suite-coverage.ts:191` | vitest.config.ts:31-33 escapes every discovered path before it becomes an include glob; the failure it prevents is caught independently by scripts/check-suite-coverage.ts:191, which grades the vitest JSON report against discoverTestFiles() (__tests__/db-reachability.ts:265-280, a readdir walk, not a glob): unescaped paren/bracket paths would be discovered but never reported, landing in `missing` (:131) and clearing `ok` (:171). test:verified = scripts/run-verified-suite.ts (package.json:126). *The escaping is the mechanism; the enforcer is the discovered-vs-reported verdict a regression would trip.* |
| CANON-496 | Every vi.mock()/vi.doMock() string specifier must resolve to a file that exists — Vitest does not error on a specifier pointing at a deleted or renamed path; it silently stops mocking (or no-ops) and the real, unmocked module runs instead. | `scripts/check-mock-paths.ts:1-6`<br>`scripts/check-mock-paths.ts:1-14` | ENFORCED | `scripts/check-mock-paths.ts` | scripts/check-mock-paths.ts:63 MOCK_CALL_RE captures every vi.mock/doMock/unmock/doUnmock string specifier; :107-127 resolves @/ and ./ ../ specifiers against the filesystem and :157-166 exits 1 on each dead one; :141-144 fail-closes on an empty scan. lint:mocks is in package.json:79 verify. *Bare package specifiers are skipped by declaration (:19-20), but vitest itself errors on an unresolvable package and rg found zero `vi.mock("@dim/...")` workspace specifiers, so that declared skip leaves no live gap of the rule's failure class.* |
| CANON-503 | No tracked source file under app/, components/, lib/, src/, db/, scripts/, e2e/ or docs/ may contain CP1252 mojibake, a U+FFFD replacement character, a U+00AD soft hyphen, a control character, or a leading BOM — the ban is stated as a CLASS, not as an enumeration of the spellings already seen. | `__tests__/encoding-fitness.test.ts:1-60` | PARTIAL (declared-limit) | `__tests__/encoding-fitness.test.ts` | __tests__/encoding-fitness.test.ts:58-62 scans the soft hyphen over CODE_SCAN_DIRS and control/BOM over BYTE_SCAN_DIRS, both of which OMIT docs/; the file's own header declares it at :14-19 and :48-50 ('docs/ is excluded from the control/BOM scans'). The rule names docs/ in its scope for all five classes, so three of the five are unenforced there. *Mojibake/U+FFFD DO cover docs (SCAN_DIRS :57). CONTROL_CHAR :86 is a genuine class range and LEADING_BOM :89 is correctly leading-only; the anti-vacuity block :265-305 gives every pattern teeth. v3 carried no analysis for this row ('added in v2 from unmapped enforcer').* |
| CANON-504 | A scheduled workflow's `ref:` pin and its red-streak alert job must be LIVE on origin/main, not merely present in the working tree; the fence reports every inert one rather than counting it as pinned. | `scripts/check-scheduled-fence-refs.ts:698-713, 760-811` | PARTIAL (declared-limit) | `scripts/check-scheduled-fence-refs.ts` | livenessRows/inertRows (scripts/check-scheduled-fence-refs.ts:680-714) compare this tree's pins and alert jobs against origin/main's copy, and __tests__/scheduled-fence-refs.test.ts:656-682 fails if any here-but-not-there row is missing from the inert list. But the CLI only PRINTS the inert rows (:799-814) — its own header :649-658 says 'WHY THIS WARNS INSTEAD OF FAILING', so an inert pin or alert job never exits 1; the pass line only avoids claiming them ('IN THIS TREE', :762-768). *The reporting half is test-enforced; the 'must be LIVE' half is a declared non-failure. Both go silent when origin/main is not in the clone (:782-787, test :660). Previous basis carried no line-level evidence for this row.* |
| CANON-505 | Every exemption entry in a fence's exemption list must carry a reason longer than 40 characters — an exemption someone cannot argue with is not an exemption. | `__tests__/scheduled-fence-refs.test.ts:353-355, 501-502` | PARTIAL (subset) | `__tests__/scheduled-fence-refs.test.ts` | __tests__/scheduled-fence-refs.test.ts:353-357 and :501-502 assert reason.length > 40 for THIS file's three lists only (REF_EXEMPT, NOT_ON_DEFAULT_BRANCH, ALERT_EXEMPT). Every other fence's exemption list is checked only where its own author wrote the same line (e.g. __tests__/public-token-throttle-coverage.test.ts:588) and nothing forces a NEW fence's list to carry one. *Non-vacuous where it applies: REF_EXEMPT[0] is dereferenced at :204 and :650, so the lists are not empty. The v3 basis is a stub ('as reported by the reclassifying group') and cites no evidence about the rule's repo-wide scope.* |
| CANON-506 | `pnpm verify` must run `pnpm build` before lint:csp-prerender and lint:route-weight, which are the only two gates that come after it, because both measure the build output. | `package.json:79` | PARTIAL (subset) | `package.json`<br>`.github/workflows/ci.yml:394` | check-route-weight.ts:147-158 and check-csp-prerender.ts:86-95 only exit 1 on a missing build WITH --require-build; package.json:52/:74 pass no flag, so inside `pnpm verify` reordering the build makes both fences print [skip] and exit 0. ci.yml:376 and :394 pass --require-build after the build step, so the ordering self-enforces in CI only. *Basis cited only package.json:79 with no evidence beyond the script string. ci.yml:379-393 records that check-route-weight sat BEFORE the build for months, took its no-manifest branch and exited 0 while lint:ci-parity certified it as covered — the uncovered half of this rule is documented in the repo itself.* |
| CANON-532 | No tracked file may carry a secret-shaped credential — a postgres URL with an inline password, a non-demo service_role JWT or sb_secret key, a live Stripe/Resend/AWS key, a private key block, a literal password or high-entropy *_SECRET/*_PASSWORD/*_TOKEN/*_API_KEY assignment in config or docs, or the seeded demo password outside the code that logs the demo accounts in; the local stack's published demo values are exempt by exact value, and every other exception is a reasoned one-kind-one-path ALLOWLIST entry that fails once it matches nothing. | `scripts/check-secrets.ts:1-68` | ENFORCED | `scripts/check-secrets.ts:332-341`<br>`scripts/check-secrets.ts:428-446`<br>`scripts/check-secrets.ts:473-520`<br>`package.json:80` lint:secrets<br>`__tests__/check-secrets.test.ts:79-97`<br>`__tests__/check-secrets.test.ts:293-299` | scripts/check-secrets.ts:473-520 runs findSecrets (:332-341, every SCANNER over every line) on every `git ls-files` path except binaries and files over 2 MiB, applies the allowlist (:428-446) and exits 1 on any unallowlisted finding (file:line + kind, never the value) and on any allowlist entry that matched nothing; :474-479 fails a zero-file listing instead of passing it. The non-vacuity floor is __tests__/check-secrets.test.ts:79-97 — every SECRET_KINDS member must fire on a synthetic sample and a new kind without one fails — and :293-299 self-scans the fence and its test. package.json:80 lint:secrets is in the verify chain and .github/workflows/ci.yml runs it in its own step (lint:ci-parity holds the pairing). *Scope is tracked files at HEAD by design (header :63-65): untracked/ignored files such as .env.local are where secrets belong, and history is not scanned — a value already published is not unpublished by this fence, which is why the failure message says to rotate. Closes the 'no secrets' half of CANON-022 for the tree, not its 'clean history' half. L-24 in dim-interno:docs/plans/PENDIENTES.md; env_secret_assignment was added 2026-09-18 after a security review found a pasted CRON_SECRET matched nothing.* |
| CANON-539 | No tracked file may cross the public/private boundary: every file under docs/ belongs to a category declared public with a reason in PUBLIC_DOC_CATEGORIES (docs/ops/ declared file by file); no non-source file sits under a reviews/, handoff/, pilotos/, outreach/ or presentation(s)/ directory, no document sits under a demo/, pilot(s)/, piloto/, critique/ or backlog/ directory, and no document is named cutover-*, *incident*, *remediation* or with a handoff, demo, pilot/piloto, critique or backlog word in its name; no file carries the maintainer's personal mailbox or the test Gmail (by local part, so every +alias too); no docs/config line pairs an e-mail with a password-looking literal (same line, or a password line right after an e-mail line, with placeholders judged on the whole value); and no tracked .env* file carries a value other than a placeholder or a loopback local-stack default. Every other exception is a reasoned one-kind-one-path EXCEPTIONS entry, and a category or exception that matches nothing fails. | `scripts/check-public-boundary.ts:1-73`<br>`docs/agents/public-private-boundary.md` | ENFORCED | `scripts/check-public-boundary.ts:385-440`<br>`scripts/check-public-boundary.ts:443-456`<br>`scripts/check-public-boundary.ts:493-526`<br>`scripts/check-public-boundary.ts:575-628`<br>`package.json:154` lint:public-boundary<br>`__tests__/check-public-boundary.test.ts:61-70`<br>`__tests__/check-public-boundary.test.ts:272-304` | scripts/check-public-boundary.ts:575-628 lists every `git ls-files` path, runs findPath (:443-456 — undeclared docs category, private path shape) and findContent (:385-440 — mailboxes, credential pairs, env values) through scanBoundary (:493-526), and exits 1 on any finding not covered by an EXCEPTIONS entry, on any stale category or exception, on a listing under MIN_TRACKED_FILES and on a scan that saw no docs/ file. The red controls are __tests__/check-public-boundary.test.ts:61-70 — every BOUNDARY_KINDS member must fire on a synthetic violation and a new kind without one fails — :108-146 pins the document-vs-code path shapes, :148-237 the credential pairs whose values contain $ % { or a comma, and :272-304 pins the stale-entry and zero-docs non-vacuity checks. lint:public-boundary is in the verify chain and .github/workflows/ci.yml runs it in its own step (lint:ci-parity holds the pairing). *Tracked files at HEAD only: history is not rewritten, so a secret published before is neutralised by ROTATION, not by moving the file (docs/agents/public-private-boundary.md). Commit-author metadata is out of scope. Complements CANON-532 (lint:secrets), which owns secret SHAPES; this fence owns categories, paths and personal identifiers. Added R9 (Tanda A, 2026-09-24).* |
