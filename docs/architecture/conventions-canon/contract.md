# Conventions canon — Contract (`packages/contract`)

> Snapshot: `d7dbf25f7` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-26
> Verified against code on 2026-09-26 by canon v4 + blind calibration · Status: reviewed
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

[← canon index](../conventions-canon.md) · scope `contract` · 22 rules (7 ENFORCED, 5 PARTIAL, 10 UNENFORCED).

| Id | Rule | Source | Status | Enforcer | Basis |
| --- | --- | --- | --- | --- | --- |
| CANON-246 | Event type identifiers must be counted from the live EVENT_TYPES const, never trusted from a number written in prose. | `AGENTS.md:34` | ENFORCED | `__tests__/event-catalog-count.test.ts:26,36,42-62` | __tests__/event-catalog-count.test.ts:26,43 derives realCount from EVENT_TYPES.length and :49-67 fails any curated doc whose stated 'Event catalog — N types' differs; :54-57 also fails a doc that states NO count, so deleting the phrasing is not an escape. Live count is 55 (packages/contract/src/events/event-types.ts:20-196, re-exported at db/schema.ts:289). *docSays: 48 (AGENTS.md:34, opened after the basis flagged it: 'the EVENT_TYPES const IS the count (48 at last read; recount there, never trust a number written here)'). COUNT_RE does not match that phrasing, but the sentence disclaims its own number and instructs recounting — the rule's predicate (derive from the const) is what the fence implements. Counted 55 entries in the const.* |
| CANON-247 | Before writing a new event type, walk through docs/event-design-checklist.md, covering cross-cutting pattern, projection target, auto-close cron idempotency, Zod schema + schemaVersion, libreta vs non-libreta, dashboard consumers, and required test surface. | `AGENTS.md:67` | UNENFORCED | — | A process instruction with seven judgement items. Two of the seven ARE separately fenced (the Zod schema by __tests__/event-schemas.test.ts:27-40, the version/upcaster by :80/:118), but nothing enforces the walkthrough or the remaining five. *Restated in CONTRIBUTING.md:175.* |
| CANON-248 | Adding a new event type is a one-line edit to the EVENT_TYPES const with no database migration required, but the Zod schema must land in lib/events/event-schemas.ts in the same PR. | `AGENTS.md:685` | ENFORCED | `__tests__/event-schemas.test.ts:27-40`<br>`db/schema.ts:1326` | __tests__/event-schemas.test.ts:28-31 asserts IMPLEMENTED_EVENT_TYPES equals EVENT_TYPES minus UNIMPLEMENTED, and UNIMPLEMENTED is [] at :25 — the strictest state, so a new entry in the const without a schema in lib/events/event-schemas.ts fails in the same run. db/schema.ts:1289 keeps event_type a plain text column ('validated in app code against EVENT_TYPES'), so the 'no migration' half is structurally true. *The empty UNIMPLEMENTED list is an escape hatch that is currently closed; adding a name to it would silently exempt that type. The const now lives in packages/contract/src/events/event-types.ts:20 and is re-exported by db/schema.ts:289, so the rule's 'in db/schema.ts' wording is stale.* |
| CANON-249 | Deprecated event types must be removed from both EVENT_TYPES and the Zod registry so no flow can write them. | `AGENTS.md:827,830` | PARTIAL (adoption-unfenced) | `__tests__/event-schemas.test.ts:27-40` | __tests__/event-schemas.test.ts:33-40 fails on any PayloadSchemas key absent from EVENT_TYPES and :28-31 fails the reverse, so removal from one registry forces removal from the other. The 'so no flow can write them' half rests on every writer calling validateEventPayload (its throw is pinned at :42-46); db/schema.ts:1289 says event_type is 'validated in app code', and no fence forces that door. *v3 chose partialShape 'subset' on a different gap — nothing decides that a type IS deprecated, so the rule's trigger has no enforcer. Both gaps are real; the deciding one for the rule's last clause is adoption of the validating writer.* |
| CANON-250 | The deprecated incident_type value 'dog_attack' must remain in the Zod enum to preserve historical rows, even though new writes use 'bite_suffered'. | `AGENTS.md:836` | UNENFORCED | — | The value is live at lib/events/event-schemas.ts:880 with the deprecation note at :851-853, but no test pins it: rg for 'dog_attack' across __tests__/ returns zero hits. Removing it would break historical parsing with no red. *lint:events would not catch it either — its predicate is payload KEYS, not enum VALUES.* |
| CANON-251 | A *_started/*_ended bounded-process pair must write a denormalized status column at open and flip it at close, with a daily idempotent cron auto-closing the happy path. | `AGENTS.md:846-853` | UNENFORCED | — | No fence enumerates *_started/*_ended pairs or checks each has a status column and an auto-close cron. The three cited files govern different subjects: cron-registry-parity is registry↔code parity, cron-budget-ceiling is the fair-share ceiling, and the rabies fence is a single worked example. *Downgraded from draft PARTIAL ('name/topic similarity only', which the draft itself admitted).* |
| CANON-252 | A *_signal system-emitted event must set author_role='system' (or the acting party for authenticated scans) and be classified as non-libreta. | `AGENTS.md:858,859` | UNENFORCED | — | No fence pairs an event-type name suffix against its author_role or its libreta classification. The libreta split exists in code but is not derived from a *_signal naming rule that anything could fail on. |
| CANON-253 | A *_proposed/*_executed two-phase transfer must auto-cancel lazily at accept time if the receiver's precondition check fails, emitting a note_added event and notifying the original proposer, with no separate sweep job. | `AGENTS.md:869` | UNENFORCED | — | A design pattern for a class of flows, with no fence enumerating the class. The pet_transfers instance has a DB partial unique index for the concurrency half (db/schema.ts:4539) but nothing asserts the lazy-cancel behaviour as a general rule. |
| CANON-254 | A new event with three or more semantically-similar variants should use a single *_reported umbrella type with a discriminator field, not N separate event_types. | `AGENTS.md:880` | UNENFORCED | — | Refuter verdict reused: a design preference ('should prefer this umbrella over N separate event_types') with no mechanical predicate. No fence counts semantic similarity between EVENT_TYPES entries. |
| CANON-255 | UI preference flips (e.g. emergencyInfoVisible, disclose_*_when_lost) are mutable entity state, not events; do not emit events for preference flips. | `AGENTS.md:885` | UNENFORCED | — | No fence classifies a write as a preference flip. A new EVENT_TYPES entry for a preference would pass every existing gate — lint:events checks payload keys, event-schemas.test.ts checks registry parity, neither judges whether the fact deserved an event. |
| CANON-256 | A payload key, once shipped for an event type, must keep its meaning forever; a later change that needs a different shape must add a new key or discriminator variant, never redefine an existing key. | `AGENTS.md:896` | UNENFORCED | — | lint:events cannot fail on this: check-event-payload-parity.ts:10-11 checks only that a read key is WRITABLE by some schema, and :13-21 declares the check is a FLAT key set, not per-event-type. Redefining a key's meaning while keeping its name and its writer passes every arm of the fence. *Downgraded from draft ENFORCED. AGENTS.md:894 says 'Three rules, enforced by pnpm lint:events' but only rule 2 (CANON-257) carries a mechanism — AGENTS.md:897 itself is the only one that says 'enforced mechanically'. Recorded in contradictionsFound.* |
| CANON-257 | A payload key may only be read (SQL payload->>'key' or JS .payload.key) if some writer schema in lib/events/event-schemas.ts is capable of emitting it, unless it is a documented legacy exception in scripts/event-parity-baseline.json with a mandatory reason. | `AGENTS.md:897` | PARTIAL (declared-limit) | `scripts/check-event-payload-parity.ts:10-11,112,401-402,448`<br>`package.json:22` lint:events | scripts/check-event-payload-parity.ts:441-449 fails any read key no schema emits unless scripts/event-parity-baseline.json carries it with a reason (:405-411; stale entries fail at :463-468) — that half matches the rule. But the JS .payload.key form the rule names is only scanned inside files matching /\bpetEvents\b/ (:358; header :57-60, :72-77 call it a known limitation), and 9 of the 14 lib/projections/*.ts read payload.* while 0 of them reference petEvents. *docSays: 0. The prior basis asserted the baseline is empty because rg for the word 'reason' returned zero hits — that was a method error: the file holds 2 live entries keyed "path#key" whose VALUES are the reason prose (both last_known_location legacy fallbacks). I opened scripts/event-parity-baseline.json; nothing checks the reason string is non-empty. The petEvents corpus gate the judge confirmed on CANON-396 bites the same JS read form this rule names.* |
| CANON-258 | A payload schema's meaning must evolve only via read-side upcasters (bumping payload_version and adding a mapping in lib/events/event-upcasters.ts), never by rewriting existing event rows. | `AGENTS.md:898` | ENFORCED | `__tests__/event-upcaster-coverage.test.ts:1-22,122-167`<br>`__tests__/event-schemas.test.ts:80,118`<br>`db/triggers.sql:195,200` | db/triggers.sql:192-200 creates pet_events_no_update / pet_events_no_delete BEFORE UPDATE/DELETE calling enforce_pet_events_append_only(), which raises restrict_violation at :187 — a rewrite of an existing row is refused by the database. The read-side half is pinned by __tests__/event-upcaster-coverage.test.ts:122-130 (a versioned schema with no registered upcaster fails). *Declared, not verified against the live catalog; created by db/migrations/0127_pet_events_append_only.sql, and a DROP sweep of db/migrations finds no later retirement of these triggers. An audited GUC override (app.allow_event_mutation) exists by design.* |
| CANON-259 | The client-side adoption-application input schema and the server-side domain validator must agree on every field-level acceptance/refusal case, with only the two named server-only refusals (institutional applicant, duplicate pending) as declared asymmetries. | `__tests__/adoption-application-input-parity.test.ts:1-30,135-188` | ENFORCED | `__tests__/adoption-application-input-parity.test.ts:1-30,135-188` | __tests__/adoption-application-input-parity.test.ts:136-147 RUNS both validators over 13 cases and asserts verdict equality; non-vacuity at :149-159 (the ordinary case must be ACCEPTED by both, and >5 cases must refuse). The two declared asymmetries are asserted at :163-187 (institutional applicant, duplicate pending). *CASES is a hand-curated matrix: housingType/otherPets/dailyRoutine field rules are not individually sampled, so drift confined to those fields would escape.* |
| CANON-260 | `packages/contract` must have zero runtime dependencies and zero framework coupling — no `next`, `react`, `drizzle-orm`, `@/*` app aliases, nothing in `dependencies`, no `@/` app-alias import anywhere in the package, and no relative import escaping the package directory. | `scripts/check-contract-purity.ts:20`<br>`packages/contract/README.md#the-one-rule` | PARTIAL (subset) | `scripts/check-contract-purity.ts:20-33,130-131`<br>`package.json:34` lint:contract | scripts/check-contract-purity.ts:227-256 fails @/ aliases, package-escaping relative imports, FORBIDDEN_PACKAGES (next/react/drizzle/postgres/server-only) and undeclared bare imports, with a zero-file floor at :314-323. The rule's 'nothing in dependencies' clause is not enforced: ALLOWED_DEPENDENCIES :130-132 approves zod, so the manifest may declare one runtime dependency. *Live ALLOWED_DEPENDENCIES size is 1 (zod, native-readiness T1.3); the README 'zero dependencies' wording the row quotes is what the enforcer no longer matches.* |
| CANON-261 | Adding a package dependency to `@dim/contract` is a deliberate architectural change: the manifest may declare nothing outside `ALLOWED_DEPENDENCIES`, and `scripts/check-contract-purity.ts` rule 5 fails the build until the reason for the addition is written down in that file. | `scripts/check-contract-purity.ts:29`<br>`packages/contract/README.md#adding-to-it` | ENFORCED | `scripts/check-contract-purity.ts:29-33,130-131,297-303`<br>`package.json:34` lint:contract | scripts/check-contract-purity.ts:288-308 reads packages/contract/package.json dependencies+peerDependencies and flags every name not in ALLOWED_DEPENDENCIES (:297); :351-372 prints the manifest violation and exits 1. A dependency therefore cannot be added without an entry (name → reason) in the fence file itself. *'Reason written down' is structurally forced by Record<string,string> though an empty reason would pass; the operative predicate — nothing outside ALLOWED_DEPENDENCIES — fails hard.* |
| CANON-262 | Relative imports inside @dim/contract must carry their .ts extension, since Node's ESM resolver (used by expo config) does not rewrite .js to .ts. | `packages/contract/README.md#packaging-choices-and-why` | ENFORCED | `scripts/check-contract-purity.ts:41-44,162,238` (MISSING_EXTENSION_REASON)<br>`package.json:34` lint:contract | scripts/check-contract-purity.ts:231-241: for a ./ or ../ specifier inside packages/contract, !specifier.endsWith(".ts") returns MISSING_EXTENSION_REASON (:162-169); runCheck :329-335 applies it to every file of PACKAGE_GLOB (:71) and :351-372 exits 1. Non-vacuity floor at :314-323. *Reason recorded at :139-160 — expo config resolves @dim/contract/links through Node's own ESM resolver, which guesses no extension and does not rewrite .js to .ts.* |
| CANON-263 | @dim/contract must publish raw TypeScript source (exports pointing at ./src/**/*.ts), never a compiled dist/ output. | `packages/contract/README.md#packaging-choices-and-why` | UNENFORCED | — | True on the live tree — packages/contract/package.json:11-23 maps all ten subpath exports to ./src/**/*.ts and :24 files:['src'] — but no fence asserts it. check-contract-purity.ts's eight rules govern IMPORTS and the dependency manifest, never the exports map. *Indirect pressure only: rule 8 (.ts extensions) presupposes source publishing, and lint:node-version pins the type-stripping floor a source-publishing package needs.* |
| CANON-264 | @dim/contract's package.json must declare both exports and main/types pointing at the same file, so the two can never disagree. | `packages/contract/README.md#packaging-choices-and-why` | UNENFORCED | — | True on the live tree (package.json:9-12 — main, types and exports['.'] are all './src/index.ts') but unfenced: nothing compares the three fields. The README's 'can never disagree' is a statement about the current file, not a mechanism. |
| CANON-265 | Something belongs in @dim/contract only if it is a fact about the domain needing no infrastructure (no DB, request, filesystem, or renderer) — pure enums, unions, derivation, math, or design tokens. | `packages/contract/README.md#adding-to-it` | PARTIAL (subset) | `scripts/check-contract-purity.ts:20-28`<br>`package.json:34` lint:contract | scripts/check-contract-purity.ts:227-256 catches infrastructure that arrives as an IMPORT (ORM, driver, renderer, app alias, any undeclared bare package), but :243 `if (specifier.startsWith("node:")) return null` waves through node: builtins — a module reading the filesystem passes. Whether the thing is a domain FACT at all is not machine-checked. *The rule names DB, request, filesystem and renderer; only the DB/renderer/framework lanes have an import-shaped signature the fence can see. Same PARTIAL as the previous basis, reached from the node: exemption line rather than from the judgement half.* |
| CANON-266 | @dim/contract/tokens values must match app/globals.css @theme values exactly, in both directions. | `scripts/check-design-token-parity.ts:1`<br>`__tests__/check-design-token-parity.test.ts:1-9,129-139` | ENFORCED | `scripts/check-design-token-parity.ts:1,30-38`<br>`package.json:14` lint:token-parity<br>`__tests__/check-design-token-parity.test.ts`<br>`scripts/check-design-token-parity.ts:1-2`<br>`__tests__/check-design-token-parity.test.ts:129-139` | scripts/check-design-token-parity.ts:132-138 compares every LN_CSS_TOKENS entry by exact string equality against the @theme map parsed at :86-121, and :209-225 exits 1 on any mismatch (missing key included). Floors at :74-75 (120 declarations, 60 tokens) fail a parse that collapsed. package.json:14 lint:token-parity sits in the :79 verify chain. *Font families use containment, not equality (:150-158), because the CSS stack carries a next/font var. A CSS token the contract does not export is out of the rule's subject by design (header :40-43). Prior basis agrees.* |
| CANON-510 | Nothing outside packages/contract may import it BY PATH (`@/packages/contract/src/...` resolves past the exports map), and a scan finding zero package files is a FAILURE, not a pass. | `scripts/check-contract-purity.ts:34-40` | PARTIAL (subset) | `package.json:34` | check-contract-purity.ts:337-349 flags every path-form import (pathFormReference at :260-266) and :313-323 exits 1 on a zero-file scan, both wired by package.json:34 inside verify. But CONSUMER_GLOBS at :74-83 lists app/components/db/lib/src/scripts/__tests__/e2e and omits apps/** — apps/mobile, the React Native consumer the package exists for, is never scanned. *P20 corpus gate: the basis quoted the header rules 6 and 7 (:34-40) and did not read the file filter. packages/** other than the contract is also outside CONSUMER_GLOBS. No live violation in apps/mobile today — it names packages/contract only in comments and tsconfig — so this is a coverage hole, not a breach.* |
