# Conventions canon — Web application

> Snapshot: `d7dbf25f7` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-26
> Verified against code on 2026-09-26 by canon v4 + blind calibration · Status: reviewed
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

[← canon index](../conventions-canon.md) · scope `web` · 170 rules (90 ENFORCED, 39 PARTIAL, 41 UNENFORCED).

| Id | Rule | Source | Status | Enforcer | Basis |
| --- | --- | --- | --- | --- | --- |
| CANON-267 | The public credential page must expose only the minimum necessary by default; richer reveals require status, explicit owner action, or verified identity. | `AGENTS.md:144` | UNENFORCED | — | No enforcer states the general predicate. Per-field siblings exist (__tests__/caretaker-public-contact.test.ts two-key gating, __tests__/public-token-in-url-invariant.test.ts) but none asserts 'minimum necessary by default' over the credential's field set. *Policy statement; the reveal gates are enforced individually elsewhere (lost status, caretaker consent keys), never as one rule over the page's field set.* |
| CANON-268 | A new dashboard must be a new query, not a new schema, and must state which layer (spine vs cache) it reads. | `AGENTS.md:147` | UNENFORCED | — | scripts/check-screen-manifest.ts requires a manifest entry (route + layer + decision) for every gob/admin page, but its `layer` is the NAV layer, not spine-vs-cache; nothing checks that a dashboard adds no schema. *The nearest fence (lint:screens) enforces a different sense of 'layer'.* |
| CANON-269 | `router.push()`/`router.replace()` must never be used for query-only navigation state; existing per-file call-site counts are frozen and may only shrink, never grow. | `AGENTS.md:1304`<br>`scripts/check-router-refresh.ts:21` | ENFORCED | `scripts/check-router-refresh.ts:18-22` | scripts/check-router-refresh.ts:208-224 runs ratchetNavCalls over per-file push/replace hits (PUSH_REPLACE_CALL :58-59, comment-stripped :71-79) against scripts/router-nav-baseline.json — 21 files summing to 25 call sites — and exits 1 naming every file that grew; the scan covers app/, components/, src/ (:171-178) and a missing baseline makes every call site fail (:143-147). lint:nav is in verify (package.json:47,79). *Live baseline total is 25, matching the row. The 'query-only navigation state' qualifier is not distinguished by the fence — it freezes ALL push/replace call sites, which is stricter than the rule.* |
| CANON-270 | `router.refresh()` must never be used as a substitute for post-mutation UI truth — zero call sites allowed, empty allowlist. | `AGENTS.md:1305`<br>`scripts/check-router-refresh.ts:16` | ENFORCED | `scripts/check-router-refresh.ts:16-17` | scripts/check-router-refresh.ts:51 ROUTER_REFRESH_ALLOWLIST is `new Set([])`; findOffenders :80-90 matches router.refresh( / useRouter().refresh( on comment-stripped source for every production .ts/.tsx under app\|components\|src, and :197-206 exits 1 listing file:line for any hit. Empty allowlist here is the strictest state, not an inert one — the predicate runs on every file. *Absolute ban with a zero allowance -> R6 second bullet.* |
| CANON-271 | A server action must never call raw `redirect()` — it returns the `redirectTo` contract instead, and the calling form performs a full document navigation via `useActionRedirect` (contract N3). | `AGENTS.md:1307`<br>`scripts/check-action-redirect.ts:12` | PARTIAL (ratchet) | `scripts/check-action-redirect.ts:28-39`<br>`scripts/action-redirect-baseline.json` | scripts/check-action-redirect.ts:324-333 fails a redirect() in a non-baselined server-action/use-case file, with anti-vacuity floors at :305-312. The rule is absolute ('must never call raw redirect()') and scripts/action-redirect-baseline.json still carries 4 files / 6 calls. The second half (the form navigates via useActionRedirect) is checked by nothing. *live baselined calls = 6 across 4 files* |
| CANON-272 | Never invent a new location-capture mode beyond L1 and L2; raise it in design instead. | `AGENTS.md:1323` | UNENFORCED | — | No fence enumerates location-capture modes. rg over scripts/ and __tests__/ for LocationCapture / location-capture returned zero files. |
| CANON-273 | Every CTA must use one of four verb shapes (Continuar, Confirmar X, Crear X, or a domain-specific verb with its object); never a bare verb like Aceptar or Guardar alone. | `AGENTS.md:1327-1332` | UNENFORCED | — | The cited scripts/check-raw-buttons.mjs is a RAW <button> ELEMENT count ratchet (header :1-19: 'literal `<button` elements do not increase'), not a copy check — wrong subject. No fence reads CTA label text against a verb-shape grammar. *Draft 1 matched on the name 'raw-buttons'. check-copy-contract.ts bans two specific literals in app/gob+app/admin, a different rule.* |
| CANON-274 | Multi-step flows with three or more sections, or any destructive step, must use WizardShell as the only multi-step chrome. | `AGENTS.md:1338` | UNENFORCED | — | rg for WizardShell across scripts/ and __tests__/ finds no fence and no test; only e2e specs and a codemod mention it. Nothing counts sections per flow. |
| CANON-275 | Trámite-style flows (denuncia, adoption application, intake, devolución, mordedura) must end on SuccessScreen; silent redirects after the final submit are forbidden. | `AGENTS.md:1344` | UNENFORCED | — | No fence maps a flow to its terminal screen. scripts/check-action-redirect.ts bans redirect() inside actions for a different reason (the App Router drops it) and would not notice a flow ending without SuccessScreen. |
| CANON-276 | Every non-empty omnibox query must log one pii_queried audit row; never add a parallel search path that skips the scope or the log. | `AGENTS.md:1354-1355` | PARTIAL (subset) | `scripts/check-audit-log-coverage.ts:14-20` | __tests__/omnibox-search.test.ts:495-518 asserts searchOmniboxAction writes a SINGLE pii_queried row (surface=omnibox + result count) and logs nothing under 2 chars — the 'one row per non-empty query' half, for the one existing door. Nothing fences the second half (no parallel search path may skip the scope or the log); check-audit-log-coverage.ts:15-20 keys on operator+MUTATING actions, not reads, so the cited fence is the wrong subject (R12). *vitest discovery is mechanical (vitest.config.ts:19-33 computeTestPartition over every test file), so the test runs under test:verified; the previous basis found no enforcer at all for the logging half* |
| CANON-277 | Destructive bulk actions must require a reason whose minimum length matches the corresponding server action's own minimum, and the UI must never weaken those minimums. | `AGENTS.md:1358` | UNENFORCED | — | No fence compares a form's minLength against its action's Zod minimum. Nothing in scripts/check-*.ts reads both sides of that pair. |
| CANON-278 | AppShell must remain the single role-variant application chrome; do not reintroduce per-surface chrome wrappers or per-component nav literals. | `AGENTS.md:1372,1373` | UNENFORCED | — | No fence detects a competing chrome wrapper. scripts/check-icon-registry.ts names AppShell only as a baselined nav-chrome file; __tests__/public-token-landing-structure.test.tsx pins one variant's <main>, not the single-chrome rule. |
| CANON-279 | The #main-content skip-link target must be preserved in every AppShell variant. | `AGENTS.md:1381` | ENFORCED | `__tests__/public-token-landing-structure.test.tsx:241-262` | AppShell declares exactly three variants (components/layout/AppShell.tsx:43 citizen, :66 operator, :83 landing), each rendering id="main-content" (:138, :190, :238). landing is pinned by __tests__/public-token-landing-structure.test.tsx:241-250 and components/layout/AppShell.landing.test.tsx:24; operator by components/layout/AppShell.operator.test.tsx:39 and :50 (id="main-content" occurrences == 1, both banner and no-banner shapes); citizen by e2e/owner-shell.spec.ts:46,71 (ci). *The basis said only landing had an assertion; the operator colocated test does the same count check, which it did not open. Residual: the citizen variant has no vitest guard — it fails only in the e2e job.* |
| CANON-280 | 'Limpiar todo' and 'Limpiar filtros' are deliberately distinct mechanisms and must not be unified into one string. | `AGENTS.md:1392` | UNENFORCED | — | scripts/check-copy-contract.ts bans two named literals ('SLA vencido', 'Acta de') in app/gob+app/admin .tsx and nothing else; no fence protects a copy DISTINCTION from being collapsed. *A do-not-unify rule is not mechanically checkable by a literal ban; it needs both strings to keep existing.* |
| CANON-281 | The 44px touch floor applies only to the md density of Field/OpField, never to LnButton/OpButton, which must state their own height at the call site when they need to match a field. | `AGENTS.md:1396,1397` | UNENFORCED | — | The cited __tests__/mobile-a11y-fences.test.ts scans apps/mobile source for <Pressable accessibilityRole and TOUCH_TARGET/hitSlop (header :1-14) — React Native, not Field/OpField/LnButton. scripts/check-ui-invariants.ts rule 1 bans h-9 everywhere; it does not encode the scoping this rule states. *Source names e2e/mobile-390.spec.ts as asserting both floors separately; Playwright is a CI-only job, not pnpm verify — and the spec was not opened in this pass.* |
| CANON-282 | Nothing new may go in `lib/` root — every module lives in a bucket by role; a new `.ts` file directly under `lib/` must appear in the grandfathered baseline or the check fails. | `AGENTS.md:1548`<br>`scripts/check-lib-root-files.ts:7` | ENFORCED | `scripts/check-lib-root-files.ts:7-11`<br>`scripts/lib-root-baseline.json` | scripts/check-lib-root-files.ts:113 globs lib/*.ts and :83-92 fails any basename absent from scripts/lib-root-baseline.json; that baseline is literally `{}` (0 entries), so ANY .ts at lib/ root fails. :114-121 fail-closes if lib/ is missing. lint:lib-root is in package.json:79 verify. *docSays: 208 - the header records 208 files baselined on 2026-06-26; the live baseline file is empty (v2 explicitly did not count it), i.e. the ratchet sits at its strictest state.* |
| CANON-283 | New use-cases added to src/modules/*/application/** may not be added to the noRestrictedImports off-override allowlist; the goal for that list is zero entries. | `AGENTS.md:1581` | ENFORCED | `scripts/check-application-fence.ts:36-39`<br>`scripts/application-fence-baseline.json` | scripts/check-application-fence.ts:308-315 demands EQUALITY with scripts/application-fence-baseline.json ('exemptions': 34), so ADDING a file to the biome off-override fails ('the exemption list GREW ... a new coupled use-case is a regression'); :227/:240/:254 exit 1 on a vacuous scan. The rule text is 'no new entries', which is what a ratchet enforces. *0 is the stated GOAL; live baseline = 34 exemptions (the header's 46/44 is a closed incident census, P17)* |
| CANON-284 | Every jurisdiction-grouped public or analyst aggregate (and every locality-grouped Pattern-B output that exposes counts per jurisdiction) must pass through `suppressSmallCells` with k=5. | `AGENTS.md:1661,1664`<br>`docs/architecture/hexagonal-lite.md#k-anonymity-invariant` | PARTIAL (adoption-unfenced) | `lib/metrics/anonymity.ts:28`<br>`lib/metrics/types.ts` (SuppressedCells branded type)<br>`lib/metrics/anonymity.test.ts` | lib/metrics/anonymity.ts:28 pins ANONYMITY_K = 5 and :19-27 records that k is deliberately not overridable per call site, so the helper's behaviour is right. But the rule's predicate IS adoption ('every jurisdiction-grouped aggregate must pass through suppressSmallCells'), and no scripts/check-*.ts mandates it: rg for suppressSmallCells\|SuppressedCells across scripts/ hits only scripts/cursor-val-deep-c.ts, a one-off with no lint: key in verify. *The branded SuppressedCells type (anonymity.ts:7-8) blocks assigning a raw Row[] where SuppressedCells is DECLARED, but nothing forces a new fetcher to declare that return type — the single-door half of P15 is missing. Same shape as the previous basis.* |
| CANON-285 | Every aggregate must name what it excludes and against which denominator it is computed. | `AGENTS.md:1668` | UNENFORCED | — | Reusing the refuter: __tests__/census-registry-counts.test.ts contains exactly ONE describe (:114) about Drizzle binding an ISO string, not about naming exclusions or denominators. The cited enforcer does not touch this rule's subject. *scripts/check-metric-contract.ts requires operator KPIs to carry a kpi-catalog descriptor, which is the nearest structural cousin but a different rule (see missingRows).* |
| CANON-286 | Publishing a caretaker's contact on a public credential requires two separately-captured consent keys (the caretaker's public_contact_consent_at and the titular's disclose_caretaker_contact_when_lost); the titular's toggle must not even render without the caretaker's key. | `AGENTS.md:1678` | ENFORCED | `__tests__/caretaker-public-contact.test.ts:12-14,45-57,157-173` | __tests__/caretaker-public-contact.test.ts:93-130 enumerates all four key combinations against resolveCaretakerPublicContact (neither/key1 only/key2 only → null; both → contact). The render half is fenced too: components/pet-profile/LostDisclosureCard.test.tsx:182-196 asserts the titular row does NOT render with caretakerConsentName null, and :197-212 that it renders once both keys are possible. *The v3 basis states 'the toggle does not render half is a UI condition no test asserts'; that file was not opened there. A grep on discloseCaretakerContactWhenLost surfaces LostDisclosureCard.test.tsx, which asserts exactly it.* |
| CANON-287 | 'Bloquear al usuario' is a prohibited mechanism for the lost-feed report function; only per-item reporting is offered, never a per-account block. | `AGENTS.md:1700` | UNENFORCED | — | A prohibition on a feature that does not exist has no failing predicate: no fence would fail if a block-user action were added. __tests__/lost-feed-content-report.test.ts covers the per-item path that DOES exist, not the absence of the other. |
| CANON-288 | The org path to reporting a lost-feed item must be refused at both doors (the mobile API command guard and the web server action), never relying on a hidden button alone. | `AGENTS.md:1719,1720` | ENFORCED | `__tests__/actions/report-lost-feed-item-action-authz.test.ts:101-148`<br>`__tests__/api-v1-lost-route.test.ts:844-862` | Both doors fail on a violation: __tests__/actions/report-lost-feed-item-action-authz.test.ts:102-111 (org path refused, writer never called) with non-vacuity at :113-123, and __tests__/api-v1-lost-route.test.ts:844-860 (org path → 403 lost_forbidden, control.writes empty) with the one-command-wide non-vacuity at :862-869. *Draft 1 had this ENFORCED on a Glob existence check only; the assertions were read in this pass.* |
| CANON-289 | Every mutation and every sensitive read must go through a Next.js Server Action backed by Drizzle that checks session, role, and ownership before any SQL; `actions.ts` is the security boundary and stays the authoritative, unbypassable authorization gate because Drizzle bypasses Postgres RLS by design. | `AGENTS.md:989-994`<br>`docs/architecture/hexagonal-lite.md:128`<br>`docs/architecture/hexagonal-lite.md#a-concrete-walkthrough--finalize-an-adoption` | PARTIAL (subset) | `scripts/check-authz-guards.ts:50-106`<br>`scripts/check-authz-scoping.ts:248`<br>`__tests__/server-actions-auth-coverage.test.ts` | scripts/check-authz-guards.ts:434-447 requires a recognised guard CALL somewhere in each exported action body — not 'session, role and ownership before any SQL' (no ordering and no role/ownership predicate) — and scripts/check-authz-scoping.ts:59-68 declares itself REPORT-ONLY/BASELINE for tenant scoping. Nothing forces sensitive READS through a server action; RSC pages query Drizzle directly. *the guarded-mutation half is fenced; the 'every sensitive read' and 'before any SQL' halves are not* |
| CANON-290 | Opening a case must pass a structured OpenedReason code, never a free string; opened_reason is audit prose that must never be rendered to a user. | `AGENTS.md:442,443` | PARTIAL (subset) | `scripts/check-opened-reason-coverage.ts:21-29` | The 'structured code, never a free string' half is fenced: scripts/check-opened-reason-coverage.ts:74-89 bans literal openedReason assignments, and OpenCaseInput.openedReason is a closed union (:15-18). The 'never rendered to a user' half has no enforcer - components/casos/CaseDetailView.tsx, components/ui/dashboard/CaseDetailShell.tsx and two app/gob/vigilancia pages read openedReason and nothing fails. *docSays: 17 - FROZEN_LEGACY_RULE_COUNT is 16 live (scripts/check-opened-reason-coverage.ts:61, and __tests__/check-opened-reason-coverage.test.ts:137 'frozen at 16'); '17th regex' is the docblock's way of saying 16+1 fails.* |
| CANON-291 | Adding a new opened-reason writer must add a union member, never a hand-written string; tsc then requires a matching renderer and prose template. | `AGENTS.md:458` | ENFORCED | `scripts/check-opened-reason-coverage.ts:15-18`<br>`src/modules/cases/domain/opened-reason-render.ts` (mapped Record)<br>`src/modules/cases/domain/opened-reason-prose.ts` | src/modules/cases/domain/opened-reason-render.ts:49 `type RendererMap = { [C in OpenedReasonCode]: Renderer<C> }` and opened-reason-prose.ts:41 `type ProseMap = { [C in OpenedReasonCode]: ProseTemplate<C> }` are mapped over the closed union, so a new member with no renderer/prose entry cannot compile; typecheck is verify gate #2 (package.json:11,79). check-opened-reason-coverage.ts:74-89 bans the hand-written-string alternative. *The modules live under src/modules/cases/domain/, not lib/domain/ as the row's enforcer list says.* |
| CANON-292 | next build is a non-negotiable part of pnpm verify because it catches 'use server' export, server-only, and module-level-evaluation errors that tsc and Vitest do not. | `CONTRIBUTING.md:91` | PARTIAL (subset) | `package.json:79` (verify string, `&& pnpm build &&`)<br>`scripts/check-server-action-exports.ts:5-8` | package.json:79 contains `&& pnpm build &&` but that string IS the mechanism — check-ci-lint-parity.ts:56-61 deliberately filters to lint:*/verify:* links and states bare `build` is out of scope, so deleting it from verify fails nothing. ci.yml:352-353 (`Build (next build)`) is a real blocking step that exits 1 on those errors; the `part of pnpm verify` half is unfenced. *The basis reasons 'the config line IS the mechanism' and holds ENFORCED — that is the exact reasoning the calibration judge overturned on CANON-002 (P10). It is not UNENFORCED either, because ci.yml:352-353 is a CI step whose own command exits 1 on the condition (R2's one exception), so the substantive half survives. Separately, check-server-action-exports.ts:10-11 states the 'use server' export defect is NOT a build error at all, so the rule's stated rationale is partly wrong; lint:server-exports covers that class inside verify.* |
| CANON-293 | domain/ must stay pure and unit-tested without a database; infrastructure/ is the only layer allowed to issue Drizzle queries. | `docs/architecture/hexagonal-lite.md:67-69` | PARTIAL (subset) | `biome.json:67-88` (src/modules/*/domain/** noRestrictedImports)<br>`scripts/check-application-fence.ts`<br>`scripts/check-dependency-direction.ts` | biome.json:74-83 bans @/db and drizzle-orm inside src/modules/*/domain/** at level error (pnpm lint is in the verify chain) — the domain-purity half. The 'infrastructure/ is the ONLY layer issuing Drizzle queries' half is unfenced: biome.json:98-104's application override lists no db specifier, and rg finds application files importing them directly (src/modules/cases/application/operator-actions.ts, notifications/application/notification-actions.ts, organizations/application/upgrade/create-organization.ts, +7 more). *check-dependency-direction.ts covers the BETWEEN-module axis only, and says so at its header :6-8; it cannot fail on an application-layer Drizzle query. Uncovered part: src/modules/*/application/** (and actions/).* |
| CANON-294 | Multi-step writes must thread a single transaction through the repository; notifications are flushed only post-transaction, best-effort. | `docs/architecture/hexagonal-lite.md:130-131` | UNENFORCED | — | No fence checks that a multi-step write is inside one db.transaction, nor that a notification flush sits after commit. scripts/check-notifications-service.ts is a write-PATH ratchet (direct .insert(notifications) must be baselined) — a different predicate. |
| CANON-295 | `fetchPetStatusDrift` must surface `driftedColumns` naming the exact field(s) that diverged, so a non-status divergence is not mislabeled as an identical pair. | `__tests__/admin-pet-status-drift.test.ts:105-115` | ENFORCED | `__tests__/admin-pet-status-drift.test.ts:61` | __tests__/admin-pet-status-drift.test.ts:105-115 calls fetchPetStatusDrift against real cron_runs rows and asserts sample[0].driftedColumns toEqual ['status','deceasedAt'] and, for the active→active pet AR-DRIFT-03, driftedColumns toEqual ['estimatedWeightKg'] — exactly the non-status divergence the rule names. Dropping the field turns it red. *Prior basis adds the read-model side (lib/analytics/admin-metrics.ts:558,622-623), consistent with this.* |
| CANON-296 | `src/modules/*/application/**` must import zero restricted framework specifiers (next, next/cache, next/navigation, next/headers, server-only, @/lib/supabase/server) except for an enumerated, only-shrinking biome.json exemption list; every exempt path must exist and must still import a restricted specifier. | `docs/architecture/hexagonal-lite.md#the-four-layers-and-the-dependency-rule`<br>`biome.json:91-111`<br>`scripts/check-application-fence.ts:14,31`<br>`docs/adr/2026-07-18-native-readiness.md#1-standing-rule-every-strangler-extraction-stays-api-exposable`<br>`__tests__/check-application-fence.test.ts:1-10,130-208` | ENFORCED | `biome.json:91-111`<br>`scripts/check-application-fence.ts:24-39`<br>`scripts/application-fence-baseline.json`<br>`__tests__/check-application-fence.test.ts` | scripts/check-application-fence.ts:219-241 fails when the override restricts nothing or the scan sees zero application files; :245 demands exactly one exemption override; :270 every exempt path exists; :278-280 every exempt path STILL imports a restricted specifier; :291-300 no unlisted application file offends; :308 exempt.length must EQUAL scripts/application-fence-baseline.json's 34. pnpm lint:app-fence is in package.json:79's verify chain. *The rule TEXT itself grants the enumerated, only-shrinking exemption list, so the ratchet IS the rule (rulebook rule 3, second sentence). Live baseline value 34 read from scripts/application-fence-baseline.json:2. The 46-vs-44 pair in the header (:17-20) is a closed 2026-08-20 incident, not a live number (P17/P18).* |
| CANON-297 | Every exported server action that establishes operator authority and reaches a database mutation must also reach an audit write, or carry a documented no-audit opt-out; discovering zero action files or zero operator-mutating candidates is a FAILURE, and the audit-coverage baseline must have no stale entries. | `scripts/check-audit-log-coverage.ts:15,31`<br>`__tests__/check-audit-log-coverage.test.ts:1-16,38-224` | PARTIAL (ratchet) | `scripts/check-audit-log-coverage.ts:14-37`<br>`__tests__/check-audit-log-coverage.test.ts` | scripts/check-audit-log-coverage.ts:301-319 exits 1 on zero action files / zero operator+mutating candidates, :342-361 fails a NEW unaudited operator mutation and :364-374 fails a stale baseline entry. But the rule's first clause is absolute and scripts/audit-log-coverage-baseline.json:63-91 still grandfathers 28 operator actions that neither reach an audit write nor carry @no-audit-required; the header at :43-50 also declares one-hop and operator-only blind spots. *docSays: 67 hand-written call sites — a header census (P17), not a live threshold. The previous basis quotes the header as 'the rule verbatim' and never opens the baseline file; 28 live entries put this on R6's third bullet (absolute rule, baseline > 0).* |
| CANON-298 | Every registered heavy DB call site must reference a real budget-wrapper call (`withDbBudget`/`loadWithTimeout`), not merely mention the wrapper's name; a `DELEGATING_ROUTES` exemption must name an owner the route actually imports and calls. | `scripts/check-db-budget.ts:14`<br>`__tests__/check-db-budget.test.ts:1-13,63-246` | ENFORCED | `scripts/check-db-budget.ts:13-26`<br>`__tests__/check-db-budget.test.ts` | scripts/check-db-budget.ts:599 requires an actual CALL (\bwrapper\w*\s*(?:<…>)?\s*\() in comment/string-stripped code, not a mention; runScan :761-768 exits 1 on registered sites with none, and :770-778 fails DELEGATING_ROUTES claims whose owner does not exist or is not imported and called (:384-410). Zero-target guard at :755-758. *The substring bug this replaced (app/gob/perdidas/page.tsx green on a stray mention) is recorded at :21-26. The known blind spot at :40-46 (an unbounded await inside a shared child component) sits outside this rule's predicate, which is about the registered site itself.* |
| CANON-300 | No interactive control's Tailwind classes may size it under 44px, JSX text must not render a raw SCREAMING_CASE enum or an English word from the UI denylist, and the raw `<button>` count in tracked files must equal `RAW_BUTTON_BASELINE` exactly. | `__tests__/check-ui-invariants.test.ts:1-7,27-503` | PARTIAL (subset) | `scripts/check-ui-invariants.ts:44,680,943-950`<br>`__tests__/check-ui-invariants.test.ts` | scripts/check-ui-invariants.ts:943-951 fails in BOTH directions against RAW_BUTTON_BASELINE=25 (:680) counted live over globSync {app/admin,app/gob}/**/*.tsx (:685), and rules 2/4 run over STANDARD_FILES (:777,:799). But the 'under 44px' limb is only the 36px family TOUCH_TARGET_TOKENS (:44): h-10 (40px) is deliberately unmatched and pinned as unmatched by __tests__/check-ui-invariants.test.ts:53, while 9 app/components files use h-10. *Two of three limbs are exact (the raw-button equality pin and the enum/denylist JSX scans); the sizing limb as worded exceeds the token denylist. Previous basis cited the same two-sided pin and did not weigh the sizing wording.* |
| CANON-301 | es-AR UI copy must carry accents on the enumerated word list when the word appears as prose, but not when it appears in a code/symbol position (object key, route segment, kebab slug). | `__tests__/check-ui-invariants.test.ts:108-333` | ENFORCED | `scripts/check-ui-invariants.ts:887` (rule 3 — the symbol-position exemption)<br>`__tests__/check-ui-invariants.test.ts:108-333` | scripts/check-ui-invariants.ts:187-202/225-238/339 hold the accent word lists, scanned over ACCENT_FILES {app,components,lib,packages,src} (:738) at :868-882; the code/symbol half is isSymbolPosition() (:408-433: kebab slug, member access, object key, lone lowercase string value) plus hasAccentedNeighbor (:398). Both halves are pinned by __tests__/check-ui-invariants.test.ts:281-320 (symbol vs copy cases) and :112-262. *ACCENT_ALLOWLIST has one justified entry (:481). The rule scopes itself to 'the enumerated word list', so list size is not a scope gap.* |
| CANON-302 | A button-shaped CSS rule (`button`, `.lp-btn`, `[type=submit]`) must use a tokenized border-radius and its font-size must not fall below the `--text-xs` floor. | `__tests__/check-ui-invariants.test.ts:538-618` | ENFORCED | `scripts/check-raw-buttons.mjs` (findCssButtonViolations)<br>`__tests__/check-ui-invariants.test.ts:538-618` | scripts/check-raw-buttons.mjs:454-455 set CSS_BUTTON_RADIUS_BASELINE=0 and CSS_BUTTON_FONT_BASELINE=0; scanCssButtons :457-498 runs findCssButtonViolations (:305-330) over app/**/*.css and exits 1 on the first hit — selector regex :271 covers bare `button`, any *btn* class (.lp-btn) and [type=button\|submit]; radius must be var(--radius-pill\|op-btn) (:274) and font-size must not fall below --text-xs read from globals.css (:276-283). __tests__/check-ui-invariants.test.ts:552-618 pins both predicates and the live floor. *Absolute rule with a 0 baseline -> R6 second bullet. CSS_SCAN_GLOB is app/**/*.css only; the one stylesheet outside it (components/layout/operator-print-escape.css) declares no button/border-radius/font-size rule, so the gap is currently empty.* |
| CANON-303 | The web and native notification inboxes must render the exact same display order and grouping for the same set of notifications, including id-descending tie-breaking on same severity and instant. | `__tests__/notification-ordering-parity.test.ts:1-37,221-278` | ENFORCED | `__tests__/notification-ordering-parity.test.ts:1-14,221-278` | __tests__/notification-ordering-parity.test.ts:222-232 runs the web projection (app/(app)/notificaciones/notification-ordering) and the wire projection (@dim/contract/notifications) over one fixture and asserts equal id sequences; :242-246 equal grouping; :264-270 the id-descending tie on equal severity+instant; :235-240 anti-vacuity (fixture is not pre-sorted). Native really uses those functions: apps/mobile/src/notifications/notifications-view-model.ts:58-60. *The adoption half the basis did not check holds: the mobile view-model calls sortForDisplay/groupForDisplay/wireNotificationFacts directly.* |
| CANON-304 | Only admin/coordinator org roles may manage coverage zones; a province-only coverage row must broadcast to any locality in that province but not to another province, while a locality-specific row matches only its exact locality. | `__tests__/org-coverage.test.ts:1-25,397-834` | ENFORCED | `__tests__/org-coverage.test.ts:397,554,629,649` | __tests__/org-coverage.test.ts:397-434 member and volunteer calling addCoverageZoneAction get { error } and write nothing; :555 remove and :630 setPrimary repeat the role gate; :657 a province-only row matches a pet in that province, :707 a DIFFERENT locality of the same province, :728 a locality row matches only its exact locality, :804 a province row does NOT match another province. Real actions against the DB. *isManagerRole itself pinned at :363-372 (admin/coordinator true, member/volunteer false).* |
| CANON-305 | The global outbox SLA-breach count must be a global `count(*)` with predicate `status='pending' AND sla_due_at < now()`, shared as the single source of truth by every surface that displays it. | `__tests__/outbox-breach-count.test.ts:1-8,79` | PARTIAL (adoption-unfenced) | `__tests__/outbox-breach-count.test.ts:1-15` | __tests__/outbox-breach-count.test.ts has exactly two its (:80, :109), both on countOutboxBreaches's own predicate (7 breached / 3 within-SLA / 2 delivered, delta == 7). Nothing asserts the sharing clause, and a surface displaying a breach count does NOT use it: app/gob/outbox/page.tsx:149 computes breachCount from the visible page rows (rows.filter(buildBreachCue…)), the exact defect the rule names. *app/admin/outbox/page.tsx:76, app/admin/layout.tsx:75 and lib/analytics/admin-metrics.ts:332 do call the shared helper; the gob twin does not. The basis established only that the helper's own test is a real DB integration test.* |
| CANON-306 | `getOwnedPetsCountCached` must be memoized with React `cache()`, take only `userId` as its argument, and count active ownerships joined to non-deceased pets — reusing exactly the predicate `/inicio`'s own redirect uses. | `__tests__/owned-pets-count-slot-signal.test.ts:1-23,34-94` | ENFORCED | `__tests__/owned-pets-count-slot-signal.test.ts:13-14,34-94` | __tests__/owned-pets-count-slot-signal.test.ts:36 pins `export const getOwnedPetsCountCached = cache(` in lib/infra/request-cache.ts; :88-94 pins the signature is exactly `async (userId: string): Promise<number>`; :42-47 and :56-61 pin the body's ownerships + isNull(endedAt) + innerJoin(pets) + ne(pets.status,"deceased") + count(); :67-81 requires the same three clauses inside fetchLivePetsForCarouselRanking (the /inicio predicate) and in the cached helper. *Source-scan style, but each anchor is extracted by slicing on the live symbol — a rename or a dropped clause fails.* |
| CANON-307 | A consolidated (FILTER-merged) analytics query must return exactly the same counts as its pre-consolidation two-query equivalent, verified against seeded fixtures with exact pinned counts. | `__tests__/pf1-consolidation-parity.test.ts:1-31` | ENFORCED | `__tests__/pf1-consolidation-parity.test.ts:9-14` | __tests__/pf1-consolidation-parity.test.ts drives the five live FILTER-merged fetchers against isolated seeded fixtures with exact pinned counts (:271-272, :355-356, :455-463, :529-531, :584) and, for the arm whose raw count the eligibility gate hides, runs the merged FILTER expression AND two pre-consolidation-style queries over the same inArray-restricted row set and asserts they agree (:676-679). *Covers every consolidation named in the header; nothing structurally forces a FUTURE merge to add a parity case (same caveat the basis notes).* |
| CANON-308 | No internal (UUID-shaped) identifier may be interpolated into a URL route segment declared as public (`public*`/`*Token`/`*Code`) — public routes must carry only public tokens. | `__tests__/public-token-in-url-invariant.test.ts:1-25,183-207` | ENFORCED | `__tests__/public-token-in-url-invariant.test.ts:1-14,183-207` | __tests__/public-token-in-url-invariant.test.ts derives routes by walking app/ keeping the PARAMETER NAME (:46-75) and link interpolations from app\|components\|lib\|src (:34, :152); :183-207 fails on any ${expr} landing in a slot isPublicSlot() calls public (/^public/i or /(Token\|Code)$/) when namesAnInternalId(expr); :158-170 are non-vacuity floors (>500 links, >20 named params) and :172-181 pins the predicate itself. *Detection is by identifier NAME, so a UUID carried in a neutrally-named variable is out of reach — inherent to a static fence, and the rule is written about the id-shaped name.* |
| CANON-309 | Every anonymous route or use-case that resolves a public credential token must apply the per-IP read throttle before the lookup, in one of four recognized forms, with a literal known bucket name; each route must spend its own named bucket, never shared. | `__tests__/public-token-throttle-coverage.test.ts:1-95,592-721` | ENFORCED | `__tests__/public-token-throttle-coverage.test.ts:1-95,592-721`<br>`lib/infra/public-token-throttle.ts` (KNOWN_BUCKETS) | __tests__/public-token-throttle-coverage.test.ts derives its census from CALL SITES of publicPetByToken over app/ and src/ (:27-31, :39-48), comments stripped (:80-87); :592-597 every in-scope resolver must carry one of the four guard forms, :599-605 all four forms must have a live user, :607-660 and :662-699 the guard must PRECEDE the lookup with non-vacuity counts (>=3 components, >=1 whole-file, ==5 src files), :701-711 every door caller passes a literal bucket, :713-721 buckets are unique and in KNOWN_BUCKETS. *Live count of recognized forms is 4 (:604 adapter/direct/port/write). Exemptions carry written reasons and are re-checked at :585-589.* |
| CANON-310 | Only the files listed in `ALIAS_RESOLVERS` may spell the authenticated alias `unerasedPetByToken(`; a new speller fails until reviewed and explicitly added, in both directions. | `__tests__/public-token-throttle-coverage.test.ts:1054-1145` | ENFORCED | `__tests__/public-token-throttle-coverage.test.ts:1054-1145` | __tests__/public-token-throttle-coverage.test.ts:1131 — expect(spellers).toEqual(sorted ALIAS_RESOLVERS) over allSources() (:257-273, every non-test .ts/.tsx under app/ and src/), so a new speller AND a stale pin both fail; :1134-1143 is the RED control proving imports and comments do not count. *rg over the tree confirms every live speller is under app/ or src/, i.e. inside the corpus; lib/ and components/ are outside the walk but hold no speller today. Basis agreed and added nothing I had not opened.* |
| CANON-311 | Every exported async function in a server-action file must be an inner writer, call a known auth guard, or carry an explicit `// @no-auth-required: <reason>` comment immediately above the export. | `scripts/check-authz-guards.ts:4`<br>`__tests__/server-actions-auth-coverage.test.ts:1-32,166-192` | ENFORCED | `scripts/check-authz-guards.ts:3-16,50-106`<br>`__tests__/server-actions-auth-coverage.test.ts` | scripts/check-authz-guards.ts:434-447 flags every exported async function in a "use server" module that is not an inner writer, does not call an AUTH_GUARDS name (on comment-stripped body) and has no @no-auth-required comment; discovery is by content (listActionFiles :1045-1055) with an exit-1 floor at :1066, and lint:authz is in verify. *the action rule accepts a BARE @no-auth-required (only the route-handler rule at :690-695 demands a written reason)* |
| CANON-312 | a11y rules noBlankTarget, noLabelWithoutControl, useButtonType, useHtmlLang, and related a11y checks are errors, not warnings. | `biome.json:33` | ENFORCED | `biome.json:33` (linter.rules.a11y) | biome.json:33-49 sets a11y recommended plus fifteen named rules — noBlankTarget :35, noLabelWithoutControl :36, useButtonType :41, useHtmlLang :43 among them — all at "error", and package.json:12 `lint` = `biome check . app/.well-known` sits in the verify chain at :79. The rule constrains CODE, and offending markup fails lint. *Same verdict as the previous basis, different ground: not 'a config block is its own mechanism' (the CANON-002 error) but R2's explicit exception — biome rules at level error with lint in verify ARE enforcers, because the failing artifact is the offending source file.* |
| CANON-313 | Cognitive complexity above 25 is an error for all files, except an enumerated list of files raised to a ceiling of 160. | `biome.json:52` | ENFORCED | `biome.json:52` (complexity.noExcessiveCognitiveComplexity + the named override block) | biome.json:52-57 sets noExcessiveCognitiveComplexity to level "error" with maxAllowedComplexity 25, and biome.json:381 is the single override raising it to 160 over an enumerated include list (:389+). `pnpm lint` runs biome check inside verify, so a function above the ceiling fails the gate. *Live values: 25 global (:55), 160 in the named override (:381). The exception list is granted by the rule text itself, so it is not a subset gap.* |
| CANON-314 | `src/modules/*/domain/**` must not import `@/db` (runtime), `drizzle-orm`, `next`, `next/cache`, `next/navigation`, `next/headers`, `server-only`, `@/lib/supabase/server`, or any repository — only other `domain/` files and `@/db/schema` types. | `docs/architecture/hexagonal-lite.md#the-four-layers-and-the-dependency-rule`<br>`biome.json:66-88` | PARTIAL (subset) | `biome.json:74-83` (src/modules/*/domain/** noRestrictedImports) | biome.json:75-82 restricts exactly 8 specifiers for src/modules/*/domain/** at error (@/db, drizzle-orm, next, next/cache, next/navigation, next/headers, server-only, @/lib/supabase/server). The rule's trailing clause 'or any repository' has no match: noRestrictedImports keys are literal specifiers, so a domain file importing ../infrastructure/*-repository or a relative repository path passes. *check-dependency-direction.ts only bans BETWEEN-module edges (header :6-8), so a same-module domain→infrastructure repository import is unfenced. Uncovered part: repository imports.* |
| CANON-315 | __tests__/**, scripts/**, and *.test.ts(x) files may use non-null assertions, forEach, and explicit any without triggering those biome rules. | `biome.json:172` | UNENFORCED | `biome.json:171-186` | biome.json:172-186 IS the cited enforcer — an override over __tests__/**, scripts/**, **/*.test.ts(x) turning noNonNullAssertion, noForEach and noExplicitAny "off". A rule at level off can never fire, and nothing reads the override back: rg for `biome.json` under __tests__/scripts hits only check-application-fence.ts, check-dependency-direction.ts and the two application-fence tests, none of which touch this block. *Self-executing declaration, true at this SHA. The previous basis upgraded this on the reasoning 'a permission rule stated as a config override IS its own mechanism' — the exact clause the judge rejected in CANON-002. The rule grants a PERMISSION, so no code state violates it; deleting the override would surface as lint errors on existing test files, but that is the edit's cost, not a check of the rule.* |
| CANON-316 | Biome must format code with double quotes, always-present semicolons, and trailing commas in all positions. | `biome.json:497` | ENFORCED | `biome.json:497` (javascript.formatter)<br>`package.json:12` lint = biome check | biome.json:497's javascript.formatter supplies the predicate (quoteStyle double, semicolons always, trailingCommas all) and package.json:12 `lint` = `biome check . app/.well-known` — in the verify chain at :79 — FAILS on any file whose formatting disagrees. The rule constrains code, and unformatted code reds the gate. *`biome check` includes the formatter diff (not `format --write`), so this is not a silent setting: the failing artifact is the source file, not the config.* |
| CANON-317 | Biome must ignore .next, node_modules, dist, out, supabase/.temp, supabase/.branches, apps/mobile/android, apps/mobile/ios, dim-interno:docs/design/handoffs/** and dim-interno:docs/reviews/results/** entirely. | `biome.json:10` | UNENFORCED | `biome.json:10` (files.ignore) | biome.json:10-21 IS the cited enforcer — the files.ignore array itself (.next, node_modules, dist, out, supabase/.temp, supabase/.branches, apps/mobile/android, apps/mobile/ios, dim-interno:docs/design/handoffs/**, dim-interno:docs/reviews/results/**). Nothing reads it back; rg for `biome.json` under __tests__/scripts/e2e returns only the application-fence pair, which parses the overrides array, never files.ignore. *Self-executing declaration, true at this SHA. The previous basis conceded the point in its own note — 'a scope declaration cannot fail on its own' — and still recorded ENFORCED; under R2 that is UNENFORCED.* |
| CANON-318 | noUnusedVariables and noUnusedImports must be errors project-wide, with an override list of files where they are turned off. | `biome.json:60` | ENFORCED | `biome.json:60` (linter.rules.correctness) + two later override blocks | biome.json:59-62 sets correctness.noUnusedVariables and noUnusedImports to "error"; package.json:79 verify runs `pnpm lint`. Two override blocks turn them off for enumerated files (biome.json:389-402 noUnusedImports for 5 files; :404-493 noUnusedVariables for ~79) — the rule text itself provides for that list. *Brief's explicit R2 carve-out: biome rules at level error with lint in verify ARE enforcers. The override list is not ratcheted by any fence, but the rule text names it as legitimate. Same conclusion as the previous basis.* |
| CANON-319 | Native-facing code must never read `next/headers` cookies for identity — `/api/v1` authentication resolves identity strictly from the Authorization header via `createClientFromBearer` → `requireLiveUser`, with no cookie fallback; a refusal returns 401 + a code, never a redirect. | `docs/architecture/api-invariants.md#9-merge-checklist`<br>`docs/adr/2026-07-18-native-readiness.md#3-auth-for-native` | UNENFORCED | — | No fence scans app/api/v1 for next/headers or cookies(): the only scripts/check-*.ts that names next/headers is check-application-fence.ts, whose corpus is src/modules/*/application/**. check-api-guard-headers.ts bans middleware-STAMPED headers, a different subject. Per-route tests use createClientFromBearer, but none asserts the absence of a cookie path as a rule over the surface. *A cheap fence exists in shape (the v1 route glob is already pinned at check-api-v1-envelope.ts:58 with a floor of 33 files) but no rule for cookies was added to it.* |
| CANON-320 | Every use-case graduating into `src/modules/<domain>/application/` must accept a plain-data input (never FormData, Request, or Headers) and return a plain-data, JSON-serializable result, signaling failure through typed discriminated results rather than throwing for expected domain conditions. | `docs/adr/2026-07-18-native-readiness.md#1-standing-rule-every-strangler-extraction-stays-api-exposable` | UNENFORCED | — | check-application-fence.ts restricts IMPORTS (next, next/cache, next/navigation, next/headers, server-only, @/lib/supabase/server) — it never inspects a use-case's parameter or return TYPE. FormData and Request are global types needing no import, so the fence cannot see them. *A real gap: the import fence is often read as covering this rule, and it structurally cannot.* |
| CANON-321 | New or touched use-case error branches should carry a stable, SCREAMING_SNAKE_CASE error code alongside the human-facing message, not a bare Spanish string. | `docs/adr/2026-07-18-native-readiness.md#2-api-contract-shape-when-native-arrives` | UNENFORCED | — | No fence inspects UseCaseResult's failure arm. packages/contract/src/api/errors.ts:17-20 says so explicitly: 'It is not a repair of UseCaseResult's failure arm, which is an untyped string carrying Spanish prose in most of the ten module copies'. *The rule is stated as 'should', and the contract package's own header records that the failure arm is untyped today.* |
| CANON-322 | Future /api/v1 pagination must be cursor-based (keyset), never offset-based. | `docs/adr/2026-07-18-native-readiness.md#2-api-contract-shape-when-native-arrives` | UNENFORCED | — | The cited __tests__/keyset-pagination.test.ts is a UNIT test of lib/keyset-pagination.ts's pure helpers (:1-8: encode/decode roundtrip, tampered input, keysetWhere shape, olderHref/newerHref). It proves the helper works; it never enumerates /api/v1 routes and cannot fail on an offset-paginated one. Refuter pattern 2. *The ADR cites the test as a PRECEDENT, and draft 1 read the citation as enforcement.* |
| CANON-323 | Any new /api/v1 event-writing route requires an Idempotency-Key header mapped to pet_events.client_idempotency_key, deduped via the pet_events_idempotency_idx partial unique index. | `docs/adr/2026-07-18-native-readiness.md#2-api-contract-shape-when-native-arrives` | UNENFORCED | — | Reusing the refuter: __tests__/idempotency-guards.test.ts is per-use-case (createIntake :172, createTattooForUser :244, createVaccineReminder :301, FosterRepository :361, confirmChipMatch* :411/:455); it never globs app/api/v1/** and never asserts the header is required. Per-route tests exist (api-v1-amend, api-v1-record-event, api-v1-lost, api-v1-shares) but no route-enumerating fence. *The DB half (pet_events_idempotency_idx partial unique index) is a live constraint; the ROUTE half is what is unenforced.* |
| CANON-324 | requirePetAccess/requireAlivePetAccess must remain the single security boundary for pet-scoped operations regardless of transport (cookie or bearer token) — no second auth model. | `docs/adr/2026-07-18-native-readiness.md#consequences` | UNENFORCED | — | check-authz-guards.ts lists requirePetAccess/requireAlivePetAccess among 20+ accepted AUTH_GUARDS (:80-81) — it accepts ANY of them, so a route that invented a second pet-access model and listed it would simply be added to the list. Nothing asserts these two are the ONLY pet-scoped boundary. *check-titular-gate.ts enforces a narrower, related rule (role inside the ownership row) and does discover by EFFECT — the shape this rule would need.* |
| CANON-325 | The public-token-resolves-to-QR-page path must stay anonymous — it needs no auth from web or native clients. | `docs/adr/2026-07-18-native-readiness.md#3-auth-for-native` | UNENFORCED | — | A 'must stay open' rule has no failing predicate in this repo's fences, which all detect MISSING guards. check-authz-guards.ts would accept the public route either way (a `// @no-auth-required` comment satisfies it, and so would adding a guard). *The throttle-coverage fence (CANON-309) covers the adjacent requirement that the anonymous path be rate-limited.* |
| CANON-326 | Push notifications must stay transport-agnostic: use-cases collect plain-data NewNotification[] with no transport-specific fields; only the action layer chooses the delivery transport. | `docs/adr/2026-07-18-native-readiness.md#fitness-hooks` | UNENFORCED | — | scripts/check-notifications-service.ts enforces the WRITE PATH (no new direct .insert(notifications); go through createNotification/createNotificationsBulk) — not the payload's transport neutrality. Nothing inspects NewNotification fields for a web-only URL scheme. *Stated as a review question ('fitness hook'), not as a mechanical predicate.* |
| CANON-327 | Anchor `lib/ui/audit-filters.ts` date filters at Argentine midnight (fixed -03:00, no DST), never at UTC midnight. | `docs/agents/README.md:77-80` | UNENFORCED | — | lib/ui/audit-filters.ts:62 does it right (`new Date(`${raw}T00:00:00.000-03:00`)`, PO decision noted at :56) but the implementation is not an enforcer. __tests__/audit-filter-options.test.ts covers only buildAuditActionOptions labels/aliases (:17-64) — nothing asserts the -03:00 anchor. lint:timezone's corpus is toLocaleDateString/toLocaleString/Intl.DateTimeFormat calls, not date PARSING. *Same shape as the refuter's CANON-202: the correct helper exists, nothing fails if someone reverts it.* |
| CANON-328 | The pet-photo upload flow must land a signed upload in a private bucket, then re-authorize, verify magic bytes and re-encode in a `confirm` step before writing to the public bucket. | `docs/agents/collaborating-writer.md:118-121` | ENFORCED | `__tests__/pet-photo-upload.test.ts:189-285` | __tests__/pet-photo-upload.test.ts:240 asserts the minted ticket's bucket is STAGING_BUCKET (the private uploads-staging); :269-283 confirm refuses SVG/HTML/ZIP by sniffing and removes the staged object; :313 fails closed when sharp throws (never the original bytes); :322 stores the RE-ENCODED bytes under a key derived from the sniffed type; :364 and :394 re-authorize at confirm (soft-deleted pet, erasure mid-transaction) and take back what was written. *Also :205-232 the staged-path prefix-equality guard, so a confirm cannot promote another pet's staged object.* |
| CANON-329 | An anonymous denuncia's rate-limiter bucket key must not leak the caller's user id into an unbounded reader population; `redactCallerId` closes this and reverting it must turn a test red. | `dim-interno:docs/agents/handoff-2026-08-30.md:136-140` | ENFORCED | `__tests__/api-v1-welfare-reports-route.test.ts:928-936` | __tests__/api-v1-welfare-reports-route.test.ts:935 asserts the fail-open error report does NOT contain the caller uuid (ME), with non-vacuity at :938 that the bucket+window text ('welfare_auth:«caller»:hour:') survives — so deleting redactCallerId (app/api/v1/welfare-reports/commands.ts:490,507) turns it red. *The claim the doc makes about the test is verifiable and true — rare in this corpus.* |
| CANON-330 | A `/api/v1` route must answer only through `apiV1Json`/`apiV1Error` (lib/infra/api-v1.ts) — never NextResponse.json(, new NextResponse(, new Response(, or Response.json( directly. | `scripts/check-api-v1-envelope.ts:19`<br>`docs/architecture/api-invariants.md#9-merge-checklist` | PARTIAL (declared-limit) | `scripts/check-api-v1-envelope.ts:19-25,246,262,305,312,340` | scripts/check-api-v1-envelope.ts:303-316 fails the four hand-built spellings and a missing apiV1Json/apiV1Error import over 33 routes (floor :246). But the fence's own header at :45-49 declares the hole: 'a response built in a helper module the route imports (the fence reads the route file only)' — a route that imports the helper and delegates one branch to a wrapper still answers outside apiV1Json, which the rule's 'answers ONLY through' clause forbids. *live MIN_V1_ROUTE_FILES = 33. The previous basis rates this ENFORCED on :305/:312/:246 and does not address the stated blind spot at :45-49, which I opened; the hole is narrow (the import check catches a route that never touches the helper) but it is self-declared and inside the rule's scope* |
| CANON-331 | Every `/api/v1` route must be rate-limited with a literal (not computed) bucket name — including any `perLookup.bucket` — and its fail-open/fail-closed direction must be stated. | `scripts/check-api-v1-envelope.ts:26`<br>`docs/architecture/api-invariants.md#9-merge-checklist` | PARTIAL (subset) | `scripts/check-api-v1-envelope.ts:26-33`<br>`__tests__/public-token-throttle-coverage.test.ts` | scripts/check-api-v1-envelope.ts:319-330 fails a non-literal surface bucket and a non-literal perLookup.bucket, but only WHERE publicTokenThrottle/perLookup already appear — no check requires a /api/v1 route to be rate-limited at all, and nothing asserts the fail-open/closed direction is stated (rg over the fence and __tests__/public-token-throttle-coverage.test.ts finds no such assertion). *the throttle test (:553-720) covers public-token resolvers, not every v1 route (/auth/login, /me)* |
| CANON-332 | Every /api/v1 response carrying pet, case, denuncia or share data must set cache-control: no-store explicitly — the middleware prefix allowlist does not cover /api/v1. | `docs/architecture/api-invariants.md#4-cache-control-no-store-is-not-inherited` | ENFORCED | `lib/infra/api-v1.ts:40-42,58-61`<br>`scripts/check-api-v1-envelope.ts:305,312`<br>`__tests__/api-v1.test.ts:17-38` | lib/infra/api-v1.ts:40-43,58-61 sets cache-control: no-store LAST, after any caller headers, so no route can override it. scripts/check-api-v1-envelope.ts:303-307 fails a v1 route that does not import the helper and :308-315 fails any hand-built response (NextResponse.json/new NextResponse/new Response/Response.json), over globSync("app/api/v1/**/route.ts") (:58) with a non-vacuity route-count floor (:342). pnpm lint:api-v1 is in verify (package.json:79). *P15 single-door shape: the fence mandates the helper, so the helper's guarantee reaches every route rather than only its own test.* |
| CANON-333 | /api/v1 errors must use the { error: "snake_case_code" } envelope with a code from the agreed vocabulary. | `docs/architecture/api-invariants.md#9-merge-checklist` | ENFORCED | `lib/infra/api-v1.ts:24-26,36`<br>`scripts/check-api-v1-envelope.ts:305,312`<br>`__tests__/api-v1.test.ts:41-63` | lib/infra/api-v1.ts:72-79 — apiV1Error(code: ApiV1ErrorCode, …) builds `const body: ApiV1Error = { error: code }`, a single-key envelope whose code is typed against @dim/contract/api (:36), so an invented code fails typecheck (verify gate #2). scripts/check-api-v1-envelope.ts:303-315 forbids any v1 route from answering except through that helper, so no route can hand-roll a different error shape. *snake_case-ness of a NEWLY added vocabulary member is not itself asserted anywhere I opened; the closed union is what makes the code set closed. The previous basis records the same caveat (CANON-344).* |
| CANON-334 | Every section of a /api/v1 read must report its own availability; nothing may degrade to a silent empty section. | `docs/architecture/api-invariants.md#9-merge-checklist` | UNENFORCED | — | check-api-v1-envelope.ts has exactly three rules — response helper, literal limiter bucket, authorization — and none inspects payload section shape. No test enumerates v1 read sections for an availability flag. *scripts/check-degraded-chrome.ts enforces the analogous rule for server components, not for /api/v1 payloads.* |
| CANON-335 | Every /api/v1 read must carry payloadVersion, issuedAt, and staleAfter. | `docs/architecture/api-invariants.md#9-merge-checklist` | UNENFORCED | — | apiV1Envelope exists and is proved by __tests__/api-v1.test.ts:65-84, but nothing REQUIRES a read route to call it: check-api-v1-envelope.ts mentions the three fields only in its motivation header (:10) and its import check names apiV1Json/apiV1Error only (:305). A read route that skipped apiV1Envelope would pass every gate. *Asymmetry worth naming: the no-store and error-envelope halves ARE structurally enforced (CANON-332/333) because they live in the two helpers the fence requires; the read envelope lives in a third helper the fence does not require.* |
| CANON-336 | Success and failure responses must be indistinguishable wherever an oracle exists, proven by a response-equality test per oracle. | `docs/architecture/api-invariants.md#9-merge-checklist` | PARTIAL (subset) | `__tests__/denuncia-access-timing-oracle.test.ts` | __tests__/denuncia-access-timing-oracle.test.ts:156-190 proves response equality (message, throttled, and the latency arm) for ONE oracle — solicitarAccesoDenunciaAction. Nothing enumerates the other oracle surfaces or requires a response-equality test per oracle; the 'wherever an oracle exists' half rests on the merge checklist in docs/architecture/api-invariants.md, which no fence reads. *Subset covered: the denuncia oracle. Draft 1 read the doc's 'reference test shape' citation as coverage of the general rule.* |
| CANON-337 | Any route resolving a public token must go through publicPetByToken so the coverage fence can see it. | `docs/architecture/api-invariants.md#9-merge-checklist` | UNENFORCED | `__tests__/public-token-throttle-coverage.test.ts:1-95` | __tests__/public-token-throttle-coverage.test.ts:277 — the census IS `allSources().filter(includes('publicPetByToken('))`, so it only grades files that already spell the door. The file records twice that a resolver using a hand-rolled `eq(pets.publicToken, …)` is invisible to it: :681-684 ("It was never exempt; it was INVISIBLE") and :692-694. Nothing fails when a route skips the door. *Rule predicate IS adoption of a single door; no P15-style structural mandate exists. The ALIAS census (:1124) closes only the second NAME, not raw drizzle resolution. The v3 basis concedes the same gap ('a route that resolved a token by another path would be invisible to it') and then credits the alias pin with closing it — the alias pin bans one spelling, not the unnamed path the file's own :681-694 note describes.* |
| CANON-338 | Any route resolving a chip or a DNI must carry its own rate limiter — lookupByChip and verifyDni bring none by design. | `docs/architecture/api-invariants.md#9-merge-checklist` | UNENFORCED | — | The throttle-coverage fence keys on publicPetByToken( call sites, not on lookupByChip/verifyDni. No fence derives a chip/DNI corpus, and check-api-v1-envelope.ts's limiter rule only bans a computed bucket where a limiter already exists. *This is the exact defect shape the public-token fence was built for, on a corpus the fence does not cover.* |
| CANON-339 | Any route accepting an upload must go through lib/infra/uploads.ts — a signed upload URL may never point at a public bucket without replicating magic-byte sniffing, the no-SVG whitelist and server-side re-encoding first; the storage key must be derived from validated MIME + randomUUID(), never the client filename. | `docs/architecture/api-invariants.md#15-upload-validation` | PARTIAL (subset) | `__tests__/pet-photo-upload.test.ts:189-285` | __tests__/pet-photo-upload.test.ts:189-350 fences ONE door (lib/infra/pet-photo-upload.ts): whitelist parity, no SVG, magic bytes, sharp re-encode, key from validated MIME + minted uuid. No fence enumerates upload entry points or requires a route to reach lib/infra/uploads.ts (rg 'uploads' over scripts/check-*.ts returns only storage-grant and scheduled-ref scripts), and other doors exist unfenced — app/api/v1/welfare-reports/route.ts evidence uploads, src/modules/events/actions.ts, app/org/[orgToken]/atender/actions.ts. *Corroboration only, not an enforcer (P17 trap the basis flagged): scripts/check-storage-write-policies.ts:193 records that closing B24 needs 'moving the ~30 Server-Action upload sites onto the signed upload primitive' and that the caller migration 'has not happened'.* |
| CANON-340 | Any /api/v1 route that mutates must accept an Idempotency-Key header and replay the first outcome for a repeated key. | `docs/architecture/api-invariants.md#9-merge-checklist` | UNENFORCED | — | Same shape as CANON-323 and consistent with the refuter's verdict there: per-route tests exercise the header (api-v1-amend, api-v1-record-event, api-v1-lost, api-v1-shares, api-v1-pets, api-v1-me-caretaker-grants) but nothing enumerates the mutating v1 routes and requires it. check-api-v1-envelope.ts has no idempotency rule. |
| CANON-341 | DNI lookup must stay deliberately unthrottled; the only mitigation is collapsing the unique-violation and generic-failure branches into the same message. | `docs/architecture/api-invariants.md#14-chip-and-dni` | UNENFORCED | — | The message-collapse half is a code fact stated in a comment, not an assertion: no test compares the two branches' strings. Nothing would fail if one branch were given a distinguishable message, which is the whole risk the rule names. *Compare CANON-342/343, where the analogous denuncia oracle IS pinned by a response-equality test.* |
| CANON-342 | The denuncia code-access endpoint must return one NEUTRAL_MESSAGE constant on every branch and rate-limit at 5/min + 20/hr per IP, fail-closed. | `docs/architecture/api-invariants.md#13-denuncia-mac-anti-oracle` | PARTIAL (subset) | `__tests__/denuncia-access-timing-oracle.test.ts:133-205` | __tests__/denuncia-access-timing-oracle.test.ts:48-52 mocks enforceRateLimit to `async () => undefined`, so the 5/min + 20/hr fail-closed half — declared live at app/(public)/denuncias/codigo/[code]/actions.ts:85-92 — has no assertion anywhere; the neutral-message half is pinned at :180-183 (every branch's message equals the matching branch's). *Live maxPerMinute 5 / maxPerHour 20 at actions.ts:86-87; the non-RateLimitError rethrow at :91 is the fail-closed arm, also unasserted. The mocked limiter (:48-52) is stronger evidence than the v3 basis's 'no limiter-constant pin was found'.* |
| CANON-343 | Denuncia redemption must return the same 303 to the same URL on success and failure, differing only in whether a cookie is set, and must compare tokens with timingSafeEqual behind a length guard. | `docs/architecture/api-invariants.md#13-denuncia-mac-anti-oracle` | PARTIAL (subset) | `__tests__/denuncia-access-timing-oracle.test.ts:156`<br>`lib/infra/denuncia-reporter-token.test.ts` | The timingSafeEqual-behind-a-length-guard half is fenced: lib/infra/denuncia-reporter-token.ts:131-132 guards the length, and lib/infra/denuncia-reporter-token.test.ts:48-74 asserts garbage/empty/MAC-tampered input returns false instead of throwing. The 303 half is not: app/(public)/denuncias/seguimiento/entrar/route.ts:53,59,61-67 has no test, and the cited denuncia-access-timing-oracle.test.ts:156 is the access-REQUEST action, not redemption. *No test file asserts a 303 for the redemption route; the four test files mentioning 303 are lost-view-model, govt-dashboards, location and turnos. The v3 basis states its assertions were not read line by line.* |
| CANON-344 | Error codes for /api/v1 must be lowercase_snake and live in packages/contract. | `docs/architecture/api-invariants.md#d2--error-code-casing-and-home` | PARTIAL (subset) | `packages/contract/src/api/errors.ts`<br>`lib/infra/api-v1.ts:36`<br>`__tests__/api-v1.test.ts:54` | lib/infra/api-v1.ts:36 types apiV1Error against ApiV1ErrorCode from @dim/contract/api, so a route cannot invent a code — __tests__/api-v1.test.ts:54-63 keeps that non-vacuous with a @ts-expect-error on "made_up_code". The casing half has no check: nothing in packages/contract/src/api/errors.ts or check-api-v1-envelope.ts matches the union members against /^[a-z0-9_]+$/, so a PascalCase entry added to the array compiles. *errors.ts:7-8 records lowercase_snake as the D2 decision in prose only, and quotes 34 as a 2026-08 handler census — a header number, not a live pin (P17), so number stays null. Basis agrees on both points.* |
| CANON-345 | The /api/v1 credential payload must project only fields the page actually renders, never the loader's full fetched shape (it must exclude the microchip number, full vaccination history rows, and internal UUIDs). | `docs/architecture/api-invariants.md#10-the-first-endpoint` | UNENFORCED | — | The payload shape lives in a Zod/TS type in packages/contract, so a WIDENING of that type compiles fine — tsc cannot object to publishing more. No fence compares the payload's field set against the page's rendered field set, and no test asserts the three named exclusions. *A derivation discipline recorded in prose; the failure mode it names (project the loader's type) would pass every gate.* |
| CANON-346 | Redaction must run before the sink is handed anything, so no provider adapter can leak by forgetting to scrub — redaction must never move behind the sink interface. | `docs/architecture/client-error-sink-pending-decision.md#what-is-already-true-no-decision-needed` | UNENFORCED | — | lib/observability/redact.ts and its two tests prove WHAT redaction removes, not WHERE it sits in the call order. No fence asserts that every sink call site is downstream of redact(), and the sink itself is a pending PO decision, so there is no adapter to fence yet. *Pending PO decision.* |
| CANON-347 | Redaction must cover DNI, all twelve product credential-code prefixes, care-grant tokens, emails, phones, JWTs, Authorization headers, capability tokens in URL path segments, and sensitive query-string values. | `docs/architecture/client-error-sink-pending-decision.md#what-is-already-true-no-decision-needed` | ENFORCED | `lib/observability/redact-prefix-coverage.test.ts:88,109`<br>`lib/observability/redact.test.ts` | redact-prefix-coverage.test.ts:88-95 fails when any minted prefix is uncovered and :109-145 when any capability route segment is; redact.test.ts:17-90 pins DNI (7 and 8 digit), emails, phones, :59 care-grant CG-, :153 JWT, :166 Authorization header, :173 query-string value. Every named vocabulary item has a failing assertion. *CREDENTIAL_TOKEN_PREFIXES in lib/observability/redact.ts:79-92 lists exactly 12; the fence asserts minted.size >= 12 at :85, so 12 is a derived floor rather than a pinned constant. Basis agrees.* |
| CANON-348 | The redactor's prefix list and capability-segment list must be re-derived from source (drift-fenced), never hand-maintained. | `docs/architecture/client-error-sink-pending-decision.md#what-is-already-true-no-decision-needed` | ENFORCED | `lib/observability/redact-prefix-coverage.test.ts:75,88,97,109,147` | redact-prefix-coverage.test.ts:52-64 re-derives the prefix set by scanning source for generatePrefixedToken("X") plus the welfare generator; :88 fails on uncovered, :97 on orphaned (both directions), :109-145 re-derives capability segments from the router glob. :75-86 is the fence's own smoke test (DIM/CAS/FP/DEN + size>=12) so the regexes cannot rot into a vacuous pass. *Basis agrees, same lines.* |
| CANON-349 | The error-report context object must be a closed allowlist — an unreviewed field is a compile error at the call site and is dropped at runtime. | `docs/architecture/client-error-sink-pending-decision.md#what-is-already-true-no-decision-needed` | UNENFORCED | — | No ClientErrorContext / ErrorReportContext type was found under lib/, packages/ or __tests__/, so the closed-allowlist type the rule describes could not be located; without it there is nothing for tsc to refuse and no test asserting the runtime drop. *Pending PO decision — the sink does not exist yet, which is the likely reason the type does not either. Re-check when the decision lands.* |
| CANON-350 | No error-telemetry SDK may be wired with its default configuration — auto-captured breadcrumbs, fetch/XHR payloads, console output, IP and session replay bypass the redactor and must be disabled explicitly in the adapter. | `docs/architecture/client-error-sink-pending-decision.md#what-must-not-happen` | UNENFORCED | — | There is no adapter and no SDK in the tree to fence, and no dependency-level ban (biome noRestrictedImports carries no telemetry package). The rule is a constraint on a future change. *Pending PO decision.* |
| CANON-351 | The physical tag activation code (generateTagActivationCode) is deliberately left unredacted by the client-error redactor. | `docs/architecture/client-error-sink-pending-decision.md#known-residual-gap-in-the-redaction-layer` | UNENFORCED | — | A declared accepted gap has no failing predicate. Note the interaction: redact-prefix-coverage.test.ts derives from generatePrefixedToken(...) literals, and this code is deliberately UNPREFIXED, so it is outside that fence's corpus by construction rather than by omission. *Stated as a known accepted gap, pending PO decision.* |
| CANON-352 | Dependencies in the module architecture must point inward: domain/ imports nothing outside itself, application/ depends only on domain/ and repository behavior, and only actions.ts/infrastructure/ may touch Next.js and the database. | `docs/architecture/hexagonal-lite.md#the-four-layers-and-the-dependency-rule` | PARTIAL (subset) | `biome.json:74-83` (domain)<br>`biome.json:98-105` (application)<br>`scripts/check-application-fence.ts`<br>`scripts/check-dependency-direction.ts` | biome.json:98-105 (application override) restricts only next, next/cache, next/navigation, next/headers, server-only, @/lib/supabase/server — NOT @/db and NOT drizzle-orm, so the rule's "only actions.ts/infrastructure may touch the database" half is unfenced for src/modules/*/application/**. The domain override (:74-83) does ban both. *Independently derived the same uncovered clause the previous basis names. Second gap: scripts/application-fence-baseline.json holds exemptions=34, an exemption list whose members legitimately import restricted specifiers (check-application-fence.ts:24-39 ratchets it down only). check-dependency-direction.ts covers the between-module axis over src/modules/**/*.{ts,tsx} with an ALLOWED_EDGES list.* |
| CANON-353 | actions.ts must stay a thin Next.js controller: parse FormData, run the auth guard, build the input DTO, invoke the use-case, flush best-effort notifications, redirect — no business rules. | `docs/architecture/hexagonal-lite.md#the-four-layers-and-the-dependency-rule` | PARTIAL (subset) | `scripts/check-action-line-budget.ts` | scripts/check-action-line-budget.ts:79-97 fails only on LINE COUNT (existing > baseline+20, new > 150); logic that fits the slack or a 149-line new file passes, so 'no business rules' is measured by a size proxy. ACTIONS_GLOB at :40 is 'app/actions/*.ts' only — src/modules/*/actions.ts controllers are outside the fence entirely. lint:actions is in verify (package.json:79). *shape switched from ratchet to subset after the previous basis pointed at the uncovered directory; verified on :40, a line I had already opened. No other fence reads app/actions for business logic (check-dependency-direction.ts mentions it only in a comment, :59).* |
| CANON-354 | An action must never widen its authorization scope — a cross-org capability check that ignores the resource's own org is a security bug. | `docs/architecture/hexagonal-lite.md#rules--conventions-do--dont` | PARTIAL (subset) | `scripts/check-confused-deputy.ts:9-22`<br>`scripts/check-authz-scoping.ts:248` | scripts/check-confused-deputy.ts:172-180 fails only one syntactic shape — an action naming orgToken/senderOrgToken/receiverOrgToken (signature or formData) that gates with bare requireCapability; scripts/check-authz-scoping.ts is a report-only ratchet with 44 baselined offenders. 'Never widen authorization scope' is far broader than either predicate. *check-authz-scoping.ts:41-57 itself names two uncovered sibling rules (identity-only guard + caller-chosen pet id; role-not-allowed-for-the-effect).* |
| CANON-355 | A strangler migration must never drop side effects — audit_log rows, idempotency keys, and cascade closes are behavior and must be preserved byte-for-byte. | `docs/architecture/hexagonal-lite.md#rules--conventions-do--dont` | PARTIAL (subset) | `scripts/check-audit-log-coverage.ts` | scripts/check-audit-log-coverage.ts:342 catches only one of the three named side effects (an audit write disappearing from a non-baselined operator+mutating action, measured one hop into the use-case). Idempotency keys and cascade closes have no fence — rg 'idempotency' over scripts/ hits only seeds/backfills, no check-*. 'Byte-for-byte' payload preservation is measured by nothing. *the audit half is itself a ratchet with a 28-entry baseline* |
| CANON-357 | Coverage thresholds must be 90% branches on domain rules and 70-75% on the module/action layer. | `docs/architecture/hexagonal-lite.md#testing-strategy-strict-tdd` | UNENFORCED | — | Reusing the refuter: vitest.config.ts:136-142 says these exact numbers 'were NEVER ENFORCED'; the live floors at :143-161 are business-rules 80, lib/** 55, app/actions 22, app/api 8, domain 88, src/modules 55 — no 90 and no 70-75. Coverage runs only under `pnpm test:coverage` (:116), which is absent from the verify chain. The named enforcer contradicts the rule it supposedly enforces. *docSays: 90 / 70-75. The live ratchet floors belong to a DIFFERENT rule, recorded by the refuter as a missing canon row (scope process).* |
| CANON-358 | lib/metrics/ (Pattern B) must not be imported from domain/ or application/ — it is infrastructure-adjacent and reads @/db directly. | `docs/architecture/hexagonal-lite.md#dependency-rule-for-pattern-b` | UNENFORCED | — | Reusing the refuter, confirmed against biome.json: the domain override (:74-83) restricts @/db, drizzle-orm, next, next/cache, next/navigation, next/headers, server-only and @/lib/supabase/server — NOT @/lib/metrics; the application override (:98-105) restricts six framework specifiers only. check-dependency-direction.ts:6-8 scopes itself to the BETWEEN-module axis. *A one-line biome paths entry would close it; nobody added one.* |
| CANON-359 | The per-offering campaign enrollment list and CSV are treated as operational data, not PII, and are exempt from k-anonymity suppression — only the geo-reach surface is k-suppressed. | `docs/architecture/privacy-known-limitations.md#ka5--per-offering-campaign-enrollment-differencing-vs-geo-reach-k-anon` | UNENFORCED | — | An EXEMPTION is not a checkable predicate here: no registry lists surfaces exempt from suppressSmallCells, so nothing would fail if the exemption were quietly widened to another surface. The branded SuppressedCells type (CANON-284) constrains only what DECLARES it. *Accepted by PO decision, 2026-07-12.* |
| CANON-360 | /gob/analytics/export is declared OUTSIDE the k-anonymity policy — it emits row-level CSV/JSON with no cell suppression, and must remain scoped to an operator's own jurisdiction with a fail-closed empty result when no jurisdiction is set. | `docs/architecture/privacy-known-limitations.md#pd1--gobanalyticsexport-is-a-row-level-padrón-declared-outside-the-k-anon-policy` | PARTIAL (subset) | `__tests__/govt-exports.test.ts` | __tests__/govt-exports.test.ts covers the pure helpers and, at :348-360, that EXPORT_PRIVACY_NOTICE calls the file row-level and outside the k-anon policy. The fail-closed guards at lib/analytics/dashboards/exports.ts:44,94,152,207 (actor.role === 'govt' && jurisdictions.length === 0 → []) have no test — no file under __tests__ imports those fetchers — and scripts/check-view-scope.ts:80 explicitly exempts app/gob/analytics/export/page.tsx. *v3 reasoned that the predicate 'is a directly testable branch'; testable is not tested. Delete line 44 and nothing in verify or test:verified goes red. The declaration half of the rule is pinned; the operative security half is not.* |
| CANON-361 | The PD1 acceptance is void the moment any export fetcher loses its jurisdiction-scope clause or stops failing closed, or the audit insert is removed, made best-effort, or moved behind a branch. | `docs/architecture/privacy-known-limitations.md#pd1--gobanalyticsexport-is-a-row-level-padrón-declared-outside-the-k-anon-policy` | UNENFORCED | — | A meta-rule about when an acceptance expires cannot itself fail; what CAN fail is the two properties it names, and those are CANON-360 (partly pinned by __tests__/govt-exports.test.ts) and the audit insert (covered only where check-audit-log-coverage.ts's operator+mutation antecedent applies — an export is a READ, so it is outside it). *The audit-insert half is the weaker one: the coverage fence's antecedent is a MUTATION.* |
| CANON-362 | Government export row schemas must never gain an attribute that identifies a person (owner name, DNI, contact, precise coordinates) — enforced by the Zod schemas in lib/analytics/govt-exports.ts. | `docs/architecture/privacy-known-limitations.md#pd1--gobanalyticsexport-is-a-row-level-padrón-declared-outside-the-k-anon-policy` | PARTIAL (subset) | `__tests__/govt-exports.test.ts`<br>`lib/analytics/govt-exports.ts` (Zod row schemas) | __tests__/govt-exports.test.ts:32-42,92-126,138-156 fails if petsExportSchema regains name/ownerDisplayName, if eventsExportSchema regains performedByUserId or coordinates, or if casesExportSchema regains party identities — the named attributes. No assertion pins the schemas' KEY SET (lib/analytics/govt-exports.ts:23-66), so adding a new optional identifying key (e.g. ownerDni) fails nothing. *Same gap v3 named. The 'keeps all whitelisted fields' case (:44-66) asserts values plus two not.toHaveProperty checks, not a closed key set — enumerate-the-forms rather than ban-the-class (P21-adjacent, but the named-attribute assertions do have teeth, so not vacuous).* |
| CANON-363 | Datos abiertos (the public open-data surface) must be governed by the k-anonymity policy without exception — it is a different surface than the operator-gated exports. | `docs/architecture/privacy-known-limitations.md#pd1--gobanalyticsexport-is-a-row-level-padrón-declared-outside-the-k-anon-policy` | ENFORCED | `lib/open-data/__tests__/datasets.test.ts:80-460`<br>`lib/open-data/__tests__/province-suppression.test.ts:36-40` | datasets.ts:580-618 routes EVERY dataset through suppressRateProvinces or suppressDensityProvinces — BUILDERS is Record<DatasetId, RateBuild\|DensityBuild> (:279), a TS-total map, so no dataset can opt out; datasets.test.ts:80-479 exercises the real buildDataset for mortalidad, antirrabica, esterilizacion and microchip and asserts the marker; province-suppression.test.ts:36-40 pins OPEN_DATA_K=5. *cumplimiento-ppp is never built directly in the test file, but its RateBuild goes through the same single door at datasets.ts:587-606, which is what closes the 'without exception' clause. Basis agrees.* |
| CANON-364 | The govt analytics export operator notice must not claim the data is "anonimizado" when it is only jurisdiction-scoped and audited, not k-anonymized. | `docs/architecture/privacy-known-limitations.md#pd1--gobanalyticsexport-is-a-row-level-padrón-declared-outside-the-k-anon-policy` | ENFORCED | `__tests__/govt-exports.test.ts`<br>`app/gob/analytics/export/privacy-notice.ts` | __tests__/govt-exports.test.ts:353 asserts EXPORT_PRIVACY_NOTICE not.toMatch(/est[aá]n anonimizados/i), :350 demands 'fila por fila', :357-358 that it says el anonimato NO se aplica, :362-363 jurisdiccion + auditoria. The constant is the copy actually rendered: app/gob/analytics/export/page.tsx:105 body={EXPORT_PRIVACY_NOTICE}. *The negative is one spelling ban; a rewrite to 'archivo anonimizado' would slip it, but the positive assertions (anonimato + no se aplica) carry the predicate. Basis agreed without the adoption check; page.tsx:104 confirms it.* |
| CANON-365 | complementarySuppress must promote exactly one sibling cell (not widen to a feasible interval ≥k); the province density marginal is accepted as published raw, by PO decision. | `docs/architecture/privacy-known-limitations.md#ka1--ka2--differencing-via-the-raw-provincial-density-marginal` | ENFORCED | `lib/open-data/__tests__/province-suppression.test.ts:67-91` | privacy-known-limitations.md:13-15 names lib/metrics/anonymity.ts as the subject, not lib/open-data. anonymity.test.ts:164-165 asserts suppressed == [Dept A, Dept B] AND visible == [Dept C] on closed sets — widening to a feasible interval >= k would add cells and fail; :169-178 and :190-196 pin the no-widen cases, :146-156 is a per-group invariant. The raw-density-marginal half is a PO acceptance record, not a code obligation. *The row and the basis both cite lib/open-data/__tests__/province-suppression.test.ts, a different module (suppressDensityProvinces); the doc's own anchor is lib/metrics/anonymity.ts:107-138, live at :142-173, and its test file was not opened by the previous pass. Genuine fork on the second clause: the basis treats 'the density marginal is accepted as published raw' as an uncovered part of the scope (subset); I read it as a PO acceptance record with no violable predicate, so nothing is missing from coverage. Lower confidence for that reason.* |
| CANON-366 | A suppressed open-data cell must be replaced with the literal text "suprimido por privacidad", never with 0. | `docs/datos-abiertos/diccionario.md#diccionario-de-datos--datos-abiertos-de-mimar` | ENFORCED | `lib/open-data/__tests__/serialize.test.ts:71,76`<br>`lib/open-data/__tests__/datasets.test.ts:137,166,322` | serialize.test.ts:81 and :97 pin the LITERAL string "suprimido por privacidad" in the CSV and JSON output (not just the constant), and :85 fails on the unquoted CSV form; datasets.test.ts:92-95 and :209-212 assert the built cell is the marker and explicitly not 0. province-suppression.ts:98 is the single definition. *province-disclosure.test.ts additionally pins SUPPRESSED_CELL_TEXT equal to SUPPRESSED_MARKER (province-suppression.ts:95-97), so neither can be reworded alone. Basis agrees; the literal-text leg matters because comparing against the constant alone would not pin the wording.* |
| CANON-367 | Rate datasets (antirrábica, esterilización, microchip, PPP) must suppress the row when the province's base population is under 5, or when the covered or uncovered group is between 1 and 4. | `docs/datos-abiertos/diccionario.md#regla-de-supresión-común` | ENFORCED | `lib/open-data/__tests__/province-suppression.test.ts:104-158` | province-suppression.test.ts:105-127 pins all three protected branches of isRateCellProtected — sub-k base (2,4), small positive numerator (2,9000), small complement (8998,9000) — plus the three negative controls; :131-158 pins suppressRateProvinces end to end. datasets.ts:587-589 makes suppressRateProvinces the single door every rate dataset passes through. *Basis agrees, same lines; I add the adoption leg (datasets.ts:587) because the rule's subject is the four datasets, not the helper.* |
| CANON-368 | The mortalidad count dataset must suppress a province's row when its count is under 5. | `docs/datos-abiertos/diccionario.md#regla-de-supresión-común` | ENFORCED | `lib/open-data/__tests__/province-suppression.test.ts:42-101`<br>`lib/open-data/__tests__/datasets.test.ts:80-134` | datasets.test.ts:81-97 builds the REAL mortalidad dataset with Santa Cruz count 3 and fails unless fallecimientos_registrados is the marker, is not 3 and is not 0, with meta.suppression.k toBe(5); province-suppression.test.ts:43-54 pins the suppressDensityProvinces predicate the mortalidad builder uses (datasets.ts:607-617). *Basis agrees.* |
| CANON-369 | If exactly one province remains suppressed nationwide after primary suppression, the next-smallest province must also be suppressed, so a suppressed value cannot be recovered by subtracting visible provinces from a national total. | `docs/datos-abiertos/diccionario.md#regla-de-supresión-común` | ENFORCED | `lib/open-data/__tests__/province-suppression.test.ts:67,80,160,173`<br>`lib/open-data/__tests__/datasets.test.ts:99` | province-suppression.test.ts:67-78 fails unless a LONE sub-k province drags the next-smallest visible one with it (toEqual on a closed set); :80-90 fails if a second cell is promoted when two are already suppressed; :160-171 and :173-190 repeat it for the rate path including the complement-triggered primary. datasets.test.ts:99-122 proves it end to end on mortalidad (suppressedCount toBe 2). *Adoption of complementarySuppress by the open-data module is additionally fenced by app/(public)/transparencia/__tests__/privacy-disclosure-parity.test.ts:104-112, which fails if the call is deleted. Basis agrees.* |
| CANON-370 | The raw numerator (e.g. exact vaccinated-dog count) must never be published in a rate dataset — only the base population and the percentage. | `docs/datos-abiertos/metodologia.md#regla-de-supresión-común` | UNENFORCED | `lib/open-data/datasets.ts:88-194` (declared column sets)<br>`lib/open-data/__tests__/datasets.test.ts:137` | [judge, sample re-refute] lib/open-data/__tests__/datasets.test.ts:137-162 read line by line: the assertions are value assertions on two NAMED columns (caba?.perros_registrados, caba?.cobertura_antirrabica_pct, cordoba?.perros_registrados === 9000, cordoba?.cobertura_antirrabica_pct === 50) plus built.meta.suppressedCount. There is no Object.keys / toEqual over the row and no column-set pin anywhere in lib/open-data/__tests__/ (serialize.test.ts:57-71 pins the header of its own synthetic fixture). Adding a `perros_vacunados` column to a rate descriptor fails nothing. — Rulebook 1+2 — lib/open-data/datasets.ts:88-194 is a declaration (P1), and the cited test cannot fail on the rule's prohibition. Nothing can FAIL → UNENFORCED, not PARTIAL. *Whether the ':137 both numeric columns' assertion would fail on a third column depends on its exact form, which was not read line by line.* |
| CANON-371 | Every published open-data cell must satisfy k-anonymity with k=5 — no cell may describe a group of fewer than 5 individuals. | `docs/datos-abiertos/metodologia.md#cómo-protegemos-la-privacidad-k-anonimato-k--5` | ENFORCED | `lib/open-data/__tests__/province-suppression.test.ts:36-40`<br>`lib/open-data/__tests__/datasets.test.ts:136-460`<br>`lib/metrics/anonymity.ts:28` (ANONYMITY_K) | province-suppression.test.ts:37-39 pins OPEN_DATA_K toBe(5) — the rule IS the value, so P21 does not bite; datasets.test.ts:96 pins meta.suppression.k toBe(5) on the built file; datasets.ts:580-618 sends every DatasetId through one of the two k-suppressors, so no published cell escapes the pass. *Basis agrees, adding anonymity.ts:28 as the shared constant.* |
| CANON-372 | A rate dataset must publish a numerator of exactly 0 (a zero re-identifies nobody); only a small-but-positive numerator (1-4) is suppressed. | `docs/datos-abiertos/metodologia.md#por-qué-un-0-se-publica-en-las-tasas-pero-un-conteo-chico-no` | ENFORCED | `lib/open-data/__tests__/province-suppression.test.ts:109,117,121` | province-suppression.test.ts:117-119 fails if isRateCellProtected(0, 9000) ever returns true (a zero must publish); :109-111 fails if a small positive numerator (2, 9000) is NOT protected; :121-123 covers the mirror full-coverage case. Both directions of the rule have a red state. *Basis agrees.* |
| CANON-373 | The mortalidad count dataset must suppress a province whose total is under 5, including small positive counts — the count itself is the sensitive group size. | `docs/datos-abiertos/metodologia.md#por-qué-un-0-se-publica-en-las-tasas-pero-un-conteo-chico-no` | ENFORCED | `lib/open-data/__tests__/province-suppression.test.ts:43,92` | province-suppression.test.ts:43-54 suppresses a count of 3 (a small POSITIVE) and :92-101 a count of 0, so the count path has no publish-if-positive escape; datasets.test.ts:92-95 asserts on the real mortalidad build that the cell is the marker and explicitly not 3 and not 0. *Basis agrees, and records that draft 1 had the rule text inverted (publish an exact 0); the live count path suppresses 0 too, which :92-101 confirms.* |
| CANON-374 | Datasets that share a base-population column, or nest one base inside another, must suppress that shared/nested base JOINTLY and transitively — including when the difference between a base and its containing base falls between 1 and 4. | `docs/datos-abiertos/metodologia.md#supresión-conjunta-de-la-base-compartida` | ENFORCED | `lib/open-data/__tests__/datasets.test.ts:219-460`<br>`lib/open-data/datasets.ts:443` (declared containment lattice) | datasets.test.ts:282-320 fails if a province suppressed only in microchip keeps its shared mascotas_activas base in esterilizacion; :390-408 fails when pets-minus-dogs = 4 does not suppress BOTH the superset and the subset base; :452-479 covers the two-link transitive case (perros_ppp in mascotas_activas); :410-450 are the negative controls. Lattice at datasets.ts:443-473. *The 1-4 difference clause is fixture 9004-9000=4 at datasets.test.ts:370/384. Basis agrees.* |
| CANON-375 | Existing app/actions/*.ts files must not grow beyond their baselined line count plus a 20-line slack. | `scripts/check-action-line-budget.ts:7` | ENFORCED | `scripts/check-action-line-budget.ts:7-9,33`<br>`scripts/action-line-budget-baseline.json` | scripts/check-action-line-budget.ts:33 EXISTING_FILE_SLACK=20 and :78-87 push an 'existing-grew' violation when actual > baseline+20, exit 1 at :163; the baseline (62 entries) is non-empty and :135-138 exits 1 if the glob finds no file. The rule TEXT is itself the ratchet ('must not grow beyond baseline + slack'). lint:actions in verify. *live EXISTING_FILE_SLACK = 20, matches the doc* |
| CANON-376 | New app/actions/*.ts files must stay under 150 lines; a CI line-budget ratchet prevents existing fat actions from growing further. | `scripts/check-action-line-budget.ts:11`<br>`docs/architecture/hexagonal-lite.md#strangler-migration-status--appactions-as-of-2026-06-26` | ENFORCED | `scripts/check-action-line-budget.ts:11-12,37` | scripts/check-action-line-budget.ts:37 NEW_FILE_THRESHOLD=150 and :88-97 flag any non-baselined app/actions/*.ts over 150 lines, exit 1 at :163; :135-138 fails when the glob matches nothing. The rule's own scope IS app/actions/*.ts, which is exactly ACTIONS_GLOB (:40). lint:actions is in the verify chain (package.json:79). *live NEW_FILE_THRESHOLD = 150* |
| CANON-377 | No /api route handler may authorize based on a middleware-stamped request header (x-nonce, x-pathname, x-full-path, x-portal-base). | `scripts/check-api-guard-headers.ts:28` | ENFORCED | `scripts/check-api-guard-headers.ts:1-22` | scripts/check-api-guard-headers.ts:224 derives the stamped names from middleware.ts, :202-217 flags a direct .get() read AND an import of a reader module, over every app/api/**/*.ts + app/**/route.ts (:180-186). Floors :226-247 (3 headers / 30 files / 8 non-api handlers) exit 1; ALLOWLIST at :77 is empty. lint:api-headers in verify. *docSays: 4 — middleware.ts stamps 5 request headers live (x-nonce, content-security-policy-report-only, x-pathname, x-full-path, x-portal-base); one per-module/per-header exemption exists (lib/infra/auth-guards.ts x-full-path, :104)* |
| CANON-378 | Every clinical writer reachable from an Atender walk-in action module must close through completeAtenderSignature() so the owner is notified. | `scripts/check-atender-owner-alerts.ts:29` | ENFORCED | `scripts/check-atender-owner-alerts.ts:1-22` | scripts/check-atender-owner-alerts.ts:24-36 derives the writer set from each app/org/**/atender/**/actions.ts module's imports of @/src/modules/events/application/ and requires every exported async function calling one to reach completeAtenderSignature (:66), plus a '?firmado=1' receipt-literal fence; :38-40 with the runScan floors (:258-269) exit 1 on a vacuous scan. lint:atender-alerts is in verify. *writer set derived, never listed — a new writer enters scope automatically* |
| CANON-379 | Every app/**/route.ts handler is subject to the same authorization-coverage rule as server actions, discovered separately from server-action files. | `scripts/check-authz-guards.ts:18` | ENFORCED | `scripts/check-authz-guards.ts:18-32,113-139` | scripts/check-authz-guards.ts:685-733 runs the coverage rule over route handlers (guard call, reasoned @no-auth-required, unreadable-method exports, and a file yielding zero readable methods is itself an offender), on a SEPARATE discovery (listRouteHandlerFiles :1037) whose floor MIN_ROUTE_HANDLER_FILES=40 exits 1 at :1115-1119. lint:authz is in verify. *handler-body-only analysis is conservative (false positives, not misses); the 47/25 census in the header is a historical incident number, not a live pin* |
| CANON-380 | A server action calling a tenant-level authorization guard and accepting a caller-supplied resource id must contain a visible scoping predicate tying the id to the caller's scope. | `scripts/check-authz-scoping.ts:8` | PARTIAL (ratchet) | `scripts/check-authz-scoping.ts:59,290`<br>`scripts/authz-scoping-baseline.json` | scripts/check-authz-scoping.ts:288-313 fails only when a file's offender count GROWS or a new action file offends; scripts/authz-scoping-baseline.json grandfathers 44 offenders across 19 files (alert-firings 6, custody-disputes 6, welfare 5...). The rule text is absolute ('must contain a visible scoping predicate'), so a live baseline >0 leaves those unproven. *lint:authz-scoping is in the verify chain (package.json:79). Header :79-82 says the ratchet does not prove the existing offenders are correctly scoped. Live baseline sum 44/19 files — kept in the basis text, not as a number (the row has no numberKey).* |
| CANON-381 | The public brand must be spelled "miMAR". | `scripts/check-brand-casing.ts:3` | ENFORCED | `scripts/check-brand-casing.ts:140`<br>`scripts/brand-casing-baseline.json` | scripts/check-brand-casing.ts:140 WRONG_CASE_BRAND /\b(MiMAR\|Mimar\|MIMAR)\b/ over the :131 glob {app,components,lib,packages,src}/**/*.ts(x), non-comment lines only; :310-318 fails any file above its baselined count. scripts/brand-casing-baseline.json holds totalViolations 0 and files {} — the ratchet's strictest state, so the absolute rule holds live. *Empty baseline = fail-closed today; header :80-84 says so. Same conclusion as the previous basis, reached independently.* |
| CANON-382 | The internal codename "DIM" may not appear as a bare standalone word outside a hyphenated public token. | `scripts/check-brand-casing.ts:4` | ENFORCED | `scripts/check-brand-casing.ts:4,18-21,41,190,204-209,353` | scripts/check-brand-casing.ts:186 INTERNAL_CODENAME /\bDIM\b(?!-)/ with findCodenameHits :212-238; :324-331 collects hits and :334-343 exits 1. No baseline — the only escape is a per-line `dim-codename-ok: <reason>` pragma that arms exactly one line of code (:221-231). *Hyphen-trailing tokens (DIM-XXXX-XXXX) are excluded by construction, which is the rule's own carve-out. Header census of 'SEVEN hits' (:36) is discovery-time, not a live pin — number left null.* |
| CANON-383 | An org-token server action gated with bare requireCapability (single-arg) instead of requireCapabilityForOrgToken(orgToken) is a confused-deputy offender. | `scripts/check-confused-deputy.ts:9` | ENFORCED | `scripts/check-confused-deputy.ts:9-22` | scripts/check-confused-deputy.ts:172-180 — org-token param (:152) or formData org token (:157) AND bare requireCapability (:80, single string-literal arg) AND no requireCapabilityForOrgToken (:83); :233-244 exits 1 per offender and :212-215 fails when zero action files are scanned. CONFUSED_DEPUTY_ALLOWLIST is EMPTY (:130). *lint:authz-orgtoken is in verify (package.json:30,79). Comments are stripped before matching (:174).* |
| CANON-384 | No raw JSX literal "SLA vencido" may appear in app/gob or app/admin .tsx outside SlaBadge. | `scripts/check-copy-contract.ts:6` | ENFORCED | `scripts/check-copy-contract.ts:6-22,28-34` | scripts/check-copy-contract.ts:156 scanLiteral(src, "SLA vencido") over listScannedFiles :171 globSync("{app/gob,app/admin}/**/*.tsx"), comments stripped (:154); :224-230 exits 1 on any hit not in the baseline, and scripts/copy-contract-baseline.json is `{}`. Zero-file guard at :195-200; stale baseline entries also fail (:232-237). *SlaBadge lives in components/, outside the scan, so any hit in the operator surface is a raw literal by construction. *.test.* excluded (:172) — the conventional test carve-out, not a production gap.* |
| CANON-385 | "Acta de" text inside a Link/a/button in app/gob or app/admin is banned unless the destination genuinely emits an acta. | `scripts/check-copy-contract.ts:35` | ENFORCED | `scripts/check-copy-contract.ts:8-10,35` | scripts/check-copy-contract.ts:129 TAGGED_ELEMENT_RE over <Link\|a\|button>…</Link> and :131-151 flag rendered text containing "Acta de" in {app/gob,app/admin}/**/*.tsx; :224-230 exits 1 unless that file:line carries a written reason in copy-contract-baseline.json, which is `{}` today — the 'unless the destination emits an acta' exception is unused and the ban is fail-closed. *Whether a baselined destination truly emits an acta stays a human judgement carried as a reason string; stale entries fail (:232-237). The element set (Link/a/button) is exactly the rule's own scope.* |
| CANON-386 | No prerendered HTML may exist under .next/server/app, because the CSP's strict-dynamic + per-request nonce refuses every un-nonced script. | `scripts/check-csp-prerender.ts:30` | ENFORCED | `scripts/check-csp-prerender.ts:1-22` | scripts/check-csp-prerender.ts:106-117 globs .next/server/app/**/*.html and exits 1 naming each prerendered route, gated on cspBreaksPrerenders(middleware.ts) at :59-67. package.json:79 chains `pnpm build && pnpm lint:csp-prerender`, so the build output it inspects always exists; the :88-96 skip cannot be the live state in verify, and CI adds --require-build (:86-93). *Chain ordering inside verify is load-bearing — a reordering would leave this fence reading a stale or absent .next.* |
| CANON-387 | A shelter_custody row opened by a titular's consent may not be handed off org-to-org through any writer that reaches the custody-transfer effect, discovered by the effect not a call-site list. | `scripts/check-custody-handoff-sponsorship.ts:11` | PARTIAL (declared-limit) | `scripts/check-custody-handoff-sponsorship.ts:1,11,60-62,85` | scripts/check-custody-handoff-sponsorship.ts:879-902 fails every org-to-org custody_transfer_proposed/custody_transferred write that reaches no discharge, discovered by event type over six globs (:223-230) with anti-vacuity floors (:845-877). Its own header caps it: :105-109 'proves the question is asked, never that the answer is right'; :113 SQL-level custody moves in migrations/scripts are out of the scan set on purpose; :115-124 an extracted shared helper is 'not fully closed'. *The previous basis upgraded this on :11/:60-62/:85 (subject, discharge, effect-propagation) — all of which I read and agree with; it does not address the header's own 'WHAT THIS DELIBERATELY DOES NOT COVER' block, which is inside the rule's scope. Strong fence otherwise: effect-keyed, transitive guard closure, 2 argued allowlist entries (:205-210) for govt transitional rows.* |
| CANON-388 | A registered budget-target page path that has been renamed or deleted must hard-fail the db-budget check rather than silently fall out of scan scope. | `scripts/check-db-budget.ts:31` | ENFORCED | `scripts/check-db-budget.ts:28-32` | scripts/check-db-budget.ts:626 missingRegisteredPages = DASHBOARD_PAGES.filter(p => !existsSync(p)); runScan :745-752 prints each missing registered site and sets failed, :806-811 exits 1. The prior silent-drop shape is recorded as the bug this replaced (:28-32). *lint:db-budget is in verify (package.json:50,79).* |
| CANON-389 | A server component that wraps a DB load in a budget wrapper must keep chrome that does not depend on the failed load in its degraded branch. | `scripts/check-degraded-chrome.ts:6` | PARTIAL (ratchet) | `scripts/check-degraded-chrome.ts:52,104,468-472`<br>`scripts/degraded-chrome-baseline.json` | scripts/check-degraded-chrome.ts:497-518 fails a degraded branch that drops chrome untainted by the failed load, unless the (file, chrome) pair sits in scripts/degraded-chrome-baseline.json — which holds one live exemption today, app/admin/auditoria/AuditoriaScreen.tsx → DateRangeFilterFields, whose own reason says 'Vale arreglarlo'. The rule is absolute, so a baseline >0 caps it. *Ratchet is strong in both directions (a stale entry and a vanished baselined file both fail, :486-492 and :505-512) plus a floor on branches inspected. Live baseline count 1 kept in the basis text; the row has no numberKey.* |
| CANON-390 | Every import from src/modules/<A>/** resolving to @/src/modules/<B>/** must be a deliberately declared entry in ALLOWED_EDGES. | `scripts/check-dependency-direction.ts:10` | ENFORCED | `scripts/check-dependency-direction.ts:10-15,26-42` | scripts/check-dependency-direction.ts:185-197 flags every @/src/modules/<B> import from a file under src/modules/<A> whose <A>:<B> edge is absent from ALLOWED_EDGES (:189) and exits 1 with file:line (:227-234); listModuleFiles :207 globs src/modules/**/*.{ts,tsx} and :214-216 fails on an empty corpus. *docSays: 9 — the header's ASCII graph (:30-39) lists nine edges, but the live ALLOWED_EDGES set has 16 entries (:53-57, :61-63, :82-84, :95, :98, :105, :115, :127), so the header census is stale and the live value wins. *.test.ts(x) excluded at :208 (production edges only); only the @/ alias form is matched, which is exactly the rule's own wording.* |
| CANON-391 | Raw Tailwind palette utilities (e.g. bg-red-700) are banned; use gob-* semantic tokens instead. | `scripts/check-design-tokens.ts:4` | ENFORCED | `scripts/check-design-tokens.ts:3-6` | scripts/check-design-tokens.ts:101-104 builds RAW_PALETTE over 21 Tailwind families x 15 utilities; :598-602 reports every hit with no baseline and :721-726 exits 1. File set :76-83 is {app,components}/**/*.{ts,tsx} minus test/spec — rg shows every other .tsx in the repo is under apps/mobile (React Native, no Tailwind). package.json:13 lint:tokens is in the :79 verify chain. *Rules 1-3 are the hard half of this fence; only rules 4-10 are ratcheted. Prior basis agrees.* |
| CANON-392 | The dark: utility prefix is banned everywhere; dark mode is disabled at the @variant level. | `scripts/check-design-tokens.ts:7` | ENFORCED | `scripts/check-design-tokens.ts:7-9` | scripts/check-design-tokens.ts:105 DARK_PREFIX, applied line-by-line at :604-609 with no baseline and exit 1 at :721-726, over {app,components}/**/*.{ts,tsx} (:76-83). rg for 'dark:' across .ts/.tsx/.css returns only app/globals.css:495-497 (prose explaining the disabled @variant) and the two codemod scripts — no live class survives. *"Everywhere" is met in practice: the scan misses .css and apps/mobile, and neither carries a dark: utility today.* |
| CANON-393 | Duplicated-lines density across app/, components/, lib/, src/ must stay at or under 7 percent (jscpd, minTokens 50). | `scripts/check-duplication.ts:12` | ENFORCED | `scripts/check-duplication.ts:10-15,20-21` | scripts/check-duplication.ts:34 THRESHOLD_PERCENT = 7 and :103-108 exits 1 when the jscpd report's total percentage exceeds it. jscpd.json:2 sets path [app, components, lib, src], :14 minTokens 50, :15 threshold 7 — the rule's stated scope and parameters. package.json:63 lint:dupes is in the :79 verify chain. *Live threshold equals the documented 7, in both the script and jscpd.json.* |
| CANON-394 | A missing jscpd binary, a failed run, or a report with zero scanned sources must fail the duplication check, never pass vacuously. | `scripts/check-duplication.ts:17` | ENFORCED | `scripts/check-duplication.ts:17-18` | scripts/check-duplication.ts:58-66 exits 1 when jscpd cannot be resolved or its bin is missing, :81-87 exits 1 when no jscpd-report.json was produced (printing the child's exit code and stderr), and :92-96 exits 1 when total.sources === 0 or total.lines === 0. All three fail-closed paths named by the rule exist and are reachable in the verify chain via lint:dupes. *The zero-scan guard is a strict === 0 comparison, so a scan reduced to one file still passes.* |
| CANON-395 | A "Sin resultados" literal in app/gob or app/admin .tsx outside an LnEmptyState block is banned except for grandfathered picker-dropdown cases. | `scripts/check-empty-state-consistency.ts:12` | ENFORCED | `scripts/check-empty-state-consistency.ts:12-20`<br>`scripts/empty-state-baseline.json` | scripts/check-empty-state-consistency.ts:69-84 counts "Sin resultados" literals outside brace-scanned <LnEmptyState> blocks across {app/gob,app/admin}/**/*.tsx (:86-89); :121-129 fails any file above its baseline and :99-104 fails an empty glob. scripts/empty-state-baseline.json holds exactly the two grandfathered picker files (1 each) the rule text itself excepts. *The rule's own exception clause is the baseline, so the ratchet is the rule rather than a weakening of it.* |
| CANON-396 | Every payload key read anywhere in app/, src/, or lib/ must be a key some event schema in lib/events/event-schemas.ts is capable of writing. | `scripts/check-event-payload-parity.ts:10` | PARTIAL (declared-limit) | `scripts/check-event-payload-parity.ts:9-21` | scripts/check-event-payload-parity.ts:389-398 globs app/, src/ and lib/, but the JS accessor scan at :358 runs only when the file text matches /\bpetEvents\b/ — header :57-60 and :72-77 state that bound. lib/projections holds 14 files, 0 matching petEvents and 9 reading payload.* (pet-status, pet-weight, pet-microchip, …), so those reads can never fail this fence. SQL payload->>'key' forms (:336-353) are scanned everywhere. *The interpolated SQL form is additionally restricted to IDENT === petEvents (:339-344), which is correct rather than a hole — other tables own their own payload column. Independently re-measured before opening the basis; matches the judge's count.* |
| CANON-397 | A section eyebrow/kicker label must not render the same text as its adjacent heading. | `scripts/check-eyebrow-title.ts:5` | ENFORCED | `scripts/check-eyebrow-title.ts:5-22` | scripts/check-eyebrow-title.ts:110-141 pairs eyebrow and heading candidates within WINDOW_LINES (:56) and compares normalized text; :200-208 fails any file above its baseline, :221-222 exits 1, :150-154 fails an empty glob. scripts/eyebrow-title-baseline.json has "files": {} — an empty baseline, so the absolute rule holds live on {app,components}/**/*.tsx. *Detection is precision-first (header :27-29): a duplication split across dynamic expressions or beyond 6 lines is missed by design. The prior basis never mentioned the ratchet; the empty baseline is what keeps it ENFORCED.* |
| CANON-398 | A source file over 1500 lines that is not in the baseline fails; a baselined file may only shrink or stay within a 25-line growth slack. | `scripts/check-file-size.ts:13` | ENFORCED | `scripts/check-file-size.ts:13-19,35,40,98`<br>`scripts/file-size-baseline.json` | scripts/check-file-size.ts:35 THRESHOLD = 1500, :40 GROWTH_SLACK = 25, :45 glob {app,components,lib,packages,src}/**/*.{ts,tsx}; :90-99 records new-too-large for an over-threshold file absent from the baseline and a growth violation past recorded+25, and :193 exits 1. scripts/file-size-baseline.json carries the 5 grandfathered files the rule's own text sanctions. *Rule text is itself the ratchet ("not in the baseline fails; a baselined file may only shrink or stay within 25 lines"), so a non-empty baseline is not a weakening. Live threshold matches the doc.* |
| CANON-399 | A newly introduced source root (e.g. packages/) must be added to the file-size scan globs or its files silently escape the corpus. | `scripts/check-file-size.ts:11` | UNENFORCED | — | The lesson is recorded (:10-12, packages/ joined the corpus on 2026-08-20 with the first workspace package) but nothing detects the NEXT unglobbed root: check-file-size.ts carries no minimum-file-count floor — its only constants are THRESHOLD (:35), GROWTH_SLACK (:40) and BASELINE_PATH (:42). Compare check-api-v1-envelope.ts:246 (MIN_V1_ROUTE_FILES) and check-degraded-chrome.ts:104 (MIN_SCANNED_FILES), which do have one. *A repo-wide convention the repo applies inconsistently — worth its own canon row about non-vacuity floors.* |
| CANON-400 | An anonymous one-shot public CTA to /encontre or /sighting routes must use a plain <a>, never a next/link <Link>. | `scripts/check-hard-nav-anchors.ts:11` | ENFORCED | `scripts/check-hard-nav-anchors.ts:11-20` | scripts/check-hard-nav-anchors.ts:39-42 matches a <Link> opening tag whose href string reaches /encontre or /sighting; :59-73 scans app/(public)/** and components/pet-profile/** (:45, 156 files today) and :75-78 exits 1 on any hit, with an empty allowlist (:48-50). The two real finder routes live at app/(public)/p/[publicToken]/{encontre,sighting}/page.tsx, inside that scope. package.json:58 lint:hard-nav is in the :79 verify chain. *A fully dynamic href (a variable, not a template literal) is not matched — declared at :18-20. Template-literal hrefs carrying ${token} do match, since the pre-segment class excludes only quotes and >. No fail-closed floor on the file list, but it is 156 files today.* |
| CANON-401 | No module outside components/Icon.tsx may import from lucide-react directly. | `scripts/check-icon-registry.ts:29` | ENFORCED | `scripts/check-icon-registry.ts:199` | scripts/check-icon-registry.ts:115 LUCIDE_IMPORT is applied at :194-204 to every scanned file except components/Icon.tsx, with no baseline (header :29-30) and exit 1 at :218-220. package.json:65 lint:icons is in the :79 verify chain. Repo-wide rg for `from "lucide-react"` returns only components/Icon.tsx (plus a doc and this script). *SOURCE_GLOB (:55) is {app,components}/**/*.{ts,tsx}: a lucide import added under lib/ or src/ would be unscanned, but no such file exists today and every .tsx outside app/components is React Native. No concrete uncovered path exists to name, so this is not scored as a subset.* |
| CANON-402 | A hand-rolled *Icon-named component rendering a raw <svg> outside the registered baseline is banned as a new occurrence. | `scripts/check-icon-registry.ts:19` | ENFORCED | `scripts/check-icon-registry.ts:19,57,128,212`<br>`scripts/icon-registry-baseline.json` | scripts/check-icon-registry.ts:72+ detects a *Icon-named component/function whose body renders a raw <svg>; :181-190 fails any file whose hit count exceeds scripts/icon-registry-baseline.json and :218-220 exits 1. The baseline holds one file (components/layout/CitizenTabBar.tsx: 1), and the rule's own text bans the NEW occurrence, which is exactly what the ratchet fails on. *The five migrated nav-chrome files are deliberately absent from the baseline (0 allowed), so a regression there fails immediately; :209-212 reports now-clean baselined files so the ratchet only tightens.* |
| CANON-403 | A govt-operator authorization gate must route through jurisdictionScopeContains/jurisdictionPairClause rather than an exact-pair (province, locality) comparison against a resource row. | `scripts/check-jurisdiction-subsumption.ts:6` | PARTIAL (declared-limit) | `scripts/check-jurisdiction-subsumption.ts:3-22` | scripts/check-jurisdiction-subsumption.ts:162-168 states the limit inside the rule's own scope: the scope-field shape only counts when REQUIRE_SCOPE_TOKEN (/jurisdiction/i, :169) appears near the predicate, so an assignment list named `scopes` or `assignments` slips past. :124-134 records that this fence stayed green through a real instance of its class (resolveScopedJurisdictions, ~32 /gob screens) until Shape 4 was added 2026-08-17. Hits exit 1 at :251. *Also carries one live single-site exception (:89-98, app/(app)/cuenta/desactivar/page.tsx:74) and gates the builder shapes on a nearby .map( (:147). The enumerated shapes themselves are hard-fail with no baseline. The prior basis quoted only the header's bug-class description and never reached the KNOWN LIMIT block.* |
| CANON-404 | The same static KPI label text must not be rendered with a different info.definition string elsewhere in the app. | `scripts/check-metric-labels.ts:11` | PARTIAL (ratchet) | `scripts/check-metric-labels.ts:10-22` | scripts/check-metric-labels.ts:184 scans {app,components}/**/*.tsx and :312-336 fails on same-label/different-definition, but :322 skips every label in METRIC_LABEL_ALLOWLIST (:57-97, 16 live entries: 'Total registradas', 'Activas', 'SLA ENO', ...) - exactly the shape the rule forbids, passing by allowlist. *Rule as canonized carries no scope-wording exception; the fence grandfathers 16 collisions. The declared static-only scope (:15-25, the only thing the v2 basis cited) matches the rule's own 'static label' wording, so that is not the gap.* |
| CANON-405 | A hand-rolled operator control (input/select/textarea wearing op chrome classes) must be counted correctly by countRawOpControls, exempting checkbox/radio. | `scripts/check-op-controls.test.ts:5` | ENFORCED | `scripts/check-op-controls.test.ts`<br>`scripts/check-op-controls.mjs:16-18` | scripts/check-op-controls.test.ts:13-63 exercises countRawOpControls on real JSX: multi-line attribute lists, a `>` inside an expression attribute (:46-54), the checkbox/radio exemption (:38-44) and non-<input> primitives (:34-36). __tests__/db-reachability.ts:265-279 walks the whole repo for *.test.ts and scripts/ is not in SKIP_DIRS (:70-76), so vitest discovers it under test:verified. *The rule's predicate IS the helper's behaviour, so a helper test is the right instrument (not an adoption case). Independently reached the same reading as v2.* |
| CANON-406 | Any function that resolves a session and makes an institutional/operator authority decision must consult the 8-hour operator shift, derived structurally rather than from a hand-listed set of call sites. | `scripts/check-operator-shift-reach.ts:30` | PARTIAL (declared-limit) | `scripts/check-operator-shift-reach.ts:3-22,30` | scripts/check-operator-shift-reach.ts:33-47 derives the antecedent structurally (session resolution + OPERATOR_SIGNALS) and :571-577 fails any resolver not transitively reaching isOperatorShiftExpired/requireLiveUser, with SHIFT_REACH_ALLOWLIST (:184) empty. But :70-73 declares that function-scoped `"use server"` closures inside a page or component are invisible to the scan - real server actions inside the rule's 'any function' scope. *lib/infra/operator-shift.ts:109 OPERATOR_SHIFT_MS = 8 * 60 * 60 * 1000 confirms the number. The other declared limits (:74-79) over-report rather than under-report, so only the closure gap caps this; the derivation the rule demands is genuinely there.* |
| CANON-407 | A route's exclusive bytes — manifest files appearing in three routes or fewer, read from the build's own manifest — may not grow beyond its recorded baseline without someone seeing it. | `scripts/check-route-weight.ts:2,14` | ENFORCED | `scripts/check-route-weight.ts:14-22,49,61`<br>`scripts/route-weight-baseline.json` | scripts/check-route-weight.ts:204-213 compares each baselined route's exclusive bytes — files appearing in <= EXCLUSIVE_SHARE_MAX=3 routes of .next/app-build-manifest.json (:52,:110) — against scripts/route-weight-baseline.json (2 routes, 220149 / 220765 B) with a +-2% band and exits 1 at :227-230; anti-vacuity floors: no manifest under --require-build (:153-156), manifest under MIN_ROUTES=400 (:165-170), empty baseline (:183-188), 0-byte file (:108). Runs after pnpm build inside verify (package.json:52,79). *Live MIN_ROUTES is 400 (:61), matching the row. A drop below the band also fails (:214-218), so the ratchet cannot rot upward.* |
| CANON-408 | A baselined route missing from the build manifest must fail the route-weight check rather than pass silently. | `scripts/check-route-weight.ts:38` | ENFORCED | `scripts/check-route-weight.ts:95` | scripts/check-route-weight.ts:94-95 — weighRoute returns {error: 'la ruta no esta en el manifiesto'} when the baselined route key is absent from manifest.pages; the runner treats that error as a hit (:196-201) and exits 1 with the count at :227-230, telling the reader to update the baseline if the route was renamed. |
| CANON-409 | A raw jurisdictionProvince/jurisdictionLocality predicate — or a direct jurisdictionPairClause call — in lib/analytics/*.ts or lib/analytics/dashboards/*.ts outside _scope.ts is a flagged (grandfathered) occurrence; new, unreviewed ones fail. | `scripts/check-scope-discipline.ts:11` | ENFORCED | `scripts/check-scope-discipline.ts:11-19`<br>`scripts/scope-discipline-baseline.json` | scripts/check-scope-discipline.ts:288-309 compares per-file, per-rule offence counts against scripts/scope-discipline-baseline.json and exits 1 (:319-323) when a file exceeds its allowance — and equally when it falls below it (:301-304) or is baselined with no occurrences left (:311-317). SCANNED_GLOBS :181 is exactly lib/analytics/*.ts + lib/analytics/dashboards/*.ts with _scope.ts excluded (:186); the three detectors are at :123-125; an empty scan is itself a failure (:278-283). lint:scope is in verify (package.json:31,79). *Rule text explicitly grandfathers existing occurrences and forbids new ones -> R6 first bullet.* |
| CANON-410 | Every gob/admin page.tsx route must resolve to an entry in lib/ui/screen-manifest.ts (route + layer + one-sentence decision) or be explicitly grandfathered in the baseline. | `scripts/check-screen-manifest.ts:7` | ENFORCED | `scripts/check-screen-manifest.ts:5-10`<br>`scripts/screen-manifest-baseline.json` | scripts/check-screen-manifest.ts:116-127 flags every app/gob\|app/admin page.tsx route absent from SCREEN_MANIFEST and from screen-manifest-baseline.json; :76-83 fails closed when the glob matches zero files; :187-192 exits 1. The rule TEXT itself grants the baseline escape, so the grandfather list is the rule, not a hole. lint:screens is in the verify chain (package.json:79). *Baseline is a fixed grandfathered route list, not a count; stale entries only warn. Previous basis read the same lines.* |
| CANON-411 | A nav item's SECTION must agree with its manifest entry's declared layer. | `scripts/check-screen-manifest.ts:20` | ENFORCED | `scripts/check-screen-manifest.ts:12-20` | scripts/check-screen-manifest.ts:159-166 walks every GOB_NAV_SECTIONS/ADMIN_NAV_SECTIONS item that has a manifest entry and pushes a violation when entry.layer !== SECTION_LABEL_TO_LAYER[section.label]; :152-157 also fails a section label with no mapping, so a renamed section cannot silently skip the check. Exit 1 at :192; lint:screens is in verify. *Nav items with no manifest entry are skipped by design (:161) — that gap is CANON-410's coverage check, not this one's. The previous basis argued from the header's design rationale (:12-19); the deciding line is the comparison at :162.* |
| CANON-412 | A seed script must not assign a seed-marker literal into a renderable column property (displayName/description/name/legalName). | `scripts/check-seed-ids.ts:10`<br>`AGENTS.md:1553` | ENFORCED | `scripts/check-seed-ids.ts:10,94-102,140`<br>`scripts/seed-ids-baseline.json`<br>`scripts/check-seed-ids.ts:94-102,140` | scripts/check-seed-ids.ts:58,71-80 matches a `displayName\|description\|name\|legalName:` assignment whose same-line literal hits a SEED_MARKER_PATTERNS regex (comments stripped via classifyLine); :144-154 fails when a file's hits exceed its baseline; :159 exits 1. scripts/seed-ids-baseline.json is _meta.totalViolations 0 with files {} — the absolute rule holds live at the fence's strictest state. *Scope is scripts/seed-*.ts minus *.test.*; line-based, not AST — a marker built from a variable escapes. No anti-vacuity floor on the glob (unlike check-server-action-exports.ts:111-118). I read the baseline counts independently and reached the same reading the previous basis defended against the earlier refuter's prose-based downgrade.* |
| CANON-413 | Every export of a module carrying the "use server" directive must be an async function. | `scripts/check-server-action-exports.ts:5` | ENFORCED | `scripts/check-server-action-exports.ts:3-20` | scripts/check-server-action-exports.ts:121-123 selects modules whose FIRST statement is the directive (what Next validates), :183-200 classifies every top-level export, and :204-259 marks every non-async runtime shape (enum, class, non-async function, star re-export, re-export list) with a problem string. Anti-vacuity floors live in the script itself (:111-118: 70 modules / 380 exports / 220 runtime). lint:server-exports is in verify. *Header states one known gap: a FUNCTION-scoped "use server" closure is not a module export and is out of subject — outside the rule's wording ("every export of a module").* |
| CANON-414 | Every top-level portal segment (gob/admin/org/(app)/(public)) must ship its own error.tsx. | `scripts/check-state-coverage.ts:8` | ENFORCED | `scripts/check-state-coverage.ts:7-11` | scripts/check-state-coverage.ts:72-86 existsSync-checks a hard list of exactly the five portal error boundaries the rule names (app/gob, app/admin, app/org/[orgToken], app/(app), app/(public)), with no baseline entry possible for rule 1 (header :54-56); the run exits 1 on any violation. lint:states is in the verify chain. *Presence-only: an error.tsx that exists but exports nothing useful still passes.* |
| CANON-415 | Each operator shell layout must mount the offline-banner and maintenance-screen state primitives. | `scripts/check-state-coverage.ts:12` | ENFORCED | `scripts/check-state-coverage.ts:12-17` | scripts/check-state-coverage.ts:94-121 requires BOTH the offline-banner and maintenance-screen symbols in each of the four shell layouts (app/(app) with the Ln* pair, app/gob, app/admin and app/org/[orgToken] with the Op* pair), and a missing FILE is itself a violation (:109-111). No baseline for rule 2 (header :54-56). lint:states is in verify. *Regex over source text: an imported-but-unrendered symbol would pass.* |
| CANON-416 | role="tablist" may only appear in components/ui/Tabs.tsx and components/ui/UrlTabs.tsx, or in a grandfathered baseline entry. | `scripts/check-tablist-ratchet.ts:6` | ENFORCED | `scripts/check-tablist-ratchet.ts:5-15,36` (TABLIST_BASELINE) | scripts/check-tablist-ratchet.ts:76-88 exempts only components/ui/Tabs.tsx and UrlTabs.tsx, then fails any other file whose non-comment role="tablist" count exceeds TABLIST_BASELINE (:36-44), exiting 1 at :93. The rule text itself allows the grandfathered entry. rg for role="tablist" across the repo returns exactly the two primitives plus the two baselined files, so the {app,components}/**/*.tsx glob leaves no live occurrence unscanned. *docSays: 3. The live TABLIST_BASELINE holds 2 entries (WelfareInspectorContent.tsx, PanoramaDock.tsx, 1 each) — PetDetailTabsPanel.tsx left it (:38-42), which the previous basis's 'three pre-existing surfaces' still reflects. Single-quoted selector form is deliberately not matched.* |
| CANON-417 | A date/time formatting call (toLocaleDateString, toLocaleString, Intl.DateTimeFormat) outside the canonical formatter module must pass a timeZone option. | `scripts/check-timezone-dates.ts:6` | ENFORCED | `scripts/check-timezone-dates.ts:11-12,20`<br>`scripts/timezone-dates-baseline.json` | scripts/check-timezone-dates.ts:72-102 matches toLocaleDateString/toLocaleTimeString/date-shaped toLocaleString/new Intl.DateTimeFormat calls, :124-127 flags any whose text lacks timeZone, :204-228 fails a file above its baseline and exits 1. scripts/timezone-dates-baseline.json is totalViolations 0 with files {} — the rule is absolute and the baseline empty, the fence's strictest state. lint:timezone is in verify. *Scope {app,components,lib,packages,src}/**/*.{ts,tsx} minus tests/e2e and the canonical lib/utils/format.ts, which is the exclusion the rule itself states. Bare numeric toLocaleString stays unflagged by design; the semantic arm (:101) catches option-object date keys whatever the receiver is named.* |
| CANON-418 | A date-formatting call that includes hour must also specify hourCycle or hour12 outside the canonical formatter module. | `scripts/check-timezone-dates.ts:14` | ENFORCED | `scripts/check-timezone-dates.ts:14-18,20`<br>`scripts/timezone-dates-baseline.json` | scripts/check-timezone-dates.ts:117-127 — HOUR_RE covers `hour:` AND `timeStyle:`, and :127 lets a call pass only when it has timeZone and either requests no clock or carries hourCycle/hour12; otherwise it is an hourCycle-reason violation (:137) failing the file at :204-228. Baseline empty (timezone-dates-baseline.json totalViolations 0, files {}), so the absolute rule holds live. *The timeStyle half was added after 15 AR-pinned timeStyle:"short" sites shipped the hybrid clock with the fence green (:113-116) — the current predicate is the post-fix one, wider than the rule's wording ('includes hour').* |
| CANON-420 | A query reading ownerships filtered on ended_at IS NULL that takes a single row must constrain the role, unless it is exempted as not resolving a titular identity. | `scripts/check-titular-row-resolution.ts:36` | ENFORCED | `scripts/check-titular-row-resolution.ts:1-18,36` | scripts/check-titular-row-resolution.ts:388 selects sites where filtersActiveOnly() && takesSingleRow() and :407 findOffenders() fails any without a role predicate; ALLOWLIST is empty (:125); non-vacuity floors 40/80/10 (:112-114) exit 1 at :391-405 if the corpus vanishes. Wired: lint:titular-row is in the verify chain (package.json:79). *Scan roots app/lib/src/scripts/db (:100) cover the rule's web scope; the header's known limits are misses, never false passes. Number left null (the '7 incidents' census is a discovery count).* |
| CANON-421 | Interactive elements' className must not include h-9/min-h-9/min-w-9/w-9, which resolve to 36px, below the 44px WCAG touch-target minimum. | `scripts/check-ui-invariants.ts:4` | ENFORCED | `scripts/check-ui-invariants.ts:24-44` | scripts/check-ui-invariants.ts:44 TOUCH_TARGET_TOKENS /\b(h-9\|min-h-9\|min-w-9\|w-9)\b/ is exactly the rule's token set; scanned over TOUCH_TARGET_FILES globSync {app,components}/**/*.tsx (:709) at :757-773, each hit counted into the exit-1 tally (:960). lint:ui is in the verify chain (package.json:79). *TOUCH_TARGET_ALLOWLIST has 10 entries (:56-82), each a documented aria-hidden/decorative element — outside the rule's 'interactive elements' subject. Line-level heuristic; multi-line JSX is out of scope.* |
| CANON-422 | No raw English UI word may appear in JSX text, nav labels, or metadata titles. | `scripts/check-ui-invariants.ts:7` | PARTIAL (declared-limit) | `scripts/check-ui-invariants.ts:804` | scripts/check-ui-invariants.ts:491 declares 'Denylist is opt-in: only words explicitly added here are flagged' — ENGLISH_UI_WORDS is 15 entries (:506-539), so any English word off the list passes; :533-536 further declares that `sub=`/`label=` custom JSX attributes are 'NOT covered by any rule yet'. The rule as stated is universal over English UI words. Arms scan STANDARD_FILES (:799), REGISTRY_LABEL_FILES (:544) and metadata titles. *ENGLISH_UI_WORD_ALLOWLIST is empty (:566-568) — the cap is the wordlist, not the allowlist. The previous basis states the same fact ('an English word nobody listed passes') while grading ENFORCED.* |
| CANON-423 | A UUID-shaped literal ([0-9a-f]{8}-[0-9a-f]{4}-) in app/, components/, lib/ or src/ source is banned unless the file is grandfathered in the baseline at or above that count. | `scripts/check-uuid-literals.ts:12` | ENFORCED | `scripts/check-uuid-literals.ts:12-18`<br>`scripts/uuid-literals-baseline.json` | scripts/check-uuid-literals.ts:40 UUID_LITERAL_RE over globSync {app,components,lib,packages,src}/**/*.{ts,tsx} (:36) minus test/fixture/migration exemptions (:42-43); an empty glob fails closed (:84-88). scripts/uuid-literals-baseline.json grandfathers exactly 2 files at count 1 each, and the rule TEXT itself carries the ratchet ('unless grandfathered at or above that count'). lint:uuid is in verify (package.json:79). *Live baseline files: app/gob/disputas/[disputeToken]/ResolveDisputeForm.tsx, lib/infra/gob-pet-subview.ts.* |
| CANON-424 | Only lib/ui/scope-chrome.ts's describeMandate may turn a raw jurisdictions array into a scope claim inside app/admin/layout.tsx or app/gob/layout.tsx. | `scripts/check-view-scope.ts:11` | ENFORCED | `scripts/check-view-scope.ts:11-18` | scripts/check-view-scope.ts:31 VIEW_SCOPE_FILES is exactly app/admin/layout.tsx + app/gob/layout.tsx (the rule's two files); :45-46 flags any jurisdictions.length not followed by a comparison, :126-129 collects offenses and :149-155 exits 1; :142-147 fails closed when zero chrome files could be read. lint:view-scope is in verify (package.json:79). *Predicate is the count-read shape; a non-.length way of rendering the array as a claim would slip, but ALLOWLISTED_FILE (:34) and the fail-closed floor are live.* |
| CANON-515 | Literal `<select` elements must not increase across app/ and components/; new form selects use the LnSelect (components/ui/Field.tsx) or OpSelect (components/ui/dashboard/OpField.tsx) primitives. Comment lines are ignored; the two primitive definition files are excluded. | `scripts/check-raw-select.mjs:1-14` | ENFORCED | `scripts/check-raw-select.mjs` | scripts/check-raw-select.mjs:113-123 exits 1 when the total `<select` count over {app,components}/**/*.tsx (:59) exceeds BASELINE=19 (:57); countRawSelect :71-83 skips line-comment and JSDoc mentions, and PRIMITIVE_FILES (:64) excludes exactly components/ui/Field.tsx and components/ui/dashboard/OpField.tsx. Empty file list is itself a failure (:96-99). lint:select is in verify (package.json:45,79). *Rule text is "must not increase" -> R6 first bullet; live BASELINE is 19.* |
| CANON-516 | No emoji anywhere in a scanned file (hard rule, no baseline; a comment occurrence is reported as INFO only), and standardized icons plus gov-grade sobriety as a ratcheted second rule (PO directive 2026-07-14). | `scripts/check-professionalism.ts:1-14` | ENFORCED | `scripts/check-professionalism.ts` | scripts/check-professionalism.ts:239-250 hard-fails on any EMOJI_RANGE match outside a comment (comment hits counted as INFO at :243-245, no baseline for rule 1) and :252-266 applies the rule-2 symbol-as-icon ratchet against scripts/professionalism-baseline.json; :269-272 exits 1. Scope {app,components,lib,packages,src}/**/*.{ts,tsx} (:64). lint:professionalism is in verify (package.json:18,79). *The rule TEXT itself declares rule 2 as a ratchet, so R6's first branch covers that half; rule 1 is absolute and baseline-free, matching the fence exactly.* |
| CANON-517 | Any source file constructing a MapLibre map (`new maplibregl.Map(…)` / `new maplibre.Map(…)`) must reference MAPLIBRE_LOCALE_ES and pass it as the `locale` option, so canvas aria-labels, markers, tooltips and the cooperative-gestures overlay speak es-AR. Marker/Popup constructors are deliberately not gated. | `scripts/check-maplibre-locale.ts:1-14` | PARTIAL (ratchet) | `scripts/check-maplibre-locale.ts` | scripts/check-maplibre-locale.ts:33 matches `new maplibregl?.Map(` and :63 requires MAPLIBRE_LOCALE_ES, but :41-48 MAPLIBRE_LOCALE_ALLOWLIST grandfathers 3 live maps - components/panorama/CabaInset.tsx, components/charts/MapChoropleth.tsx, components/LocationPicker.tsx - which construct a map with no locale and pass at :68-71. *Rule text is absolute ('any source file ... must reference'); allowlist live count 3 > 0. The v2 basis quoted only header lines 1-14 and never reached the allowlist.* |
| CANON-518 | Every KPI rendered on an operator surface must come from a lib/metrics/kpi-catalog.ts descriptor (question/target/semaphore/guards declared as data), consumed via <OpKpi descriptorId="…">; a descriptor-less tile (label + raw value + ad-hoc tone) is the C1 violation. | `scripts/check-metric-contract.ts:1-14` | PARTIAL (subset) | `scripts/check-metric-contract.ts` | scripts/check-metric-contract.ts:217 scans only `{app/gob,app/admin}/**/*.tsx`. components/admin/AdminKpiStrip.tsx:113 and app/org/[orgToken]/censo/page.tsx:93 render `<OpKpi label= value= tone=` with no descriptorId - the exact C1 shape - outside that corpus, so nothing can fail on them. *Baseline scripts/metric-contract-baseline.json is {} so the ratchet is at its floor for gob/admin pages; the gap is the corpus (components/**, app/org/**) plus the declared <OpKpiSm exclusion (:31-33). Same P20 shape as the CANON-396 calibration row.* |
| CANON-519 | No new file under app/, lib/ or src/ may write the notifications table directly (`.insert(notifications)` in any receiver form); every new notification goes through createNotification()/createNotificationsBulk() in lib/infra/notification-service.ts, which supplies ON CONFLICT (dedupe_key) idempotency and the notification_dead_letter durability surface. | `scripts/check-notifications-service.ts:1-14` | ENFORCED | `scripts/check-notifications-service.ts` | scripts/check-notifications-service.ts:50 INSERT_RE `.insert(notifications)` over app/**, lib/**, src/** (:42) with comments stripped (:123); :87-93 fails any matching file not keyed true in scripts/notifications-service-baseline.json (63 legacy entries). The rule TEXT is 'no NEW file', which is exactly this ratchet. lint:notifications is in verify (package.json:48,79). *Baseline live count 63, shrink-only by convention (:18-21).* |
| CANON-520 | Spanish plurals must not be inlined as ad-hoc count-vs-1 ternaries whose arms are an empty string and a bare "s"/"es" suffix; use pluralizeEs(n, singular, plural?) from lib/utils/format.ts. | `scripts/check-pluralize-es.ts:1-14` | PARTIAL (ratchet) | `scripts/check-pluralize-es.ts` | scripts/check-pluralize-es.ts:41-42 ADHOC_PLURAL_RE matches exactly the banned ternary over {app,components,lib,packages,src} (:37), but enforcement is a per-file ratchet against scripts/pluralize-es-baseline.json, which grandfathers 57 files at live nonzero counts (e.g. app/(app)/cuenta/memberships/page.tsx: 1). The rule is absolute, so those 57 pass while violating it. *Fail-closed on an empty scan (:23). New files, or counts above baseline, do fail.* |
| CANON-521 | The app-wide count of segment-specific loading.tsx files may only go UP — a ratcheted floor, raised from 13 in Wave 2 to the count recorded in the baseline. | `scripts/check-state-coverage.ts:18-20` | ENFORCED | `scripts/check-state-coverage.ts` | scripts/check-state-coverage.ts:128-132 counts app/**/loading.tsx and :344-346 fails when the count drops below baseline.minLoadingSegments, live 157 (scripts/state-coverage-baseline.json:6). The rule TEXT is itself the ratchet ('may only go UP'), so a floor is the matching enforcement shape. lint:states is in verify. *docSays: 13 (the Wave 2 starting floor quoted in the rule); the live floor is 157. The previous basis carried no opened evidence for this row ('as reported by the reclassifying group'); this verdict is derived from the fence and the baseline file.* |
| CANON-522 | Raw `<button` counts are tracked with SEPARATE baselines per surface — operator/government/org (app/gob, app/admin, app/org) and citizen (components/**, app/(app), app/(public), app/(auth)) — and the two counts are never merged, so an operator regression cannot be masked by citizen headroom. | `scripts/check-raw-buttons.mjs:3-19` | ENFORCED | `scripts/check-raw-buttons.mjs` | scripts/check-raw-buttons.mjs:87,139 hold OPERATOR_BASELINE=54 and CITIZEN_BASELINE=307 against separate globs (:88,:140); runScan :509-541 calls scanSurface once per surface and ANDs the booleans, so either surface over its OWN baseline exits 1 (:373-382) and citizen headroom cannot absorb an operator rise. lint:buttons is in the verify chain (package.json:44,79). *Rule text is a per-surface ratchet ("do not increase"), baselines >0 -> R6 first bullet. The structural half ("never merged") is implemented but not itself pinned by a test; the substantive failure mode it protects against does fail.* |
| CANON-523 | Raw `<input>`/`<select>`/`<textarea>` elements carrying the operator chrome class `border-ln-op-line` under app/ and components/ must not increase (checkboxes and radios exempt); the one operator control chrome is defined once in components/ui/dashboard/OpField.tsx (OpInput/OpSelect/OpTextarea plus OP_CONTROL_CLASS). | `scripts/check-op-controls.mjs:1-18` | ENFORCED | `scripts/check-op-controls.mjs` | scripts/check-op-controls.mjs:72-83 counts input/select/textarea opening tags carrying border-ln-op-line, exempting type=checkbox\|radio (:79), across {app,components}/**/*.tsx (:40); BASELINE = 0 (:38), so :112-122 exits 1 on the first hand-rolled control. :96-99 fail-closes on an empty glob. lint:op-controls is in verify (package.json:46,79). *Rule text says 'must not increase' and the baseline is 0 - both R6 branches give ENFORCED.* |
| CANON-524 | Eight further design-token bans beyond the palette and dark: rules — arbitrary hex in Tailwind arbitrary-value classnames, arbitrary text-[Npx] sizes, arbitrary p/m/gap/space-[Npx\|rem], arbitrary rounded-[Npx], arbitrary shadow-[...], hex literals in style=/inline CSS props, and the two DEAD forms text-[var(--text-*)] (compiles to `color`) and font-[var(--font-*)] (compiles to `font-weight`). | `scripts/check-design-tokens.ts:10-22` | PARTIAL (ratchet) | `scripts/check-design-tokens.ts` | scripts/check-design-tokens.ts:652-660 counts the seven ratchet categories per file and :688-697 fails only when the count exceeds scripts/design-tokens-baseline.json. That baseline is live and non-zero: 119 file-entries for space, 37 shadow, 10 rounded, 4 text, 4 hexStyle. Those five of the eight bans do not hold on existing code; only arbitrary hex (:610-616, 3-entry allowlist :115-125) and the two dead-var forms (baseline 0) fail on any occurrence. *Rule text states the bans absolutely, so a >0 baseline caps at PARTIAL. New violations in any category still fail. The prior basis carried no line evidence for the status ("as reported by the reclassifying group") and never read the baseline, so nothing in it moved me.* |
| CANON-525 | No snake_case internal token may be rendered in JSX text — a sibling of the SCREAMING_CASE enum ban, catching raw payload/enum codes leaking into copy. | `scripts/check-ui-invariants.ts:9 (rule 6)` | PARTIAL (ratchet) | `scripts/check-ui-invariants.ts` | scripts/check-ui-invariants.ts:585 SNAKE_CASE_TOKEN is a general regex (not a wordlist), scanned in JSX-text position at :905-935 plus the payload-fragment arm (:589-614) — a real predicate for the rule. But :582 declares the mechanism a baseline, and SNAKE_CASE_ALLOWLIST carries 4 live grandfathered tokens rendered in JSX text (:629-638), inside the rule's absolute 'no snake_case token in JSX text' subject. *The 4 entries are argued as deliberate <code>/font-mono technical references rather than leaks; anything new fails. Previous basis was inherited from the reclassifying group with no line-level evidence.* |
| CANON-526 | Writes to the database from `app/**` go ONLY through a `src/modules/<domain>` use case called from `actions.ts`; page-level READS from `app/**` straight through Drizzle are tolerated under a shrink-only baseline; and no `app/**` file beyond the one already recorded may insert into the `petEvents` spine. | `dim-interno:docs/reviews/2026-09-fresh/BACKLOG.md:242-253` | PARTIAL (ratchet) | `scripts/check-app-db-boundary.ts`<br>`scripts/app-db-boundary-baseline.json`<br>`__tests__/check-app-db-boundary.test.ts` | scripts/check-app-db-boundary.ts classifies every non-test app/**/*.{ts,tsx} file that imports @/db or drizzle-orm and fails on four things: a writer absent from scripts/app-db-boundary-baseline.json, a baselined entry whose file is gone or no longer writes, a baselined writer whose idiom or table set changed, and any drift in the count of app/ files inserting straight into petEvents (pinned exactly at _meta.spineWriters = 1). Anti-vacuity: an empty corpus, or a corpus in which nothing reaches the database, is a failure and not a pass. Wired as lint:app-db-boundary inside `verify` and mirrored in .github/workflows/ci.yml, so lint:ci-parity holds the two together. R6 caps it at PARTIAL: the rule as the PO stated it is ABSOLUTE ('writes go ONLY through src/modules use cases, no exceptions') and the baseline still carries 22 live app/ writers, so the ratchet stops the 23rd without closing the 22. *The READ half is deliberately NOT enforced — 175 read-class files are counted and printed, and a new one fails nothing. That is the decision (page reads are tolerated so the pilot is not blocked on a rewrite), not a gap in the fence. Postdates the d7dbf25f7 snapshot: decision taken by the PO on 2026-09-02, fence landed at c10f4ff03. scripts/check-dependency-direction.ts polices only src/modules/<A> to <B> edges and has never had anything to say about app/, which is the hole this closes.* |
| CANON-531 | Every runtime load of maplibre-gl must go through lib/ui/maplibre-loader.ts, which calls setWorkerUrl() once before any Map is constructed; the worker and its shared chunk are generated into public/maplibre/ by prebuild/predev and are never committed. | `lib/ui/maplibre-loader.ts:1-30`<br>`scripts/copy-maplibre-worker.ts:1-26` | ENFORCED | `scripts/check-maplibre-worker.ts:104-175`<br>`lib/ui/maplibre-loader.test.ts:30-60` | check-maplibre-worker.ts scans app/components/lib/src with comments stripped and fails on any import("maplibre-gl") outside the loader; it also fails when package.json loses the prebuild/predev hooks, when MAPLIBRE_WORKER_URL and the copy destination disagree, when the installed worker stops importing ./maplibre-gl-shared.mjs, and when a served copy differs from node_modules. A non-vacuity check fails if the scan cannot even see the loader's own import. lint:maplibre-worker is in verify (package.json) and in ci.yml; lint:ci-parity keeps the two in step. *MEASURED, not stylistic. maplibre-gl 6 resolves its worker at runtime through new URL('./maplibre-gl-worker.mjs', import.meta.url) — a form no bundler detects statically, so webpack emits no asset: after a clean pnpm build, `find .next -iname "*worker*"` returned nothing. The fetch does not 404; Next answers the app's own HTML with a 200, the worker executes HTML as JavaScript and dies silently, and the map renders an empty canvas with every layer and source registered and zero features in all of them — no console output, no failed request, no CSP violation. It cost a day of ruling out the database, WebGL2, the load handler, legacy expressions and the CSP. The A/B on one tree: canvas PNG 141099 bytes with the worker present, 82981 with the same file taken away. The e2e spec that covered this page only asserted the canvas was VISIBLE, which it was, which is why the blank map survived months of green.* |
| CANON-538 | Every field of a useActionState form that React 19 does not control must survive the automatic reset that follows a rejected action: a text input/textarea is genuinely React-controlled (value+onChange) or seeds from useKeptFields's kept(); a <select> is keyed off kept() (or off plain state when it never round-trips through the action), never off live state alone; a checkbox/radio seeds its defaultChecked from useKeptFields's keptChecked(), OR from plain live state PAIRED WITH BOTH a changing key and an onChange (proof the expression tracks what was actually clicked, not a value fixed once at mount) — checked= (controlled) is NEVER safe on its own for a checkbox/radio, and neither is a defaultChecked keyed only for React's ordinary list-key requirement with no onChange; anything this fence cannot see is already safe is allowlisted with a written reason. | — | PARTIAL | `scripts/check-kept-fields-adoption.ts`<br>`scripts/check-kept-fields-adoption.test.ts` | scripts/check-kept-fields-adoption.ts walks every useActionState file field by field (not file-wide, so a form that adopts the hook and misses one select still fails) and flags an unsafe select/checkbox/radio/text that neither seeds from kept()/keptChecked() nor carries an allowlist reason. Wired as lint:kept-fields inside `verify` and as its own CI step; scripts/check-kept-fields-adoption.test.ts covers the shapes. Since batch 3, detectLocalTextWrappers() extends scanning through single-level local field-wrapper components (a helper that forwards a bare name={name} to a recognized primitive). Since batch 4: the checkbox/radio check no longer only flags checked= (controlled) — an uncontrolled defaultChecked that is not itself keptChecked(...), and not a plain-state defaultChecked paired with BOTH a changing key and an onChange (mirroring the <select> defaultValue+key exception, but requiring onChange too since a checkbox/radio's defaultChecked resyncs on react-dom's UPDATE path regardless of remounts — a key alone, the ordinary React list-key every list-rendered checkbox carries, proves nothing; FinalizeAdoptionForm.tsx's __appChoice/useFosterShortcut are the genuinely-safe key+onChange shape, AgendaRuleForm.tsx's original key-only defaultChecked was the bug this refinement re-catches after a first version missed it) — is flagged, since a native form.reset() restores checked to whatever was true at mount otherwise; and the file-level useActionState( detection now also matches useActionState<StateType, PayloadType>(, which the old regex missed entirely (found via ShareLibretaSheet.tsx, previously invisible to this fence in full). A fresh-context review of batches 3-4 then found and closed four more gaps: the select/checkbox key= check required only that a key EXIST, not that it CHANGE (a bare key={value} or an ordinary per-item list key like key={d.value} satisfied it; now the key expression itself must be a template literal — a backtick or ${ — the same spelling every hand-verified fix already used); a local wrapper detected only for its text-ish passthrough could still be called with checked=/defaultChecked= and fall through to the TEXT rule's controlled check, which a checkbox's own submitted value= satisfies just as well as a text field's does (latent, closed by testing for checked=/defaultChecked= ahead of tagName/type=); attrExpr's [^}]* regex truncated at the FIRST }, wrong for any expression containing one before its own close (a template literal's ${...}, an object literal) — brace- and quote-balanced now, same technique as readOpeningTag; and the allowlist marker exempted a file from every check above ENTIRELY and forever, uncounted by anything — a second ratchet, ALLOWLISTED_BASELINE (currently 7), now fails the same way a new unsafe form does if a marker is added without a deliberate, reviewed bump. Also that review: kept(name) could not distinguish 'nothing submitted yet' from 'submitted, and this field was blank', so the kept(name) \|\| fallback pattern every call site used resurrected the fallback over a field someone had deliberately CLEARED — kept now takes the fallback as a second, explicit argument (kept(name, fallback), same contract keptChecked already had), returning it ONLY pre-submit; 19 call sites across the batch-3/4 forms were migrated off \|\|, one of which (WeightForm.tsx) had silently made a falsy-but-valid default fall through — fixed by building the fallback argument with ??, not \|\|. PARTIAL because a ratchet BASELINE still admits the forms not yet converted. *React 19 resets an uncontrolled form once its action resolves, including when it returns an error, which wipes what the person typed. Batch 1 also found that keying a select off LIVE state remounts it on every pick, not only at the reset — the key must come from SOMETHING THAT CHANGES WHEN THE PICK DOES. kept() is the usual source (a value that round-trips through the action's FormData), but it is NOT the only sanctioned one: a value that is genuinely parent-owned and never submitted through the action at all — LegalMetadataFieldset's requirement-tier select, NoteForm's category select, FinalizeAdoptionForm's __appChoice/useFosterShortcut, and, since batch 4, 7 of PetForm's selects (sex, speciesGroup, speciesSubgroup, breed, trainingLevel, acquisitionMethod, microchipLocation) — keys off that PLAIN REACT STATE directly instead, with the SAME defaultValue+key shape, because kept() is simply not available to it (there is nothing for the action to echo back). The fence enforces the MECHANISM (a template-literal key that changes with the tracked value), not the SOURCE of that value. This notes field used to say 'the key must come from kept()' without qualification, which stopped being true the moment the first parent-owned-state select shipped and was never corrected here — it contradicted this row's own rule field, which already stated the exception correctly.* |
| CANON-540 | No surface may read the device's location (W8, PO 2026-09-24): no navigator.geolocation in any spelling, no Geolocation API method, no MapLibre GeolocateControl, no geolocation permission query and no 'Usar mi ubicación'-style control on the web; no native location module, dependency or OS location permission in apps/mobile; and next.config.ts must send Permissions-Policy geolocation=(). Places are entered by address search or the map pin; the credential scan keeps only the server-side IP-derived area. | `scripts/check-no-device-gps.ts:1-39` | ENFORCED | `scripts/check-no-device-gps.ts:52-81`<br>`scripts/check-no-device-gps.ts:191-204`<br>`package.json:86` lint:no-device-gps<br>`__tests__/check-no-device-gps.test.ts:31-126`<br>`__tests__/check-no-device-gps.test.ts:128-144` | scripts/check-no-device-gps.ts scans comment-stripped web source (app/, components/, lib/, src/, packages/*/src) and mobile source (apps/mobile/app, apps/mobile/src) against WEB_RULES/MOBILE_RULES (:52-81), checks the mobile manifest and package.json for location permissions and modules, and requires Permissions-Policy geolocation=() in next.config.ts (:191-204); it fails below MIN_WEB_FILES/MIN_MOBILE_FILES. __tests__/check-no-device-gps.test.ts:31-126 plants a sample for every rule (dot, optional-chaining and bracket access, destructured methods, GeolocateControl, permission query, copy with and without accent, each native module, manifest permissions, the header) and pins the look-alikes it must spare (comments, MapLibre locale keys, the legacy 'gps' enum value); :128-144 runs the real tree. lint:no-device-gps is in the verify chain and in .github/workflows/ci.yml (lint:ci-parity holds the pairing). *Legacy values stay valid by design: location_source 'gps' and scan_coords/scan_accuracy_m remain in lib/events/event-schemas.ts so pre-W8 events keep validating (append-only). Writers no longer accept 'gps' from form input (src/modules/surveillance/actions.ts, src/modules/pets/application/sighting/report-pet-sighting.ts). Companion to CANON-174.* |
