# Conventions canon — Mobile (`apps/mobile`)

> Snapshot: `d7dbf25f7` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-24
> Verified against code on 2026-09-24 by canon v4 + blind calibration · Status: reviewed
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

[← canon index](../conventions-canon.md) · scope `mobile` · 39 rules (15 ENFORCED, 4 PARTIAL, 20 UNENFORCED).

| Id | Rule | Source | Status | Enforcer | Basis |
| --- | --- | --- | --- | --- | --- |
| CANON-425 | The mobile-export nightly job must export the Android Expo bundle via node node_modules/expo/bin/cli directly, not through the pnpm bin shim, because the shim's injected NODE_PATH masks undeclared dependencies EAS's Gradle build would fail on. | `.github/workflows/mobile-export-nightly.yml:117` | UNENFORCED | `.github/workflows/mobile-export-nightly.yml:137-147` | .github/workflows/mobile-export-nightly.yml:147 `run: node node_modules/expo/bin/cli export --platform android` is the invocation string itself; swapping it back to `pnpm -C apps/mobile export` exits 0 and fails nothing. No fence reads the workflow's run commands (check-ci-lint-parity.ts:20 covers ci.yml lint tokens only; check-scheduled-fence-refs parses schedule/ref/uses/audited). *Self-executing declaration, true at this SHA — the v3 basis says 'The run line IS the mechanism'. The command does exit 1 on an undeclared dependency, but that is the bundle's health (CANON-426's subject), not this rule's predicate, which is which invocation form the job uses.* |
| CANON-426 | The mobile-export nightly job must assert a written .hbc bundle file exists and is at least 512 KiB, not just trust expo export's exit code. | `.github/workflows/mobile-export-nightly.yml:149` | ENFORCED | `.github/workflows/mobile-export-nightly.yml:149-170` | .github/workflows/mobile-export-nightly.yml:155-171 — the step finds the `*.hbc` under apps/mobile/dist/_expo/static/js/android, `exit 1` at :160 when none exists, stats it at :162, and `exit 1` at :168-170 when SIZE < 524288. The step's own command exits non-zero on exactly the rule's condition. *Live floor 524288 bytes at :168 matches the rule's 512 KiB.* |
| CANON-427 | The word 'denunciar' must never appear in the report-an-item copy; only 'reportar' is used. | `AGENTS.md:1702` | PARTIAL (subset) | `apps/mobile/src/lost/lost-view-model.test.ts:354,445-446` | apps/mobile/src/lost/lost-view-model.test.ts:449-456 sweeps an ENUMERATED array — REPORT_ACTION_LABEL, REPORT_INTRO, the category labels, commandDoneLabel/commandUnchangedLabel — for `denunci`. The report sheet's own literals are outside it: LostScreen.tsx:764 `Card title="Reportar un mensaje"`, :773 "¿Qué pasa con este mensaje?", :791, :795, :799 never reach that array. *AGENTS.md:1702 claims the fence 'recorre toda la copy de la función'; it walks the view-model's exported constants only. The previous basis confirmed the describe and the wiring, which were never in doubt, and did not test the rule's scope. Uncovered part: apps/mobile/src/lost/LostScreen.tsx inline report-sheet copy.* |
| CANON-428 | Every `<Pressable` block in `apps/mobile/src` must declare an `accessibilityRole`, and every file rendering a `<Pressable>` must reference `TOUCH_TARGET` or `hitSlop`; the `EmptyState` primitive must keep at least 4 consumer files. | `__tests__/mobile-a11y-fences.test.ts:1-27,79-121` | ENFORCED | `__tests__/mobile-a11y-fences.test.ts:87-98,102-108,112-119` | __tests__/mobile-a11y-fences.test.ts:44 walks the whole apps/mobile/src tree; :87-99 fails on any <Pressable opening tag without accessibilityRole (per block, brace-depth scan :54-72), :102-109 on any pressable file lacking TOUCH_TARGET/hitSlop, :112-119 EmptyState consumers >= 4. :79-85 is an explicit non-vacuity floor (files > 80, pressableFiles >= 15). *Live floor is 4 (:119), matching the rule text.* |
| CANON-429 | apps/mobile tests must run under Jest via jest-expo, kept as a separate runner from the root Vitest suite, and apps/mobile must be excluded from the root vitest test-file walk. | `apps/mobile/jest.config.js:3` | PARTIAL (subset) | `apps/mobile/jest.config.js:34`<br>`__tests__/db-reachability.ts:60-67,75`<br>`package.json:78-79` verify:mobile<br>`__tests__/project-partition.guard.test.ts` | __tests__/db-reachability.ts:70-80 keeps 'apps' in SKIP_DIRS, feeding the vitest.config.ts:60/:76 include lists; removing it makes vitest collect the Expo jest files and check-suite-coverage report them as broken, so that conjunct does fail on violation. The runner conjunct is not checked: apps/mobile/jest.config.js:34 `preset: "jest-expo"` and package.json:78 verify:mobile are declarations, and __tests__/project-partition.guard.test.ts:22-70 asserts only the vitest unit/db split (exhaustive, disjoint, no DB leak into unit) — nothing about apps. *Uncovered part: nothing asserts apps/mobile is tested by jest-expo rather than some other runner. The v3 basis cited project-partition.guard.test.ts as making re-inclusion 'not silent'; I opened it and its subject is the unit/db partition, not the apps exclusion — R12, the fence's name resembling the rule is not the fence checking it.* |
| CANON-430 | apps/mobile Jest must run under a react-native export-conditions test environment (jest-environment-node + customExportConditions=["require","react-native"]), not jsdom. | `apps/mobile/jest.config.js:39` | UNENFORCED | `apps/mobile/jest.config.js:39-41,54` | apps/mobile/jest.config.js:54 `testEnvironment: require.resolve("@react-native/jest-preset/jest/react-native-env.js")` is the config declaration itself. rg for `testEnvironment\|customExportConditions\|react-native-env` across __tests__/, apps/mobile/src/ and scripts/ returns only prose hits (scripts/check-mock-paths.ts:41, __tests__/db-reachability.ts:63) — no assertion pins it, so switching the environment to jsdom fails nothing by itself. *Self-executing declaration, true at this SHA — the v3 basis says 'The config line IS the mechanism'. apps/mobile/src/release/release-config.test.ts pins eas.json, app.json, babel and the ignore files, not jest.config.js.* |
| CANON-431 | Jest's testMatch must stay a relative glob with roots anchoring the path, because an absolute Windows path containing a backslash-escaped directory breaks micromatch. | `apps/mobile/jest.config.js:60` | UNENFORCED | `apps/mobile/jest.config.js:60-71` | apps/mobile/jest.config.js:70-71 IS the cited enforcer: `roots: ["<rootDir>/src"]`, `testMatch: ["**/*.test.ts", ...]`. No artifact reads it back — rg for `jest.config` outside node_modules returns only __tests__/db-reachability.ts (a comment), docs, and three .tsx files; release-config.test.ts never mentions testMatch. Violation degrades to jest's own 0-match behaviour, and only on Windows. *Self-executing declaration, true at this SHA. The previous basis said 'regression is self-evident: a wrong glob collects zero files on the PO's machine' — that is the runner's behaviour, not a check; on Linux CI an absolute forward-slash glob satisfies jest and still violates the rule text.* |
| CANON-432 | apps/mobile Jest's transformIgnorePatterns must explicitly add lucide-react-native to the RN-ecosystem exception list because its ESM build is otherwise unparseable. | `apps/mobile/jest.config.js:73` | UNENFORCED | `apps/mobile/jest.config.js:73-84` | apps/mobile/jest.config.js:83-87 IS the cited enforcer — the transformIgnorePatterns array naming `lucide-react-native`. rg for `transformIgnorePatterns` across the repo matches that one file; no test asserts the entry. The rule constrains the CONFIG, and only the config states it. *Self-executing declaration, true at this SHA. Removing the entry would red apps/mobile/src/ui/Icon.test.tsx:16 (direct `import * as lucide from "lucide-react-native"`, rendered at :38) as a side effect, but that test's predicate is icon-name resolution, not the transform config (R12) — the same shape the judge rejected in CANON-002.* |
| CANON-433 | A geocoder outage on the mobile denuncia flow must answer 503, never render an empty match list, since an infrastructure failure must not be disguised as a user error. | `dim-interno:docs/agents/handoff-2026-08-30.md:37-42` | ENFORCED | `__tests__/api-v1-welfare-reports-route.test.ts:99-107,244-249`<br>`app/api/v1/welfare-reports/commands.ts:121-123,190,208` | __tests__/api-v1-welfare-reports-route.test.ts:465-479 loops the geocoder's three throw reasons and asserts 503 + temporarily_unavailable envelope + retry-after, with :478 proving the geocoder was actually reached; :482-498 asserts the failure status is NOT the empty-match 200 status. Route arm at app/api/v1/welfare-reports/commands.ts:121-123,200-207. *Draft cited the source file as the enforcer; corrected to the test that can fail, with the source line kept as the mechanism.* |
| CANON-434 | The picker adapter's JPEG re-encode step is not optional — it converts HEIC to an accepted format and strips EXIF/GPS from every picked photo before upload. | `docs/mobile/camera-modules-handback.md#the-three-modules-and-the-one-command` | UNENFORCED | — | No jest test asserts the re-encode step runs, and no fence scans the picker adapter for the expo-image-manipulator call. The leak the doc names (EXIF/GPS surviving to upload) would be invisible to every gate. *A high-value gap: this is a privacy invariant with no mechanism.* |
| CANON-435 | The chip-scan camera adapter must not pre-filter the scanned string — chipCodeFromScan in the view-model is the single validation door for both keyboard entry and camera scan. | `docs/mobile/camera-modules-handback.md#appsmobilesrcnativeexpo-camera-adaptertsx` | UNENFORCED | — | No fence asserts the adapter passes the raw string through, and no test compares the two input paths' verdicts (the shape __tests__/adoption-application-input-parity.test.ts uses for the web validators would be the instrument here). |
| CANON-436 | Permission-dialog strings for camera/photo access must be es-AR, since the UI-language invariant applies to permission dialogs too. | `docs/mobile/camera-modules-handback.md#permissions--config-plugins` | UNENFORCED | — | apps/mobile/src/release/release-config.test.ts fences eas.json, .easignore, app identity assets, babel declarations and SDK-pinned modules, but no describe block covers config-plugin permission strings. lint:maplibre is the analogous es-AR fence on the web side; mobile has none. |
| CANON-437 | The Play Data Safety form must be revised and submitted before or together with any build that adds photo/camera collection — never after the build reaches Play. | `docs/mobile/camera-modules-handback.md#data-safety-first-board-po-gated-item-2` | UNENFORCED | — | An action outside the repository entirely (Play Console). Nothing in the tree can observe whether the form was submitted, let alone its ordering against a rollout. *PO-gated by construction; recorded so the audit distinguishes 'unenforceable here' from 'nobody built the fence'.* |
| CANON-438 | `eas.json`'s `cli.appVersionSource` must be `"remote"` so `versionCode`/`buildNumber` is issued once by EAS's server via `autoIncrement`, never incremented by hand in the repo; a locally-built artifact (`expo run:android`) must never be uploaded to Play, since its `versionCode` is always 1 in a numbering universe disconnected from EAS's remote counter. | `docs/mobile/eas-build-profiles.md#cliappversionsource-remote--the-decision-that-costs-something` | PARTIAL (subset) | `apps/mobile/src/release/release-config.test.ts:19-20,55-56,89-95` | apps/mobile/src/release/release-config.test.ts:96 pins `easJson.cli.appVersionSource` to "remote" and :128 pins production `autoIncrement` true — the config half. The rule's second half (a locally built `expo run:android` artifact must never be uploaded to Play) has no enforcer: rg for `run:android\|expo run` outside node_modules hits only apps/mobile/package.json's script and four docs. *Uncovered part: the upload discipline itself — no gate can observe what a human hands to Play Console.* |
| CANON-439 | Each eas.json build profile's channel must map 1:1 to the profile name, and channels must be declared only in eas.json, never in the app config. | `docs/mobile/eas-build-profiles.md#the-channel-field-and-where-ota-fits` | ENFORCED | `apps/mobile/src/release/release-config.test.ts:103-111` | release-config.test.ts:103-111 asserts `easJson.build[profile].channel === profile` for each of development/preview/production, and :100 closes the corpus (`Object.keys(easJson.build).sort()` toEqual those three). The second clause IS fenced, at :187-192: `Object.keys(resolved.updates).sort()` toEqual exactly checkAutomatically/enabled/fallbackToCacheTimeout/url, with :178-181 naming the reason ('A channel pinned in the app config would apply to EVERY build'). *The previous basis capped this at PARTIAL saying the 'never declared in the app config' half 'was not found as an assertion'; :177-193 is that assertion, and it runs against the config the factory RESOLVES (built at :84-87 from app.json + app.config.ts), so an app.json-side channel is covered too. `updates.requestHeaders["expo-channel-name"]` is the only app-config route to a channel and the exact-key-set assertion excludes it.* |
| CANON-440 | The root package.json must pin packageManager to an exact pnpm version (pnpm@11.1.1) so the PO's machine, CI, and the EAS build worker all resolve native dependency paths with the same pnpm. | `docs/mobile/eas-build-profiles.md#one-pnpm-everywhere--why-packagemanager-is-in-the-root-packagejson` | ENFORCED | `apps/mobile/src/release/release-config.test.ts:786-805` | release-config.test.ts:801-804 reads the ROOT package.json from disk and asserts `packageManager` matches /^pnpm@\d+\.\d+\.\d+$/ — the field's absence or a range pin (`pnpm@11`) fails. The rule's predicate is exactness of the pin, which the shape assertion covers. *docSays: pnpm@11.1.1; the fence deliberately pins the SHAPE, not the literal (comment at :798-800), so a deliberate upgrade is one edit. The specific version 11.1.1 is therefore not itself fenced — number left null on purpose.* |
| CANON-441 | CI workflow steps using pnpm/action-setup must not pass a version: input alongside the packageManager field, since the action compares them as raw strings and any mismatch fails the setup step in every job. | `docs/mobile/eas-build-profiles.md#the-eleven-version-11-inputs-had-to-go-and-not-for-tidiness` | ENFORCED | `apps/mobile/src/release/release-config.test.ts:807-835` | release-config.test.ts:815-838 readdirs .github/workflows, walks every `pnpm/action-setup` step to the next list item collecting any `version:` input, asserts offenders toEqual([]) and adds a non-vacuity floor `stepsSeen > 0`. The `.endsWith(".yml")` filter covers 4 of 4 workflow files (rg --files .github/workflows: staging-health, mobile-export-nightly, codeql, ci — all .yml; the staging-facing nightlies moved to dim-interno on 2026-09-24 and are outside this test). *Latent filter risk only: a future .yaml workflow would escape the readdir filter. No .yaml exists at this SHA, so the corpus gate does not exclude any subject file (P20).* |
| CANON-442 | apps/mobile/.gitignore must list /android/ and /ios/ (with the listing living inside apps/mobile/, not the repo root, since the fingerprint tool globs .gitignore from the Expo project root), and apps/mobile/.easignore must list the same two paths so a locally-run prebuild is never uploaded to EAS. | `docs/mobile/eas-build-profiles.md#cause-2--eas-prebuilds-android-and-nothing-was-ignoring-it` | ENFORCED | `apps/mobile/.gitignore:42-43`<br>`.easignore:188-189`<br>`apps/mobile/src/release/release-config.test.ts:681-707` | Both halves are pinned by real assertions the v3 pass did not reach. apps/mobile/src/release/release-config.test.ts:849-854 reads apps/mobile/.gitignore FROM MOBILE_ROOT, splits it into trimmed non-comment lines, and expects the array to contain `/android/` and `/ios/` — the load-bearing location. :722-729 expects the root .easignore rule set to contain `apps/mobile/android/` and `apps/mobile/ios/`. Live files agree (apps/mobile/.gitignore:42-43, .easignore:188-189). *The v3 basis capped this at PARTIAL on the ground that release-config.test.ts:694-707 (the gitignore-parity and credential-path tests) does not cover the two entries — correct about :694-707, but it missed the two dedicated tests at :722-729 and :841-854, which I opened after reading the basis and which do exactly that. Mobile jest file; runs via pnpm verify -> verify:mobile -> `pnpm --filter mimar test` (package.json:78-79), which R8 buckets as test:verified. The basis is right that the rule's second path is loose: the .easignore is at the repo root, not apps/mobile/.easignore.* |
| CANON-443 | Every package a mobile config file imports by name must be declared in apps/mobile/package.json, checked by deriving standardized package names from every babel preset/plugin entry rather than hardcoding one name. | `docs/mobile/eas-build-profiles.md#what-guards-it-now` | PARTIAL (subset) | `apps/mobile/src/release/release-config.test.ts:1326` | release-config.test.ts:1296 is the line that FAILS when the rule is broken — `expect(named.filter((name) => !declared.has(name))).toEqual([])`, with a non-vacuity floor at :1302. The citation was `:931` until 2026-09-04; that line had drifted onto an unrelated assertion in the workflow-pin test, the icon work of that day shifted it onto a comment (`:1189`), which is what the canon-parity fence (b) reported, and the feature-graphic tests added later that day moved it once more. The test requires ../../babel.config.js, CALLS it, and derives package names from presets+plugins against apps/mobile/package.json's deps+devDeps — babel.config.js ONLY. The rule says 'every package a mobile config file imports by name': apps/mobile/metro.config.js:25 requires "expo/metro-config" and jest.config.js:54/:97 require "@react-native/jest-preset" and "jest-expo/jest-preset", none of them swept. *Independently reached the judge's conclusion, and jest.config.js is a second uncovered config file the judge's note did not name. The derivation-not-hardcoding half of the rule is genuinely honoured (standardizeBabelName at :1208).* |
| CANON-444 | Every native dependency resolved into apps/mobile must satisfy the SDK's own bundledNativeModules.json pin, checked against the lockfile rather than declarations, so an undeclared auto-installed optional peer cannot drift silently. | `docs/mobile/eas-build-profiles.md#what-guards-it-now` | ENFORCED | `apps/mobile/src/release/release-config.test.ts:1138-1158` | release-config.test.ts:1014-1015 reads expo/bundledNativeModules.json; :1026-1027 parses pnpm-lock.yaml's packages: section (the LOCKFILE, not declarations); :1138-1153 asserts every resolution of every bundled name satisfies the SDK pin, with a non-vacuity floor `checked >= 20` at :1158. The two exempt web-owned names are re-pinned at :1161-1179. *The exemption (react, react-dom) is closed by a compensating assertion: whatever apps/mobile itself declares for an exempt name must equal the SDK pin (:1172), plus an exemption-list non-vacuity check (:1177-1178).* |
| CANON-445 | No .env* file may be read, created, or edited for mobile-emulator setup — the anon key must be taken from the running Supabase stack and passed inline. | `docs/mobile/emulator-runbook.md#3-the-environment-recipe` | UNENFORCED | — | A PO rule about agent behaviour during a session, not a property of the tree. Nothing observes a read of a gitignored file. The related structural fact — .easignore must restate .env* (release-config.test.ts:705-707) — is a different rule (upload hygiene). *scripts/check-env-local.ts exists but is env:doctor (package.json:97), not in verify, and its subject is env completeness rather than this prohibition.* |
| CANON-446 | git status must be checked after any expo start or expo run:android and before any gate or commit, since either command can silently rewrite tracked config files. | `docs/mobile/emulator-runbook.md#4-build-and-install` | UNENFORCED | — | A session procedure. No hook, workflow step or fence asserts a clean tree after an expo command; CI never runs expo start at all. |
| CANON-447 | A polling loop on a backgrounded mobile build/launch process must poll process liveness or an unconditional terminal marker, never match only for a success string. | `docs/mobile/emulator-runbook.md#t7--poll-on-the-process-not-on-a-success-string` | UNENFORCED | — | An agent-operations rule about how to wait on a child process; there is no committed launcher script whose polling shape a fence could inspect. |
| CANON-448 | An OTA update must be subtractive only — after it, the app must not be able to do anything it could not do before. | `docs/mobile/ota-policy.md#what-may-go-out-over-the-air` | UNENFORCED | — | The policy's own framing is a judgement question. Its one MECHANISED consequence is the fingerprint runtimeVersion (CANON-449), which blocks native additions but says nothing about a JS-only capability addition. |
| CANON-449 | Anything that changes the native runtime must never ship OTA (native module add/remove/upgrade, SDK/RN bump, config-plugin change, permissions/icon/splash, URL scheme/intent filters, build settings) — mechanically blocked by the runtimeVersion fingerprint policy. | `docs/mobile/ota-policy.md#anything-that-changes-the-native-runtime` | ENFORCED | `apps/mobile/src/release/release-config.test.ts:159-167,755` | release-config.test.ts:166 asserts `resolved.runtimeVersion` toEqual { policy: "fingerprint" } on the config the app.config factory actually returns — a flip to appVersion fails. The rule names the fingerprint policy as ITS mechanism, and :771-783 / :785-805 / :841+ pin the two known fingerprint-reproducibility causes so the policy is not merely declared. *The per-change-kind list (native module, SDK bump, config plugin, permissions, icon/splash, intent filters) is enforced by EAS at publish time, not here; what the repo can fail on is the policy value and its reproducibility, both pinned.* |
| CANON-450 | Anything the store review would have needed to see (a new feature/surface, a data-collection change, a stated-purpose change, anything that changes a store questionnaire answer, anything worth announcing) must never ship OTA. | `docs/mobile/ota-policy.md#anything-the-store-review-would-have-needed-to-see` | UNENFORCED | — | The source itself says 'Not fenced by anything. This is the list that requires judgement.' Confirmed: no fence exists and none could without deciding what counts as a new surface. *The rule is explicitly self-declared as unenforceable; that is a design statement, not a gap draft 1 missed.* |
| CANON-451 | An OTA update must never assume a server contract not already deployed — the server must stay compatible with the oldest install still opening, since a released OTA cannot be recalled. | `docs/mobile/ota-policy.md#anything-that-assumes-a-server-contract-not-already-deployed` | UNENFORCED | — | No fence compares a mobile bundle's API expectations against the deployed server contract. @dim/contract is the shared type surface, and lint:contract keeps it installable — but nothing checks version skew between a published bundle and a deployed API. |
| CANON-452 | An OTA fix must land in the tree normally first — through pnpm verify and pnpm test:verified and review — with no OTA-only code path and no patch that exists solely on a channel. | `docs/mobile/ota-policy.md#procedure` | UNENFORCED | — | A release procedure. Nothing can detect that a published bundle contains a commit absent from main; that comparison happens on EAS's servers and nothing in the repo reads it. |
| CANON-453 | An OTA update must be proven JS-only via eas fingerprint:compare against the build being patched — never inferred from the diff. | `docs/mobile/ota-policy.md#procedure` | UNENFORCED | — | A manual command in a release runbook, not a gate. The related mechanism (runtimeVersion: fingerprint, CANON-449) makes a native-carrying update reach nobody, but nothing requires the operator to run the comparison first. |
| CANON-454 | Every OTA hotfix must publish to the preview channel first, be installed and opened twice, and be confirmed before publishing to production. | `docs/mobile/ota-policy.md#procedure` | UNENFORCED | — | A manual release sequence on a physical device. The only mechanised part of the surrounding policy is the channel↔profile 1:1 mapping (CANON-439), which keeps `--channel preview` off production installs but does not order the two publishes. |
| CANON-455 | The next store release after any OTA hotfix must contain the same commit — an OTA that stays only on a channel across multiple releases is a fork. | `docs/mobile/ota-policy.md#procedure` | UNENFORCED | — | Requires comparing published channel contents to store build contents across time; neither side is readable from the repo, and no workflow reconciles them. |
| CANON-456 | The pinned Node version must be >= 22.18.0 so packages/contract's raw .ts exports type-strip correctly for expo config. | `scripts/check-node-version.ts:31` | UNENFORCED | `scripts/check-node-version.ts:20-36`<br>`package.json:77` lint:node-version | 22.18.0 occurs in scripts/check-node-version.ts only at :23, :32-36, :42 (the measurement table) and :273 (an error message) - all comment/string text. The predicates (:150-258) compare .nvmrc/.node-version/engines/workflows to EACH OTHER and :262 the running Node to engines; none compares any version against 22.18.0, and :197-209 explicitly permits a floor below the pin. *True at this SHA by declaration (pin 22.23.2, engines floor 22.23.0). v2 upgraded this to ENFORCED because the constraint is 'stated verbatim with the measurement behind it' - a comment stating a constraint is not a check. Lowering the pin below 22.18.0 fails nothing until a machine actually running that Node hits `expo config` inside verify:mobile (package.json:78).* |
| CANON-501 | eas.json's development profile must be an internal-distribution development client, its preview profile internal, and its production profile store-distribution; expo-updates must be configured; and app identity assets (icon/adaptive foreground/splash) must satisfy the store's shape rules. | `apps/mobile/src/release/release-config.test.ts:121-127,132,144,502` | ENFORCED | `apps/mobile/src/release/release-config.test.ts` | release-config.test.ts:121-122 pins development+preview distribution "internal", :140 development `developmentClient: true`, :127 production distribution "store", :144-193 the expo-updates block (projectId/url agreement, fingerprint policy, fallbackToCacheTimeout 0, exact four-key set), :502-526 icon 1024² with hasAlpha false and the adaptive foreground at 1024² inside the safe zone, :553-573 the splash crop and aspect ratio. Every clause of the row has a failing assertion. *Previous basis was second-hand ('as reported by the reclassifying group'); verified line by line here. Assets are read as BYTES from disk (readPng / inkBoundsOpaque at :470-499), so the assertions are on what the store consumes, not on a declared shape.* |
| CANON-502 | The root `.easignore` must restate every rule the root `.gitignore` carries — .easignore REPLACES .gitignore for the EAS upload rather than extending it — and must explicitly carry `.env*`, `.env.local` and `e2e/.auth/` so a service-role key or a live session token is never uploaded. | `apps/mobile/src/release/release-config.test.ts:681-707` | ENFORCED | `apps/mobile/src/release/release-config.test.ts:694-707` | release-config.test.ts:694-698 diffs the parsed rule sets — every .gitignore rule absent from .easignore lands in `missing`, asserted toEqual([]) — and :704-708 separately asserts `.env*`, `.env.local`, `e2e/.auth/` and `qa-sessions*.json` are present, so relaxing the mirror cannot take the credential rules with it. *Previous basis was second-hand; verified here. Both halves of the rule (mirror + explicit secret rules) have their own failing assertion; ignoreRules() strips comments and blanks at :676-678.* |
| CANON-511 | The pinned Node version must also satisfy the registerHooks constraint (>= 22.13/22.15, or every tsx-loader script dies on import) and the WHATWG windows-1252 constraint (>= 22.23.0, or the fiscalia-PDF text helper decodes 0x97 to U+0097 instead of an em dash). | `scripts/check-node-version.ts:20-40` | UNENFORCED | `package.json:77` | check-node-version.ts:59-63 enumerates all four checks and every one is an AGREEMENT check: .nvmrc == .node-version, engines.node a closed range whose floor is <= the pin (:200-209) with ceiling pin.major+1 (:210-217), workflows on the same major, running Node inside the range. No absolute 22.15.0 or 22.23.0 constant exists in the file or in __tests__/check-node-version.test.ts. *The basis cited check-node-version.ts:20-40 — that range is the header COMMENT table stating the two floors, and the code below it never compares against them. Lowering .nvmrc, .node-version and engines.node together to 22.14.0 passes every check, so the registerHooks and windows-1252 floors survive only as a package.json engines declaration (R2). __tests__/check-node-version.test.ts:25-149 tests parse/compare/satisfies helpers on fixtures only.* |
| CANON-527 | The shared pet-profile icon vocabulary may not map a concept to a bare geometric placeholder, and no two concept keys may wear the same glyph without a declared alias. | `scripts/check-mobile-icon-vocabulary.ts:3` | ENFORCED | `scripts/check-mobile-icon-vocabulary.ts:231`<br>`scripts/check-mobile-icon-vocabulary.ts:253`<br>`scripts/check-mobile-icon-vocabulary.ts:286`<br>`__tests__/check-mobile-icon-vocabulary.test.ts`<br>`components/Icon.test.tsx` | scripts/check-mobile-icon-vocabulary.ts parses PET_PROFILE_ICONS out of packages/contract/src/icons/pet-profile-icons.ts and applies three rules through the exported `evaluate(source)`. Rule A (:231) fails any value in PLACEHOLDER_GLYPHS (Circle, Square, Box, Dot, Minus, Slash, HelpCircle, CircleDashed, SquareDashed) with no baseline; Rule B (:253) fails any glyph used by more than one key unless the key set matches a DECLARED_ALIASES entry exactly, and :266 fails the reverse — a declared alias the table no longer duplicates; Rule C (:200-227) fails an unreadable source (one violation, not two), a parse of zero, a count below MIN_ENTRIES, or any key→glyph pair that differs from the pinned PINNED_TABLE pair list, naming the rows added and removed. Exit 1 at :286 on any violation. __tests__/check-mobile-icon-vocabulary.test.ts exercises all four shapes against fixtures, including the real pre-4fa613500 table. components/Icon.test.tsx closes the other side by asserting every table key exists in the web ICON_MAP and maps to the SAME component by reference. package.json lint:mobile-icons is in the verify chain and .github/workflows/ci.yml runs it in the `check` job, so CI blocks a merge on it too (check-ci-lint-parity enforces that pairing). *Written 2026-09-03 after `embarazo: "Baby"` and `fallecimiento: "Circle"` both shipped to a store build. apps/mobile/src/ui/Icon.test.tsx had fenced this table since 2026-09-01 and was GREEN on both, because it only asserts that each value resolves to a real lucide-react-native export — a question that cannot detect a glyph which resolves and means nothing. The three rules here are the mechanically decidable part; semantic correctness is NOT automatable, and Rule C's pin is what makes a change to the table visible in review instead of quiet. Reworked the same day after review: the pin was a COUNT, which reports that something moved and not what, so a same-count glyph swap read as clean — it is now a pair list whose failure message shows `- embarazo:CalendarHeart / + embarazo:Baby` inside the fence file itself. The header's claim that both mechanical rules had each caught a real defect was also corrected: Rule A caught one, Rule B has caught nothing that shipped and is a forward guard. This fence is also the first that scans anything outside the root Next.js tree: the six web design fences (lint:icons, lint:buttons, lint:states, lint:empty-states, lint:screens, lint:copy-contract) all resolve their globs against app/ and components/ and none of them includes apps/mobile, so the Expo client had no design fence at all before this one.* |
| CANON-528 | The native amend form may offer a fact row only when the server's curated projection renders that (event type, field) pair VERBATIM and the value is free text; every formatted or date-pushed row is read-only on the phone. | `__tests__/mobile-amend-passthrough-fence.test.ts:1-40`<br>`apps/mobile/src/pets/event-detail-view-model.ts:210-275` | ENFORCED | `__tests__/mobile-amend-passthrough-fence.test.ts:245-360` | The fence parses lib/events/events.ts's eventPayloadDetails switch into (arm, label, field, transformed, dated) rows and apps/mobile/src/pets/event-detail-view-model.ts's PASS_THROUGH_FACTS into (type, field) pairs, then offends when any allowlisted pair is rendered through a transform or pushDate (:246), or names a type outside AMENDABLE_EVENT_TYPES where no form can reach it (:250). It is a SUBSET check on purpose — the phone being more conservative than the projection must not go red. Non-vacuity at :284-297 pins arms >= 8, rows >= 28, pass-through >= 20, transformed >= 5, dated >= 3, phone types >= 5 and pairs >= 13, all measured against the real corpus (10/34/24/6/4 and 6/16) on 2026-09-07. :300 re-asserts the three rows the defect was reported on; :315 offends on a key pushed twice when either push is formatted; :329 refuses an allowlist it cannot actually parse (zero-field entry, key-count mismatch, quoted-string-count mismatch); :347 proves it recognises a transform newly added to an allowlisted key. *Written 2026-09-07 as the containment for A2-alta-asentar-02: the amend form pre-filled the FORMATTED es-AR value and posted the edited text as the raw payload value, so a date correction wrote '15/03/2026' into next_due_at and 12/04/2026 was parsed US-style as 4 December — silently wrong on an APPEND-ONLY spine, where only another correction can sit on top and nothing marks the value as wrong. The rule is an ALLOWLIST so a new event type, a new fact key or a changed rendering lands OUTSIDE it and is refused by default. Review found the first draft's rule ('renders verbatim') necessary but not sufficient: to_country and origin_country are ISO-3166 codes rendered verbatim, so the phone drew a text box reading 'AR' that an owner would helpfully correct to 'Argentina' — which then fails the toCountry === 'AR' gate in refresh-pet-cache-after-amendment.ts and nulls pets.localityId. The rule is now verbatim AND free text. The proper fix (rawValue on EventFactV1, enum rows as a Choice, per-field schema validation) is server-first and not OTA-able over the installed build.* |
| CANON-529 | The native amend form may not post a null for a fact the spine declares non-nullable; the phone's NON_NULLABLE_FACTS list must agree with the real payload schemas in both directions. | `__tests__/mobile-amend-passthrough-fence.test.ts:467-524`<br>`apps/mobile/src/pets/event-detail-view-model.ts:355` | ENFORCED | `__tests__/mobile-amend-passthrough-fence.test.ts:492-523` | buildAmendChanges emits a change per edited box and used to emit value: null for an emptied one; nothing re-validates the target payload after an amendment, so clearing 'Vacuna' wrote null into a required field and the projection then dropped the row. NON_NULLABLE_FACTS names the spine-required keys and the change is dropped rather than posted, with clearedRequiredFact/amendRequiredFactMessage making the refusal visible. Because that list is a second cache of a server fact, the fence runs a REAL base payload through the REAL PayloadSchemas and flips one allowlisted field to null at a time (:492), judging both directions — a key the phone calls required that the spine accepts as null, and a key the phone omits that the spine refuses. :481 first proves every base payload it judges against is itself valid, and :489 floors BASE_PAYLOADS at 8, :523 floors probed fields at 13. *Written 2026-09-07 alongside CANON-528. The allowlist governs which ROWS may be edited and never which VALUES may be written, which is how the null case survived the first draft; this row closes the value half. The two-direction check is the point — a one-direction fence would let the phone quietly become more permissive than the spine as schemas change.* |
| CANON-530 | The native app may not fall silently behind the owner's web: every owner-guarded server action must reach a use-case the v1 surface also reaches, every receipt fact the web shows must be obtainable from a v1 read, the three kind vocabularies (contract, phone, router) must agree, and every deliberate divergence must be declared with a reason and what would close it. | `scripts/check-owner-surface-parity.ts:1`<br>`apps/mobile/src/pets/record-event-view-model.ts:143` | ENFORCED | `scripts/check-owner-surface-parity.ts:567-607`<br>`scripts/check-owner-surface-parity.ts:609-663`<br>`scripts/check-owner-surface-parity.ts:665-693`<br>`scripts/check-owner-surface-parity.ts:695-721`<br>`scripts/check-owner-surface-parity.ts:771`<br>`__tests__/check-owner-surface-parity.test.ts` | scripts/check-owner-surface-parity.ts derives both sides at run time and enumerates nothing: the owner guards come from the `require…PetAccess\|PetByToken\|TitularAccess` exports of lib/infra/pet-access.ts + lib/infra/pets.ts; the web set is every exported async function in a "use server" file under app/actions, app/(app)/mis-mascotas and src/modules whose body CALLS one of those guards; the app set is every src/modules/**/application/** use-case that a file under app/api/v1 imports and names, with relative (`./application/x`) and absolute (`@/src/modules/…`) specifiers resolved to one id. findDivergences (:609-663) fails an action whose called use-case no v1 route reaches (`write:`), an action with no use-case at all (`unjoined:`), and a result field the action reads that no v1 file reads and no packages/contract/src/api DTO declares (`read:`). vocabularyFailures (:665-693) parses `kind: z.literal` from record-event.ts, WRITABLE_KINDS (+RECORD_KINDS spread) from the phone's view-model and EVENT_TYPE_OF_KIND from writers.ts and fails any set difference, naming the kinds. declarationFailures (:695-721) fails an undeclared divergence, a declaration missing `reason` or `closes`, and a stale declaration. vacuityFailures (:567-607) fails when any scan is under its floor (guards 3, use-server files 40, owner actions 30, joined actions 15, v1 use-cases 20, kinds 12, DTO keys 200 — measured 4/82/44/34/95/18/359 on 2026-09-09) or a vocabulary source cannot be parsed. Exit 1 at :771. __tests__/check-owner-surface-parity.test.ts mutates the live inputs one thing at a time: dropping `bite` from the phone is named as `in contract only: bite`; renaming WRITABLE_KINDS fails the floor and reports no disagreement; a synthetic action with an unreached use-case, a synthetic receipt field, an emptied v1 set, an emptied guard set, a hollow declaration, a stale declaration and an undeclared finding are each named. package.json lint:owner-parity is in the verify chain and .github/workflows/ci.yml runs it in the `check` job (lint:ci-parity holds the pairing). *Written 2026-09-09, the day the app reached the eighteenth owner kind (post_adoption_checkin) and the PO asked for parity to be MEASURED before the next native build rather than believed from prose. The first run found 12 divergences, all declared in DECLARED_DIVERGENCES with reason + closure and reported to the PO: the mordedura case code no v1 read carries (the one gap the router's own header already named), the tattoo asiento the '18 of 18' never counted, species correction, the vaccine-reminder pair, the three rehome titular actions, the physical-tag interest toggle, the web-only 'primeros pasos' dismissal, and two that are parity in fact but not in the one-hop join (recordMoveAction reaches recordMovementWriter through recordJurisdictionMove; disclosureKeyRequiresTitular is a guard-chooser, not a door). Deliberately NOT decided here: the reverse direction (a v1 door the web lacks), the field-for-field shape of each kind's input (the contract's job), and whether a v1 route's guard is as wide as the web's (lint:authz). The join is one hop deep by design — a use-case reached through another use-case is declared, not inferred.* |
