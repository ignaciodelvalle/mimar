# Conventions canon — Documentation

> Snapshot: `d7dbf25f7` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-25
> Verified against code on 2026-09-25 by canon v4 + blind calibration · Status: reviewed
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

[← canon index](../conventions-canon.md) · scope `docs` · 12 rules (0 ENFORCED, 3 PARTIAL, 9 UNENFORCED).

| Id | Rule | Source | Status | Enforcer | Basis |
| --- | --- | --- | --- | --- | --- |
| CANON-483 | Append new locked-in design decisions worth preserving across sessions to AGENTS.md. | `AGENTS.md:1760` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-484 | Any change that adds a new event type or modifies the schema requires a spec in docs/superpowers/specs/ written and iterated before the code PR is opened, with the spec link in the PR description. | `CONTRIBUTING.md:181-185` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. *The event catalog itself is fenced (lint:events, __tests__/event-catalog-count.test.ts); what has no enforcer is the requirement that a spec exist and be linked from the PR.* |
| CANON-485 | The system's aggregate open data must be published separately at /transparencia under CC-BY 4.0, not as open code, since the source is published for inspection and audit only and stays proprietary. | `README.md:119-123` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-486 | Every prose statement of the subject-rights table counts must equal the value derived from the live `IN_EXPORT`/`IN_ERASE`/`KNOWN_GAP` lists, and AGENTS.md §7 is the only document allowed to state the live `KNOWN_GAP` count. | `__tests__/documented-subject-rights-counts.test.ts:1-33,128-246` | PARTIAL (subset) | `__tests__/documented-subject-rights-counts.test.ts:128-206` | __tests__/documented-subject-rights-counts.test.ts:212-244 sweeps for contradicting counts over PROSE_PATHS, which is a hand-listed TWO-document corpus (:55 — AGENTS.md and dim-interno:docs/agents/open-work.md); every other document stating the KNOWN_GAP count is outside the scan, and the header at :19-26 declares the sentence-anchor limit for the rest. *The regex at :230 also only catches a number within 60 chars of KNOWN_GAP followed by tablas/tables, so a count stated in other wording escapes even inside the two swept documents.* |
| CANON-487 | Every live, authoritative doc that states the event catalog's type count (AGENTS.md, CLAUDE.md, docs/event-design-checklist.md) must agree with `EVENT_TYPES.length`, and any `#event-catalog--N-types` anchor must match it too. | `__tests__/event-catalog-count.test.ts:1-19,42-82` | PARTIAL (subset) | `__tests__/event-catalog-count.test.ts:32-81` | __tests__/event-catalog-count.test.ts:36 COUNT_RE matches only 'Event catalog — N types' / 'Event catalog (N types)'. AGENTS.md:34 states a catalog count in a third phrasing — '(48 at last read)' — inside a doc the fence scans, and passes: live is 55 (AGENTS.md:83,680 and CLAUDE.md:70 say 55). The anchor loop :70-81 is complete (a repo-wide rg finds #event-catalog--N-types only in those two docs). *Changed from my draft ENFORCED after opening AGENTS.md:34, which the v3 basis cited and I had not read. The uncovered occurrence disclaims itself ('never trust a number written here'), so whether it is a live violation is arguable — but the fence recognises two phrasings and would miss an unhedged third the same way (the enumerate-the-forms shape).* |
| CANON-488 | `dim-interno:docs/archive/**` and anything superseded in `docs/superpowers/README.md` describe the past and must not be cited as the current state — AGENTS.md is the authoritative current spec. | `docs/README.md:7`<br>`dim-interno:docs/design/handoffs/README.md:40-44` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-489 | Living planning docs must stay out of dim-interno:docs/archive/, e.g. docs/superpowers/plans/2026-05-27-spec-later-tracker.md, which code TODO(spec-later) markers link to. | `docs/README.md:18` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-490 | Every number quoted in a handoff document must be recounted from the tree at the time of writing, not carried over from an earlier page. | `dim-interno:docs/agents/handoff-2026-08-30.md:1-19` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-491 | A landed work block on the open-work board must state what it decided AND what it did not solve, not just what shipped. | `dim-interno:docs/agents/open-work.md:157` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-493 | A row removed from the open-work board must take its narrowed remainder with it, or leave the remainder explicitly written — a row deleted without its remainder is lost work. | `dim-interno:docs/agents/open-work.md:158-161` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
| CANON-494 | Numbers must be derived or fenced rather than transcribed into prose; where a number must appear in prose, a test should recompute it from source and pin it — a paragraph that reports a census of matches within the same file it lives in cannot be kept true, and must argue by location instead of count. | `dim-interno:docs/agents/recommendations-2026-08-30.md:99-121`<br>`dim-interno:docs/agents/recommendations-2026-08-30.md:113-121` | PARTIAL (subset) | `__tests__/event-catalog-count.test.ts`<br>`__tests__/documented-subject-rights-counts.test.ts` | __tests__/event-catalog-count.test.ts:32 covers one number across three docs and __tests__/documented-subject-rights-counts.test.ts (:1-32, derived from IN_ERASE/IN_EXPORT/KNOWN_GAP) covers one topic's counts. Every other prose number is transcribed and unfenced — AGENTS.md:1531 '(46 at last read — package.json is the source of truth, this number is not)' is one such, live in the tree. *documented-subject-rights-counts.test.ts:20-32 states its own limit: it anchors on SENTENCES, so a claim moved into unknown wording is invisible. The rule's second clause — a same-file census must argue by location instead of count — has no enforcer at all.* |
| CANON-495 | A handoff document must be named `YYYY-MM-DD-<topic>-{proposal\|audit\|handoff}.md`. | `dim-interno:docs/design/handoffs/README.md:13` | UNENFORCED | — | No enforcer found: no scripts/check-*.ts, no vitest/jest fence, no biome override, no CI step and no DB constraint has this rule's subject as its predicate. It is a working norm applied by hand. |
