# Superpowers — Specs & Plans

> Snapshot: `c10f4ff03` (`main`) · Facts: `docs/architecture/facts.json` generated 2026-09-02
> Verified against code on 2026-09-02 by writer D (sonnet subagent) · Status: draft
> Numbers in this file are `<!-- fact:key -->` markers checked by `__tests__/architecture-facts.test.ts`.

Index of design docs (`specs/`) and implementation plans (`plans/`) for MiMAR features.

- **`specs/`** — design docs. The *what* and *why* of each feature. Reviewed and locked before implementation.
- **`plans/`** — implementation plans for Claude Code. The *how*. Self-contained, file-level detail, executable.

A typical feature lifecycle: brainstorm in chat → spec in `specs/` → review/iterate → plan in `plans/` → Claude Code executes → mark done.

## ADRs (architecture decision records)

Standing decisions that outlive any one spec/plan pair — `docs/adr/`.

| ADR | What it locks in |
|---|---|
| [`2026-07-18-native-readiness.md`](../adr/2026-07-18-native-readiness.md) | The API-exposability standing rule: every use-case that graduates into `src/modules/<domain>/application/` stays plain-data-in/plain-data-out and framework-free, so a future `/api/v1` for native (iOS/Android) clients never has to rewrite a strangler extraction that already shipped. No native code lands from this ADR — it's a contract for extractions happening anyway. |

---

## What to attack next

> **▶ ORQUESTADOR ACTIVO (2026-06-24) para CC:** `dim-interno:docs/superpowers-private/plans/archive/2026-06-24-CONSOLIDATED-pending-backlog.md` **Parte D** = la cola de PRs lista para ejecutar (design system F1–F4 + fixes A2/A3/A4/A7/A9 + higiene de docs Parte B). Todas las decisiones de producto/seguridad están cerradas ahí. El detalle del design system vive en [`plans/2026-06-24-operator-design-system-unification.md`](./plans/2026-06-24-operator-design-system-unification.md). Empezar por la Parte D.

Recommended order based on dependency chains and leverage. Each item is a Claude Code session that produces 1+ PRs.

Auditado 2026-05-19 contra `develop`: varios items que estaban acá ya están en código y se movieron a la sección "Implementado" abajo.

| Priority | Feature | Plan file | Why now |
|----------|---------|-----------|---------|
| 🟡 P1 | **UX audit remediation — Fases 2-3** (Fase 0 + Fase 1 ✅ hechas) | `plans/2026-06-20-ux-audit-remediation.md` | De la auditoría de UX 2026-06-20 (estática + en vivo). **Fase 0 ✅ COMPLETA** (5 crashes cerrados): 0.1 credencial `lost` (verificado en vivo), 0.2 portal de org (schema-drift, resuelto por migración 0102-0106), 0.3 `/cuenta` (era **escala** —owner ~2025 mascotas, agregación sin bound—, #680), 0.4 404 branded (#678), 0.5 login org-aware (#679). **Fase 1 ✅ COMPLETA** (design system universalizado): 1.1 omnibox en org + descope de mascotas en operador (#682), 1.2 breadcrumbs gob/admin derivados de ruta (#683), 1.3 adopciones+casos como `CaseQueue` con bulk/SLA (#684/#685), 1.4 retiro de KPI gemelo (`MetricCard`) + label "Mi actividad" (#687), 1.5 field kit con aria auto + forms migrados (#686). **Fase 2 ✅** (2.1+2.2 touch targets 44px + estado no-solo-color #689 · 2.3 data-viz #691 · 2.4 a11y estructural #690) **y Fase 3 ✅** (3.1 dead-ends #692 · 3.2 denuncia #693 · 3.3 stubs/claim a11y #694 · 3.4 localización es-AR #695 · 3.5 owner polish #696 · 3.6 org/admin polish — este PR). **UX audit remediation COMPLETO** → sigue `plans/2026-06-21-design-system-hardening.md`. Fuentes: `docs/ux-usability-audit.md` + `docs/ux-usability-audit-live.md`. Endurecimiento de regresión: `plans/2026-06-21-design-system-hardening.md` (corre después). |
| ~~⭐~~ | ~~**Metrics & operator-IA handoff** (Items 0–4) — projection primitives + nav regrouping + disease/disposal/enforcement métricas~~ | `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-metrics-ia-handoff-design.md` (umbrella) + 6 item specs | ✅ **Items 0–4 implementados** (PRs #626–#637): Item 0 (`lib/metrics/` + k-anon), Item 1 (nav regrouping), Item 2 (`/gob/mortalidad`), Item 3 (surveillance hardening), Item 4 (compliance enforcement). Item 5 (owner nudges `/inicio`) se implementó (#638) y se **removió** el 2026-07-21 por código muerto (`AGENTS.md:1158-1160`). Ver filas individuales en la tabla de specs abajo. |
| ~~⭐~~ | ~~**Centro de Situación Nacional** (Panorama) — consola geoespacial por capas (admin/govt)~~ | `specs/2026-06-21-national-situational-console-design.md` + `plans/2026-06-21-national-situational-console.md` + `plans/2026-06-21-panorama-demo-dataset.md` + `specs/2026-06-23-panorama-v2-design.md` | ✅ **Implementado** (#705 consola live, #698 seed, #706 D1 trends, #708 rediseño v2). **Paquete de 3 docs** (1 spec + 2 planes) + redesign v2. La **cara integradora** de las proyecciones metrics-IA: consola geoespacial por capas (`/admin/panorama` + `/gob/panorama`) live como primer ítem del riel en ambos portales. Seed `seed:panorama` sintético-pero-realista (Censo 2022 + anclas metro, ~46k mascotas). Rediseño v2: agregación determinística por unidad, modelo de capas 1-base+≤1-señal, presets por pregunta, detalle on-demand, coropleta divergente anclada a meta. Nota: `/gob/analytics` está VIVO como página propia en el nav; la absorción vía redirect 308 es una decisión de producto **pendiente que no se ejecutó**. |
| ~~1~~ | ~~**Sistema de casos (expedientes)**~~ | `plans/archive/2026-05-19-cases-system.md` | ✅ **Implementado** (multi-PR; plan archivado, marcado COMPLETO 2026-06-04). Auditoría 2026-06-19: las 7 fases A-G están en código — schema (mig `0033_cases.sql`), lib (`src/modules/cases/` + `lib/case-*`), 4 crons en `vercel.json`, los 5 workflows con `openCase`/`closeCase`, rutas `/casos/` en los 4 scopes, RLS `can_read_case` (mig `0094_cases_rls.sql`). Los **12 case_kinds** activos (los 5 que estaban deferred ya activados). El índice estaba stale. |
| ~~2~~ | ~~Welfare moderation admin UI~~ | — | ✅ **Cerrado**: audit 2026-05-19 confirma que `/admin/moderacion` ya está montado (list + detail + `ModerationActions` con ambas server actions). Nav link en `app/admin/layout.tsx`. El README listaba el path equivocado (`/admin/maltrato/moderacion`). |
| 3 | **Bulk revoke UI** (follow-up Fase 13) | (mismo plan, Fase 13) | Server action `bulkRevokeAction` ya implementado y testeado. Falta la UI para las 4 queues de revoke (`/admin/usuarios`, `/gob/usuarios`, `/admin/organizaciones`, `/gob/organizaciones`) — necesita attachment uploader bulk. ~½ día. |
| ~~4~~ | ~~Validación canonical en `events.ts`~~ | — | ✅ **Cerrado**: audit 2026-05-19 confirma que `events.ts` no acepta province/locality de `formData` (todo viene de `pet.jurisdictionProvince`/`Locality` ya canónicos desde el registro). Las 5 actions con input de usuario (`pets.ts`, `upgrade.ts`, `service-offerings.ts`, `welfare.ts`, `intake.ts`) canonicalizan; `admin-institutional.ts` también vía `resolveCanonicalJurisdiction`. Sin gap real. |
| 5 | **Physical tag** (`/t/[serial]`) | (plan a escribir post-OK del spec v1.0) | Chapa identificadora física que cuelga del collar. Spec con preguntas abiertas en §15 (material, fabricante, auto-revoke on death). ~3 días cuando se decidan los §15. |
| ~~6~~ | ~~**CABA barrios — execution**~~ | `plans/2026-05-19-caba-barrios-import-execution.md` | ✅ **Implementado** (commit 225aadd6): 48 barrios CABA importados, `source='caba_open_data'`, mig 0028. |
| ~~7~~ | ~~**Fix: Vet portal routing default**~~ | `plans/archive/2026-05-19-fix-vet-portal-routing.md` | ⚪ **Superseded** — `/pro` eliminado (PR #84); routing ahora vía `resolveVetLanding`. Plan movido a `plans/archive/`. |
| ~~8~~ | ~~**Fix: 404 de service dog**~~ | `plans/2026-05-19-fix-service-dog-404.md` | ✅ **Implementado** (commit 51779f2a). |
| ~~9~~ | ~~**Pet profile v2 + Achievements**~~ | — | ✅ **Implementado**: `AchievementsSection.tsx`, `PpPCard.tsx`, `ServiceDogCredentialCard.tsx` cableados en `mis-mascotas/[publicToken]/page.tsx`. |
| ~~10~~ | ~~**Cross-org transfer UX**~~ | — | ✅ **Implementado**: `app/org/[orgToken]/transferencias/` full UI + cron `expire-cross-org-transfers`. |
| ~~11~~ | ~~**ENO direct vet report + owner alerts**~~ | — | ✅ **Implementado** (commit de70afeb): `lib/disease-legal-anchors.ts`, `lib/disease-public-alert-catalog.ts`, `lib/owner-disease-alerts.ts` (ruta obsoleta 2026-09 — hoy `lib/reference/disease-legal-anchors.ts`, `lib/reference/disease-public-alert-catalog.ts`, `lib/infra/owner-disease-alerts.ts`). |
| ~~12~~ | ~~**Org abuse investigation**~~ | — | ✅ **Implementado** (commit a4d03240): `reporter_organization_id` mig 0035, `createOrgWelfareReportAction` severity critical, OA9. |
| ~~13~~ | ~~**Decomiso → refugio chain**~~ | — | ✅ **Implementado**: `app/gob/decomisos/nuevo/` + `DecomisoForm` + `executeDecomisoAction` + cron + receiver handshake. |
| ~~14~~ | ~~**Pregnancy tracking**~~ | — | ✅ **Implementado**: `app/.../eventos/nuevo/embarazo/`, `app/actions/pregnancy.ts`, `pets.pregnancy_status`. |
| 15 | **Performed_by autocomplete** — vets/clinics linkeados + texto libre (UI pendiente) | (plan a escribir post-OK del spec) | Schema dual: FK opcional + texto snapshot. 6 events afectados. Combobox `PerformedByCombobox` reusable. Display libreta distingue linked vs free-text. NO backfill retroactivo. Independiente del sistema de casos. **Parcial — UI sin implementar.** ~5-6 días. |
| ~~16~~ | ~~**Govt business rules POC**~~ | — | ✅ **Implementado**: `govt_business_rules` mig 0037, `lib/business-rules-resolver.ts` (ruta obsoleta 2026-09, hoy `lib/infra/business-rules-resolver.ts`), `/admin/jurisdicciones/*/reglas/` + `/gob/reglas/`. |
| 17 | **Bite from unowned animal** (víctima reporta mordedura sin pet registrada) | (plan a escribir post-OK del spec) | Extiende bite-rabies-observation con subject polimórfico. Tabla `temporary_pet_descriptions` + reconciliation post-signup por DNI matching + reasignación manual por govt. Cumplimiento legal (Decreto 4669, Ord. 41.831) sobre animales no-registrados. Solo víctimas autenticadas en v1. ~8 días cuando se priorice. **Sin implementar.** |
| 3 | **Bulk revoke UI** (follow-up Fase 13) | (mismo plan, Fase 13) | Server action `bulkRevokeAction` ya implementado y testeado. Falta la UI para las 4 queues de revoke. **Sin implementar.** ~½ día. |
| 18 | **Adoption listing v1.4** — consent + historial del postulante | (plan a escribir post-OK del spec) | §12.5: consent obligatorio + vista "Historial del postulante en MiMAR" scope-bound durante review + RLS extensions. Depende del sistema de casos para los `cases.adoption_application_id` chained policies. **Parcial — applicant-history view + RLS sin implementar.** |
| 19 | **Physical credential hub** (Fase A) | — | En progreso — PR #672 (Fase A). |

---

## Admin fresh-sweep fixes (extiende Fase 3.6 de la remediación)

> [`plans/2026-06-22-admin-fresh-sweep-fixes.md`](./plans/2026-06-22-admin-fresh-sweep-fixes.md) — barrido en vivo de **todas** las pantallas admin post-Fases 0-2 (verifica lo arreglado + deja 7 hallazgos A1-A7). **Entregados + verificados (los tres 🟡):** A1 `not-found.tsx` branded por route-group (admin/gob/(app), extiende 0.4); A2 fix del `RuleImpactBanner` que no calculaba (binding `= ANY(${array})` sin tipo → throw; ahora `inArray`, con test de integración); A3 acentos es-AR residuales (sistema, gob/organizaciones, servicios, govts/new, auditoría, rule form). **🟢 polish — cerrados** (commit `59f3b932` "fresh-sweep A4-A7"): A4 crumb labels localizados, A5 action-code crudo en auditoría (mismo `auditActionLabel` que historial), A6 required markers en forms de creación, A7 separar cuentas de sistema.

## Admin executive-review remediation (auditoría ejecutiva 2026-06-22)

> [`plans/2026-06-22-admin-executive-review-fixes.md`](./plans/2026-06-22-admin-executive-review-fixes.md) — remediación de la auditoría ejecutiva del perfil **admin** (`dim-interno:docs/archive/admin-design-critique-2026-06-22.md`, 40 hallazgos C1-C40). **✅ Completo** — 10 PRs sobre `review/all-session-prs` (mergear en orden PR-1 → PR-10; PR-7 supersede el stopgap de C22 de PR-1):
>
> | PR | Rama | Hallazgos | PR |
> |----|------|-----------|----|
> | 1 | `fix/sec-admin-queue-counts` | C1, C2, C3, C22 — keyset cola, banner SLA global, await PII audit, guard humano | PR #714 (repositorio anterior, privado) |
> | 2 | `fix/admin-destructive-confirmations` | C4, C5, C6, C7, C8, C10 — confirmación + motivo proporcional | PR #715 (repositorio anterior, privado) |
> | 3 | `fix/admin-audit-trail-body` | C11, C12 — actor/motivo/evidencia/target en el audit trail | PR #716 (repositorio anterior, privado) |
> | 4 | `chore/admin-operator-data-cards` | C13, C14, C15, C16 — tarjetas, links de entidad, codes traducidos | PR #717 (repositorio anterior, privado) |
> | 5 | `fix/admin-surface-boundaries` | C17, C18, C19 — link operador, copy "por nombre", eyebrow por rol | PR #718 (repositorio anterior, privado) |
> | 6 | `fix/sec-magic-link-handling` | C20 — enmascarar, TTL real, feedback de copia, título contextual | PR #719 (repositorio anterior, privado) |
> | 7 | `fix/admin-rosters-evidence-scale` | C21, C23, C24, C9 — `profiles.is_system` (mig 0109), evidencia en submit, badge 0-loc, gate PPP | PR #720 (repositorio anterior, privado) |
> | 8 | `fix/admin-ia-landing-nav` | C25, C26, C27, C28 — copy landing, KPI strip compartido, regroup nav, helper delta | PR #721 (repositorio anterior, privado) |
> | 9 | `fix/admin-filters-tables` | C29, C30, C31, C32 — filtros casos/moderación, filtros auditoría, provincias drilleables, período | PR #722 (repositorio anterior, privado) |
> | 10 | `chore/admin-consistency-polish` | C33-C40 — constante SLA, `router.refresh`, color severidad, footnote k-anon, KPI tasa retorno, registries JSON, jurisdicciones localidad-aware, dead-end physical-credential | PR #723 (repositorio anterior, privado) |
>
> Cada PR: SDD test-first, `pnpm verify` (tsc + Biome + lint:tokens + lint:ui + next build) + `pnpm test` verdes, cero regresiones sobre el baseline pre-existente. No duplica el fresh-sweep (A2/A3/A5/A7 ya cerrados, cross-referenciados).

### Performance fixes — WS-PERF (P1 + P2) ✅

> Branch `perf/admin-audit-casos`. P1: `audit_log_performed_at_idx` (migration 0110) — leading `(performed_at DESC, id DESC)` index eliminates the full sort on every unfiltered `/admin/auditoria` load. P2: `/admin/casos` now defaults to `status=open` (the triager's actionable view); explicit `status=all` or `status=closed` still shows closed cases; the UI status select reflects "Abiertos" as the default. Tests: `__tests__/perf-indexes-fitness.test.ts` (index presence + validity) + `__tests__/ws-perf.test.ts` (index + filter unit + integration).

### Access-control hardening — WS-AC (AC1-AC4) ✅

> [`plans/2026-06-22-access-control-hardening.md`](./plans/2026-06-22-access-control-hardening.md) — extiende el barrido admin 2026-06-22 desde el *critique del modelo de acceso* (control · acceso a la información · seguridad) sobre `/gob` y `/admin`. **✅ Completo** (rama `fix/ac-access-control-hardening`, SDD test-first):
>
> - **AC1 🔴 — guard compartido de `/gob` no chequeaba `deactivatedAt`.** `requireAdminOrGovtOrRedirect` (`lib/infra/auth-guards.ts`) ahora rechaza cuentas desactivadas (redirect a `/`), igual que `requireAdminOrRedirect`; `requireDecomisoPrincipal` queda cubierto al reusar el guard. Defense-in-depth: `loadActorAuthority` (`app/actions/admin-proposals.ts`) también rechaza al actor desactivado. Comentario del layout (`app/gob/layout.tsx`) corregido para describir la realidad. Tests: `__tests__/auth-guards.test.ts` (unit, govt+admin desactivados + control activo + decomiso) y `__tests__/access-control-deactivated-proposals.test.ts` (integración sobre el writer de propuestas).
> - **AC2 🟡 — auditoría de PII incompleta en las listas.** `app/gob/usuarios/page.tsx` y `app/gob/organizaciones/page.tsx` ahora loguean **también el landing sin query** (`query=""`) vía `logPiiReadSafely` (`await`ed, con `try/catch` → `console.error` que no rompe el render). Tests: `__tests__/admin-pii-audit-log.test.ts` (landing + result_count) y `__tests__/pii-read-safely-unit.test.ts` (el fallo del insert no tira el render).
> - **AC3 🟡 — páginas admin muertas + nav cruzaba de portal vía 308.** Borradas las carpetas `app/admin/{cola,usuarios,organizaciones}/`; `ADMIN_NAV_SECTIONS` (`components/layout/nav-presets.ts`) y los KPI links (`AdminKpiStrip`, `/admin/programa`, `/admin/historial`) repuntados a `/gob/*`. Los 308 del middleware se mantienen para bookmarks externos. Invariante de nav extendido: ningún href de `ADMIN_NAV_SECTIONS` matchea una regla 308 (`components/layout/nav-presets.test.ts`); snapshots actualizados en `nav-presets.test.ts` + `lib/shell-nav-phase-b.test.ts`.
> - **AC4 🟡 — reglas a nivel localidad inalcanzables por UI.** `app/admin/jurisdicciones/page.tsx` gana un drill-down por provincia (`LocalityRuleDrilldown`, typeahead sobre el catálogo INDEC `ar_localities`) que navega con la localidad **real** en el segmento (no `"_"`), permitiendo crear un override de localidad nuevo. Resolver puro `lib/jurisdiction-rules-href.ts` (ruta obsoleta 2026-09, hoy `lib/domain/jurisdiction-rules-href.ts`). Test: `__tests__/jurisdiction-rules-href.test.ts` (segmento de localidad ≠ `"_"`).

### Libro de eventos — WS-L ✅

> [`plans/2026-06-22-event-ledger-libro.md`](./plans/2026-06-22-event-ledger-libro.md) — vista admin **read-only** que hace tangible el event-sourcing en 3 beats: (1) **stream** append-only de `pet_events`, (2) **enmienda, no edición** (las correcciones son eventos nuevos `event_amended` que referencian al original; el original se conserva) y (3) **reproducción temporal** (deep-link `/admin/panorama?asOf=<occurredAt>`). **Sin schema · sin nuevos event types · sin migraciones** — pura proyección (`lib/metrics`) + UI read-only. Branch `feat/ws-l-libro`.
>
> - **Portal surface**: `/admin/libro` (**Admin — Live**), sección "Gobernanza" del nav admin.
> - **Proyección**: `lib/metrics/event-ledger.ts` — `fetchEventLedger(ctx, filters, cursor?, limit?)` scopeado vía `petEventsScopeClause` (admin universal · govt intersect-jurisdicción), **paginación keyset** sobre `(occurred_at DESC, id DESC)` (sin OFFSET), `hasAmendment` resuelto en batch (sin N+1), **PII gating** (solo token público + rol/org del actor, nunca datos del dueño). Audit obligatorio `logEventLedgerView` → `pii_queried` (`surface='event_ledger'`), modelado sobre `logOutreachPiiQuery`.
> - **UI**: `app/admin/libro/{page,loading,EventLedgerRow,view,actions}.tsx` + `components/admin/EventLedgerTable.tsx`. Labels es-AR reusando `eventTypeLabel` (`lib/format`), `<AmendedBadge>`, `DashboardFreshnessFooter`. A11y: tabla `scope="col"` + `<caption>`, toggle `aria-expanded`, foco al expandir, icono+texto (no color solo).
> - **Tests**: `lib/metrics/event-ledger.test.ts` (scope · keyset · filtros · hasAmendment · PII · audit, contra Postgres local) + `__tests__/event-ledger-ui.test.tsx` (a11y · stream · enmienda · filtro tipo · deep-link round-trip `parseAsOf`). Snapshots de nav actualizados: `nav-presets.test.ts` + `lib/shell-nav-phase-b.test.ts`.

### Forecast / proyección de tendencia — WS-J ✅ (J3 cobertura-% diferido)

> [`plans/2026-06-22-forecast-proyeccion.md`](./plans/2026-06-22-forecast-proyeccion.md) — banda de **proyección a futuro** sobre las series de **flujo** que ya graficamos ("al ritmo actual, ¿cruzamos la meta y cuándo?"). **No es ML** — proyección de tendencia simple (OLS, default `linear`, horizonte 3). **Sin schema · sin nuevos event types · sin migraciones** — pura proyección (`lib/metrics`) + chart aditivo. Branch `feat/ws-j-forecast`. Fases **J0 → J1 → J2** entregadas; **J3 (forecast de cobertura-% vs meta legal) explícitamente diferido** — requiere una serie de tasa-en-el-tiempo que hoy no existe (la cobertura se computa current-state), a coordinar con la Fase 0 incremental del vNext.
>
> - **J0 — lib pura** (`lib/metrics/forecast.ts` + `.test.ts`, sin Postgres): `projectSeries(points, {horizon=3, method="linear"|"holt"}) → ForecastResult{points, method, slopePerBucket, insufficient}`. Banda de confianza desde el error estándar de los residuales, **se ensancha con el horizonte** (`sqrt(1 + 1/n + (x0-meanX)²/ssx)·1.96`), clampeada `≥ 0`. `targetCrossing(result, target, "above"|"below")` → buckets hasta cruzar la meta, o `null`. `< MIN_POINTS` (4) → `insufficient:true`, solo actuals (nunca una recta inventada). Gaps por k-anon se tratan como faltantes, no como 0 (documentado en el header).
> - **J1 — chart aditivo** (`components/charts/ForecastChart.tsx` + `Dynamic`, **no toca `TimeSeriesChart`**): recharts `ComposedChart` con tramo **actual sólido** + tramo **forecast punteado** (distinto por estilo, no solo color) + **banda** `Area` lo/hi + `ReferenceLine` de meta opcional. Honestidad obligatoria: footnote "Proyección de tendencia — no es una garantía. n=, método=". A11y: `figure[role="img"]` + `aria-label`, leyenda "proyección", tabla accesible. Estado `insufficient` → mensaje "Datos insuficientes para proyectar", sin banda.
> - **J2 — wiring aditivo** (únicas ediciones a páginas existentes, card nueva por página): `/admin/poblacion` card "Proyección de esterilizaciones" (reusa el trend ya fetcheado) · `/admin/programa` card "Proyección de vacunación antirrábica" (`fetchRabiesVaccinationTrend`). **§J-D3 respetado**: ambas series son **conteos de eventos** (flujo) y la meta legal es **cobertura %** (stock, otra unidad) → **no se pinta línea de meta-% sobre el eje de conteos**; la banda de volumen va sola. El forecast de cobertura-% es J3.
> - **Tests**: `lib/metrics/forecast.test.ts` (18) + `__tests__/forecast-chart.test.tsx` (11) + `__tests__/forecast-pages-smoke.test.tsx` (4). renderToStaticMarkup no renderiza los hijos del `ResponsiveContainer` de recharts en SSR → los asserts del chart usan marcadores `data-forecast-*` en el `<figure>` + la tabla accesible.

### Ciclo de población/custodia — nav *deferred* (admin) 🟡 listo para CC

> `dim-interno:docs/superpowers-private/plans/archive/2026-06-23-population-cycle-deferred-nav-handoff.md` — del critique del perfil admin (2026-06-23): el **ciclo de población/custodia** (la North Star) está casi sin proyectar (ver `specs/2026-06-23-dashboards-vnext-roadmap.md` §1). Decisión de producto: **no construir las consolas todavía**, pero dejar el hueco **legible** en la IA. Agrega un estado `deferred` al modelo de nav + dos botones greyed/"Próximamente" no-clickeables en la sección Analítica del riel admin — **Control poblacional** (⭐ Paquete G) y **Custodia & tránsito** (Paquete F). **Sin schema · sin rutas reales · sin migraciones** — puro chrome de nav. Branch `feat/nav-deferred-population-cycle`.
>
> - **D1 — modelo**: `NavItem.deferred?: boolean` aditivo (`components/layout/HeaderNav.tsx`), back-compat total. `href` sentinel `#defer-…` → nunca es ruta ni matchea las reglas 308.
> - **D2/D3 — render**: riel (`OpRailNav`) + drawer (`OpMobileDrawer`) pintan deferred como `<span>` (no `<Link>`), `aria-disabled="true"`, fuera del tab order, token `text-ln-op-rail-mute`, pill textual "Próximamente" (no solo color, Ley 26.653). **D4**: `isActive` excluye deferred en ambos renderers.
> - **D5 — entradas**: 2 items al final de la sección "Analítica" de `ADMIN_NAV_SECTIONS`. **D6**: invariante 308 filtra deferred + snapshots (`nav-presets.test.ts`, `shell-nav-phase-b.test.ts`, `shell-nav.test.ts`). **D7**: test de render deferred (`OpRailNav.test.tsx`).
> - **Diferido**: paridad GOB + construcción de las consolas reales (Paquetes F/G, cross-ref `specs/2026-06-23-dashboards-vnext-roadmap.md`) + consolidación analítica (`2026-06-22-gob-analytics-retirement.md`). Convertir un botón en real luego = cambio aditivo de una línea (`deferred: true` → `href`/`matchPrefix`).

### Design system operador — unificación estado/badges/botones 🔴🟡 listo para CC

> [`plans/2026-06-24-operator-design-system-unification.md`](./plans/2026-06-24-operator-design-system-unification.md) — del critique de frontend del tier **operador** (`/admin` + acceso a `/gob`, 2026-06-24: `dim-interno:docs/design/critique-2026-06-24-frontend.md`), integrando los 3 hallazgos de Design (`dim-interno:docs/archive/Hallazgos Design System.md`). El tier operador tiene su kit `Op*` sobre `ln-op-*` pero **dos sistemas (ciudadano `Ln*` + operador `Op*`) crecieron sin capa semántica común**. **PR-1** `fix/operator-status-token-layer` 🔴 (F1): capa `--st-*` como indirección por piel + auditoría de contraste `ln-op-warn/danger` + colapsar violetas. **PR-2** `fix/operator-status-badge-grammar` 🟡 (F2): primero el **bug** de tono contradictorio (`open`/abierto = verde en `CaseStatusBadge` vs ámbar en `OpPill`), luego primitivo `OpStatusPill` único. **PR-3** `chore/operator-button-primitive` 🟡 (F3): `OpButton` (hoy 1 import de `LnButton` vs **133 `<button>` crudos**; primario verde/azul inconsistente). **PR-4** 🟢 polish densidad de Casos. Requisito duro: **cero diff visual en superficies ciudadanas**. SDD test-first; `pnpm verify` + `pnpm test` por PR.

## All specs & plans

> Reconciliado 2026-06-19 contra `develop`: la mayoría de los items 🟢 ya estaban en código (ver `git log`).
>
> **▶ Archivado 2026-06-24 (auditoría docs↔código).** Specs de features ya vivas (37) → [`specs/archive/`](./specs/archive/); planes ejecutados (24) → [`plans/archive/`](./plans/archive/). Las filas de abajo quedan como registro histórico (el `code-span` ya no es link; el archivo vive bajo `*/archive/`). **En `specs/` hoy (12):** roadmaps (dashboards-vnext-roadmap, panorama-north-star, gov-portal-admin-reuse) · no construidos o parciales (physical-tag, bite-from-unowned, performed-by-autocomplete, pet-spaces-catalog, event-trust-tiers-2-4, physical-credential-hub) · panorama (national-situational-console, panorama-v2, panorama-seed-full-history). **En `plans/` hoy (4):** spec-later-tracker · bite-from-unowned · physical-credential-hub · strangler-finish-plan. Los handoffs, backlogs y planes de demo que figuraban acá salieron al repo privado el 2026-09-18 y se citan como `dim-interno:docs/superpowers-private/specs/…` (wave5-launch-hardening, dashboards-sell-completeness, owner-compliance-first-slice, reviews-hardening) y `dim-interno:docs/superpowers-private/plans/…` (executive-e2e-readiness y los archivados). Backlog de pendientes: `dim-interno:docs/superpowers-private/plans/archive/2026-06-24-CONSOLIDATED-pending-backlog.md`.

### Specs (design docs)

| Spec | Status | Plan | Notas |
|------|--------|------|-------|
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-metrics-ia-handoff-design.md` (umbrella) | 🟢 Ready for CC | (per-item, abajo) | Master del paquete metrics-IA: secuencia los 5 items, define el workflow SDD test-first, reglas globales de test/docs/privacy (k-anon, sin schema, sin event types). Empezar acá. Referencia cruzada: `reference-intl-pet-systems-and-metrics.md`. |
| `2026-06-18-projection-primitives-design.md` (Item 0) | ✅ Implementado (#626) | `plans/2026-06-18-projection-primitives.md` | **Foundation — hacer primero.** `lib/metrics/`: `ProjectionContext`, scope/denominador único (hoy `petsScopeClause` está duplicado en 2 archivos + re-derivado inline ~14×), **`suppressSmallCells` k=5 (la política k-anon de AGENTS.md NO está implementada hoy)**, period unificado, `React.cache` dedup. Migra fetchers existentes con value-pinning tests. Documenta el Pattern B en `hexagonal-lite.md`. Bloquea Items 2-4. |
| `2026-06-18-operator-nav-regrouping-design.md` (Item 1) | ✅ Implementado (#628, #629) | `plans/2026-06-18-operator-nav-regrouping.md` | Refactor puro: `GOB_NAV`/`ADMIN_NAV`/`buildOrgNav` → `NavSection[]` vía `OpRailNav sections=`. Sin rutas nuevas. Tests de invariante (ningún href perdido) + capability filtering org. |
| `2026-06-18-mortality-disposal-dashboard-design.md` (Item 2) | ✅ Implementado (#PR) | (plan post-OK) | **Mayor leverage.** Nuevo `/gob/mortalidad`: disposition mix, traceable-disposal rate, unknown-disposition %, reportable-death share (B1-B9). Ley CABA 5470. `disposition_method` ya se captura — pura proyección. |
| `2026-06-18-surveillance-metrics-hardening-design.md` (Item 3) | ✅ (#636) | `plans/2026-06-18-surveillance-metrics-hardening.md` | Extiende `/gob/vigilancia`: ENO-notification SLA (A7), compliance observación 10d (A8/A9), AMR density (A12), reportable incidence + lab-confirmation (A6/A10). Reusa `lib/metrics/anonymity.ts` (Item 0), no crea `lib/anonymity.ts`. |
| `2026-06-18-compliance-enforcement-metrics-design.md` (Item 4) | ✅ Implementado (#637) | `plans/2026-06-18-compliance-enforcement-metrics.md` | Microchip penetration (C1), ISO-validity (C2), chip-fraud (C5), dangerous-breed registry compliance (C7 graceful-0%), reunification rate (D4), decomisos (D5). Leyes 14.107/4078/14.346. C3 deferred (sin denominador externo). |
| `2026-06-21-national-situational-console-design.md` (v1.0) | ✅ Implementado (#705) | `plans/2026-06-21-national-situational-console.md` + `plans/2026-06-21-panorama-demo-dataset.md` | **Centro de Situación Nacional (Panorama) — la cara integradora de las proyecciones metrics-IA.** Spec del paquete de 3 docs (1 spec + 2 planes). Consola geoespacial por capas para admin/govt (`/admin/panorama` + `/gob/panorama`, primer ítem del riel en ambos). Un mapa con capas toggleables sobre el event log; KPIs viewport-aware; drill a expediente; reproducción temporal (event-sourced). Subsume los mapas en silos como capas. Nota: la absorción de `/gob/analytics` vía redirect 308 es una decisión de producto **pendiente que no se ejecutó** — `/gob/analytics` está VIVO como página propia en el nav. |
| `2026-06-23-panorama-v2-design.md` | ✅ Implementado (#708) | — | **Panorama v2 — rediseño de la consola**: agregación determinística por unidad (mata el re-cluster), modelo de compatibilidad de capas (1 base + ≤1 señal + referencia), presets por pregunta, detalle on-demand por unidad (historial + sparkline), coropleta divergente anclada a meta + leyendas fijas, y escape de free-text en popups (XSS hardening). 5 fases F1–F5. Reusa basemap + `lib/metrics`. Sin schema. |
| `2026-06-23-dashboards-vnext-roadmap.md` | 🟡 Roadmap | — | **Dashboards vNext — completitud de proyecciones (foco admin).** Diagnostica que falta el ciclo de población/custodia (la North Star). Propone paquetes E (censo) · F (custodia/adopción) · G⭐ (control poblacional) · H (salud operativa) + Fase 0 incremental + QOL. Visión del plan ejecutable homónimo. |
| `2026-06-18-pet-profile-v21-reorder-and-action-consolidation-design.md` (Item 6) | ✅ (#639) | `plans/2026-06-18-pet-profile-v21-reorder.md` | De la crítica de diseño 2026-06-18. Reordena el perfil de mascota (hero primero, `PetAlertStrip` priorizado, credenciales/logros dentro de Resumen) y colapsa los 3 hubs de acción: **`/anotar` canónico, `/eventos/nuevo` redirige a él**, una sola forma de anotar en el perfil. Consolida `PetReminders`. Tabs = modelo final del timeline. Lost cockpit conserva acceso al perfil. Sin cambios de datos. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 12**) | ✅ **Implementado** (`feat/wave2-item12-case-ux`) | — | Case (expediente) UX coherence. `CaseStatusBadge` (shared tones: open/escalated/closed/merged, cross-kind), `CaseDetailShell` (header: `OpCodeBadge` + `CaseStatusBadge` + parties PII-gated + normativa + jurisdicción + subject descriptor), `CaseQueue` (unified queue table: filter chips + shared status/kind/sort + optional `OpBulkBar`). Exported from `components/ui/dashboard`. `/casos/[publicCode]` migrado a `CaseDetailShell`. `/gob/disputas` migrado a `CaseQueue`. 30 unit tests (`__tests__/case-ux-components.test.tsx`). Edge cases: `unowned_animal` (graceful descriptor), multi-party dispute (lista mobile-safe), `isPublic=true` (PII gating). Deferred: `/gob/maltrato`, `/gob/decomisos`, `/admin/observaciones` — datos no son `CaseListItem`-shaped; follow-up cuando tengan `publicCode` linking. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2, Items 8–15, 24) | ✅ Implementado (Items 8–15, 24 — todos) | `plans/2026-06-18-wave2-item10-operator-action.md` | Wave 2 UX hardening. **Item 10 — Operator action layer** shipped (PR #643): `OpOmnibox` + `OpBulkBar`. Todos los items 8–15 + 24 implementados (PRs #641–#653). |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 11**) | ✅ **Implementado** (`feat/wave2-item11-a11y`) | `plans/2026-06-18-wave2-item11-a11y.md` | Accessibility hardening fuera de Track E (Ley 26.653). `OpStateBadge`: icon+texto por estado (no color solo). `OpKpi` delta: `aria-hidden` en flecha + sr-only dirección. 8 tablas densas (`/gob`, `/admin`, `/org`): `scope="col"` + `<caption>`. `BulkRevokeModal`: `aria-labelledby`. Axe e2e extendido: `/adoptar`, `/refugios`, `/casos`, `/denuncias/buscar`, páginas de token inválido (`/libreta/compartir`, `/r/invite`). Nuevo `e2e/a11y-operator-auth.spec.ts` para redirects de rutas operadoras. 18 unit tests nuevos (a11y-badge-kpi + a11y-table-scope). |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 9**) | ✅ **Implementado** (`feat/wave2-item9-forms-mobile`) | `plans/2026-06-18-wave2-item9-forms-mobile.md` | Event-forms consistency + mobile hardening. 17 forms auditados contra 4 design rules: 3 verb fixes (`clinico`, `medicacion-inicio`, `sintoma`), 1 SuccessScreen añadida (`mordedura` trámite), inputMode en campos numéricos/tel, font-size 16px (iOS zoom), min-h-44px (touch target), sticky CTA footer, `useFormErrorFocus` hook cross-form. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · **Item 13**) | ✅ **Implementado** (`feat/wave2-item13-onboarding-qr`) | — | Onboarding aha — primera mascota → credencial QR. `createPetAction` redirige a `/mis-mascotas/nueva/[token]/credencial` (antes `?recienCreado=true` + `PostCreateModal`). Nueva página servidor genera QR SVG inline (240px, `qrcode`). `PetCreatedAha` client component: foco al heading en mount (a11y), Web Share API + fallback clipboard, 3 CTAs (Compartir / Ver perfil / Ver credencial pública), QR con `role=img` + `aria-label` con URL. `/mis-mascotas/nueva` envuelta en `LnWizardShell`. 9 unit tests. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave2-ux-hardening-handoff.md` (Wave 2 · Items 8–15) | ✅ Items 8, 9, 10, 11, 12, 13, 14, 15, 24 — todos implementados (PRs #641–#653) | `plans/2026-06-18-wave2-item8-loading-skeletons.md` (Item 8) · (plan post-OK, por item) | Dev-handoff de las críticas forward-looking + owner. **Al FINAL del bloque autónomo, sin reordenar lo en curso.** **8: ✅ loading/skeletons (`loading.tsx`=0→16 archivos, 4 skeleton components, shimmer tokens, a11y).** 9: 17 event-forms. 10: omnibox + bulk. 11: a11y fuera de Track E. 12: expedientes. **13: ✅ onboarding QR.** **14: ✅ owner hub `/cuenta`+notice→action loop+libreta export (dep. 5/6) — PR feat/wave2-item14-owner-hub.** **15: ✅ corrección por enmienda — implementa el principio locked #2. `event_amended` (Zod + EVENT_TYPES, sin migración), `lib/amendment.ts` (dominio puro: allowlist D4, canAmendEvent D3, applyAmendments D2), server action `amendEventAction` (D5: audit+notif sensible), `AmendEventButton`/`AmendEventForm`/`AmendedBadge` en event-detail. 22 unit tests.** **24: ✅ público — login intent-aware + UX de token expirado. `lib/auth-intent-copy.ts` (pure map `intent→{headline,subcopy}`, covers `apply`, `foster`, generic returnTo-driven). Login + signup screens read intent and show contextual gate copy (flow unchanged). `/libreta/compartir/[shareToken]`: `ExpiredView` now shows exact expiry date ("venció el {fecha}"), `RevokedView` uses gender-neutral copy ("el dueño"), both keyboard-operable. `/r/invite/[token]`: distinct headings for revoked/expired/used + CTA to org's `/refugios/[orgToken]` when org is known. 9 unit tests (`lib/auth-intent-copy.test.ts`).** |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave4-govt-actionability-and-viz-handoff.md` (Wave 4 · **Item 23**) | ✅ **Implementado** (`feat/wave4-item23-viz-primitives`) | — | Primitivas de dashboard interactivo (foundational — Items 20–22 las consumen). **23.1 `MapChoropleth` v2**: drill jerárquico provincia→departamento→barrio (usa los 3 GeoJSON de CC), breadcrumb + "Volver", cross-filter via searchParams, escala tokenizada (`lib/viz-scales.ts` — ruta obsoleta 2026-09, hoy `packages/contract/src/viz/viz-scales.ts`, colorblind-safe), join robusto por nivel (`lib/geo-join.ts` — ruta obsoleta 2026-09, hoy `lib/infra/geo-join.ts`: normalizador ISO provincia / INDEC depto / slug barrio; orphanData explícito; flag suppressed). **40 tests de join** (`__tests__/geo-join.test.ts`). **23.2 `OpKpi` v2**: backward-compatible + 4 props opcionales nuevas (`info` tooltip ⓘ, `deltaV2` numérico, `sparkline` mini AreaChart, `drillHref`). **23.3 `DashboardChart` + `DashboardTooltip`**: wrapper recharts con tooltip descriptivo (unidad + % + nota metodológica k-anon), freshness "Datos al {hora}", botón export CSV, states vacío/loading. `lib/viz-scales.ts` (ruta obsoleta 2026-09, hoy `packages/contract/src/viz/viz-scales.ts`): ramps blue/orange/purple/green (colorblind-safe), `COLOR_NO_DATA`, `COLOR_SUPPRESSED`, `CHART_COLORS`. Sin schema. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave4-govt-actionability-and-viz-handoff.md` (Wave 4 · **Item 21**) | ✅ **Implementado** (`feat/wave4-item21-outreach`) | — | Pipelines accionables de outreach — "del dato a la acción". `/gob/outreach`: 3 pipelines v1 — **(a) antirrábica vencida** (pets activas en jurisdicción sin vacuna vigente >365d, incl. sin registro), **(b) densidad de escaneos callejeros** (localidades con credential_scanned no-self-scan en 30d), **(c) ranking de esterilización por vet** (reconocimiento, 30d). `lib/outreach-pipelines.ts` (ruta obsoleta 2026-09, hoy `lib/infra/outreach-pipelines.ts`): `fetchOverdueRabiesVaccine`, `fetchStrayDensityAreas`, `fetchSterilizationVetRanking`, `logOutreachPiiQuery`. **PII audit obligatorio**: cada vista de lista escribe un `pii_queried` audit row (`surface='outreach_pipeline'`, `pipeline=<id>`, `result_count`) — sin migración (payload JSONB). Export CSV: `/gob/outreach/export?pipeline=<id>` — también escribe audit row. Empty state: "Sin pets que cumplan el criterio en tu jurisdicción". Capability-gated (`requireAdminOrGovtOrRedirect` + jurisdictions check). Nav entry "Outreach" en sección "Vigilancia sanitaria". Snapshots actualizados: `nav-presets.test.ts` + `shell-nav-phase-b.test.ts`. Sin schema. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave4-govt-actionability-and-viz-handoff.md` (Wave 4 · **Item 22**) | ✅ **Implementado** (`feat/wave4-item22-analytics`) | — | Analytics enhancements — 3 mejoras baratas sobre lo ya construido. **22.1 Ranking cross-region**: tabla rankeada top/bottom 5 provincias por cobertura antirrábica junto al choropleth en `/gob/analytics` (`lib/analytics-ranking.ts` — ruta obsoleta 2026-09, hoy `lib/analytics/analytics-ranking.ts`: `rankByField` pura + `fetchRegionRanking`, `<RegionRankingTable>` con `<th scope>` + `<caption>` a11y). **22.2 Atajo "Analítica nacional" en admin**: entrada a `/gob/analytics` en modo todas-las-provincias desde `admin/page.tsx` — admin ve scope global; label claro, no duplica la vista. **22.3 Preview de impacto de reglas**: en los forms PPP peso/lista, "esta regla afecta a ~N mascotas" ANTES de confirmar (`app/actions/rule-impact-preview.ts` server action, `<RuleImpactBanner>` client component con debounce/abort). Sin schema. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave3-org-ops-handoff.md` (Wave 3 · **Item 16**) | ✅ **Implementado** (`feat/wave3-item16-census`) | — | Censo & ocupación del refugio. Migración **aditiva 0102** (`capacity_dogs`, `capacity_cats`, `capacity_other`, `capacity_total` nullable integer en `organizations` — único schema change del paquete Wave 3). `lib/org-census.ts` (ruta obsoleta 2026-09, hoy `lib/analytics/org-census.ts`; proyección pura sobre `shelter_custody` activas: `fetchOrgCensus`, `computeOccupancyBreakdown`). Sección "Capacidad" en `/configuracion` (gated por `org_type` shelter/rescue_network). Página `/censo` con desglose por especie + link a listado filtrado + over-capacity `danger` tone + CTA "Declarar capacidad" cuando no hay capacidad configurada. Nav entry "Censo" gated por `intake.create`. 15 tests integración (`org-census.test.ts`): proyección sobre seed, exclusión de no-custody, cross-org isolation, capacidad nullable → sin %, sobre-capacidad, smoke aditivo. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave3-org-ops-handoff.md` (Wave 3 · **Item 17**) | ✅ **Implementado** (`feat/wave3-item17-org-panel`) | — | Panel de operaciones del refugio. Rediseño de `app/org/[orgToken]/page.tsx`: de 3-KPI to-do list a dashboard operativo completo. Sin migración (proyecciones read-only). `lib/org-dashboard.ts` (ruta obsoleta 2026-09, hoy `lib/analytics/org-dashboard.ts`): `fetchIntakesLastWeek` (ingresos 7 días), `fetchAvailableForAdoption` (en adopción elegibles), `fetchActiveAdoptions` (raw SQL, mismo patrón adopciones/page.tsx), `fetchRequiresAction` (CTE con flags: vacuna vencida, desparasitación vencida, medicación sin dosis, estadía larga >60 días), `fetchOrgDashboardMetrics` (parallel). `actionReasonLabel` / `actionReasonIcon` (puras, a11y icon+texto). KPI row (4 tiles): Ocupación (reusa `computeOccupancyBreakdown`) · Ingresos (semana) · Disponibles · Adopciones en curso. OpCard "Requieren acción" (accent=warn cuando hay items; mensaje positivo cuando vacío). OpCard "Pendientes" (3 KPIs previos demotados). Empty states alentadores para refugios nuevos. A11y: icon+texto, no color solo (patrón Item 11). 18 tests integración (`__tests__/org-dashboard.test.ts`). |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave3-org-ops-handoff.md` (Wave 3 · **Item 18**) | ✅ **Implementado** (`feat/wave3-item18-pipeline`) | — | Vista de pipeline (tablero) de animales. Toggle **Lista / Tablero** en `app/org/[orgToken]/mascotas/`. Sin nueva página, sin migración — misma data y filtros. `lib/pet-pipeline.ts` (ruta obsoleta 2026-09, hoy `lib/infra/pet-pipeline.ts`; puro, sin DB): `derivePipelineColumn`, `groupIntoPipelineColumns`, `PIPELINE_COLUMNS`. 6 columnas derivadas de custody states existentes: **Ingreso** (`shelter_custody`, sin evaluar) · **Evaluación** (`shelter_custody`, `adoptionEligible=false`) · **Disponible** (`shelter_custody`, eligible, no listado) · **En adopción** (`adoptionListedAt!=null`) · **Tránsito** (tiene foster row activo) · **Otros** (owner/co_owner/caretaker). `OrgMascotasPipelineBoard` (client): columnas scroll horizontales, conteo por columna, click-through al perfil. Reusa `OrgMascotasBulkList` — selección + bulk-vaccinate intactos en vista lista. A11y: `role="group"` en toggle, `aria-pressed`, `role="region"` + `aria-label` por columna. 18 unit tests (`__tests__/pet-pipeline.test.ts`): `derivePipelineColumn` (todos los estados + prioridad foster), `groupIntoPipelineColumns` (vacío, single, multi, orden, empty columns). |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave3-org-ops-handoff.md` (Wave 3 · **Item 19**) | ✅ **Implementado** (`feat/wave3-item19-org-onboarding`) | — | Onboarding guiado de organización (first-run checklist). `lib/org-setup-checklist.ts` (ruta obsoleta 2026-09, hoy `lib/infra/org-setup-checklist.ts`; derivación pura, sin DB): `deriveSetupSteps`, `isSetupComplete`, `firstPendingStep`. 5 pasos derivados del estado real: **Zonas de cobertura** (coverage rows > 0) · **Miembros** (memberCount > 1) · **Servicios** (si `service_offering.create` concedido + offerings > 0) · **Capacidad** (shelter only — capacity columns not null) · **Verificación** (`org.verified`). Gating `org_type`: clínica no muestra capacidad; refugio sí. Auto-oculta cuando todos los pasos están completos. Solo visible para admins de la org. `components/OrgSetupChecklist.tsx`: OpCard con `<fieldset>`+`<legend>`, `<ul>`/`<li>`, `aria-label` progreso, `aria-current="step"` en el primer pendiente, `autoFocus` en el primer CTA pendiente, icon+texto (no color solo). Cableado en `app/org/[orgToken]/page.tsx` — 3 queries adicionales (coverage count, member count, services count) en paralelo. Sin migración, sin persistencia, sin nuevos event types. 29 unit tests (`__tests__/org-setup-checklist.test.ts`). |
| `2026-06-18-owner-health-status-nudges-design.md` (Item 5) | Removido 2026-07-21 (código muerto, AGENTS.md:1158-1160) | `plans/2026-06-18-owner-health-status-nudges.md` | `/inicio` nudges per-pet derivados (vacuna vencida, chip, recordatorios, scans, esterilización) + rollup "Al día"/"N pendientes". Owner-data only, sin surveillance (umbrella §6). `lib/infra/owner-nudges.ts` (ruta obsoleta 2026-09 — no se encontró bajo ningún nombre; ver `dim-interno:docs/agents/open-work.md`; derivación pura + reads owner-scoped, reusa `lib/projections/pet-microchip`) + `PetHealthStatusStrip` presentacional en `/inicio`. 12 tests integración TDD. Sin schema/event types/migrations. Coordina con Item 6 el split `/inicio` agregador vs perfil detalle (D10). |
| `2026-06-18-unified-app-shell-design.md` (Item 7) | ✅ Implementado (PRs #630–#634, strangler A–D) | (plan post-OK) | De la crítica de navegación 2026-06-18. Un solo `AppShell` con variantes `citizen` (barra superior) / `operator` (riel lateral) que reemplaza los 3 chromes (`LnOwnerNav`/`AppHeader`/`OpShell`). Fuente única de nav, decisión auth-aware (`lib/shell-nav.ts` — ruta obsoleta 2026-09, hoy `lib/ui/shell-nav.ts`) que **arregla el varado del usuario logueado en surfaces públicas**, drawer mobile único, context switcher. Depende de Item 1 (consume sus `NavSection[]`). Strangler en 4 fases. Sin cambios de datos/rutas. |
| `2026-05-15-timeline-type-filter-design.md` | ✅ Implementado | `plans/2026-05-15-timeline-type-filter.md` | `EventTimeline` tiene prop `chips` + `DEFAULT_FILTER_CHIPS` export |
| `2026-05-16-health-campaigns-and-scheduling-design.md` (v2.1) | ✅ Implementado | `plans/2026-05-16-health-campaigns-and-scheduling.md` | Provider polymorphic (org o vet independiente via sub-rutas `/pro/servicios`). Approval routing a govt scope-matching + admin fallback. Paths `/org/[orgToken]`, `/pro`, `/gob`, `/admin`. 10 fases. |
| `2026-05-17-admin-page-design.md` (v2.3) | ✅ Implementado (Fases 0-9) | — | Cuatro roles (`owner`, `vet`, `govt`, `admin`), dos `account_type`s (personal / institutional). Surfaces `/gob` y `/admin` activos. Migraciones 0010/0011/0015/0016. Fases 10+ continúan en `2026-05-18-admin-page-next-phases-design.md`. |
| `2026-05-18-admin-page-next-phases-design.md` (v3.0) | ✅ Implementado (Fases 10-14) | `plans/2026-05-18-admin-page-fases-10-14.md` | Fases 10-14 shipped en `6f6444a` (F11) + `465b1fc` (F12+13+14+10). Custody disputes (`/gob/disputas`), dashboards regionales govt (`/gob/vigilancia` + `/gob/perdidas`), métricas admin (`/admin/sistema`), bulk ops en cola, auto-expiry cron 60d (`cron_runs` table). Follow-up menor: UI de bulk revoke en las 4 queues de usuarios/orgs (server action ya implementado). |
| `2026-05-18-localities-catalog-design.md` (v1.0) | 🚫 **Superseded** por v2.0 — no implementar | — | Versión inicial chiquita (catálogo curado de 94 entradas hardcoded en TS). Reemplazada antes de implementar por v2.0 que importa el catálogo INDEC completo (~4500). Se mantiene en el repo como referencia histórica del razonamiento. |
| `2026-05-18-localities-catalog-indec-design.md` (v2.0) | ✅ Implementado (parcial — ver follow-ups) | `plans/2026-05-18-localities-catalog-indec.md` | **Reemplaza v1.0**. 4027 localidades INDEC importadas a `ar_localities` desde `https://infra.datos.gob.ar/georef/localidades_censales.csv`. Typeahead `<LocalityCombobox>` montado en `LocationFields` mode="jurisdiction" + CreateGovtForm + AssignLocalityForm. Validación canonical (`resolveCanonicalJurisdiction`) en las 2 actions govt críticas. Migrations 0019 + 0020. Script `normalize-existing-jurisdictions.ts` listo. **Follow-ups:** (a) validar 5 server actions restantes (vet upgrade, org creation, service-offerings, welfare, events) — siguen aceptando texto libre; (b) CABA barrios — INDEC trata CABA como 1 sola localidad, importar barrios de `data.buenosaires.gob.ar` con `source='caba_open_data'`. |
| `2026-05-17-symptom-disease-surveillance-design.md` | ✅ Implementado | `plans/2026-05-17-symptom-disease-surveillance.md` | Match fuzzy texto libre → enfermedades reportables → signal silencioso a autoridad. Owner no ve diagnósticos. |
| `2026-05-17-lost-and-found-complete-design.md` (v1.1) | ✅ Implementado | `plans/2026-05-17-lost-and-found-complete.md` | Microchip cross-check + return-to-owner + broadcast + disclosure prefs owner-controlled + enriched flow para pets sin chip. 7 fases. Paths via `/org/[orgToken]/mascotas/{petToken}`. |
| `2026-05-17-bidirectional-geocoding-design.md` | ✅ Implementado | `plans/2026-05-17-bidirectional-geocoding.md` | Sync bidireccional text ↔ map pin vía Nominatim/OSM proxy. `LocationFields` mode="point" integra text + sync, `MarkLostForm` migrado. UX-only, sin schema changes. |
| `2026-05-18-bite-rabies-observation-design.md` (v1.1) | ✅ Implementado | `plans/2026-05-18-bite-rabies-observation.md` | Bite reporting + observación 10 días, end-to-end. Migración 0021 + columna `pets.rabies_observation_status`. 2 event_types nuevos (`rabies_observation_started/ended`). Owner self-report `/mis-mascotas/[token]/eventos/nuevo/mordedura` + banner. Org-side reporting `/org/[orgToken]/mordedura/nuevo` (capability `bite.report`, vets + shelters). Surveillance escalation: rabia high-spec durante observación → `urgent` + nudge owner. Cron auto-close + script CLI. Death hook (auto-close con outcome=`dead` + escalada urgent). `/admin/observaciones` para cierre profesional con outcome configurable (negative / positive_rabies / dead / lost_to_followup). **Follow-up menor:** configurar cron schedule en vercel.json. Anclaje legal: Decreto 4669/1973 PBA, Ord. CABA 41.831/1987, Res. MS 1144/2018. |
| `2026-05-17-additional-species-design.md` | ✅ Implementado | — | **Bloque 1** (rabbit / guinea_pig / ferret): nested select en `PetForm` + `speciesLabel` extendida + fallback graceful en `lib/diseases.ts` (ruta obsoleta 2026-09, hoy `lib/reference/diseases.ts`; zoonoses `any`-tagged) y `lib/drugs.ts` (ruta obsoleta 2026-09, hoy `lib/reference/drugs.ts`; catálogo completo con comentario no-prescriptivo). **Bloque 2** (service dogs Ley 26.858): tabla `petServiceDog` en schema, migration `0026_pet_service_dog.sql`, 5 actions en `app/actions/service-dog.ts` (`upsertServiceDogAction`, `submitServiceDogVerificationRequestAction`, `setServiceDogVisibilityAction`, `retireServiceDogAction`, `revokeServiceDogCredentialAction`), event_type `service_dog_credential_verification`, banner en `/p/[publicToken]` con sub-warning rabies. **Bloque 3** (compliance checklist) son notas técnicas, no implementación. |
| `2026-05-18-physical-tag-design.md` (v1.0) | 🟡 Spec only — pending decisions in §15 open questions | (plan a escribir post-OK) | Chapa identificadora física que cuelga del collar con QR a `/p/[publicToken]`. Modelo Estonia-style: identidad canónica en software, chapa como UX layer. Multi-tag por pet, redirect `/t/[serial]` → `/p/[publicToken]`, activación self-serve, revocación granular. **Material y fabricante son placeholders explícitos** — research de opciones AR (Luna Accesorios, Laser Eleven, K9, Grabados Piroso) + opciones import. Decisiones pendientes: auto-revoke on death, DIY QR, interop con otros sistemas. ~3 días cuando se planee. |
| `2026-05-18-foster-volunteers-pool-design.md` (v1.4) | ✅ Implementado | `plans/2026-05-18-foster-volunteers-pool.md` | Pool de owners voluntarios proactivos para foster. Tabla `foster_volunteers` (preferences + status + `available_slots`) + `foster_proposals` (org-initiated, pet-específica, timeframe estimado, 7d expiry). Volunteer acepta → materializa `ownership.role='foster'` con `allow_co_foster` flag opcional. **Decisiones cerradas v1.4**: D16 modelo de slots single-use (cada inscripción +1, cada aceptación -1, prompt post-termination "¿volver al pool?"); D17 co-foster opt-in por checkbox del primer foster; D18 cascade auto-cancel cuando slots queda en 0. Todo público entre orgs del pool (sin gating de detalle). Notes visibles en listado + count agregado aceptaciones/rechazos. Match warnings (no bloqueos) por mismatch de preferences — la org elige con criterio, el voluntario decide al recibir. Heredados v1.1-v1.3: D13 DNI verificado + perfil hidratado, D14 + §17 adoption eligibility flag, D15 + §6.10 foster con capacidades plenas como owner. 4 fases A-B-C-D shipped. **Follow-up:** aplicar `db/foster_rls.sql` en Supabase Studio. |
| `../archive/2026-05-18-maltreatment-reporting-design.md` (dim-interno:docs/archive/2026-05-18-maltreatment-reporting-design.md) | ⚪ **SUPERSEDED — archived** | — | Quedó superseded por la implementación real en `welfare_reports` table (`db/schema.ts:885-983`, `app/actions/welfare.ts`, `app/denuncias/nueva/`). La arquitectura `ghost_subject` propuesta NO debe seguirse — el código usa una tabla separada con `subjectKind` enum polimórfico. Movido a `dim-interno:docs/archive/` en sprint 1 PR-007. Mantenido solo como referencia histórica del análisis comparativo con el form del MPF CABA (https://denuncias.fiscalias.gob.ar/). Para trabajar el feature ver `plans/2026-05-18-welfare-reports-polish.md` + futuro spec del welfare-officer queue. |
| `2026-05-18-adoption-listing-public-design.md` (v1.3 ✅ + v1.4 🟢) | ✅ v1.3 Implementado / 🟢 v1.4 Ready for CC | (v1.4 addendum requiere sistema de casos) | **v1.3 implementado**: 6 fases shipped + F5.5 cascade + org-side review surface. Páginas: `/adoptar`, `/adoptar/[petToken]`, `/adoptar/[petToken]/postular`, `/refugios/[orgToken]`, `/mis-mascotas/postulaciones`, `/org/[orgToken]/adopciones`, `/org/[orgToken]/adopciones/[appEventId]`. Helper `lib/adoption-listing.ts` con `queryAdoptionListing` (4 cross-spec guards D18-D21). Apply intent JWT en `lib/apply-intent.ts` (ruta obsoleta 2026-09, hoy `lib/domain/apply-intent.ts`; HMAC, 15min TTL, cookie httpOnly). Migrations 0023 (eligibility) + 0030 (listing columns). 3 server actions de application lifecycle. Cascade auto-rejection en `finalizeAdoptionAction` (F5.5). Después del catalog phase-2 cleanup, los event_types collapsaron a `adoption_application_resolved` (outcome discriminator). **v1.4 addendum** (§12.5): consent obligatorio + vista "Historial del postulante en MiMAR" scope-bound durante review + RLS extensions a pets/ownerships/pet_events del applicant. Depende del sistema de casos para los `cases.adoption_application_id` chained policies. |
| `2026-05-19-cases-event-attachment-design.md` (v1.1) | ✅ Implementado | `plans/archive/2026-05-19-cases-system.md` | Define el objeto **Caso** (expediente) como capa de coordinación liviana sobre el event log: tabla `cases` minimal, columna `case_id` nullable en `pet_events` y `welfare_reports`, 5 modos de attachment (`opens` / `requires-open` / `attaches-when-open` / `optional` / `never`), 13 case_kinds preliminares (7 en subset v1), cascade-emission rules para multi-case closures, visibility scope-bound vía RLS. Catálogo evento × case-kind completo para los 41 event_types. **v1.1** desdobla `adoption_pipeline` en `adoption_listing` (org-side) + `adoption_application` (applicant-side) por asimetría de flujos. Decisiones D1-D5. |
| `2026-05-19-cases-lifecycles-design.md` (v1.0) | ✅ Implementado | `plans/archive/2026-05-19-cases-system.md` | Sucesor del attachment spec. Define lifecycle interno de los 7 kinds del subset v1: estados (open/escalated/closed/merged), phases observables (derived), eventos que abren/cierran/avanzan, crones de auto-cierre + escalation, normativas aplicables (lookup `lib/case-normatives.ts` — ruta obsoleta 2026-09, hoy `lib/domain/case-normatives.ts`), matrix de notifications por kind, visibility tweaks específicos. 1 reapertura permitida (`adoption_reversed`), 3 crones de cierre automático, 2 crones de escalation visible. Decisiones cross-kind L1-L10. |
| `2026-05-19-pet-profile-v2-design.md` (v1.0) | ✅ Implementado | (plan a escribir post-OK) | Rediseño UX del profile en `/mis-mascotas/[publicToken]`: saca el `<EventTimeline>` (queda en `/libreta` y `/historial`), introduce "Casos abiertos" prominent, "Logros" (achievements chips) y "Estado actual" como info derivada del state. 5 achievements POC: `service_dog` + `i_was_adopted` + `lost_and_found` ya computables; `i_had_litter` + `globetrotter` marcados `not_yet_computable`. `AchievementsSection.tsx`, `PpPCard.tsx`, `ServiceDogCredentialCard.tsx` cableados en `mis-mascotas/[publicToken]/page.tsx`. |
| `2026-05-19-bite-from-unowned-animal-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Víctima autenticada reporta bite donde el animal NO está en MiMAR. Tabla `temporary_pet_descriptions` + `cases.primary_subject_kind='unowned_animal'`. 3 vías de matching automatic (microchip / DIM token / DNI). Reconciliation hook on signup: si DNI matchea temps, propone vincular cases históricos a pets nuevas del user. Reasignación manual govt/admin a pet real. Extiende bite-rabies-observation cumpliendo el mismo período legal (10 días) sobre animales no-registrados. Solo autenticados en v1. Depende del sistema de casos. ~8 días. |
| `2026-05-19-pet-profile-v2-design.md` (v1.1) | ✅ Implementado | (plan a escribir post-OK) | **v1.1** agrega §4.7 PPP card (Ley 4078 / 14.107) y §4.8 Service Dog credential card (Ley 26.858). `PpPCard.tsx` + `ServiceDogCredentialCard.tsx` cableados en `mis-mascotas/[publicToken]/page.tsx`. |
| `2026-05-19-pregnancy-tracking-design.md` (v1.0) | ✅ Implementado | (plan a escribir post-OK) | Embarazo como `clinical_info_logged(sub_kind='pregnancy')`. Pair `pregnancy_phase='started'/'ended'`. `app/.../eventos/nuevo/embarazo/`, `app/actions/pregnancy.ts`, `pets.pregnancy_status`. |
| `2026-05-19-eno-vet-direct-report-and-owner-alerts-design.md` (v1.0) | ✅ Implementado | (plan a escribir post-OK) | ENO vet directo + owner alerts. Commit de70afeb: `lib/disease-legal-anchors.ts`, `lib/disease-public-alert-catalog.ts`, `lib/owner-disease-alerts.ts` (ruta obsoleta 2026-09 — hoy `lib/reference/disease-legal-anchors.ts`, `lib/reference/disease-public-alert-catalog.ts`, `lib/infra/owner-disease-alerts.ts`). |
| `2026-05-19-cross-org-transfer-ux-design.md` (v1.0) | ✅ Implementado | (plan a escribir post-OK) | UI completa del handshake refugio→refugio. `app/org/[orgToken]/transferencias/` full UI + cron `expire-cross-org-transfers`. |
| `2026-05-19-org-abuse-investigation-design.md` (v1.0) | ✅ Implementado | (plan a escribir post-OK) | Commit a4d03240: `reporter_organization_id` mig 0035, `createOrgWelfareReportAction` severity critical, OA9. |
| `2026-05-19-decomiso-welfare-authority-design.md` (v1.0) | ✅ Implementado | (plan a escribir post-OK) | `app/gob/decomisos/nuevo/` + `DecomisoForm` + `executeDecomisoAction` + cron expire daily + receiver handshake. |
| `2026-05-19-performed-by-autocomplete-design.md` (v1.0) | 🟢 Ready for CC | (plan a escribir post-OK) | Combobox dual: linked (FK a orgs/vets verified) + texto libre fallback. Schema delta a 6 events (vaccination, deworming, sterilization, vet_visit, clinical_info, microchip) — payload extensions, NO columnas SQL. Helper `searchVetsAndClinics(query, jurisdiction)` con boost por jurisdiction match. Component `PerformedByCombobox` reusable. NO backfill retroactivo. Display libreta distingue linked vs texto. Independiente del sistema de casos. ~5-6 días. |
| `2026-05-19-govt-business-rules-poc-design.md` (v1.0) | ✅ Implementado | (plan a escribir post-OK) | `govt_business_rules` mig 0037, `lib/business-rules-resolver.ts` (ruta obsoleta 2026-09, hoy `lib/infra/business-rules-resolver.ts`), `/admin/jurisdicciones/*/reglas/` + `/gob/reglas/`. |
| `2026-05-19-pet-spaces-catalog-design.md` (v1.0) | 🟡 Spec only — pending review + plan | (plan a escribir post-OK) | Catálogo polimórfico de lugares físicos relevantes para mascotas: 7 kinds en v1 (`public_pet_space`, `vaccination_point`, `shelter`, `vet_clinic`, `pet_store`, `crematorium`, `pet_cemetery`, `municipal_pet_handling_facility`). Surface owner-facing en `/mis-mascotas/espacios` + widget `/inicio` (MapLibre + clustering). Tres caminos de ownership coexistentes: admin seed (113 espacios CABA) → govt curation scoped por jurisdicción → org self-request via nuevo `approval_request.type='map_listing_request'`. Owner solo lee + reporta correcciones (`pet_space_correction`). 100% informativo (sin event_type nuevo). Kind sensible `municipal_pet_handling_facility` con disclaimer + anclaje Ley 14.346. 10 fases A-J. ~2-3 semanas. 8 preguntas abiertas §15 antes del plan. |

### Plans (implementation, listos para Claude Code)

| Plan | Status | Spec relacionado | Notas |
|------|--------|------------------|-------|
| `2026-06-24-operator-design-system-unification.md` | 🔴🟡 Ready for CC | — (critique `dim-interno:docs/design/critique-2026-06-24-frontend.md` + `dim-interno:docs/archive/Hallazgos Design System.md`) | **Unificación del design system del tier operador** (`/admin` + `/gob`). Dos sistemas (`Ln*` ciudadano / `Op*` operador) sin capa semántica común. **PR-1** capa `--st-*` por piel + auditoría contraste `ln-op-warn/danger` + colapsar violetas (F1 🔴). **PR-2** bug de tono contradictorio `open`/`closed` (`OpPill` vs `CaseStatusBadge`) + primitivo `OpStatusPill` único (F2). **PR-3** `OpButton` — 1 import de `LnButton` vs 133 `<button>` crudos; primario verde/azul inconsistente (F3). **PR-4** 🟢 densidad Casos. Cero diff visual en superficies ciudadanas. SDD test-first. |
| `2026-06-21-national-situational-console.md` | ✅ Implementado (#705) | `specs/2026-06-21-national-situational-console-design.md` (v1.0) | **Centro de Situación Nacional (Panorama) — plan ejecutable (5 fases F1–F5).** Doc 2/3 del paquete Panorama. Módulo `src/modules/panorama/` (hexagonal-lite), frontend `components/panorama/` (MapLibre GL), rutas `app/{admin,gob}/panorama/`. F1–F4 ✅. F5 privacidad ✅ parcial — la absorción de `/gob/analytics` vía redirect 308 **no se ejecutó** (decisión de producto pendiente; `/gob/analytics` está VIVO como página propia en el nav). |
| `2026-06-21-panorama-demo-dataset.md` | ✅ Implementado (#698) | `specs/2026-06-21-national-situational-console-design.md` (v1.0) | **Dataset demo realista para el Panorama** (`seed:panorama`). Doc 3/3 del paquete Panorama. ~46k mascotas sintéticas ponderadas por Censo 2022 + anclas metro, set-pieces rabia NOA / perdidas AMBA / k-anon. Idempotente, determinístico, local-only. Sin schema nuevo. Nota: el banner "Datos de demostración" se muestra en TODOS los entornos (flag #20 pendiente) — esperado, no un bug. |
| `2026-06-22-admin-fresh-sweep-fixes.md` | 🟡 En curso (A1-A3 ✅ #702) | — (extiende `2026-06-20-ux-audit-remediation.md` Fase 3.6) | **Fresh sweep en vivo de las pantallas admin** (7 ítems A1-A7, sev 🔴·🟡·🟢). A1 (404 branded por route-group), A2 (`RuleImpactBanner` — calc `inArray()` en vez de `= ANY` untyped), A3 (acentos es-AR) ✅ en #702. Restan A4-A7 (🟢 polish: crumbs estáticos, action-code labels en feeds) + A1 e2e. Test-first. |
| `2026-06-22-dashboards-deep-dive.md` | 🟡 En curso (D3/D5/D6/D7 ✅ · D1 ✅) | — (extiende Fase 3.6 + paquete metrics-IA) | **Deep dive de dashboards admin/gob en vivo con `seed:panorama`** (~45.7k pets; D1-D7, sev 🟡·🟢). D3 (localización analítica: `deathCauseLabel`, "Analítica", copy es-AR) + D5 (drill `href` en KPIs admin) + D6 (cross-link admin→gob) ✅ #703; D7 (404 branded `/admin`) ✅ #702. **D2** (señales de vigilancia) + **D4** (reconciliación de disposición) son fixes de **seed** → viven en #698 (pendiente re-run para verificar). **D1 ✅ #706** (series temporales / trends): `lib/metrics/{timeseries,trends}.ts`, `StackedTimeSeriesChart`, tendencias en `/gob/mortalidad` + `/gob/analytics`. D6 sustantivo = consola Panorama (task Centro de Situación). |
| `2026-06-23-dashboards-vnext.md` | 🟢 Ready for CC | `specs/2026-06-23-dashboards-vnext-roadmap.md` | **Plan ejecutable Dashboards vNext** (deriva del roadmap homónimo). Orden Fase 0 (incrementales: deltas+sparklines, targets, frescura, drill) → E (censo/registro) → ⭐G (control poblacional) → F (custodia/adopción) → H (operativa + alertas). Sin schema salvo suscripciones de alertas (H). Depende de `lib/metrics` + rollup `level` (U5) + enriquecimiento de datos (U4). |
| `2026-06-19-welfare-coordinates-precision.md` | ✅ Implementado | — (standalone; auditoría de rutas públicas 2026-06-19) | **Coordenadas de denuncia precisas-por-audiencia.** Helper puro `coarsenPoint`/`PointPrecision` en `lib/location.ts` (redondeo determinístico a 3 decimales ~110 m, D1 default). Comprobante público `/denuncias/codigo/[code]` → **aproximada** (localidad/provincia + mapa "Ubicación aproximada", sin `toFixed(6)` ni `locationAddress` a nivel calle; Ley 25.326). Autoridad `/gob/maltrato/[id]` + `/admin/moderacion/[id]` → **exacta** + label "uso oficial (Ley 14.346)" + acceso logueado (`logWelfareLocationViewed` → `audit_log` action `welfare_location_viewed`, TEXT sin migración). Tests: `coarsenPoint` puro + source-scan del contrato por audiencia. **Sin schema.** PDF comprobante hereda aproximada (D2 default). **Fase D retención (D3) NO implementada — opt-in del dueño.** |
| `dim-interno:docs/superpowers-private/plans/archive/2026-06-19-git-packed-refs-repair.md` | ✅ Resuelto (sin cambios) | — (infra/chore) | **No requirió acción.** Verificado 2026-06-19: `git status`/`rev-parse develop`/`fsck` corren limpios; `.git/packed-refs` termina en `\n` (sano), sin línea truncada ni artefactos `.bak`/`.fixed`. La corrupción `unterminated line` de la auditoría ya se había normalizado (un `fetch`/`pack-refs` posterior reescribió el ref remoto). Sin pérdida de datos; cero cirugía sobre internals de git. |
| `2026-05-15-timeline-type-filter.md` | ✅ Implementado | `specs/2026-05-15-timeline-type-filter-design.md` | — |
| `2026-05-16-event-agent-foundations.md` | ✅ Implementado | — (diseño en chat) | `lib/event-agent-registry.ts` (ruta obsoleta 2026-09 — el módulo fue renombrado y su reemplazo vive en `lib/events/event-capture-registry.ts`, ver la fila "captura-rápida" más abajo) con `EVENT_AGENT_REGISTRY` + `buildAgentDeeplink()`. Form de peso retrofitteado como referencia de URL-prefill (acepta `?kg=&occurredAt=&notes=`). Tests en `lib/event-agent-registry.test.ts` (idem — renombrado junto al módulo). |
| `2026-05-16-vecino-mascota-en-transito.md` | ✅ Implementado | — (diseño en chat) | `custodyKind` field en `PetForm`, `shelter_custody` ownership branch en `createPetAction` |
| `2026-05-16-libreta-sanitaria-parte-a.md` | ✅ Implementado | — | `lib/libreta-sanitaria.ts` (ruta obsoleta 2026-09, hoy `lib/infra/libreta-sanitaria.ts`) con `LIBRETA_SANITARIA_EVENT_TYPES`, `NON_LIBRETA_EVENT_TYPES`, `LIBRETA_FILTER_CHIPS`, helpers + test de cobertura |
| `2026-05-16-libreta-sanitaria-parte-b.md` | ✅ Implementado | — | Ruta `/mis-mascotas/{token}/libreta` con vista agrupada + cronológica + print stylesheet. Componentes `LibretaSanitariaView`, `LibretaIdentityHeader`. Grouping en `lib/libreta-sanitaria.ts` (ruta obsoleta 2026-09, hoy `lib/infra/libreta-sanitaria.ts`). |
| `2026-05-16-libreta-sanitaria-parte-c.md` | ✅ Implementado | — | Tabla `libreta_share_tokens`, ruta pública `/libreta/compartir/[shareToken]`, server actions `createLibretaShareAction`/`revokeLibretaShareAction`, `SharesManager` owner UI, view-tracking via `pet_events` type `libreta_shared_viewed`. |
| `2026-05-17-mimar-rebrand-and-portal-restructure.md` | ✅ Implementado | — | Paths alineados (`/pro`, `/gob`) + pasada de copy user-facing completa: títulos, notifications, error messages, banners y subjects de email todos como "MiMAR". `DIM` queda exclusivamente como codename interno (schema, tokens `DIM-XXXX-XXXX`, code identifiers, comments, audit logs). |
| `2026-05-17-code-rename-refugio-to-org.md` | ✅ Implementado | — | `app/refugio/` ya no existe; código vive en `app/org/[orgToken]/`. |
| `2026-05-17-symptom-disease-surveillance.md` | ✅ Implementado | `specs/2026-05-17-symptom-disease-surveillance-design.md` | 5 fases shipped (Fases 3+4 combinadas en un solo commit por estar acopladas). Catálogo de 23 síntomas, matcher fuzzy, `outbreak_signal` event type, server action + form, integration tests. TODO inline en `routeOutbreakSignalNotification` para swap a govt-scope routing cuando lande admin page Fase 0. |
| `2026-05-17-lost-and-found-complete.md` | ✅ Implementado | `specs/2026-05-17-lost-and-found-complete-design.md` | 7 fases shipped en PRs #49–#55. Cross-check + match flow + disclosure prefs + enriched + return-to-owner + broadcast + polish. |
| `2026-05-17-bidirectional-geocoding.md` | ✅ Implementado | `specs/2026-05-17-bidirectional-geocoding-design.md` | Server action `app/actions/geocoding.ts` (auth-gated) delega a `lib/geocoding.ts` (proxy Nominatim + token bucket 5 req/sec). `LocationFields` mode="point" integra text + map con debounce 600ms + skipNextForward flag. `LocationPicker` ahora soporta drag del pin además de click. `MarkLostForm` migrado: el input separado `lastKnownLocation` se removió y vive integrado en `LocationFields` con `inputNames.description="lastKnownLocation"`. 18 tests con fetch mockeado en `__tests__/geocoding.test.ts`. Privacy D10: server NO loguea queries. |
| `2026-05-18-bite-rabies-observation.md` | ✅ Implementado | `specs/2026-05-18-bite-rabies-observation-design.md` (v1.1) | Las 6 fases shipped en 6 commits. F0=schema + Zod + capability. F1=owner self-report + UI. F2=surveillance escalation hook. F3=cron + CLI auto-close. F4=org-side reporting + `OrgBiteForm`. F5=death-during-observation hook + `during_rabies_observation` flag. F6=`professionalCloseRabiesObservationAction` + `/admin/observaciones[/[token]]` surface con outcome configurable. Follow-up menor: vercel.json cron schedule. |
| `2026-05-18-event-catalog-cleanup.md` | ✅ Implementado | — | Cleanup estructural del catálogo: borrados 4 events redundantes (subsumidos por `clinical_info_logged`), bites refactoreados bajo `incident_reported` (umbrella + `incident_type` discriminator), agregados 5 events nuevos (`adoption_withdrawn`, `custody_dispute_raised/resolved`, `microchip_replaced/revoked`), deprecado `adoption_application_reviewed`. Migración 0018 agrega `pets.in_custody_dispute`. Test existente de cobertura (`__tests__/event-schemas.test.ts`) ajustado + nuevo orphan-schema check. AGENTS.md Event catalog actualizado a 39 types con sección "Deprecated event types" + nueva subsección "Cross-cutting event design patterns" (4 patterns). Spec de bite-rabies-observation bumpeada a v1.1. |
| `2026-05-19-event-catalog-phase-2.md` | ✅ Implementado | — | Segunda fase del cleanup: 5 colapsos / movimientos. (1) `foster_proposal_{accepted,rejected,cancelled,expired}` → `foster_proposal_resolved` con `outcome` discriminator. (2) `adoption_{revoked,withdrawn}` → `adoption_reversed` con `actor` discriminator. (3) `adoption_application_{approved,rejected}` → `adoption_application_resolved` con `outcome` discriminator. (4) `libreta_shared_viewed` movido fuera de `pet_events` a tabla nueva `share_telemetry` (migration 0032). (5) `microchip_revoked` absorbido por `microchip_replaced` con `new_chip_number: nullable`. Net del catálogo: 48 → 41 event_types. Tests nuevos en `lib/event-schemas.test.ts` (24 assertions para los 3 resolvers + microchip merged); tests existentes adaptados (foster, libreta-share, adoption-review, adoption-cascade). User opt-out de preservation: DB se va a limpiar pronto, no se agregaron catch-all renderers ni retro-compat tests; seed scripts quedan stale hasta el wipe. |
| `2026-05-18-admin-page-fases-10-14.md` | ✅ Implementado (5 fases) | `specs/2026-05-18-admin-page-next-phases-design.md` (v3.0) | Cinco fases shipped: F11 (`6f6444a`) regional dashboards govt `/gob/vigilancia` + `/gob/perdidas` con scoping por jurisdicción. F12+13+14+10 (`465b1fc`) — `/admin/sistema` con 4 cards de métricas + actividad govt; bulk approve/reject en `/admin/cola` y `/gob/cola` con `bulk_action_id` shared en audit_log; cron `/api/cron/auto-expire-approvals` diario 04:00 UTC + tabla `cron_runs` + RLS; `/gob/disputas` + `/gob/disputas/[token]` para resolver custody disputes con migration 0025 (tablas `custody_disputes` + `custody_dispute_parties`). Decisiones operativas registradas en commit: authorRole 'admin' colapsa a 'govt' en pet_events.author_role (precise role en payload); bulk revoke UI queda como follow-up porque el attachment uploader bulk es trabajo aparte. |
| `2026-05-18-localities-catalog-indec.md` | ✅ Implementado (parcial) | `specs/2026-05-18-localities-catalog-indec-design.md` (v2.0) | Fases A-E aplicadas. A=schema (`0019_ar_localities.sql` + `0020_drop_slug_unique.sql`), B=importer `scripts/import-indec-localities.ts` (4027 rows imported), C=helpers `lib/ar-localidades.ts` + `app/actions/localities.ts` (60 req/min rate limit), D=`LocalityCombobox` + 3 forms refactoreados, E=`lib/jurisdiction-validation.ts` + 2 actions govt + `scripts/normalize-existing-jurisdictions.ts`. **Follow-ups en READMEpriorities #7**: validar 5 actions restantes + importar CABA barrios separately. |
| `2026-05-16-health-campaigns-and-scheduling.md` | ✅ Implementado | `specs/2026-05-16-health-campaigns-and-scheduling-design.md` (v2.1) | 10 fases shipped en 7 commits (`f0430f3`..`8ed5a9a`). Schema (4 tablas polymorphic + reminders FK), aprobación org/vet via `findAuthoritiesForJurisdiction`, schedule rules, materialización cron + script + botón, owner search + book (advisory lock + DB constraint para races), attendance + cancelaciones, integración con flujo existente, review UI en `/gob/servicios` + `/admin/servicios`, polish. 24h reminder cron documentado como TODO en `plans/2026-05-18-scheduling-24h-reminder-cron-todo.md`. |
| `2026-05-18-foster-volunteers-pool.md` | ✅ Implementado | `specs/2026-05-18-foster-volunteers-pool-design.md` (v1.4) | 4 fases A-B-C-D shipped en commits `7069d72` (A) → `2a5e3a3` (B) → `fb3d9bb`+`401ea03` (C) → `3308e28`+`8d522a9` (D) → `a92366e` (D12 E2E). A=schema foster_volunteers + foster_proposals + ownerships.allow_co_foster + 6 nuevos EVENT_TYPES. B=adoption_eligible columns + adoption_eligibility_set event + setAdoptionEligibilityAction + gate en finalizeAdoption. C=9 server actions (3 voluntarios + 5 propuestas + matching helper) + extensiones de endFoster/recordDeath/finalizeAdoption + cron `expire-foster-proposals` (patrón close-rabies-observations) + RLS policies en `db/foster_rls.sql`. D=10 pages/rutas UI (mix `/cuenta/*` + `/org/[orgToken]/*`). D12=test E2E al action layer (vi.mock createClient en lugar de Playwright); cazó bug real de ordering en acceptFosterProposalAction que violaba CHECK `foster_proposals_response_consistent` — fix: crear ownership primero, single UPDATE flip atómico. **Follow-up:** aplicar `db/foster_rls.sql` en Supabase Studio. |
| `2026-05-18-welfare-reports-polish.md` | ✅ Implementado | — (cerró gaps de la implementación welfare_reports existente) | 4 fases shipped. F1=bridge bug fix en `app/actions/welfare.ts` (ruta obsoleta 2026-09, hoy `src/modules/welfare/actions.ts`) — los 3 pet_events INSERT (abandonment/maltreatment/symptom) ahora copian `locationLat/locationLng` desde el welfare_report. F2=`EventMap` renombrado a `LocationMap` y movido a `components/`; los 2 detail pages de denuncia (`/denuncias/[id]` y `/denuncias/codigo/[code]`) renderean el mapa, no solo coords text. F3=rate-limit anónimo persistente — migration 0027 `rate_limit_buckets` + `enforceRateLimit` en `lib/rate-limit.ts` (ruta obsoleta 2026-09, hoy `lib/infra/rate-limit.ts`; UPSERT atómico con `ON CONFLICT DO UPDATE`); welfare action consume 1/min + 3/hour por IP solo para anonymous. F4=docs ya marcaban maltreatment-reporting como SUPERSEDED. **NO incluyó** welfare-officer queue `/gob/maltrato` ni moderation queue (specs/plans separados — siguen pending). |
| `archive/2026-05-19-cases-system.md` | ✅ Implementado | `specs/2026-05-19-cases-event-attachment-design.md` (v1.1) + `specs/2026-05-19-cases-lifecycles-design.md` (v1.0) | **COMPLETO** (multi-PR, plan en `plans/archive/`). Auditoría 2026-06-19 confirma las 7 fases A-G en código (schema mig 0033, lib en `src/modules/cases/`, 4 crons, 5 workflows refactoreados, rutas `/casos/`, RLS `can_read_case` mig 0094, 12 case_kinds). Fase G (backfill) omitida por decisión (DB se wipea). Plan ejecutable original: 7 fases A-G secuenciales con sub-PRs por fase: A=schema (tabla `cases`, `case_id` columns, RLS shells), B=lib code (`case-kinds`, `case-attachment` rules, `case-normatives` lookup, `case-lifecycles/*.ts` per kind, `case-helpers`, `notification-templates`), C=cron infra (4 nuevos + refactor rabies a 12h), D=refactor 5 server-action workflows (welfare, bite/rabies, lost/found, custody dispute, foster) — D6 adoption queda stub hasta plan adoption-listing-public, E=UI `/casos/[publicCode]` + 4 entry points (con 1-click pet button visible en case detail), F=RLS completo `db/cases_rls.sql` + función `can_read_case` + smoke tests cross-case, G=backfill opcional (3 categorías: welfare_reports open, custody_disputes open, observaciones rábicas open). Verificación final con checklist. ~2-3 semanas. |
| `2026-05-19-caba-barrios-import-execution.md` | ✅ Implementado | `specs/2026-05-18-localities-catalog-indec-design.md` (v2.0) — D6 follow-up | Commit 225aadd6: 48 barrios CABA importados, `source='caba_open_data'`, mig 0028. |
| `archive/2026-05-19-fix-vet-portal-routing.md` | ⚪ Superseded | — | `/pro` eliminado (PR #84); routing ahora vía `resolveVetLanding`. Plan movido a `plans/archive/`. |
| `2026-05-19-fix-service-dog-404.md` | ✅ Implementado | — (fix doc puntual) | Commit 51779f2a: link oculto a non-owners + mensaje friendly. |
| `2026-05-18-additional-species-and-permanent-conditions.md` | ✅ Implementado | `specs/2026-05-17-additional-species-design.md` (species blocks 1+2) + diseño ad-hoc para permanent conditions | Catálogo `lib/permanent-conditions.ts` con 17 códigos (`ciego`, `vision_reducida`, `sordo`, `audicion_reducida`, `tres_patas`, `miembro_no_funcional`, `paralisis_posterior`, `usa_carrito`, `incontinencia_urinaria/fecal`, `epilepsia`, `diabetes`, `fiv_positivo`, `felv_positivo`, `cardiopatia`, `cognitiva`, `otra`). Migration `0031_pet_permanent_conditions.sql` agrega 3 columnas a `pets` + CHECK + GIN index. `PetForm` con fieldset agrupado (sensorial / motor / médico / otro), conditional `permanent_conditions_other` cuando hay `otra`, toggle `disclose_conditions_publicly`. Render condicional en `/p/[publicToken]` (banner indigo) + sección "Necesidades especiales" en `/adoptar/[petToken]`. |
| _(captura-rápida — sin plan formal)_ | ✅ Implementado (commits `bc5f827`, `52498ec`, `c2ca343`) | — (diseñado en chat) | Captura rápida sin LLM. `lib/event-capture-registry.ts` (rename del viejo `event-agent-registry`) con 14 entradas + `buildCaptureDeeplink`. `lib/event-capture-matcher.ts` con patrones regex es-AR para 14 event_types + extractor de fechas naturales (`hoy`, `ayer`, `anteayer`, `hace N días`, `DD/MM[/AAAA]`, `el DD de {mes}`). Página `/mis-mascotas/[publicToken]/anotar` con textarea + 8 quick-action cards. 41 tests del matcher. 13 forms retrofiteados con URL-prefill (peso ya tenía; vacuna/antiparasitario/esterilización/vet/microchip/nota/fallecimiento/checkin con slots, los 5 complejos solo abren sin slots). |
| _(inicio-dashboard — sin plan formal)_ | ✅ Implementado (commits `8c259b2` → `c389418`) | — (diseñado en chat) | Dashboard `/inicio` con 9 widgets: greeting, captura rápida (pet picker), notificaciones (top 5 unread), mis mascotas (top 6), próximos turnos, tratamientos en curso (medication_started sin _stopped), workflows abiertos (7 dominios fan-out con severity badges), workflows previos (top 10 resueltos), news placeholder, regulations placeholder (lista localities de mascotas vivas). Foundation en `lib/owner-dashboard.ts` (8 funciones Promise.all-able). 3 componentes compartidos extraídos (`PetCard`, `NotificationCard`, `AppointmentCard`). Redirect post-login owner: `/mis-mascotas` → `/inicio`. |

| `2026-06-18-projection-primitives.md` | ✅ Implementado | `specs/2026-06-18-projection-primitives-design.md` + `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-metrics-ia-handoff-design.md` | **Item 0 — metrics-IA foundation.** `lib/metrics/` con `ProjectionContext`, `suppressSmallCells` (k=5 enforced via branded type), `resolveAnalyticsPeriod`/`windows`, `activePetsCondition`/`dogsInScopeCondition`, React.cache dedup (`cachedActivePetCount`/`cachedDogCount`). `lib/govt-home-kpis.ts` migrado a `ProjectionContext`; duplicados `DashboardActor`/`DashboardJurisdiction`/`petsScopeClause`/`petEventsScopeClause` eliminados de ambos fetcher files. Pattern B documentado en `docs/architecture/hexagonal-lite.md`. K-anonymity gap cerrado. |
| `2026-06-18-wave2-item8-loading-skeletons.md` | ✅ Implementado | `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave2-ux-hardening-handoff.md` (Item 8) | **Wave 2 Item 8 — Loading & skeleton states.** 4 new skeleton components (`Skeleton`, `OpKpiSkeleton`, `OpCardSkeleton`, `LnCardSkeleton`), shimmer CSS in globals.css, 16 `loading.tsx` files across gob/admin/org/owner/(public) segments. `prefers-reduced-motion` → static via global CSS. `role="status"` + `aria-busy` + SR "Cargando…" on every loading region. 23 RTL/unit assertions in `__tests__/skeleton.test.tsx`. |
| `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave4-govt-actionability-and-viz-handoff.md` (Item 20) | ✅ Implementado | `dim-interno:docs/superpowers-private/specs/archive/2026-06-18-wave4-govt-actionability-and-viz-handoff.md` | **Wave 4 Item 20 — Campaign performance (govt).** `/gob/campañas` — performance de campañas sanitarias para la autoridad: enrollment (bookings), completitud (attended), ausencias (no_show), alcance geográfico (choropleth por localidades). Proyección pura sobre `appointments` + `service_offerings` (sin schema). `lib/analytics/campaign-metrics.ts` con `fetchCampaignDashboard`, `computeDelta`, `formatDelta`. Usa `OpKpi` v2 (deltaV2 vs período previo, sparkline 6m, info tooltip, drillHref). `MapChoropleth` v2 con `RAMP_BLUE`. Empty state con descripción de qué es una campaña. A11y: icon+text (no color-only), tabla accesible bajo el mapa. Nav entry en "Vigilancia sanitaria". Snapshots actualizados: `nav-presets.test.ts` + `shell-nav-phase-b.test.ts`. 14 tests (8 unit: computeDelta, formatDelta; 6 integration: vacío, enrollment math, admin scope, null rates, sparkline shape, geoReach). |
| _(Wave 5 Item 25a — DNI-less data model + HMAC + OIDC scaffold)_ | ✅ Implementado (PR feat/wave5-item25a-dni-less) / 25b pending OIDC creds | `dim-interno:docs/superpowers-private/specs/2026-06-19-wave5-launch-hardening-handoff.md` (Item 25) | **Wave 5 Item 25a — DNI-less identity model.** DROP `profiles.dni_number`; ADD `miarg_sub`, `identity_source`, `dni_hash` (HMAC-SHA256 + pepper), `dni_last4`, `dni_verified_at` (migration 0106 + pgcrypto backfill). HMAC helper `lib/dni-hash.ts` (ruta obsoleta 2026-09, hoy `lib/utils/dni-hash.ts`; `hashDni`, `dniLast4`; pepper from `DNI_HASH_PEPPER` env). All consumers migrated: `app/actions/auth.ts`, `app/actions/dni-verification.ts`, `app/actions/claim.ts`, `app/actions/ppp-export-caba.ts`, `lib/admin-search.ts`, `adoption-repository.ts`. OIDC scaffold: `lib/infra/miarg-oidc.ts` + `app/auth/miarg/callback/route.ts` — gated behind `isMiArgOidcEnabled()`, returns 404/501 until 25b lands. `erase_subject_data()` updated (nulls new columns). Identity model documented in `AGENTS.md §Identity model & DNI handling`. 25b: real Mi Argentina connection, gated on owner credentials. |
| _(Wave 5 Item 26 — RLS defense-in-depth backstop + cross-tenant e2e)_ | ✅ Implementado (PR feat/wave5-item26-rls) | `dim-interno:docs/superpowers-private/specs/2026-06-19-wave5-launch-hardening-handoff.md` (Item 26) | **Wave 5 Item 26 — RLS defense-in-depth backstop + cross-tenant e2e.** Two-layer authz model documented: Server Actions = primary gate (BYPASSRLS); RLS = PostgREST backstop. (1) Migration `0105`: permissive SELECT policies for `pet_identifications` (owner-via-ownerships + admin + govt-in-jurisdiction) and `pet_transfers` (sender + receiver + admin) — previously both were deny-all. Service-role path unaffected (BYPASSRLS). (2) `e2e/cross-tenant-isolation.spec.ts`: 9 Playwright tests covering Owner A cannot access Owner B data via page render and PostgREST; anon cannot read any owner table; fabricated pet tokens 404 cleanly. (3) `scripts/rls-smoke.ts` extended: 2 new anon-read probes (`pet_identifications`, `pet_transfers`) + 2 cross-account probes (B cannot read A's identifications / transfers). (4) `__tests__/rls/matrix.data.ts` extended: `pet_identifications` and `pet_transfers` entries with full role × op matrix. (5) Two-layer authz model documented in `AGENTS.md §Authorization architecture`. |
| _(Wave 5 Item 27 — PII-exposure fixes)_ | ✅ Implementado (PR feat/wave5-item27-pii) | `dim-interno:docs/superpowers-private/specs/2026-06-19-wave5-launch-hardening-handoff.md` (Item 27) | **Wave 5 Item 27 — PII-exposure fixes (payloads + predicates).** Two concrete leaks closed: (1) `findApplicationForReview` in `adoption-repository.ts` previously returned `ApplicationEventRow` — the full event payload containing applicant name/phone/address/DNI. Now projects only `{ id, applicantUserId }` (the sole field callers need for notification routing). All downstream callers (`review-adoption-application.ts`, `actions.ts`) updated. (2) `queryLostListing` in `lost-listing-read.ts` previously fetched location payloads for ALL pets then redacted in JS. Now splits the Stage 2 query: disclosing pets get their location payload fetched; non-disclosing pets get timestamp-only fetch — location data is never retrieved from Postgres. 17 tests: 4 integration (DB) + 13 unit. |
| _(Wave 5 Item 28 — Scan retention & privacy)_ | ✅ Implementado (PR feat/wave5-item28-scan-retention) | `dim-interno:docs/superpowers-private/specs/2026-06-19-wave5-launch-hardening-handoff.md` (Item 28) | **Wave 5 Item 28 — Scan retention & privacy (TTL=90 days).** (1) Purge cron `/api/cron/purge-scan-events` (`lib/infra/scan-retention.ts`): deletes `credential_scanned` events with `author_role='scanner'` older than 90 days. Runs daily (Hobby plan). cron_runs telemetry. (2) Migration `0104`: adds narrow trigger exception in `enforce_pet_events_append_only` — `app.allow_scan_purge='true'` GUC permits DELETE for scanner events beyond TTL; writes `scan_event_purged` audit rows. `db/triggers.sql` kept in sync. (3) IP/geo audit: `app/actions/scans.ts` payload is `{is_self_scan, viewer_authenticated}` only — no IP, no lat/lng (clean). (4) Scan privacy model documented in `AGENTS.md §Scan privacy model`. (5) `__tests__/scan-retention.test.ts`: 8 integration tests (purges >90d, preserves ≤90d, author_role isolation, event_type isolation). |
| _(Wave 5 Item 31 — AGENTS.md slim index + privacy checklist)_ | ✅ Implementado (`feat/wave5-item31-agents-slim`) | `dim-interno:docs/superpowers-private/specs/2026-06-19-wave5-launch-hardening-handoff.md` (Item 31) | **Wave 5 Item 31 — AGENTS.md slim index + "Privacidad y manejo de datos" section.** Option A (lowest churn): slim always-load index (~1.5k tokens) at the top of `AGENTS.md` linking to existing deep sections via anchors. New `## Privacidad y manejo de datos` section (7 rules: DNI no plaintext, RLS backstop, no raw payload returns, privacy predicates in-query, scan payload contract + 90d TTL, k-anon suppressSmallCells k=5, subject rights Ley 25.326) — cross-referenced to real enforcement files. Cross-link: `CONTRIBUTING.md` marks AGENTS.md as lectura obligatoria with slim-index load instruction + per-task privacy gate pointer. `docs/superpowers/README.md` "Lectura obligatoria" block updated with same guidance. |
| _(Wave 5 Item 29 — Test-runner reliability)_ | ✅ Implementado (PR feat/wave5-item29-test-reliability) | — | **Wave 5 Item 29 — Test-runner reliability hardening.** Fixes chronic worker-exit errors + connection pool exhaustion. (1) `db/index.ts`: test-mode pool cap (`max:3`, `idle_timeout:20s`, `max_lifetime:60s`, `connect_timeout:10s`) via `VITEST=true` env flag. (2) `__tests__/global-setup.ts`: new `globalSetup` in vitest.config.ts drains postgres.js pool via `db.$client.end()` after full suite — eliminates "Worker exited unexpectedly" spam from forcibly-closed sockets. (3) `pet-cache-rederivation.test.ts` Layer 1: sweep scoped to `publicToken LIKE 'DIM-%'` seed pets only (not all DB pets) — eliminates state-dependent sweep flake from leftover test pets. (4) `db/migrations/0084`: add `DROP INDEX IF EXISTS pets_microchip_lookup_idx` before `DROP COLUMN microchip_id` — fixes migration failure when run in strict migration-first order (index from 0012 blocked the column drop). Conventions documented in AGENTS.md §Test-runner conventions. |
| _(Rehome by titular — SDD en Engram: `sdd/rehome-by-titular/{explore,proposal,spec,design,tasks,apply-progress}`; decisión PO `architecture/rehome-by-titular`)_ | ✅ Implementado (integration/all-20260703, WU1-WU7) | Engram artifact store (sin spec file — SDD) | **Acompañamiento de adopción.** El titular le pide a una org verificada que publique a su mascota y evalúe postulantes mientras el animal sigue en su casa. Dos casos, no uno (ADR-1): `rehome_request` (consentimiento + bandeja de la org, kind 13) y el `adoption_listing` existente (el acompañamiento). La org recibe `shelter_custody` AL LADO de la fila `owner` del titular; la baja del titular siempre gana (REQ-8/10); el finalize cierra TODAS las filas vivas antes de abrir la del adoptante (REQ-9, el bug que el WU1 dejó rojo a propósito). Eventos 53-54 `rehome_sponsorship_started/ended` (titular-only, 0194); un solo org con custodia viva por mascota (0195, acotada a org); `receiver_organization_id` como arm de la bandeja acotado al kind (decisión PO). Copy condicionado a posesión (REQ-11/12): `SponsorshipPossessionNotice` en cada pantalla de la org, ficha pública con "Organización que acompaña" + localidad de la mascota. Muerte → CASCADE D (`lib/infra/rehome-death-cascade.ts`, `pet_deceased`). Rollback (ADR-7): `scripts/rollback-rehome-sponsorships.ts` PRIMERO, revert después. Módulo: `src/modules/rehome/README.md`. |
| _(Movilidad jurisdiccional Fase 1 — SDD en Engram: `sdd/movilidad-jurisdiccional/{proposal,spec,design,tasks,apply-progress}`)_ | ✅ Implementado (integration/all-20260703, WU1-WU6) | Engram artifact store (sin spec file — SDD) | **Movilidad jurisdiccional Fase 1.** Evento 48 `movement_recorded` (`z.discriminatedUnion` sobre `sub_kind`: `jurisdiction_changed`/`cvi_issued`/`transport_recorded`); tabla cerrada `STRICTNESS_DIRECTION` (11 rule types, min/max/union); registro de 5 corredores con coverage-throw en `lib/reference/cross-border-corridors.ts` (valores regulatorios **citation-pending** — validación PO requerida antes de poblarlos); proyección pura `deriveTravelCompliance` (hermana de `deriveComplianceState`, que queda intocada); `recordMovementWriter` (tx única event-first; denormalización de `pets.jurisdiction*` SOLO en `jurisdiction_changed`, con regression test de divergencia clase-microchipHeroTag); ruta owner-only `/mis-mascotas/[publicToken]/viaje` (semáforo rojo/amarillo/verde + checklist + disclaimer R3.5); export PDF único multi-sección a bucket privado `travel-exports` (creación del bucket = ops del owner, nunca desde código); migración `0120` (CHECK `govt_business_rules_rule_type_valid` 8→9 con `travel_corridor_requirements`, aplicación owner-gated). |

**Leyenda:**

- ✅ Implementado — código en main, verificado en repo
- 🟢 Ready for CC — plan auto-contenido, listo para sesión de Claude Code
- 🟡 Spec only — necesita plan escrito antes de implementar
- 🔵 In progress — Claude Code está trabajando ahora

---

## Cross-cutting dependencies

Conviene saberlas porque cruzan varios specs:

- **Portal rename (este plan)** destraba:
  - Coherencia entre todos los specs/plans (todos referencian `/org/[orgToken]`, `/gob`, `/admin`, `/pro` consistentemente)
  - Reducción de fricción cuando llegue el code rename físico de `app/refugio/` → `app/org/`
  - Habilita la implementación de sub-rutas `/pro/servicios` + `/pro/agenda` para vets independientes
- **Admin page Fase 0** (schema foundation: `account_type`, `admin_grants`, `approval_requests`, `audit_log`) destraba:
  - El routing a govts del symptom surveillance (hoy fallback a admins vía `/admin`; post-Fase 0 irá a `/gob`)
  - La aprobación real de vet upgrades (hoy se setea matriculaVerified=true por Studio)
  - La verificación 