// Stub for `server-only` in Vitest environments.
// The real package throws at import time to prevent usage in client bundles.
// In Node.js test environments this guard is unnecessary — we stub it as a no-op.
export {};
