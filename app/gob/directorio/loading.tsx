/**
 * loading.tsx — skeleton for /gob/directorio (the Directorio hub, F3+F7 fusion).
 */

import { OpDashboardSkeleton } from "@/components/ui/dashboard/OpDashboardSkeleton";

export default function GobDirectorioLoading() {
  return <OpDashboardSkeleton cards={[7]} />;
}
