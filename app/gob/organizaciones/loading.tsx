/**
 * loading.tsx — skeleton for /gob/organizaciones (organization directory + revoke list).
 */

import { OpDashboardSkeleton } from "@/components/ui/dashboard/OpDashboardSkeleton";

export default function GobOrganizacionesLoading() {
  return <OpDashboardSkeleton cards={[7]} />;
}
