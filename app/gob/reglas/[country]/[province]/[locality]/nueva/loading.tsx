/**
 * loading.tsx — skeleton for /gob/reglas/[country]/[province]/[locality]/nueva (new business rule form).
 */

import { OpDashboardSkeleton } from "@/components/ui/dashboard/OpDashboardSkeleton";

export default function GobReglaNuevaLoading() {
  return <OpDashboardSkeleton filterBar={false} cards={[4]} />;
}
