/**
 * loading.tsx — skeleton for /org/[orgToken]/transferencias/nueva (new transfer form).
 */

import { OpDashboardSkeleton } from "@/components/ui/dashboard/OpDashboardSkeleton";

export default function OrgTransferenciaNuevaLoading() {
  return <OpDashboardSkeleton filterBar={false} cards={[4]} />;
}
