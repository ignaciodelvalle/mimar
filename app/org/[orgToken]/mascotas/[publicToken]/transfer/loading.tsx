/**
 * loading.tsx — skeleton for /org/[orgToken]/mascotas/[publicToken]/transfer (transfer action form).
 */

import { OpDashboardSkeleton } from "@/components/ui/dashboard/OpDashboardSkeleton";

export default function OrgMascotaTransferLoading() {
  return <OpDashboardSkeleton filterBar={false} cards={[3]} />;
}
