// Thin admin-portal wrapper — same skeleton as the /gob route (the page itself
// is also a thin re-export, see app/admin/suscripciones/page.tsx).
export { default } from "@/app/gob/suscripciones/loading";
