// Thin admin-portal wrapper — same implementation as the /gob route,
// rendered inside the ADMIN shell (portal-follows-viewer, mirrors
// app/admin/cola/page.tsx). The page component carries its own authz guard;
// chrome comes from this segment's admin layout.
export { default } from "@/app/gob/suscripciones/page";
