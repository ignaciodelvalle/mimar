/**
 * loading.tsx — skeleton for /admin/observaciones (observations queue).
 */

import { OpDashboardSkeleton } from "@/components/ui/dashboard/OpDashboardSkeleton";

export default function AdminObservacionesLoading() {
  return <OpDashboardSkeleton cards={[8]} />;
}
