/**
 * loading.tsx — skeleton for /admin/cuentas (the Cuentas privilegiadas hub,
 * privileged-accounts fusion 2026-08-02).
 */

import { OpDashboardSkeleton } from "@/components/ui/dashboard/OpDashboardSkeleton";

export default function AdminCuentasLoading() {
  return <OpDashboardSkeleton cards={[7]} />;
}
