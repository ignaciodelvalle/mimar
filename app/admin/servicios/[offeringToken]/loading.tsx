// Thin admin-portal wrapper — same implementation as the /gob route,
// rendered inside the ADMIN shell (portal-follows-viewer, 2026-07-02).
export { default } from "@/app/gob/servicios/[offeringToken]/loading";
