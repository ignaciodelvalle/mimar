/**
 * loading.tsx — skeleton for /transferencias/[transferToken] (pet transfer detail).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function TransferenciaDetailLoading() {
  return <LnPageSkeleton rows={2} />;
}
