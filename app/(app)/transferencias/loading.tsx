/**
 * loading.tsx — skeleton for /transferencias (pet transfers list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function TransferenciasLoading() {
  return <LnPageSkeleton rows={6} />;
}
