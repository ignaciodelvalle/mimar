/**
 * loading.tsx — skeleton for /notificaciones (notifications list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function NotificacionesLoading() {
  return <LnPageSkeleton rows={6} avatar={false} />;
}
