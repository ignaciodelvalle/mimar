/**
 * loading.tsx — skeleton for /mis-mascotas/reclamar (claim pet form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function ReclamarLoading() {
  return <LnPageSkeleton rows={2} avatar={false} />;
}
