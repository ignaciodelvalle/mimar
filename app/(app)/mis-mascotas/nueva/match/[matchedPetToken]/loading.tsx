/**
 * loading.tsx — skeleton for /mis-mascotas/nueva/match/[matchedPetToken] (pet match confirmation).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function NuevaMatchLoading() {
  return <LnPageSkeleton rows={1} />;
}
