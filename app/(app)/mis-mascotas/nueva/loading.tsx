/**
 * loading.tsx — skeleton for /mis-mascotas/nueva (new pet registration entry).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function MisMascotasNuevaLoading() {
  return <LnPageSkeleton rows={1} avatar={false} cta />;
}
