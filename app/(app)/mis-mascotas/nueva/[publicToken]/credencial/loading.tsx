/**
 * loading.tsx — skeleton for /mis-mascotas/nueva/[publicToken]/credencial (new pet credential display).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function CredencialLoading() {
  return <LnPageSkeleton rows={1} />;
}
