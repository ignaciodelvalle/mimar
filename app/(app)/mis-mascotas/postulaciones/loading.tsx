/**
 * loading.tsx — skeleton for /mis-mascotas/postulaciones (adoption applications list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function PostulacionesLoading() {
  return <LnPageSkeleton rows={6} cta />;
}
