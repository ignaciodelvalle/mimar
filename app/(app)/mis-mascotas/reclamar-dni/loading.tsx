/**
 * loading.tsx — skeleton for /mis-mascotas/reclamar-dni (claim by DNI form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function ReclamarDniLoading() {
  return <LnPageSkeleton rows={2} avatar={false} />;
}
