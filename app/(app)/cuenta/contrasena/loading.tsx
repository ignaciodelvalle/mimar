/**
 * loading.tsx — skeleton for /cuenta/contrasena (change-password form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function CambiarContrasenaLoading() {
  return <LnPageSkeleton rows={1} avatar={false} />;
}
