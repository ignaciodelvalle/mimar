/**
 * loading.tsx — skeleton for /cuenta/ofrecerme-como-transito (foster offer form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function OfrecermeTransitoLoading() {
  return <LnPageSkeleton rows={2} cta avatar={false} />;
}
