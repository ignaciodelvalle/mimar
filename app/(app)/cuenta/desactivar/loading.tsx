/**
 * loading.tsx — skeleton for /cuenta/desactivar (account deactivation confirmation).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function DesactivarCuentaLoading() {
  return <LnPageSkeleton rows={1} avatar={false} />;
}
