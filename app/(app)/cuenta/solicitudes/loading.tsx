/**
 * loading.tsx — skeleton for /cuenta/solicitudes (requests list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function SolicitudesLoading() {
  return <LnPageSkeleton rows={6} avatar={false} />;
}
