/**
 * loading.tsx — skeleton for /cuenta/upgrade (account upgrade form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function UpgradeCuentaLoading() {
  return <LnPageSkeleton rows={3} cta avatar={false} />;
}
