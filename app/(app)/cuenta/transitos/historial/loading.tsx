/**
 * loading.tsx — skeleton for /cuenta/transitos/historial (foster history list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function TransitosHistorialLoading() {
  return <LnPageSkeleton rows={4} />;
}
