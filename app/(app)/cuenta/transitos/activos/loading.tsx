/**
 * loading.tsx — skeleton for /cuenta/transitos/activos (active fosters list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function TransitosActivosLoading() {
  return <LnPageSkeleton rows={4} />;
}
