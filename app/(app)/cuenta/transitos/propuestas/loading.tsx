/**
 * loading.tsx — skeleton for /cuenta/transitos/propuestas (foster proposals list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function TransitosPropuestasLoading() {
  return <LnPageSkeleton rows={4} />;
}
