/**
 * loading.tsx — skeleton for /cuenta/transitos/propuestas/[proposalToken] (foster proposal detail).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function TransitoPropuestaDetailLoading() {
  return <LnPageSkeleton rows={2} />;
}
