/**
 * loading.tsx — skeleton for /cuenta/crear-consultorio (vet office creation form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function CrearConsultorioLoading() {
  return <LnPageSkeleton rows={2} cta />;
}
