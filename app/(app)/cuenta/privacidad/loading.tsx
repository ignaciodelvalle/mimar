/**
 * loading.tsx — skeleton for /cuenta/privacidad (privacy settings).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function PrivacidadLoading() {
  return <LnPageSkeleton rows={2} avatar={false} />;
}
