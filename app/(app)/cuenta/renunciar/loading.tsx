/**
 * loading.tsx — skeleton for /cuenta/renunciar (membership resignation confirmation).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function RenunciarLoading() {
  return <LnPageSkeleton rows={1} avatar={false} />;
}
