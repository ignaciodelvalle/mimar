/**
 * loading.tsx — skeleton for /cuenta/memberships (organization memberships list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function MembershipsLoading() {
  return <LnPageSkeleton rows={4} avatar={false} />;
}
