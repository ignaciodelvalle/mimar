/**
 * loading.tsx — skeleton for /cuenta/verificar-dni (DNI verification form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function VerificarDniLoading() {
  return <LnPageSkeleton rows={1} avatar={false} />;
}
