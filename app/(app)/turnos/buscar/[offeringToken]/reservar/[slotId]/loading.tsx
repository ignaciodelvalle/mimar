/**
 * loading.tsx — skeleton for /turnos/buscar/[offeringToken]/reservar/[slotId] (booking confirmation form).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function ReservarSlotLoading() {
  return <LnPageSkeleton rows={1} cta avatar={false} />;
}
