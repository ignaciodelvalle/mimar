/**
 * loading.tsx — skeleton for /turnos/buscar/[offeringToken] (offering detail/slots).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function OfferingDetailLoading() {
  return <LnPageSkeleton rows={3} avatar={false} />;
}
