/**
 * loading.tsx — skeleton for /mis-turnos/[appointmentToken] (appointment detail).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function MiTurnoDetailLoading() {
  return <LnPageSkeleton rows={3} avatar={false} />;
}
