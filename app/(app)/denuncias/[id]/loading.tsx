/**
 * loading.tsx — skeleton for /denuncias/[id] (complaint detail).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function DenunciaDetailLoading() {
  return <LnPageSkeleton rows={5} />;
}
