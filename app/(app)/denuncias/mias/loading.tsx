/**
 * loading.tsx — skeleton for /denuncias/mias (my complaints list).
 */

import { LnPageSkeleton } from "@/components/ui/LnPageSkeleton";

export default function DenunciasMiasLoading() {
  return <LnPageSkeleton rows={4} />;
}
